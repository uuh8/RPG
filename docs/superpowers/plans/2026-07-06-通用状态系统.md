# 通用状态系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建第一版角色通用状态系统，让玩家和敌人可以拥有 `Burning / Wet / Poisoned / Sticky` 四类 `0-100` 百分比状态，并支持衰减、DoT、Wet 灭火、Sticky 减速、UI 显示。

**Architecture:** 状态核心放在 `Game.Combat`，由 `StatusController` 统一管理运行时状态；状态配置使用 `StatusDefinition` / `StatusDatabase` ScriptableObject。UI 放在 `Game.UI`，通过 `EventBus<StatusChangedEvent>` 监听状态变化；角色移动层只读取 `StatusController.MoveSpeedMultiplier`，不让 Combat 反向依赖 Character。

**Tech Stack:** Unity 6.3, C#, ScriptableObject, MonoBehaviour, UGUI, EventBus, NUnit EditMode tests.

## Global Constraints

- 默认使用简体中文命名 spec/plan 内容。
- 本计划只写第一版 `Status System`，不做 voxel sandbox、Element Zone、复杂元素反应链。
- 新代码遵守 asmdef 边界：`Game.Combat` 不依赖 `Game.Character` / `Game.UI` / `Game.Skills`。
- 跨模块 UI 通知使用 `Game.Core.EventBus<T>`。
- 日志使用 `GameLog`，不直接使用 `Debug.Log`。
- `Update` 热路径不使用 LINQ、不每帧 new 对象、不每帧分配临时数组。
- 手工编辑使用 `apply_patch`，不手动创建 `.meta` 文件。
- Unity Editor 中的 asset / prefab / scene 连接由用户手动完成。

---

## File Structure

### Create

- `Assets/_Project/Scripts/Combat/Status/StatusKind.cs`
  - 定义第一版状态枚举：`Burning / Wet / Poisoned / Sticky`。
- `Assets/_Project/Scripts/Combat/Status/StatusDefinition.cs`
  - 单个状态的数据资产，保存 icon、衰减、DoT、移速倍率、VFX 等配置。
- `Assets/_Project/Scripts/Combat/Status/StatusDatabase.cs`
  - 状态定义集合，提供按 `StatusKind` 查询 definition 的接口。
- `Assets/_Project/Scripts/Combat/Status/StatusInstance.cs`
  - 运行时状态数据结构，保存 `Intensity / TickTimer / Source`。
- `Assets/_Project/Scripts/Combat/Status/StatusController.cs`
  - 挂在角色身上的状态管理器，负责 Apply、衰减、反应、DoT、VFX、事件。
- `Assets/_Project/Scripts/Combat/Events/StatusChangedEvent.cs`
  - 状态变化事件，供 UI 监听。
- `Assets/_Project/Scripts/UI/StatusIconView.cs`
  - 单个状态 icon + 百分比显示。
- `Assets/_Project/Scripts/UI/PlayerStatusBar.cs`
  - 玩家状态栏，订阅 `StatusChangedEvent` 并刷新 4 个固定 slot。
- `Assets/_Project/Tests/Combat/StatusControllerTests.cs`
  - EditMode 测试状态累加、衰减、反应、DoT、移速倍率。

### Modify

- `Assets/_Project/Scripts/Combat/Projectiles/Fireball.cs`
  - 从旧 `BurnStatus` 迁移到 `StatusController.ApplyStatus(StatusKind.Burning, ...)`。
- `Assets/_Project/Scripts/Character/Controllers/PlayerControllerBase.cs`
  - 读取 `StatusController.MoveSpeedMultiplier`，让 `Sticky` 影响玩家移动速度。
- `Assets/_Project/Scripts/Combat/Status/BurnStatus.cs`
  - 若迁移后无引用，删除；如果担心 prefab / scene 旧引用，保留并标记 legacy，不再作为主路径使用。

### Editor Assets

由用户在 Unity Editor 创建：

- `Assets/_Project/ScriptableObjects/Status/Status_Burning.asset`
- `Assets/_Project/ScriptableObjects/Status/Status_Wet.asset`
- `Assets/_Project/ScriptableObjects/Status/Status_Poisoned.asset`
- `Assets/_Project/ScriptableObjects/Status/Status_Sticky.asset`
- `Assets/_Project/ScriptableObjects/Status/StatusDatabase.asset`

---

## Task 1: 状态核心数据与事件

**Files:**
- Create: `Assets/_Project/Scripts/Combat/Status/StatusKind.cs`
- Create: `Assets/_Project/Scripts/Combat/Status/StatusDefinition.cs`
- Create: `Assets/_Project/Scripts/Combat/Status/StatusDatabase.cs`
- Create: `Assets/_Project/Scripts/Combat/Status/StatusInstance.cs`
- Create: `Assets/_Project/Scripts/Combat/Events/StatusChangedEvent.cs`

**Interfaces:**
- Produces: `StatusKind`
- Produces: `StatusDefinition`
- Produces: `StatusDatabase.TryGet(StatusKind kind, out StatusDefinition definition)`
- Produces: `StatusInstance`
- Produces: `StatusChangedEvent : IGameEvent`

- [ ] **Step 1: Create `StatusKind.cs`**

```csharp
namespace Game.Combat
{
    public enum StatusKind : byte
    {
        Burning = 0,
        Wet = 1,
        Poisoned = 2,
        Sticky = 3,
    }
}
```

- [ ] **Step 2: Create `StatusDefinition.cs`**

```csharp
using UnityEngine;

namespace Game.Combat
{
    [CreateAssetMenu(menuName = "Game/Combat/Status Definition", fileName = "StatusDefinition")]
    public class StatusDefinition : ScriptableObject
    {
        [Header("通用")]
        public StatusKind Kind;
        public string DisplayName = "";
        public Sprite Icon;
        [Min(0f)] public float NaturalDecayPerSecond = 8f;
        public GameObject VfxPrefab;

        [Header("持续伤害")]
        public bool DealsDamage;
        [Min(0.05f)] public float DamageInterval = 1f;
        [Min(0f)] public float BaseDamagePerTick = 0f;
        public DamageType DamageType = DamageType.Magical;
        public bool TriggerHitReaction = false;

        [Header("移动修正")]
        public bool AffectsMoveSpeed;
        [Range(0f, 0.95f)] public float MaxMoveSpeedSlowRatio = 0f;
    }
}
```

- [ ] **Step 3: Create `StatusDatabase.cs`**

```csharp
using UnityEngine;

namespace Game.Combat
{
    [CreateAssetMenu(menuName = "Game/Combat/Status Database", fileName = "StatusDatabase")]
    public class StatusDatabase : ScriptableObject
    {
        [SerializeField] private StatusDefinition[] _definitions;

        public bool TryGet(StatusKind kind, out StatusDefinition definition)
        {
            if (_definitions != null)
            {
                for (int i = 0; i < _definitions.Length; i++)
                {
                    StatusDefinition candidate = _definitions[i];
                    if (candidate != null && candidate.Kind == kind)
                    {
                        definition = candidate;
                        return true;
                    }
                }
            }

            definition = null;
            return false;
        }
    }
}
```

- [ ] **Step 4: Create `StatusInstance.cs`**

```csharp
namespace Game.Combat
{
    public struct StatusInstance
    {
        public bool Active;
        public float Intensity;
        public float TickTimer;
        public int SourceId;
        public byte SourceTeam;
    }
}
```

- [ ] **Step 5: Create `StatusChangedEvent.cs`**

```csharp
using UnityEngine;
using Game.Core;

namespace Game.Combat
{
    public struct StatusChangedEvent : IGameEvent
    {
        public int TargetId;
        public StatusKind Kind;
        public float Intensity;
        public bool IsActive;
        public Sprite Icon;
        public string DisplayName;
    }
}
```

- [ ] **Step 6: Build Combat**

Run:

```powershell
dotnet build Game.Combat.csproj --no-restore /m:1
```

Expected:

```text
已成功生成。
0 个警告
0 个错误
```

---

## Task 2: StatusController Apply / 查询 / 自然衰减

**Files:**
- Create: `Assets/_Project/Scripts/Combat/Status/StatusController.cs`
- Test: `Assets/_Project/Tests/Combat/StatusControllerTests.cs`

**Interfaces:**
- Consumes: `StatusKind`, `StatusDefinition`, `StatusDatabase`, `StatusInstance`, `StatusChangedEvent`
- Produces: `StatusController.ApplyStatus(StatusKind kind, float amount, int sourceId, byte sourceTeam)`
- Produces: `StatusController.HasStatus(StatusKind kind)`
- Produces: `StatusController.GetIntensity(StatusKind kind)`
- Produces: `StatusController.TryGetIntensity(StatusKind kind, out float intensity)`
- Produces: `StatusController.TickForTests(float deltaTime)`

- [ ] **Step 1: Write failing tests for apply and clamp**

Create `Assets/_Project/Tests/Combat/StatusControllerTests.cs`:

```csharp
using NUnit.Framework;
using UnityEngine;
using Game.Combat;

namespace Game.Combat.Tests
{
    public class StatusControllerTests
    {
        private static StatusDefinition Def(StatusKind kind, float decay = 0f)
        {
            var def = ScriptableObject.CreateInstance<StatusDefinition>();
            def.Kind = kind;
            def.DisplayName = kind.ToString();
            def.NaturalDecayPerSecond = decay;
            return def;
        }

        private static StatusController Controller(params StatusDefinition[] definitions)
        {
            var go = new GameObject("status-test");
            var controller = go.AddComponent<StatusController>();
            controller.SetDefinitionsForTests(definitions);
            return controller;
        }

        [Test]
        public void ApplyStatus_AddsIntensityAndClampsTo100()
        {
            StatusController controller = Controller(Def(StatusKind.Burning));

            controller.ApplyStatus(StatusKind.Burning, 40f, 10, 1);
            controller.ApplyStatus(StatusKind.Burning, 70f, 10, 1);

            Assert.IsTrue(controller.HasStatus(StatusKind.Burning));
            Assert.AreEqual(100f, controller.GetIntensity(StatusKind.Burning), 1e-4f);
        }
    }
}
```

- [ ] **Step 2: Run test and verify it fails**

Run:

```powershell
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
```

Expected:

```text
error CS0246: 未能找到类型或命名空间名“StatusController”
```

- [ ] **Step 3: Implement minimal `StatusController` apply/query**

Create `Assets/_Project/Scripts/Combat/Status/StatusController.cs`:

```csharp
using UnityEngine;
using Game.Core;

namespace Game.Combat
{
    [DisallowMultipleComponent]
    public class StatusController : MonoBehaviour
    {
        private const int StatusCount = 4;

        [SerializeField] private StatusDatabase _database;
        [SerializeField] private float _wetExtinguishBurningPerSecond = 35f;

        private readonly StatusInstance[] _instances = new StatusInstance[StatusCount];
        private readonly StatusDefinition[] _definitions = new StatusDefinition[StatusCount];
        private HealthComponent _health;
        private IDamageable _damageable;
        private int _targetId;

        public float MoveSpeedMultiplier { get; private set; } = 1f;

        private void Awake()
        {
            _health = GetComponent<HealthComponent>();
            _damageable = GetComponent<IDamageable>();
            _targetId = gameObject.GetInstanceID();
            CacheDefinitions();
            RecalculateMoveSpeedMultiplier();
        }

        private void Update()
        {
            Tick(Time.deltaTime);
        }

        public void ApplyStatus(StatusKind kind, float amount, int sourceId, byte sourceTeam)
        {
            if (amount <= 0f)
                return;

            int index = ToIndex(kind);
            StatusInstance instance = _instances[index];
            instance.Active = true;
            instance.Intensity = Mathf.Clamp(instance.Intensity + amount, 0f, 100f);
            instance.SourceId = sourceId;
            instance.SourceTeam = sourceTeam;

            StatusDefinition definition = GetDefinition(kind);
            if (definition != null && definition.DealsDamage && instance.TickTimer <= 0f)
                instance.TickTimer = Mathf.Max(0.05f, definition.DamageInterval);

            _instances[index] = instance;
            RecalculateMoveSpeedMultiplier();
            PublishStatusChanged(kind);
        }

        public bool HasStatus(StatusKind kind)
        {
            return _instances[ToIndex(kind)].Active;
        }

        public float GetIntensity(StatusKind kind)
        {
            return _instances[ToIndex(kind)].Intensity;
        }

        public bool TryGetIntensity(StatusKind kind, out float intensity)
        {
            StatusInstance instance = _instances[ToIndex(kind)];
            intensity = instance.Intensity;
            return instance.Active;
        }

        public void TickForTests(float deltaTime)
        {
            Tick(deltaTime);
        }

        public void SetDefinitionsForTests(params StatusDefinition[] definitions)
        {
            for (int i = 0; i < _definitions.Length; i++)
                _definitions[i] = null;

            if (definitions == null)
                return;

            for (int i = 0; i < definitions.Length; i++)
            {
                StatusDefinition definition = definitions[i];
                if (definition != null)
                    _definitions[ToIndex(definition.Kind)] = definition;
            }
        }

        private void Tick(float deltaTime)
        {
            if (deltaTime <= 0f)
                return;

            for (int i = 0; i < StatusCount; i++)
            {
                StatusInstance instance = _instances[i];
                if (!instance.Active)
                    continue;

                StatusKind kind = (StatusKind)i;
                StatusDefinition definition = GetDefinition(kind);
                float decay = definition != null ? definition.NaturalDecayPerSecond : 0f;
                instance.Intensity = Mathf.Max(0f, instance.Intensity - decay * deltaTime);

                if (instance.Intensity <= 0f)
                {
                    instance.Active = false;
                    instance.TickTimer = 0f;
                }

                _instances[i] = instance;
                PublishStatusChanged(kind);
            }

            RecalculateMoveSpeedMultiplier();
        }

        private void CacheDefinitions()
        {
            for (int i = 0; i < StatusCount; i++)
            {
                StatusKind kind = (StatusKind)i;
                if (_database != null && _database.TryGet(kind, out StatusDefinition definition))
                    _definitions[i] = definition;
            }
        }

        private StatusDefinition GetDefinition(StatusKind kind)
        {
            return _definitions[ToIndex(kind)];
        }

        private void RecalculateMoveSpeedMultiplier()
        {
            MoveSpeedMultiplier = 1f;
        }

        private void PublishStatusChanged(StatusKind kind)
        {
            StatusDefinition definition = GetDefinition(kind);
            StatusInstance instance = _instances[ToIndex(kind)];
            EventBus<StatusChangedEvent>.Publish(new StatusChangedEvent
            {
                TargetId = _targetId,
                Kind = kind,
                Intensity = instance.Intensity,
                IsActive = instance.Active,
                Icon = definition != null ? definition.Icon : null,
                DisplayName = definition != null ? definition.DisplayName : kind.ToString(),
            });
        }

        private static int ToIndex(StatusKind kind)
        {
            return (int)kind;
        }
    }
}
```

- [ ] **Step 4: Add tests for natural decay and removal**

Append to `StatusControllerTests.cs`:

```csharp
[Test]
public void Tick_ReducesIntensityByNaturalDecay()
{
    StatusController controller = Controller(Def(StatusKind.Burning, decay: 8f));

    controller.ApplyStatus(StatusKind.Burning, 100f, 10, 1);
    controller.TickForTests(1f);

    Assert.AreEqual(92f, controller.GetIntensity(StatusKind.Burning), 1e-4f);
}

[Test]
public void Tick_RemovesStatusWhenIntensityReachesZero()
{
    StatusController controller = Controller(Def(StatusKind.Burning, decay: 10f));

    controller.ApplyStatus(StatusKind.Burning, 5f, 10, 1);
    controller.TickForTests(1f);

    Assert.IsFalse(controller.HasStatus(StatusKind.Burning));
    Assert.AreEqual(0f, controller.GetIntensity(StatusKind.Burning), 1e-4f);
}
```

- [ ] **Step 5: Run Combat tests**

Run:

```powershell
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
```

Expected:

```text
已成功生成。
0 个警告
0 个错误
```

---

## Task 3: Wet 灭火、DoT、Sticky 移速倍率

**Files:**
- Modify: `Assets/_Project/Scripts/Combat/Status/StatusController.cs`
- Modify: `Assets/_Project/Tests/Combat/StatusControllerTests.cs`

**Interfaces:**
- Consumes: `StatusController.TickForTests(float deltaTime)`
- Produces: `Wet` accelerates `Burning` decay
- Produces: DoT through `IDamageable.ReceiveHit(in DamageRequest)`
- Produces: `StatusController.MoveSpeedMultiplier`

- [ ] **Step 1: Add tests for Wet extinguishing Burning**

Append to `StatusControllerTests.cs`:

```csharp
[Test]
public void Wet_AcceleratesBurningDecay()
{
    StatusController controller = Controller(
        Def(StatusKind.Burning, decay: 8f),
        Def(StatusKind.Wet, decay: 0f));

    controller.ApplyStatus(StatusKind.Burning, 100f, 10, 1);
    controller.ApplyStatus(StatusKind.Wet, 100f, 10, 1);
    controller.TickForTests(1f);

    Assert.AreEqual(57f, controller.GetIntensity(StatusKind.Burning), 1e-4f);
}
```

Expected math:

```text
Burning 100 - natural 8 - wet extinguish 35 = 57
```

- [ ] **Step 2: Add tests for Sticky move speed multiplier**

Append helper and tests:

```csharp
private static StatusDefinition StickyDef()
{
    var def = Def(StatusKind.Sticky, decay: 0f);
    def.AffectsMoveSpeed = true;
    def.MaxMoveSpeedSlowRatio = 0.4f;
    return def;
}

[Test]
public void Sticky_ProducesMoveSpeedMultiplier()
{
    StatusController controller = Controller(StickyDef());

    controller.ApplyStatus(StatusKind.Sticky, 100f, 10, 1);
    Assert.AreEqual(0.6f, controller.MoveSpeedMultiplier, 1e-4f);

    controller = Controller(StickyDef());
    controller.ApplyStatus(StatusKind.Sticky, 50f, 10, 1);
    Assert.AreEqual(0.8f, controller.MoveSpeedMultiplier, 1e-4f);
}
```

- [ ] **Step 3: Add tests for Poisoned DoT**

Append test:

```csharp
[Test]
public void Poisoned_DealsDotWithoutHitReaction()
{
    var go = new GameObject("poison-target");
    var health = go.AddComponent<HealthComponent>();
    var controller = go.AddComponent<StatusController>();

    var poison = Def(StatusKind.Poisoned, decay: 0f);
    poison.DealsDamage = true;
    poison.DamageInterval = 1f;
    poison.BaseDamagePerTick = 4f;
    poison.DamageType = DamageType.Magical;
    poison.TriggerHitReaction = false;

    controller.SetDefinitionsForTests(poison);
    controller.ApplyStatus(StatusKind.Poisoned, 100f, 99, 2);

    float before = health.CurrentHp;
    controller.TickForTests(1f);

    Assert.AreEqual(before - 4f, health.CurrentHp, 1e-4f);
}
```

- [ ] **Step 4: Implement Wet reaction**

In `StatusController.Tick`, before applying decay to `Burning`, add extra decay:

```csharp
if (kind == StatusKind.Burning && HasStatus(StatusKind.Wet))
    decay += _wetExtinguishBurningPerSecond;
```

Place it after reading natural decay:

```csharp
float decay = definition != null ? definition.NaturalDecayPerSecond : 0f;
if (kind == StatusKind.Burning && HasStatus(StatusKind.Wet))
    decay += _wetExtinguishBurningPerSecond;
```

- [ ] **Step 5: Implement DoT tick**

In `StatusController.Tick`, before final removal check, add:

```csharp
if (definition != null && definition.DealsDamage && _damageable != null && _damageable.IsAlive)
{
    instance.TickTimer -= deltaTime;
    if (instance.TickTimer <= 0f)
    {
        instance.TickTimer += Mathf.Max(0.05f, definition.DamageInterval);
        float amount = definition.BaseDamagePerTick * (instance.Intensity / 100f);
        if (amount > 0f)
        {
            var req = new DamageRequest(instance.SourceId, instance.SourceTeam, amount,
                                        definition.DamageType, transform.position, Vector3.up,
                                        definition.TriggerHitReaction);
            _damageable.ReceiveHit(in req);
        }
    }
}
```

Important: use the current `instance.Intensity` after decay/reaction so low-intensity DoT deals less damage.

- [ ] **Step 6: Implement move speed multiplier**

Replace `RecalculateMoveSpeedMultiplier` with:

```csharp
private void RecalculateMoveSpeedMultiplier()
{
    float multiplier = 1f;

    for (int i = 0; i < StatusCount; i++)
    {
        StatusInstance instance = _instances[i];
        if (!instance.Active)
            continue;

        StatusDefinition definition = _definitions[i];
        if (definition == null || !definition.AffectsMoveSpeed)
            continue;

        float slowRatio = definition.MaxMoveSpeedSlowRatio * (instance.Intensity / 100f);
        multiplier *= Mathf.Clamp01(1f - slowRatio);
    }

    MoveSpeedMultiplier = multiplier;
}
```

- [ ] **Step 7: Run Combat tests**

Run:

```powershell
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
```

Expected:

```text
已成功生成。
0 个警告
0 个错误
```

---

## Task 4: 状态 VFX 生命周期与火球迁移

**Files:**
- Modify: `Assets/_Project/Scripts/Combat/Status/StatusController.cs`
- Modify: `Assets/_Project/Scripts/Combat/Projectiles/Fireball.cs`
- Optional Delete/Legacy: `Assets/_Project/Scripts/Combat/Status/BurnStatus.cs`

**Interfaces:**
- Consumes: `StatusController.ApplyStatus(StatusKind kind, float amount, int sourceId, byte sourceTeam)`
- Produces: Fireball applies `StatusKind.Burning`
- Produces: Status VFX instantiated once and destroyed when status ends

- [ ] **Step 1: Add runtime VFX slots to `StatusController`**

Add field:

```csharp
private readonly GameObject[] _vfxInstances = new GameObject[StatusCount];
```

- [ ] **Step 2: Instantiate VFX on first status activation**

In `ApplyStatus`, after writing `_instances[index]`, call:

```csharp
EnsureVfx(kind);
```

Add method:

```csharp
private void EnsureVfx(StatusKind kind)
{
    int index = ToIndex(kind);
    if (_vfxInstances[index] != null)
        return;

    StatusDefinition definition = _definitions[index];
    if (definition == null || definition.VfxPrefab == null)
        return;

    _vfxInstances[index] = Instantiate(definition.VfxPrefab, transform.position, transform.rotation, transform);
}
```

- [ ] **Step 3: Destroy VFX when status becomes inactive**

When `instance.Intensity <= 0f`, after setting inactive:

```csharp
DestroyVfx(kind);
```

Add method:

```csharp
private void DestroyVfx(StatusKind kind)
{
    int index = ToIndex(kind);
    if (_vfxInstances[index] == null)
        return;

    Destroy(_vfxInstances[index]);
    _vfxInstances[index] = null;
}
```

- [ ] **Step 4: Update `Fireball.cs` serialized fields**

Replace old burn fields:

```csharp
[Header("燃烧 (DoT)")]
[SerializeField] private GameObject _onFirePrefab;
[SerializeField] private float _burnDamagePerTick = 3f;
[SerializeField] private float _burnInterval = 0.5f;
[SerializeField] private float _burnDuration = 3f;
```

with:

```csharp
[Header("燃烧状态")]
[SerializeField, Min(0f)] private float _burningApplyAmount = 45f;
```

Reason: DoT damage / VFX / decay now comes from `StatusDefinition`, not Fireball.

- [ ] **Step 5: Replace `ApplyBurn` in `Fireball.cs`**

Replace method body:

```csharp
private void ApplyBurn(Collider hitCollider)
{
    HealthComponent health = hitCollider.GetComponentInParent<HealthComponent>();
    if (health == null) return;

    BurnStatus burn = health.GetComponent<BurnStatus>();
    if (burn == null) burn = health.gameObject.AddComponent<BurnStatus>();
    burn.Apply(_attackerId, _attackerTeam, _burnDamagePerTick, _type,
               _burnInterval, _burnDuration, _onFirePrefab);
}
```

with:

```csharp
private void ApplyBurn(Collider hitCollider)
{
    StatusController status = hitCollider.GetComponentInParent<StatusController>();
    if (status == null)
        return;

    status.ApplyStatus(StatusKind.Burning, _burningApplyAmount, _attackerId, _attackerTeam);
}
```

- [ ] **Step 6: Handle `BurnStatus.cs`**

If `rg -n "BurnStatus" Assets/_Project/Scripts Assets/_Project/Tests` only finds `BurnStatus.cs`, delete `BurnStatus.cs`.

If Unity scene/prefab references are uncertain, keep it but add `[System.Obsolete]`:

```csharp
[System.Obsolete("Use StatusController with StatusKind.Burning instead.")]
public class BurnStatus : MonoBehaviour
```

Recommended for this project: keep it as obsolete for one iteration, then remove after Editor confirms no references.

- [ ] **Step 7: Build Combat**

Run:

```powershell
dotnet build Game.Combat.csproj --no-restore /m:1
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
```

Expected:

```text
已成功生成。
0 个警告
0 个错误
```

---

## Task 5: 玩家 Sticky 移速接入

**Files:**
- Modify: `Assets/_Project/Scripts/Character/Controllers/PlayerControllerBase.cs`

**Interfaces:**
- Consumes: `StatusController.MoveSpeedMultiplier`
- Produces: player movement uses `MoveSpeed * statusMultiplier`

- [ ] **Step 1: Add StatusController field**

In `PlayerControllerBase`, add:

```csharp
private Game.Combat.StatusController _statusController;
```

Place near other component references:

```csharp
private CharacterController _characterController;
private Animator _animator;
private InputSystem_Actions _inputActions;
private GroundChecker _groundChecker;
private Camera _mainCamera;
private Game.Combat.StatusController _statusController;
```

- [ ] **Step 2: Cache component in Awake**

In `Awake`, after `_groundChecker = GetComponent<GroundChecker>();`, add:

```csharp
_statusController = GetComponent<Game.Combat.StatusController>();
```

- [ ] **Step 3: Add movement speed multiplier property**

Add public property:

```csharp
public float StatusMoveSpeedMultiplier => _statusController != null ? _statusController.MoveSpeedMultiplier : 1f;
```

- [ ] **Step 4: Apply multiplier where movement speed is used**

Search:

```powershell
rg -n "MoveSpeed|_moveSpeed" Assets/_Project/Scripts/Character
```

In shared movement states, replace direct movement speed usage:

```csharp
_player.MoveSpeed
```

with:

```csharp
_player.MoveSpeed * _player.StatusMoveSpeedMultiplier
```

Expected likely files:

```text
Assets/_Project/Scripts/Character/States/PlayerGroundedState.cs
Assets/_Project/Scripts/Character/States/PlayerAirborneState.cs
```

Do not apply Sticky to dash speed in first version. Dash remains a burst movement ability.

- [ ] **Step 5: Build Character**

Run:

```powershell
dotnet build Game.Character.csproj --no-restore /m:1
```

Expected:

```text
已成功生成。
0 个警告
0 个错误
```

---

## Task 6: 玩家状态 UI

**Files:**
- Create: `Assets/_Project/Scripts/UI/StatusIconView.cs`
- Create: `Assets/_Project/Scripts/UI/PlayerStatusBar.cs`

**Interfaces:**
- Consumes: `StatusChangedEvent`
- Consumes: `HealthComponent.Id`
- Produces: fixed status icon slots updated by event

- [ ] **Step 1: Create `StatusIconView.cs`**

```csharp
using UnityEngine;
using UnityEngine.UI;

namespace Game.UI
{
    public class StatusIconView : MonoBehaviour
    {
        [SerializeField] private Image _icon;
        [SerializeField] private Text _percentageText;

        public void Set(Sprite icon, float intensity)
        {
            if (_icon != null)
                _icon.sprite = icon;

            if (_percentageText != null)
                _percentageText.text = $"{Mathf.RoundToInt(intensity)}%";

            gameObject.SetActive(true);
        }

        public void Clear()
        {
            if (_percentageText != null)
                _percentageText.text = "";

            gameObject.SetActive(false);
        }
    }
}
```

If the project later switches status UI to TextMeshPro, replace `Text` with `TMP_Text` in a separate UI-only change.

- [ ] **Step 2: Create `PlayerStatusBar.cs`**

```csharp
using UnityEngine;
using Game.Core;
using Game.Combat;

namespace Game.UI
{
    public class PlayerStatusBar : MonoBehaviour
    {
        [SerializeField] private HealthComponent _playerHealth;
        [SerializeField] private StatusIconView _burningView;
        [SerializeField] private StatusIconView _wetView;
        [SerializeField] private StatusIconView _poisonedView;
        [SerializeField] private StatusIconView _stickyView;

        private void OnEnable()
        {
            EventBus<StatusChangedEvent>.Subscribe(OnStatusChanged);
        }

        private void OnDisable()
        {
            EventBus<StatusChangedEvent>.Unsubscribe(OnStatusChanged);
        }

        private void Start()
        {
            _burningView?.Clear();
            _wetView?.Clear();
            _poisonedView?.Clear();
            _stickyView?.Clear();
        }

        private void OnStatusChanged(StatusChangedEvent e)
        {
            if (_playerHealth == null || e.TargetId != _playerHealth.Id)
                return;

            StatusIconView view = GetView(e.Kind);
            if (view == null)
                return;

            if (e.IsActive)
                view.Set(e.Icon, e.Intensity);
            else
                view.Clear();
        }

        private StatusIconView GetView(StatusKind kind)
        {
            switch (kind)
            {
                case StatusKind.Burning:
                    return _burningView;
                case StatusKind.Wet:
                    return _wetView;
                case StatusKind.Poisoned:
                    return _poisonedView;
                case StatusKind.Sticky:
                    return _stickyView;
                default:
                    return null;
            }
        }
    }
}
```

- [ ] **Step 3: Build UI**

Run:

```powershell
dotnet build Game.UI.csproj --no-restore /m:1
```

Expected:

```text
已成功生成。
0 个警告
0 个错误
```

---

## Task 7: 全量验证与符号扫描

**Files:**
- No source changes unless verification reveals a compile issue.

- [ ] **Step 1: Run focused builds**

Run sequentially:

```powershell
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
dotnet build Game.Combat.csproj --no-restore /m:1
dotnet build Game.Character.csproj --no-restore /m:1
dotnet build Game.UI.csproj --no-restore /m:1
```

Expected for each:

```text
已成功生成。
0 个错误
```

- [ ] **Step 2: Scan status symbols**

Run:

```powershell
rg -n "StatusController|StatusDefinition|StatusDatabase|StatusChangedEvent|BurnStatus|ApplyStatus|MoveSpeedMultiplier" Assets/_Project/Scripts Assets/_Project/Tests
```

Expected:

```text
StatusController / StatusDefinition / StatusDatabase / StatusChangedEvent are present.
Fireball references ApplyStatus(StatusKind.Burning).
BurnStatus has no runtime references, or is marked obsolete.
PlayerControllerBase reads MoveSpeedMultiplier.
```

- [ ] **Step 3: Check git status**

Run:

```powershell
git status --short
```

Expected:

```text
Only intended source/test/plan files are modified or added.
No unexpected deletes.
No manual .meta creation from Codex.
```

---

## Task 8: Unity Editor 配置与 Play Mode 验证

**Files:**
- No code changes.
- User performs Unity Editor setup.

## 你需要在 Unity Editor 中做的事情

### 8.1 创建 Status ScriptableObject 资产

路径：

```text
Assets/_Project/ScriptableObjects/Status/
```

创建：

```text
Status_Burning.asset
Status_Wet.asset
Status_Poisoned.asset
Status_Sticky.asset
StatusDatabase.asset
```

配置建议：

```text
Status_Burning
Kind = Burning
DisplayName = 燃烧
Icon = 燃烧图标
NaturalDecayPerSecond = 8
DealsDamage = true
DamageInterval = 1
BaseDamagePerTick = 5
DamageType = Magical
TriggerHitReaction = false
VfxPrefab = OnFire
```

```text
Status_Wet
Kind = Wet
DisplayName = 潮湿
Icon = 潮湿图标
NaturalDecayPerSecond = 10
DealsDamage = false
AffectsMoveSpeed = false
```

```text
Status_Poisoned
Kind = Poisoned
DisplayName = 中毒
Icon = 中毒图标
NaturalDecayPerSecond = 6
DealsDamage = true
DamageInterval = 1
BaseDamagePerTick = 4
DamageType = Magical
TriggerHitReaction = false
```

```text
Status_Sticky
Kind = Sticky
DisplayName = 粘稠
Icon = 粘稠图标
NaturalDecayPerSecond = 8
DealsDamage = false
AffectsMoveSpeed = true
MaxMoveSpeedSlowRatio = 0.4
```

`StatusDatabase.asset`:

```text
Definitions[0] = Status_Burning
Definitions[1] = Status_Wet
Definitions[2] = Status_Poisoned
Definitions[3] = Status_Sticky
```

### 8.2 给角色挂 StatusController

给以下对象挂 `StatusController`：

```text
WizardPlayer
SwordPlayer
BowPlayer
所有需要吃状态的 Enemy prefab / scene enemy
```

每个 `StatusController` 的 Inspector：

```text
Database = StatusDatabase.asset
Wet Extinguish Burning Per Second = 35
```

注意：

```text
StatusController 应和 HealthComponent 挂在同一个根物体上。
```

原因：DoT 需要通过同物体的 `IDamageable / HealthComponent` 扣血，UI 也用同一个 `TargetId` 过滤事件。

### 8.3 配置 Fireball

打开火球 prefab 或火球脚本所在 prefab：

```text
Fireball
Burning Apply Amount = 45
```

旧字段 `_onFirePrefab / _burnDamagePerTick / _burnInterval / _burnDuration` 会不再使用；OnFire VFX 改在 `Status_Burning.asset` 中配置。

### 8.4 配置玩家状态 UI

在 PlayerHUD / Canvas 下创建：

```text
StatusBar
  BurningSlot
    Icon Image
    Percent Text
  WetSlot
    Icon Image
    Percent Text
  PoisonedSlot
    Icon Image
    Percent Text
  StickySlot
    Icon Image
    Percent Text
```

组件：

```text
StatusBar 挂 PlayerStatusBar
每个 Slot 挂 StatusIconView
PlayerStatusBar.PlayerHealth = 当前玩家 HealthComponent
PlayerStatusBar.BurningView = BurningSlot
PlayerStatusBar.WetView = WetSlot
PlayerStatusBar.PoisonedView = PoisonedSlot
PlayerStatusBar.StickyView = StickySlot
```

`StatusIconView`：

```text
Icon = slot 内的 Image
Percentage Text = slot 内的 Text
```

### 8.5 Play Mode 验证

测试 1：火球燃烧

```text
用 Fireball 命中敌人
```

预期：

```text
敌人获得 Burning
如果敌人有可见 VFX，显示 OnFire
敌人持续掉血
DoT 不让敌人每 tick 硬直
```

测试 2：玩家状态 UI

```text
让玩家获得 Burning
```

预期：

```text
血条下方 Burning icon 出现
百分比从初始值逐渐下降
归零后 icon 消失
```

测试 3：Wet 灭火

```text
给同一目标先 Apply Burning，再 Apply Wet
```

预期：

```text
Burning 百分比下降速度明显加快
Wet 自己也随时间下降
```

测试 4：Poisoned

```text
临时调试施加 Poisoned
```

预期：

```text
Poisoned icon 出现
目标持续掉血
状态归零后停止掉血
```

测试 5：Sticky

```text
给玩家施加 Sticky
```

预期：

```text
玩家移动速度降低
Dash 速度不受影响
Sticky 归零后移动速度恢复
```

### 8.6 常见问题排查

症状：

```text
火球命中后没有 Burning
```

检查：

```text
目标根物体是否挂 StatusController
目标根物体是否挂 HealthComponent
StatusController.Database 是否拖入 StatusDatabase
Fireball.Burning Apply Amount 是否大于 0
```

症状：

```text
Burning 有状态但不扣血
```

检查：

```text
Status_Burning.DealsDamage 是否勾选
BaseDamagePerTick 是否大于 0
DamageInterval 是否大于 0
StatusController 是否和 HealthComponent 在同一根物体
```

症状：

```text
UI 不显示状态
```

检查：

```text
PlayerStatusBar.PlayerHealth 是否拖入当前玩家 HealthComponent
StatusIconView 的 Icon / Text 是否拖入
StatusDefinition.Icon 是否配置
状态事件的 TargetId 是否和玩家 HealthComponent.Id 一致
```

症状：

```text
Sticky 不减速
```

检查：

```text
Status_Sticky.AffectsMoveSpeed 是否勾选
MaxMoveSpeedSlowRatio 是否大于 0
玩家根物体是否挂 StatusController
PlayerControllerBase 是否成功缓存 StatusController
```

---

## Self-Review

- [x] Spec coverage: 覆盖百分比模型、累加强度 clamp、四种状态、Wet 灭火、DoT、Sticky 移速、UI、Editor 配置、测试策略。
- [x] Scope check: 本计划不实现 voxel sandbox、不实现 Element Zone、不实现复杂 reaction graph。
- [x] Type consistency: `StatusKind`、`StatusDefinition`、`StatusDatabase`、`StatusController`、`StatusChangedEvent`、`MoveSpeedMultiplier` 命名一致。
- [x] Architecture boundary: `Game.Combat` 只依赖 Core/Unity；`Game.UI` 通过 EventBus 读状态变化；`Game.Character` 只读取 Combat 的 `MoveSpeedMultiplier`。
- [x] Placeholder scan: 没有 `TBD`、`TODO`、`待定` 占位。
