# 可交互护盾法术 Implementation Plan（修正版：Emit 型护盾投射物）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 把上一版“可交互静态护盾”修正为“Emit 型护盾投射物”：护盾像火球一样从法杖发射出去，并在飞行中反弹其他投射物。

**Architecture:** 新护盾不再走 `StaticProjectile + StaticAtPoint + SpellPhysicsBody`，而是走 `SpellKind.Emit + SpellSpawnMode.ForwardProjectile + ShieldProjectile : ProjectileBase`。旧静态护盾继续保留，`ProjectileShield` 作为通用反弹组件复用给旧护盾和新护盾。

**Tech Stack:** Unity 6.3, C#, `ScriptableObject`, `ProjectileBase`, `Rigidbody`, `Collider`, `OnTriggerEnter`, `Vector3.Reflect`, EditMode NUnit tests.

## Global Constraints

- 直接在本地工作区修改，不使用 git worktree。
- 手工编辑使用 `apply_patch`。
- 不手动创建 `.meta` 文件。
- 不新增 `SpellKind`。
- 不新增 `SpellSpawnMode`。
- 新护盾必须配置为 `Kind=Emit`、`SpawnMode=ForwardProjectile`。
- 旧护盾必须保持 `Kind=StaticProjectile`、`SpawnMode=StaticAtPoint`，行为不回退。
- `Game.Skills` 只做数据和解释器，不接触场景 runtime。
- `Game.Combat` 不依赖 `Game.Skills` 或 `Game.Character`。
- 日志使用 `GameLog`，不使用 `Debug.Log`。
- 不在 `Update` / `FixedUpdate` 热路径引入 LINQ、闭包或每帧分配。

---

## File Structure

### Delete / Revert

- Delete: `Assets/_Project/Scripts/Combat/Projectiles/ISpellMotionTarget.cs`
  - 上一版为了让静态护盾吃运动修正而新增；本版不需要。

- Delete: `Assets/_Project/Scripts/Combat/Projectiles/SpellPhysicsBody.cs`
  - 上一版的非伤害型运动体；本版由 `ShieldProjectile : ProjectileBase` 复用原投射物运动系统。

### Create

- Create: `Assets/_Project/Scripts/Combat/Projectiles/ShieldProjectile.cs`
  - Emit 型护盾投射物，继承 `ProjectileBase`，但重写碰撞默认行为，避免像火球一样命中就爆炸/销毁。

### Modify

- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs`
  - 保留 `ReflectByShield` 和 `RefreshSameTeamProjectileIgnores`。
  - 移除上一版 `ISpellMotionTarget` 接口实现。
  - 增加一个 collision seam，让 `ShieldProjectile` 可以拦截默认命中销毁逻辑。

- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileShield.cs`
  - 保留旧静态护盾初始化。
  - 支持绑定 `ShieldProjectile` owner，或从父级 `ShieldProjectile` 读取 team / attackerId。
  - 继续使用 Trigger 反弹其他 `ProjectileBase`。

- Modify: `Assets/_Project/Scripts/Character/Spells/SpellCaster.cs`
  - 撤销上一版 `ISpellMotionTarget` 相关逻辑。
  - 普通 Emit 仍走 `SpawnForwardProjectile`。
  - `StaticAtPoint` 只负责旧静态护盾，不再给静态对象配置运动修正。

- Modify: `Assets/_Project/Scripts/Skills/SpellDefinition.cs`
  - 保留 `ShieldReflectCount`。
  - `ShieldReflectImpulse` 可删除，或保留为后续高级参数。本计划推荐删除，减少无用字段。

- Modify: `Assets/_Project/Scripts/Skills/EmitCommand.cs`
  - 保留 `ShieldReflectCount`。
  - 删除 `ShieldReflectImpulse`。

- Modify: `Assets/_Project/Scripts/Skills/CastEvaluator.cs`
  - 删除 `ShieldReflectImpulse` bake。
  - 确保普通 `Emit` 也能携带 `ShieldReflectCount`，供 `ShieldProjectile` 初始化使用。

- Modify: `Assets/_Project/Tests/Skills/CastEvaluatorTests.cs`
  - 删除 `ShieldReflectImpulse` 测试。
  - 增加测试：`Kind=Emit` 的护盾法术能 bake 出 `SpawnMode=ForwardProjectile` 和 `ShieldReflectCount`。

---

## Task 1: 回滚上一版静态运动体路线

**Files:**
- Delete: `Assets/_Project/Scripts/Combat/Projectiles/ISpellMotionTarget.cs`
- Delete: `Assets/_Project/Scripts/Combat/Projectiles/SpellPhysicsBody.cs`
- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs`
- Modify: `Assets/_Project/Scripts/Character/Spells/SpellCaster.cs`

**Interfaces:**
- Removes: `ISpellMotionTarget`
- Removes: `SpellPhysicsBody`
- Restores: `SpellCaster.ConfigureProjectileMotion(ProjectileBase proj, EmitCommand cmd, int index, int count)`

- [x] **Step 1: 删除上一版接口文件**

删除：

```text
Assets/_Project/Scripts/Combat/Projectiles/ISpellMotionTarget.cs
```

- [x] **Step 2: 删除上一版运动体文件**

删除：

```text
Assets/_Project/Scripts/Combat/Projectiles/SpellPhysicsBody.cs
```

- [x] **Step 3: ProjectileBase 移除接口实现**

把：

```csharp
public abstract class ProjectileBase : MonoBehaviour, ISpellMotionTarget
```

改回：

```csharp
public abstract class ProjectileBase : MonoBehaviour
```

删除：

```csharp
public void InitSpellMotion(byte team, int attackerId, float damage, DamageType damageType,
                            Vector3 velocity, Collider casterCollider, bool useGravity)
{
    Init(team, attackerId, damage, damageType, velocity, casterCollider, useGravity);
}
```

- [x] **Step 4: SpellCaster 恢复 ProjectileBase 专用运动配置**

把方法：

```csharp
private void ConfigureSpellMotionTarget(ISpellMotionTarget target, EmitCommand cmd, int index, int count)
```

改回：

```csharp
private void ConfigureProjectileMotion(ProjectileBase proj, EmitCommand cmd, int index, int count)
```

方法体中把 `target.` 全部改回 `proj.`。

- [x] **Step 5: SpawnForwardProjectile 恢复 ProjectileBase Init**

把：

```csharp
ConfigureSpellMotionTarget(proj, cmd, index, count);
proj.InitSpellMotion(team, attackerId, cmd.Damage, cmd.DamageType, dir * cmd.Speed, casterCollider, cmd.UseGravity);
```

改回：

```csharp
ConfigureProjectileMotion(proj, cmd, index, count);
proj.Init(team, attackerId, cmd.Damage, cmd.DamageType, dir * cmd.Speed, casterCollider, useGravity: cmd.UseGravity);
```

- [x] **Step 6: SpawnSkyfallProjectileNow 恢复 ProjectileBase Init**

把：

```csharp
ConfigureSpellMotionTarget(proj, cmd, index, count);
proj.InitSpellMotion(team, attackerId, cmd.Damage, cmd.DamageType, fallDir * cmd.Speed, casterCollider, cmd.UseGravity);
```

改回：

```csharp
ConfigureProjectileMotion(proj, cmd, index, count);
proj.Init(team, attackerId, cmd.Damage, cmd.DamageType, fallDir * cmd.Speed, casterCollider, useGravity: cmd.UseGravity);
```

- [x] **Step 7: StaticAtPoint 只初始化旧静态护盾**

`SpawnStaticProjectileAtPoint` 保持或恢复为：

```csharp
private void SpawnStaticProjectileAtPoint(EmitCommand cmd, Vector3 spawnPos, Vector3 baseDir,
                                          byte team, int attackerId, Collider casterCollider)
{
    Quaternion rotation = cmd.ProjectilePrefab.transform.rotation;
    if (baseDir.sqrMagnitude > 1e-6f)
        rotation = Quaternion.LookRotation(baseDir.normalized) * rotation;

    GameObject go = Object.Instantiate(cmd.ProjectilePrefab, spawnPos, rotation);
    ProjectileShield shield = go.GetComponent<ProjectileShield>();
    if (shield == null)
        shield = go.GetComponentInChildren<ProjectileShield>();

    if (shield != null)
    {
        shield.Init(team, attackerId, cmd.ShieldReflectCount, casterCollider);
        return;
    }

    GameLog.Warn($"StaticAtPoint 预制体 {cmd.ProjectilePrefab.name} 上没有 ProjectileShield 组件", "Skills");
}
```

`RunCast` 中 `StaticAtPoint` 分支调用也恢复为：

```csharp
SpawnStaticProjectileAtPoint(cmd, spawnPos, baseDir, team, attackerId, casterCollider);
```

- [x] **Step 8: 验证 Task 1**

运行：

```powershell
dotnet build Game.Combat.csproj --no-restore /m:1
dotnet build Game.Character.csproj --no-restore /m:1
```

预期：如果尚未新增 `ShieldProjectile`，仍应能通过，因为只是回滚上一版接口路线。

---

## Task 2: 清理 ShieldReflectImpulse 数据链路

**Files:**
- Modify: `Assets/_Project/Scripts/Skills/SpellDefinition.cs`
- Modify: `Assets/_Project/Scripts/Skills/EmitCommand.cs`
- Modify: `Assets/_Project/Scripts/Skills/CastEvaluator.cs`
- Modify: `Assets/_Project/Tests/Skills/CastEvaluatorTests.cs`

**Interfaces:**
- Removes: `SpellDefinition.ShieldReflectImpulse`
- Removes: `EmitCommand.ShieldReflectImpulse`
- Keeps: `SpellDefinition.ShieldReflectCount`
- Keeps: `EmitCommand.ShieldReflectCount`

- [x] **Step 1: SpellDefinition 删除 ShieldReflectImpulse**

删除字段：

```csharp
[Min(0f)]
[Tooltip("保护盾每次成功反弹投射物时，施加给护盾自身 Rigidbody 的瞬时冲量。0 表示只反弹投射物，不推动护盾。")]
public float ShieldReflectImpulse = 0f;
```

保留：

```csharp
public int ShieldReflectCount = 3;
```

- [x] **Step 2: EmitCommand 删除 ShieldReflectImpulse**

删除字段：

```csharp
public readonly float ShieldReflectImpulse;
```

构造函数删除参数：

```csharp
float shieldReflectImpulse
```

删除赋值：

```csharp
ShieldReflectImpulse = shieldReflectImpulse;
```

- [x] **Step 3: CastEvaluator 删除 ShieldReflectImpulse bake**

把：

```csharp
spell.ShieldReflectCount, spell.ShieldReflectImpulse,
```

改回：

```csharp
spell.ShieldReflectCount,
```

- [x] **Step 4: CastEvaluatorTests 清理 helper**

把：

```csharp
private static SpellDefinition Shield(int reflectCount = 3, float mana = 0f, float reflectImpulse = 0f)
```

改回：

```csharp
private static SpellDefinition Shield(int reflectCount = 3, float mana = 0f)
```

删除：

```csharp
s.ShieldReflectImpulse = reflectImpulse;
```

- [x] **Step 5: 删除冲量相关测试**

删除测试：

```csharp
StaticProjectile_Shield_BakesReflectImpulse
```

并删除其他测试里对：

```csharp
ShieldReflectImpulse
reflectImpulse
```

的断言或参数。

- [x] **Step 6: 验证 Task 2**

运行：

```powershell
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

预期：Build succeeded。

---

## Task 3: 为 ProjectileBase 增加碰撞生命周期 seam

**Files:**
- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs`

**Interfaces:**
- Produces: `protected virtual bool TryHandleCollisionBeforeDefault(Collision collision, IDamageable target)`

`ShieldProjectile` 需要飞行，但不应像火球一样撞到东西就走默认伤害 / OnImpact / Destroy。  
所以要在 `ProjectileBase.OnCollisionEnter` 中给子类一个拦截点。

- [x] **Step 1: 新增虚方法**

在 `ProjectileBase` 中新增：

```csharp
protected virtual bool TryHandleCollisionBeforeDefault(Collision collision, IDamageable target)
{
    return false;
}
```

- [x] **Step 2: 在 OnCollisionEnter 中调用 seam**

在 `TryBounce(collision, target)` 之后，默认伤害逻辑之前，加入：

```csharp
if (TryHandleCollisionBeforeDefault(collision, target))
    return;
```

目标结构：

```csharp
if (target != null && target.TeamId == _attackerTeam)
{
    ...
    return;
}

if (TryBounce(collision, target))
    return;

if (TryHandleCollisionBeforeDefault(collision, target))
    return;

Vector3 hitPoint = collision.GetContact(0).point;
...
```

- [x] **Step 3: 验证 Task 3**

运行：

```powershell
dotnet build Game.Combat.csproj --no-restore /m:1
```

预期：Build succeeded。

---

## Task 4: 新增 ShieldProjectile

**Files:**
- Create: `Assets/_Project/Scripts/Combat/Projectiles/ShieldProjectile.cs`
- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileShield.cs`
- Modify: `Game.Combat.csproj` if Unity has not refreshed it

**Interfaces:**
- Produces: `public sealed class ShieldProjectile : ProjectileBase`
- Consumes: `ProjectileShield.Init(byte ownerTeam, int ownerAttackerId, int reflectCount, Collider ownerCollider)`
- Consumes: `ProjectileBase.ReflectByShield(Vector3 shieldNormal, byte newTeam, int newAttackerId)`

- [x] **Step 1: 让 ProjectileBase.Init 可重写**

把 `ProjectileBase.Init` 从：

```csharp
public void Init(byte attackerTeam, int attackerId, float damage, DamageType type,
                 Vector3 velocity, Collider casterCollider, bool useGravity = true)
```

改为：

```csharp
public virtual void Init(byte attackerTeam, int attackerId, float damage, DamageType type,
                         Vector3 velocity, Collider casterCollider, bool useGravity = true)
```

这样 `ShieldProjectile` 可以在 `base.Init` 后初始化自己的 `ProjectileShield`。

- [x] **Step 2: 新建 ShieldProjectile.cs**

创建：

```text
Assets/_Project/Scripts/Combat/Projectiles/ShieldProjectile.cs
```

内容：

```csharp
using UnityEngine;

namespace Game.Combat
{
    public sealed class ShieldProjectile : ProjectileBase
    {
        [SerializeField] private ProjectileShield _shield;

        private int _reflectCount;

        protected override void Awake()
        {
            base.Awake();

            if (_shield == null)
                _shield = GetComponentInChildren<ProjectileShield>();
        }

        public void ConfigureShield(int reflectCount)
        {
            _reflectCount = Mathf.Max(0, reflectCount);
        }

        public override void Init(byte attackerTeam, int attackerId, float damage, DamageType type,
                                  Vector3 velocity, Collider casterCollider, bool useGravity = true)
        {
            base.Init(attackerTeam, attackerId, damage, type, velocity, casterCollider, useGravity);

            if (_shield == null)
                _shield = GetComponentInChildren<ProjectileShield>();

            if (_shield != null)
                _shield.Init(attackerTeam, attackerId, _reflectCount, casterCollider);
        }

        protected override bool TryHandleCollisionBeforeDefault(Collision collision, IDamageable target)
        {
            return true;
        }
    }
}
```

- [x] **Step 3: ProjectileShield 保持通用**

`ProjectileShield` 不需要知道自己挂在旧静态护盾还是新护盾投射物上。  
它只需要：

```csharp
Init(ownerTeam, ownerAttackerId, reflectCount, ownerCollider)
```

然后在 `OnTriggerEnter` 中调用：

```csharp
projectile.ReflectByShield(normal, _ownerTeam, _ownerAttackerId)
```

上一版的 `reflectImpulse` 参数应删除，保持简单。

- [x] **Step 4: 如 csproj 未刷新，手动 include**

如果本地 `dotnet build Game.Combat.csproj` 找不到 `ShieldProjectile`，在 `Game.Combat.csproj` 添加：

```xml
<Compile Include="Assets\_Project\Scripts\Combat\Projectiles\ShieldProjectile.cs" />
```

- [x] **Step 5: 验证 Task 4**

运行：

```powershell
dotnet build Game.Combat.csproj --no-restore /m:1
```

预期：Build succeeded。

---

## Task 5: SpellCaster 给 ShieldProjectile 注入反弹次数

**Files:**
- Modify: `Assets/_Project/Scripts/Character/Spells/SpellCaster.cs`

**Interfaces:**
- Consumes: `ShieldProjectile.ConfigureShield(int reflectCount)`
- Consumes: `EmitCommand.ShieldReflectCount`

`ShieldProjectile` 是普通 `Emit`，所以它会走 `SpawnForwardProjectile`。  
在调用 `proj.Init` 之前，需要给它配置反弹次数。

- [x] **Step 1: 在 SpawnForwardProjectile 中识别 ShieldProjectile**

在：

```csharp
WirePayload(proj, cmd, team, attackerId, casterCollider);
ConfigureProjectileMotion(proj, cmd, index, count);
proj.Init(...);
```

之前加入：

```csharp
if (proj is ShieldProjectile shieldProjectile)
    shieldProjectile.ConfigureShield(cmd.ShieldReflectCount);
```

目标结构：

```csharp
WirePayload(proj, cmd, team, attackerId, casterCollider);
ConfigureProjectileMotion(proj, cmd, index, count);
if (proj is ShieldProjectile shieldProjectile)
    shieldProjectile.ConfigureShield(cmd.ShieldReflectCount);
proj.Init(team, attackerId, cmd.Damage, cmd.DamageType, dir * cmd.Speed, casterCollider, useGravity: cmd.UseGravity);
```

- [x] **Step 2: Skyfall 不需要配置 ShieldProjectile**

`ShieldProjectile` 不应作为 `SkyfallAtPoint` 使用。  
不在 `SpawnSkyfallProjectileNow` 加特殊处理。

- [x] **Step 3: 验证 Task 5**

运行：

```powershell
dotnet build Game.Character.csproj --no-restore /m:1
```

预期：Build succeeded。

---

## Task 6: 更新 Skills 纯逻辑测试

**Files:**
- Modify: `Assets/_Project/Tests/Skills/CastEvaluatorTests.cs`

**Interfaces:**
- Consumes: `ShieldReflectCount`
- Produces: tests proving Emit shield data bakes correctly

- [x] **Step 1: 新增 Emit 型护盾 helper**

在 `CastEvaluatorTests` 中新增：

```csharp
private static SpellDefinition ShieldEmit(int reflectCount = 3, float speed = 10f, float mana = 0f)
{
    var s = ScriptableObject.CreateInstance<SpellDefinition>();
    s.Kind = SpellKind.Emit;
    s.SpawnMode = SpellSpawnMode.ForwardProjectile;
    s.BaseDamage = 0f;
    s.BaseSpeed = speed;
    s.DamageType = DamageType.Magical;
    s.ManaCost = mana;
    s.ShieldReflectCount = reflectCount;
    return s;
}
```

- [x] **Step 2: 新增测试：Emit 护盾 bake 为 ForwardProjectile**

```csharp
[Test]
public void EmitShield_BakesForwardProjectileAndReflectCount()
{
    Run(1, 999f, ShieldEmit(reflectCount: 5, speed: 12f));

    Assert.AreEqual(1, _out.Count);
    Assert.AreEqual(SpellSpawnMode.ForwardProjectile, _out[0].SpawnMode);
    Assert.AreEqual(5, _out[0].ShieldReflectCount);
    Assert.AreEqual(12f, _out[0].Speed, 1e-4f);
}
```

- [x] **Step 3: 新增测试：Modify 影响 Emit 护盾**

```csharp
[Test]
public void EmitShield_BakesMotionModifiers()
{
    Run(1, 999f,
        Mod(bounce: 2, useGravity: true, homingRadius: 6f, homingDuration: 0.6f, homingTurnRate: 120f),
        ShieldEmit(reflectCount: 3, speed: 10f));

    Assert.AreEqual(1, _out.Count);
    Assert.AreEqual(SpellSpawnMode.ForwardProjectile, _out[0].SpawnMode);
    Assert.AreEqual(2, _out[0].BounceCount);
    Assert.IsTrue(_out[0].UseGravity);
    Assert.AreEqual(6f, _out[0].HomingRadius, 1e-4f);
    Assert.AreEqual(0.6f, _out[0].HomingDuration, 1e-4f);
    Assert.AreEqual(120f, _out[0].HomingTurnRateDegrees, 1e-4f);
}
```

- [x] **Step 4: 保留旧静态护盾测试**

保留：

```text
StaticProjectile_Shield_BakesStaticAtPointAndReflectCount
ShieldStaticProjectile_ConsumesDrawBudgetLikeOtherStaticProjectiles
TriggerPayload_CanCaptureShieldStaticProjectile
```

这些测试证明旧静态护盾没有被新方案破坏。

- [x] **Step 5: 验证 Task 6**

运行：

```powershell
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

预期：Build succeeded。

---

## Task 7: 全量构建与符号检查

**Files:**
- No source changes unless verification reveals an issue.

- [x] **Step 1: 顺序构建**

不要并行跑三个 build，避免 `obj/Debug/Game.Combat.dll` 文件锁。

```powershell
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
dotnet build Game.Combat.csproj --no-restore /m:1
dotnet build Game.Character.csproj --no-restore /m:1
```

预期：三项都是 `0 个警告，0 个错误`。

- [x] **Step 2: 符号搜索**

```powershell
rg -n "ShieldProjectile|ConfigureShield|TryHandleCollisionBeforeDefault|ISpellMotionTarget|SpellPhysicsBody|ShieldReflectImpulse" Assets/_Project/Scripts Assets/_Project/Tests
```

预期：

- 能找到 `ShieldProjectile`。
- 能找到 `ConfigureShield`。
- 能找到 `TryHandleCollisionBeforeDefault`。
- 不应再找到 `ISpellMotionTarget`。
- 不应再找到 `SpellPhysicsBody`。
- 不应再找到 `ShieldReflectImpulse`。

---

## Task 8: Unity Editor 配置与 Play Mode 验证

**Files:**
- No code changes.
- 由用户在 Unity Editor 中配置 prefab 和 ScriptableObject。

## 你需要在 Unity Editor 中做的事情

### 8.1 新建护盾投射物 prefab

Prefab 推荐结构：

```text
ShieldProjectileRoot
  Rigidbody
  SphereCollider（非 Trigger，满足 ProjectileBase 要求，也可用于环境碰撞）
  ShieldProjectile
  Visual
  ShieldReflectTrigger
    SphereCollider（Is Trigger = true）
    ProjectileShield
```

Root 设置：

```text
Rigidbody.Use Gravity = 默认 false，由重力修正运行时打开
Rigidbody.Collision Detection = Continuous Dynamic
Rigidbody.Interpolate = Interpolate
SphereCollider.Is Trigger = false
```

子 Trigger：

```text
ShieldReflectTrigger.SphereCollider.Is Trigger = true
半径略大于视觉护盾球体
ProjectileShield 挂在该子物体
```

### 8.2 新建护盾投射物 SpellDefinition

建议命名：

```text
Spell_Emit_ShieldProjectile
```

字段：

```text
Kind = Emit
SpawnMode = ForwardProjectile
Display Name = 护盾投射物
Projectile Prefab = ShieldProjectile prefab
Base Damage = 0
Base Speed = 8 到 14 先试
Damage Type = Magical
Shield Reflect Count = 3
Mana Cost = 根据强度设置
Icon = 新图标
```

### 8.3 SpellLibrary

把 `Spell_Emit_ShieldProjectile` 加入 `SpellLibrary`。

旧的 `Spell_Static_Shield` 不要删除，用于保留原地护盾玩法。

### 8.4 Play Mode 验证

测试 1：旧静态护盾

```text
旧护盾
```

预期：

- 仍然原地生成。
- 仍然反弹投射物。

测试 2：新护盾投射物

```text
护盾投射物
```

预期：

- 从法杖方向飞出。
- 飞行中反弹玩家/敌方投射物。
- 反弹次数耗尽后销毁。

测试 3：重力修正

```text
重力修正 -> 护盾投射物
```

预期：

- 护盾投射物飞出后受重力下坠。

测试 4：弹跳修正

```text
弹跳修正 -> 护盾投射物
```

预期：

- 撞到环境后弹跳，不像火球一样爆炸。

测试 5：追踪修正

```text
追踪修正 -> 护盾投射物
```

预期：

- 检测敌人后朝敌人方向偏移。

测试 6：轨道修正 + 三重射击

```text
轨道修正 -> 三重射击 -> 护盾投射物 -> 护盾投射物 -> 护盾投射物
```

预期：

- 三个护盾投射物以错开的相位/轨道面飞行。

测试 7：触发火球联动

```text
触发火球 -> 护盾投射物
```

预期：

- 火球命中后，护盾从命中点沿命中方向飞出。

---

## Self-Review

- [x] Spec coverage: 已覆盖用户指出的设计错误：移动护盾应为 `Emit`，不是 `StaticProjectile`。
- [x] Placeholder scan: 没有 `TBD` / `TODO` / “后续补充”。
- [x] Type consistency: `ShieldProjectile`、`ConfigureShield`、`TryHandleCollisionBeforeDefault`、`ShieldReflectCount` 命名一致。
- [x] Architecture boundary: `Game.Skills` 只保留数据；`Game.Combat` 负责护盾投射物和反弹；`Game.Character.SpellCaster` 只负责初始化。
- [x] Editor checklist: 已明确新 prefab、SpellDefinition、SpellLibrary 和 Play Mode 验证。
