# P0 可持续开发基线实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 为现有法术编程系统增加结构化求值 Trace、投射物 Scene Gizmos 和可重复运行的 `SpellPerformanceLab`，让逻辑、空间和性能问题都能被定位，同时不改变任何 Gameplay 语义。

**架构：** `Game.Skills` 只记录纯求值事实，`Game.Character.SpellCaster` 决定是否格式化到 Console，`Game.Combat` 暴露投射物只读诊断快照并绘制 Gizmos。独立 Performance Lab 使用真实 `SpellCaster -> CastEvaluator -> ProjectileBase` 链路，不复制测试用施法逻辑。

**技术栈：** Unity 6.3、C#、NUnit EditMode Tests、`GameLog`、Unity `Gizmos`、`Unity.Profiling.ProfilerMarker`、`Rigidbody`、Unity Profiler。

**设计依据：** `docs/superpowers/specs/2026-07-10-P0可持续开发基线设计.md`

## 全局约束

- 直接在本地 `main` 工作区执行，不创建 branch 或 worktree。
- 使用 `apply_patch` 手工编辑文件。
- 不手工创建 `.meta` 或 `.unity`；Unity Editor 负责生成和保存。
- 不改变 `CastEvaluator`、mana、payload、投射物运动或碰撞的 Gameplay 语义。
- `Game.Combat` 不得引用 `Game.Skills` 或 `Game.Character`。
- `Game.Skills` 不得调用 `Instantiate`、查找场景对象或直接打印 Console。
- 只使用 `GameLog`，禁止直接调用 `Debug.Log`。
- `Update` / `FixedUpdate` 热路径禁止 LINQ、closure、boxing 和每帧 `new`。
- Trace 默认 `Off`；正式性能基线要求 Trace、Gizmos、Deep Profile 全部关闭。
- 每一阶段完成编译后，等待用户在 Unity Editor 验证通过，才能继续下一阶段。

---

## 文件结构与职责

### 新增文件

| 文件 | 职责 |
|---|---|
| `Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStepKind.cs` | Trace 步骤枚举 |
| `Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStep.cs` | 一次求值步骤的只读快照 |
| `Assets/_Project/Scripts/Skills/Diagnostics/CastTraceCollector.cs` | 预分配并复用 Trace 容器 |
| `Assets/_Project/Scripts/Character/Spells/CastTraceLevel.cs` | `Off / Summary / Detailed` |
| `Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugSnapshot.cs` | 投射物只读空间快照 |
| `Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugGizmoDrawer.cs` | Scene View 调试绘制 |
| `Assets/_Project/Scripts/Character/_Debug/SpellPerformanceLabController.cs` | 标准阶梯和自定义压力测试 |
| `Assets/_Project/Tests/Skills/CastTraceTests.cs` | Trace 与求值等价性测试 |
| `Assets/_Project/Tests/Combat/ProjectileDebugSnapshotTests.cs` | ActiveCount 与 Snapshot 测试 |

### 修改文件

| 文件 | 改动 |
|---|---|
| `Assets/_Project/Scripts/Skills/CastEvaluator.cs` | 唯一 `EvaluateCore`、Trace 和 `Spell.Evaluate` Marker |
| `Assets/_Project/Scripts/Character/Spells/SpellCaster.cs` | Trace Level、CastId/Depth、Formatter、`Spell.RuntimeSpawn` Marker |
| `Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs` | ActiveCount、Snapshot、反射数据、`Projectile.Motion` Marker |

---

# 第一阶段：结构化 Cast Trace

## Task 1：建立 Trace 数据模型与 Collector

**Files:**

- Create: `Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStepKind.cs`
- Create: `Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStep.cs`
- Create: `Assets/_Project/Scripts/Skills/Diagnostics/CastTraceCollector.cs`
- Create: `Assets/_Project/Tests/Skills/CastTraceTests.cs`

**Interfaces:**

- Produces: `CastTraceStepKind`
- Produces: `CastTraceStep`
- Produces: `CastTraceCollector(int initialCapacity = 64)`
- Produces: `Record(CastTraceStep)`、`Clear()`、`Count`、indexer

- [ ] **Step 1：先写 Collector 的失败测试**

创建 `CastTraceTests.cs`：

```csharp
using NUnit.Framework;
using Game.Skills;

namespace Game.Skills.Tests
{
    public class CastTraceTests
    {
        [Test]
        public void Collector_RecordAndClear_ReusesLogicalBuffer()
        {
            var collector = new CastTraceCollector(4);
            var step = new CastTraceStep(
                CastTraceStepKind.CastStarted,
                -1,
                null,
                1,
                1,
                10f,
                10f,
                CastModifierState.Default,
                CastModifierState.Default);

            collector.Record(step);
            Assert.AreEqual(1, collector.Count);
            Assert.AreEqual(CastTraceStepKind.CastStarted, collector[0].Kind);

            collector.Clear();
            Assert.AreEqual(0, collector.Count);
            Assert.IsFalse(collector.ExpandedDuringLastCollection);
        }

        [Test]
        public void Collector_Constructor_ClampsNegativeCapacity()
        {
            Assert.DoesNotThrow(() => new CastTraceCollector(-10));
        }

        [Test]
        public void Collector_WhenCapacityIsExceeded_ReportsExpansion()
        {
            var collector = new CastTraceCollector(1);
            var step = new CastTraceStep(
                CastTraceStepKind.CastStarted, -1, null,
                1, 1, 10f, 10f,
                CastModifierState.Default, CastModifierState.Default);

            collector.Record(step);
            collector.Record(step);

            Assert.IsTrue(collector.ExpandedDuringLastCollection);
        }
    }
}
```

- [ ] **Step 2：刷新 Unity 项目文件后验证测试先失败**

```powershell
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

Expected：FAIL，缺少 `CastTraceCollector` 或 `CastTraceStep`。

- [ ] **Step 3：实现 CastTraceStepKind**

```csharp
namespace Game.Skills
{
    public enum CastTraceStepKind : byte
    {
        CastStarted = 0,
        NullSpellSkipped = 1,
        ModifyApplied = 2,
        MulticastApplied = 3,
        DrawBudgetBlocked = 4,
        EmitProduced = 5,
        PayloadCaptured = 6,
        ManaFizzle = 7,
        CastCompleted = 8
    }
}
```

`DrawBudgetBlocked` 不能省略，否则预算为 0 的 Emit 会在 Detailed Trace 中静默消失。

- [ ] **Step 4：实现 CastTraceStep**

```csharp
namespace Game.Skills
{
    public readonly struct CastTraceStep
    {
        public readonly CastTraceStepKind Kind;
        public readonly int SpellIndex;
        public readonly SpellDefinition Spell;
        public readonly int DrawBudgetBefore;
        public readonly int DrawBudgetAfter;
        public readonly float ManaLeftBefore;
        public readonly float ManaLeftAfter;
        public readonly CastModifierState ModifiersBefore;
        public readonly CastModifierState ModifiersAfter;
        public readonly int EmitIndex;
        public readonly EmitCommand Emit;
        public readonly int PayloadStartIndex;
        public readonly int PayloadCount;
        public readonly PayloadTriggerMode PayloadTrigger;

        public CastTraceStep(
            CastTraceStepKind kind,
            int spellIndex,
            SpellDefinition spell,
            int drawBudgetBefore,
            int drawBudgetAfter,
            float manaLeftBefore,
            float manaLeftAfter,
            CastModifierState modifiersBefore,
            CastModifierState modifiersAfter,
            int emitIndex = -1,
            EmitCommand emit = default,
            int payloadStartIndex = -1,
            int payloadCount = 0,
            PayloadTriggerMode payloadTrigger = PayloadTriggerMode.None)
        {
            Kind = kind;
            SpellIndex = spellIndex;
            Spell = spell;
            DrawBudgetBefore = drawBudgetBefore;
            DrawBudgetAfter = drawBudgetAfter;
            ManaLeftBefore = manaLeftBefore;
            ManaLeftAfter = manaLeftAfter;
            ModifiersBefore = modifiersBefore;
            ModifiersAfter = modifiersAfter;
            EmitIndex = emitIndex;
            Emit = emit;
            PayloadStartIndex = payloadStartIndex;
            PayloadCount = payloadCount;
            PayloadTrigger = payloadTrigger;
        }
    }
}
```

- [ ] **Step 5：实现可复用 Collector**

```csharp
using System.Collections.Generic;

namespace Game.Skills
{
    public sealed class CastTraceCollector
    {
        private readonly List<CastTraceStep> _steps;

        public int Count => _steps.Count;
        public CastTraceStep this[int index] => _steps[index];
        public IReadOnlyList<CastTraceStep> Steps => _steps;
        public bool ExpandedDuringLastCollection { get; private set; }

        public CastTraceCollector(int initialCapacity = 64)
        {
            _steps = new List<CastTraceStep>(initialCapacity > 0 ? initialCapacity : 0);
        }

        public void Clear()
        {
            _steps.Clear();
            ExpandedDuringLastCollection = false;
        }

        public void Record(CastTraceStep step)
        {
            if (_steps.Count == _steps.Capacity)
                ExpandedDuringLastCollection = true;
            _steps.Add(step);
        }
    }
}
```

- [ ] **Step 6：运行编译和 EditMode Tests**

```powershell
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

Expected：0 errors。然后在 Unity Test Runner 的 EditMode 中运行 `CastTraceTests`，2 tests PASS。

- [ ] **Step 7：提交 Task 1**

Unity 生成 `.meta` 后，只暂存本 Task 文件：

```powershell
git add -- `
  'Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStepKind.cs' `
  'Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStepKind.cs.meta' `
  'Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStep.cs' `
  'Assets/_Project/Scripts/Skills/Diagnostics/CastTraceStep.cs.meta' `
  'Assets/_Project/Scripts/Skills/Diagnostics/CastTraceCollector.cs' `
  'Assets/_Project/Scripts/Skills/Diagnostics/CastTraceCollector.cs.meta' `
  'Assets/_Project/Tests/Skills/CastTraceTests.cs' `
  'Assets/_Project/Tests/Skills/CastTraceTests.cs.meta'
git commit -m "feat(skills): add reusable cast trace data model"
```

---

## Task 2：在唯一 CastEvaluator 核心中记录 Trace

**Files:**

- Modify: `Assets/_Project/Scripts/Skills/CastEvaluator.cs`
- Modify: `Assets/_Project/Tests/Skills/CastTraceTests.cs`

**Interfaces:**

- Keeps: `Evaluate(IReadOnlyList<SpellDefinition>, int, float, CastModifierState, List<EmitCommand>)`
- Produces: `EvaluateWithTrace(IReadOnlyList<SpellDefinition>, int, float, CastModifierState, List<EmitCommand>, CastTraceCollector)`
- Produces: Profiler sample `Spell.Evaluate`

- [ ] **Step 1：添加 Trace 语义失败测试**

在 `CastTraceTests` 增加引用和三个 helper：

```csharp
using System.Collections.Generic;
using UnityEngine;
using Game.Combat;

private static SpellDefinition Emit(
    float damage = 10f,
    float mana = 0f,
    PayloadTriggerMode trigger = PayloadTriggerMode.None)
{
    var spell = ScriptableObject.CreateInstance<SpellDefinition>();
    spell.Kind = SpellKind.Emit;
    spell.DisplayName = "测试火球";
    spell.BaseDamage = damage;
    spell.BaseSpeed = 20f;
    spell.DamageType = DamageType.Magical;
    spell.ManaCost = mana;
    spell.PayloadTrigger = trigger;
    return spell;
}

private static SpellDefinition DamageMod(float multiplier, float mana = 0f)
{
    var spell = ScriptableObject.CreateInstance<SpellDefinition>();
    spell.Kind = SpellKind.Modify;
    spell.DisplayName = "测试增伤";
    spell.ModDamageMul = multiplier;
    spell.ManaCost = mana;
    return spell;
}

private static SpellDefinition Multicast(int extraDraws)
{
    var spell = ScriptableObject.CreateInstance<SpellDefinition>();
    spell.Kind = SpellKind.Multicast;
    spell.DisplayName = "测试多重";
    spell.ExtraDraws = extraDraws;
    return spell;
}
```

加入以下测试：

```csharp
[Test]
public void EvaluateWithTrace_SingleEmit_RecordsStartEmitAndComplete()
{
    var output = new List<EmitCommand>();
    var trace = new CastTraceCollector();

    CastEvaluator.EvaluateWithTrace(
        new[] { Emit(15f) }, 1, 100f,
        CastModifierState.Default, output, trace);

    Assert.AreEqual(1, output.Count);
    Assert.AreEqual(3, trace.Count);
    Assert.AreEqual(CastTraceStepKind.CastStarted, trace[0].Kind);
    Assert.AreEqual(CastTraceStepKind.EmitProduced, trace[1].Kind);
    Assert.AreEqual(15f, trace[1].Emit.Damage, 1e-4f);
    Assert.AreEqual(CastTraceStepKind.CastCompleted, trace[2].Kind);
}

[Test]
public void EvaluateWithTrace_ModifyAndMulticast_RecordsBeforeAfterState()
{
    var output = new List<EmitCommand>();
    var trace = new CastTraceCollector();

    CastEvaluator.EvaluateWithTrace(
        new[] { Multicast(2), DamageMod(2f), Emit(10f) },
        1, 100f, CastModifierState.Default, output, trace);

    Assert.AreEqual(CastTraceStepKind.MulticastApplied, trace[1].Kind);
    Assert.AreEqual(1, trace[1].DrawBudgetBefore);
    Assert.AreEqual(3, trace[1].DrawBudgetAfter);
    Assert.AreEqual(CastTraceStepKind.ModifyApplied, trace[2].Kind);
    Assert.AreEqual(1f, trace[2].ModifiersBefore.DamageMul, 1e-4f);
    Assert.AreEqual(2f, trace[2].ModifiersAfter.DamageMul, 1e-4f);
}

[Test]
public void EvaluateWithTrace_ManaFizzle_RecordsFailingIndex()
{
    var output = new List<EmitCommand>();
    var trace = new CastTraceCollector();

    CastSummary summary = CastEvaluator.EvaluateWithTrace(
        new[] { DamageMod(2f, 5f), Emit() },
        1, 3f, CastModifierState.Default, output, trace);

    Assert.IsTrue(summary.Fizzled);
    Assert.AreEqual(CastTraceStepKind.ManaFizzle, trace[1].Kind);
    Assert.AreEqual(0, trace[1].SpellIndex);
}

[Test]
public void EvaluateWithTrace_ZeroBudget_RecordsBlockedEmit()
{
    var output = new List<EmitCommand>();
    var trace = new CastTraceCollector();
    CastEvaluator.EvaluateWithTrace(
        new[] { Emit() }, 0, 100f,
        CastModifierState.Default, output, trace);

    Assert.AreEqual(0, output.Count);
    Assert.AreEqual(CastTraceStepKind.DrawBudgetBlocked, trace[1].Kind);
}

[Test]
public void EvaluateWithTrace_Trigger_RecordsPayloadCapture()
{
    var output = new List<EmitCommand>();
    var trace = new CastTraceCollector();
    CastEvaluator.EvaluateWithTrace(
        new[] { Emit(trigger: PayloadTriggerMode.OnImpact), Emit() },
        1, 100f, CastModifierState.Default, output, trace);

    Assert.AreEqual(CastTraceStepKind.PayloadCaptured, trace[2].Kind);
    Assert.AreEqual(1, trace[2].PayloadStartIndex);
    Assert.AreEqual(1, trace[2].PayloadCount);
}

[Test]
public void EvaluateWithTrace_NullCollector_Throws()
{
    Assert.Throws<System.ArgumentNullException>(() =>
        CastEvaluator.EvaluateWithTrace(
            new[] { Emit() }, 1, 100f,
            CastModifierState.Default, new List<EmitCommand>(), null));
}
```

再增加等价性测试：

```csharp
[Test]
public void EvaluateAndEvaluateWithTrace_ProduceEquivalentCommands()
{
    SpellDefinition[] spells =
    {
        Multicast(2), DamageMod(1.5f),
        Emit(10f), Emit(12f), Emit(14f)
    };
    var normal = new List<EmitCommand>();
    var traced = new List<EmitCommand>();
    var trace = new CastTraceCollector();

    CastSummary normalSummary = CastEvaluator.Evaluate(
        spells, 1, 100f, CastModifierState.Default, normal);
    CastSummary tracedSummary = CastEvaluator.EvaluateWithTrace(
        spells, 1, 100f, CastModifierState.Default, traced, trace);

    Assert.AreEqual(normalSummary.ManaSpent, tracedSummary.ManaSpent, 1e-4f);
    Assert.AreEqual(normalSummary.Fizzled, tracedSummary.Fizzled);
    Assert.AreEqual(normal.Count, traced.Count);

    for (int i = 0; i < normal.Count; i++)
    {
        Assert.AreEqual(normal[i].Damage, traced[i].Damage, 1e-4f);
        Assert.AreEqual(normal[i].Speed, traced[i].Speed, 1e-4f);
        Assert.AreEqual(normal[i].SpawnMode, traced[i].SpawnMode);
        Assert.AreEqual(normal[i].MotionMode, traced[i].MotionMode);
        Assert.AreEqual(normal[i].PayloadTrigger, traced[i].PayloadTrigger);
    }
}
```

- [ ] **Step 2：验证测试先因 API 缺失而失败**

```powershell
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

Expected：FAIL，`CastEvaluator` 没有 `EvaluateWithTrace`。

- [ ] **Step 3：增加唯一 EvaluateCore 和 ProfilerMarker**

在 `CastEvaluator.cs` 增加：

```csharp
using System;
using Unity.Profiling;

private static readonly ProfilerMarker s_evaluateMarker =
    new ProfilerMarker("Spell.Evaluate");
```

入口固定为：

```csharp
public static CastSummary Evaluate(
    IReadOnlyList<SpellDefinition> spells,
    int baseDraws,
    float availableMana,
    CastModifierState incomingMods,
    List<EmitCommand> output)
{
    return EvaluateCore(spells, baseDraws, availableMana, incomingMods, output, null);
}

public static CastSummary EvaluateWithTrace(
    IReadOnlyList<SpellDefinition> spells,
    int baseDraws,
    float availableMana,
    CastModifierState incomingMods,
    List<EmitCommand> output,
    CastTraceCollector trace)
{
    if (trace == null)
        throw new ArgumentNullException(nameof(trace));

    trace.Clear();
    return EvaluateCore(spells, baseDraws, availableMana, incomingMods, output, trace);
}
```

- [ ] **Step 4：移动原循环到 EvaluateCore 并在语义点 Record**

`EvaluateCore` 保留原语义，并在以下位置记录：

```text
进入 -> CastStarted
null slot -> NullSpellSkipped
Modify 成功 -> ModifyApplied（保存 mods before/after）
Multicast 成功 -> MulticastApplied（保存 budget before/after）
drawBudget <= 0 -> DrawBudgetBlocked
TrySpend 失败 -> ManaFizzle
output.Add 后 -> EmitProduced（保存完整 EmitCommand）
触发 Emit -> PayloadCaptured（保存 start/count/trigger）
退出 -> CastCompleted
```

完整方法按下面的控制流实现：

```csharp
private static CastSummary EvaluateCore(
    IReadOnlyList<SpellDefinition> spells,
    int baseDraws,
    float availableMana,
    CastModifierState incomingMods,
    List<EmitCommand> output,
    CastTraceCollector trace)
{
using (s_evaluateMarker.Auto())
{
    output.Clear();
    int drawBudget = baseDraws;
    float manaLeft = availableMana;
    float manaSpent = 0f;
    bool fizzled = false;
    CastModifierState mods = incomingMods;

    trace?.Record(new CastTraceStep(
        CastTraceStepKind.CastStarted, -1, null,
        drawBudget, drawBudget, manaLeft, manaLeft, mods, mods));

    if (spells == null || spells.Count == 0)
    {
        trace?.Record(new CastTraceStep(
            CastTraceStepKind.CastCompleted, -1, null,
            drawBudget, drawBudget, manaLeft, manaLeft, mods, mods));
        return new CastSummary(0f, false);
    }

    for (int i = 0; i < spells.Count; i++)
    {
        SpellDefinition spell = spells[i];
        int budgetBefore = drawBudget;
        float manaBefore = manaLeft;
        CastModifierState modsBefore = mods;

        if (spell == null)
        {
            trace?.Record(new CastTraceStep(
                CastTraceStepKind.NullSpellSkipped, i, null,
                budgetBefore, drawBudget, manaBefore, manaLeft,
                modsBefore, mods));
            continue;
        }

        bool ended = false;
        switch (spell.Kind)
        {
            case SpellKind.Modify:
                if (!TrySpend(spell, ref manaLeft, ref manaSpent))
                {
                    fizzled = true;
                    trace?.Record(new CastTraceStep(
                        CastTraceStepKind.ManaFizzle, i, spell,
                        budgetBefore, drawBudget, manaBefore, manaLeft,
                        modsBefore, mods));
                    break;
                }

                mods = mods.Apply(spell);
                trace?.Record(new CastTraceStep(
                    CastTraceStepKind.ModifyApplied, i, spell,
                    budgetBefore, drawBudget, manaBefore, manaLeft,
                    modsBefore, mods));
                break;

            case SpellKind.Multicast:
                if (!TrySpend(spell, ref manaLeft, ref manaSpent))
                {
                    fizzled = true;
                    trace?.Record(new CastTraceStep(
                        CastTraceStepKind.ManaFizzle, i, spell,
                        budgetBefore, drawBudget, manaBefore, manaLeft,
                        modsBefore, mods));
                    break;
                }

                drawBudget += spell.ExtraDraws;
                trace?.Record(new CastTraceStep(
                    CastTraceStepKind.MulticastApplied, i, spell,
                    budgetBefore, drawBudget, manaBefore, manaLeft,
                    modsBefore, mods));
                break;

            case SpellKind.Emit:
            case SpellKind.StaticProjectile:
                if (drawBudget <= 0)
                {
                    trace?.Record(new CastTraceStep(
                        CastTraceStepKind.DrawBudgetBlocked, i, spell,
                        budgetBefore, drawBudget, manaBefore, manaLeft,
                        modsBefore, mods));
                    break;
                }

                if (!TrySpend(spell, ref manaLeft, ref manaSpent))
                {
                    fizzled = true;
                    trace?.Record(new CastTraceStep(
                        CastTraceStepKind.ManaFizzle, i, spell,
                        budgetBefore, drawBudget, manaBefore, manaLeft,
                        modsBefore, mods));
                    break;
                }

                IReadOnlyList<SpellDefinition> payload =
                    spell.PayloadTrigger != PayloadTriggerMode.None
                        ? CaptureSuffix(spells, i + 1)
                        : null;
                EmitCommand command = BakeEmit(spell, mods, payload);
                int emitIndex = output.Count;
                output.Add(command);
                drawBudget--;

                trace?.Record(new CastTraceStep(
                    CastTraceStepKind.EmitProduced, i, spell,
                    budgetBefore, drawBudget, manaBefore, manaLeft,
                    modsBefore, mods, emitIndex, command));

                if (spell.PayloadTrigger != PayloadTriggerMode.None)
                {
                    trace?.Record(new CastTraceStep(
                        CastTraceStepKind.PayloadCaptured, i, spell,
                        budgetBefore, drawBudget, manaBefore, manaLeft,
                        modsBefore, mods, emitIndex, command,
                        i + 1,
                        command.HasPayload ? command.Payload.Count : 0,
                        command.PayloadTrigger));
                    ended = true;
                }
                break;
        }

        if (fizzled || ended)
            break;
    }

    trace?.Record(new CastTraceStep(
        CastTraceStepKind.CastCompleted, -1, null,
        drawBudget, drawBudget, manaLeft, manaLeft, mods, mods));
    return new CastSummary(manaSpent, fizzled);
}
}
```

实现过程中不要另写第二份 switch；`Evaluate` 与 `EvaluateWithTrace` 必须共用这一方法。

- [ ] **Step 5：运行全套 Skills Tests**

```powershell
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

Expected：0 errors。Unity Test Runner 中运行全部 `Game.Skills.Tests`，0 failed。

- [ ] **Step 6：扫描纯逻辑边界**

```powershell
rg -n "GameLog|Debug\.Log|Instantiate" Assets/_Project/Scripts/Skills/CastEvaluator.cs Assets/_Project/Scripts/Skills/Diagnostics
```

Expected：无匹配。ProfilerMarker 允许存在，但 Skills 不打印、不实例化。

- [ ] **Step 7：提交 Task 2**

```powershell
git add -- 'Assets/_Project/Scripts/Skills/CastEvaluator.cs' 'Assets/_Project/Tests/Skills/CastTraceTests.cs'
git commit -m "feat(skills): add structured cast evaluation trace"
```

---

## Task 3：SpellCaster 输出 Trace 并标记 payload 层级

**Files:**

- Create: `Assets/_Project/Scripts/Character/Spells/CastTraceLevel.cs`
- Modify: `Assets/_Project/Scripts/Character/Spells/SpellCaster.cs`

**Interfaces:**

- Produces: `CastTraceLevel.Off / Summary / Detailed`
- Produces: `public CastTraceLevel TraceLevel { get; }`
- Changes private: `RunCast(IReadOnlyList<SpellDefinition>, int, CastModifierState, Vector3, Vector3, byte, int, Collider, int castId, int depth)`
- Changes private: `WirePayload(ProjectileBase, EmitCommand, byte, int, Collider, int castId, int depth)`
- Produces: `Spell.RuntimeSpawn` Profiler sample

- [ ] **Step 1：创建 CastTraceLevel**

```csharp
namespace Game.Character
{
    public enum CastTraceLevel : byte
    {
        Off = 0,
        Summary = 1,
        Detailed = 2
    }
}
```

- [ ] **Step 2：增加复用字段和 Marker**

在 `SpellCaster` 增加：

```csharp
using Unity.Profiling;

[Header("开发诊断")]
[SerializeField] private CastTraceLevel _traceLevel = CastTraceLevel.Off;

private static int s_nextCastId;
private static readonly ProfilerMarker s_runtimeSpawnMarker =
    new ProfilerMarker("Spell.RuntimeSpawn");

#if UNITY_EDITOR || DEVELOPMENT_BUILD
private readonly CastTraceCollector _traceCollector = new CastTraceCollector(64);
#endif

public CastTraceLevel TraceLevel
{
    get
    {
#if UNITY_EDITOR || DEVELOPMENT_BUILD
        return _traceLevel;
#else
        return CastTraceLevel.Off;
#endif
    }
}
```

- [ ] **Step 3：根施法创建 CastId，payload 继承并增加 Depth**

`CastWand` 根调用：

```csharp
int castId = ++s_nextCastId;
return RunCast(
    _wand.Spells, _wand.BaseDraws, CastModifierState.Default,
    spawnPos, baseDir, team, attackerId, casterCollider,
    castId, 0);
```

`RunCast` 和 `WirePayload` 都增加 `int castId, int depth` 参数。OnImpact/AfterDelay callback 调用 `RunCast` 时传同一个 `castId` 和 `depth + 1`。

- [ ] **Step 4：按 Trace Level 选择求值入口**

```csharp
CastSummary summary;
#if UNITY_EDITOR || DEVELOPMENT_BUILD
if (_traceLevel == CastTraceLevel.Detailed)
{
    summary = CastEvaluator.EvaluateWithTrace(
        spells, baseDraws, float.PositiveInfinity,
        incomingMods, _emits, _traceCollector);
}
else
#endif
{
    summary = CastEvaluator.Evaluate(
        spells, baseDraws, float.PositiveInfinity,
        incomingMods, _emits);
}
```

保持 `float.PositiveInfinity`，因为真实 mana 已由 `EstimateManaCost + TrySpendMana` 一次扣除；本轮不改变资源语义。

- [ ] **Step 5：实现 Summary/Detailed Formatter**

增加 `LogTrace(castId, depth, requiredMana, summary)`：

```csharp
private void LogTrace(int castId, int depth, float requiredMana, CastSummary summary)
{
    if (_traceLevel == CastTraceLevel.Off)
        return;

    GameLog.Info(
        $"Cast #{castId} Depth={depth} Emits={_emits.Count} " +
        $"RequiredMana={requiredMana:0.##} EvaluatedMana={summary.ManaSpent:0.##} " +
        $"Fizzled={summary.Fizzled}",
        "SpellTrace");

    if (_traceLevel != CastTraceLevel.Detailed)
        return;

    for (int i = 0; i < _traceCollector.Count; i++)
        LogTraceStep(castId, depth, _traceCollector[i]);

    if (_traceCollector.ExpandedDuringLastCollection)
    {
        GameLog.Warn(
            $"Cast #{castId} Trace 超出预分配容量，本次诊断发生 List 扩容",
            "SpellTrace");
    }
}
```

将 `LogTrace`、`LogTraceStep` 及其调用整体放在 `#if UNITY_EDITOR || DEVELOPMENT_BUILD` 中。这样 Release 即使 Prefab 曾序列化为 Detailed，也不会收集 step 或执行字符串格式化。

`LogTraceStep` 完整分支：

```csharp
private static void LogTraceStep(int castId, int depth, CastTraceStep step)
{
    string spellName = step.Spell != null
        ? step.Spell.DisplayName
        : "<none>";

    switch (step.Kind)
    {
        case CastTraceStepKind.CastStarted:
            GameLog.Info($"Cast #{castId} D{depth} START Draw={step.DrawBudgetBefore}", "SpellTrace");
            break;
        case CastTraceStepKind.ModifyApplied:
            GameLog.Info(
                $"Cast #{castId} D{depth} [{step.SpellIndex}] {spellName} MODIFY " +
                $"DamageMul {step.ModifiersBefore.DamageMul:0.##}->{step.ModifiersAfter.DamageMul:0.##} " +
                $"SpeedMul {step.ModifiersBefore.SpeedMul:0.##}->{step.ModifiersAfter.SpeedMul:0.##} " +
                $"Motion {step.ModifiersBefore.MotionMode}->{step.ModifiersAfter.MotionMode}",
                "SpellTrace");
            break;
        case CastTraceStepKind.MulticastApplied:
            GameLog.Info(
                $"Cast #{castId} D{depth} [{step.SpellIndex}] {spellName} MULTICAST " +
                $"Draw {step.DrawBudgetBefore}->{step.DrawBudgetAfter}", "SpellTrace");
            break;
        case CastTraceStepKind.DrawBudgetBlocked:
            GameLog.Info(
                $"Cast #{castId} D{depth} [{step.SpellIndex}] {spellName} BLOCKED Draw=0",
                "SpellTrace");
            break;
        case CastTraceStepKind.EmitProduced:
            GameLog.Info(
                $"Cast #{castId} D{depth} [{step.SpellIndex}] {spellName} EMIT#{step.EmitIndex} " +
                $"Damage={step.Emit.Damage:0.##} Speed={step.Emit.Speed:0.##} " +
                $"Gravity={step.Emit.UseGravity} Motion={step.Emit.MotionMode} " +
                $"Draw {step.DrawBudgetBefore}->{step.DrawBudgetAfter}", "SpellTrace");
            break;
        case CastTraceStepKind.PayloadCaptured:
            GameLog.Info(
                $"Cast #{castId} D{depth} [{step.SpellIndex}] PAYLOAD " +
                $"Trigger={step.PayloadTrigger} Start={step.PayloadStartIndex} Count={step.PayloadCount}",
                "SpellTrace");
            break;
        case CastTraceStepKind.ManaFizzle:
            GameLog.Info(
                $"Cast #{castId} D{depth} [{step.SpellIndex}] {spellName} FIZZLE " +
                $"ManaLeft={step.ManaLeftBefore:0.##}", "SpellTrace");
            break;
        case CastTraceStepKind.NullSpellSkipped:
            GameLog.Info($"Cast #{castId} D{depth} [{step.SpellIndex}] NULL SKIPPED", "SpellTrace");
            break;
        case CastTraceStepKind.CastCompleted:
            GameLog.Info($"Cast #{castId} D{depth} COMPLETE DrawLeft={step.DrawBudgetAfter}", "SpellTrace");
            break;
    }
}
```

不得使用 `StringBuilder` 静态共享对象；Trace 是离散开发功能，直接字符串插值更清楚，且 Off 早返回保证正常路径不格式化。

- [ ] **Step 6：用 Marker 包住运行时 Spawn 循环**

在 `int count = _emits.Count;` 后开启 scope，在原 for 循环结束后关闭 scope。循环中的 `EmitCommand cmd`、SFX 去重、空 prefab 检查和三个 `SpawnMode` 分支逐行保留，不移动到新 helper，也不改变 `return count`。

- [ ] **Step 7：编译相关程序集**

```powershell
dotnet build Game.Skills.csproj --no-restore /m:1
dotnet build Game.Character.csproj --no-restore /m:1
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

Expected：全部 0 errors。

- [ ] **Step 8：Editor Gate 1，用户验证 Trace**

1. 选中 `WizardPlayer/SpellCaster`。
2. `Cast Trace Level = Off`，发射后不出现 `[SpellTrace]`。
3. 改 `Summary`，普通火球只输出施法摘要。
4. 改 `Detailed`，测试普通火球、三重、重力+追踪、命中触发、定时触发。
5. payload 必须保持相同 CastId，Depth 从 0 变成 1。
6. Detailed 顺序必须与法杖从左到右一致。
7. 验证完恢复 `Off`。

Gate：用户明确确认后才进入 Task 4。

- [ ] **Step 9：提交 Task 3**

```powershell
git add -- `
  'Assets/_Project/Scripts/Character/Spells/CastTraceLevel.cs' `
  'Assets/_Project/Scripts/Character/Spells/CastTraceLevel.cs.meta' `
  'Assets/_Project/Scripts/Character/Spells/SpellCaster.cs'
git commit -m "feat(character): add runtime spell trace diagnostics"
```

# 第二阶段：Projectile Debug Snapshot 与 Gizmos

## Task 4：暴露只读 Snapshot 与 ActiveCount

**Files:**

- Create: `Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugSnapshot.cs`
- Create: `Assets/_Project/Tests/Combat/ProjectileDebugSnapshotTests.cs`
- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs`

**Interfaces:**

- Produces: `public static int ProjectileBase.ActiveCount`
- Produces: `internal static IReadOnlyList<ProjectileBase> ActiveProjectiles`
- Produces: `public ProjectileDebugSnapshot GetDebugSnapshot()`
- Architecture: Snapshot 不得引用 `Game.Skills.ProjectileMotionMode`

- [ ] **Step 1：写 ActiveCount 与 Snapshot 失败测试**

创建测试：

```csharp
using System.Collections;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

namespace Game.Combat.Tests
{
    public sealed class ProjectileDebugTestProjectile : ProjectileBase
    {
        public void PrepareForEditModeTest(Vector3 velocity, bool useGravity)
        {
            _rb = GetComponent<Rigidbody>();
            _collider = GetComponent<Collider>();
            _rb.useGravity = useGravity;
            _rb.linearVelocity = velocity;
        }
    }

    public class ProjectileDebugSnapshotTests
    {
        [UnityTest]
        public IEnumerator Init_RegistersActiveProjectile_AndDestroyUnregisters()
        {
            yield return new EnterPlayMode();

            int before = ProjectileBase.ActiveCount;
            GameObject go = CreateProjectile(out ProjectileDebugTestProjectile projectile);

            projectile.Init(
                1, 10, 5f, DamageType.Magical,
                Vector3.forward * 20f, null, false);

            Assert.AreEqual(before + 1, ProjectileBase.ActiveCount);
            Object.Destroy(go);
            yield return null;
            Assert.AreEqual(before, ProjectileBase.ActiveCount);

            yield return new ExitPlayMode();
        }

        [Test]
        public void Snapshot_AfterInit_ContainsVelocityGravityAndFlags()
        {
            GameObject go = CreateProjectile(out ProjectileDebugTestProjectile projectile);
            projectile.ConfigureHoming(6f, 0.5f, 120f);
            projectile.PrepareForEditModeTest(Vector3.forward * 20f, true);

            ProjectileDebugSnapshot snapshot = projectile.GetDebugSnapshot();

            Assert.AreEqual(new Vector3(0f, 0f, 20f), snapshot.Velocity);
            Assert.IsTrue(snapshot.UsesGravity);
            Assert.IsTrue(snapshot.HomingEnabled);
            Assert.IsFalse(snapshot.OrbitEnabled);
            Assert.AreEqual(6f, snapshot.HomingRadius, 1e-4f);
            Object.DestroyImmediate(go);
        }

        [Test]
        public void ShieldReflection_RecordsNormalAndDirection()
        {
            GameObject go = CreateProjectile(out ProjectileDebugTestProjectile projectile);
            projectile.PrepareForEditModeTest(Vector3.forward * 20f, false);

            Assert.IsTrue(projectile.ReflectByShield(Vector3.back, 2, 99));
            ProjectileDebugSnapshot snapshot = projectile.GetDebugSnapshot();

            Assert.IsTrue(snapshot.HasRecentReflection);
            Assert.AreEqual(Vector3.back, snapshot.LastCollisionNormal);
            Assert.AreEqual(Vector3.back, snapshot.LastReflectedDirection);
            Object.DestroyImmediate(go);
        }

        private static GameObject CreateProjectile(out ProjectileDebugTestProjectile projectile)
        {
            var go = new GameObject("DebugSnapshotProjectile");
            go.AddComponent<Rigidbody>();
            go.AddComponent<SphereCollider>();
            projectile = go.AddComponent<ProjectileDebugTestProjectile>();
            return go;
        }
    }
}
```

- [ ] **Step 2：验证测试先失败**

```powershell
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
```

Expected：FAIL，缺少 Snapshot/ActiveCount API。

- [ ] **Step 3：实现不依赖 Skills 的 ProjectileDebugSnapshot**

```csharp
using UnityEngine;

namespace Game.Combat
{
    public readonly struct ProjectileDebugSnapshot
    {
        public readonly Vector3 Position;
        public readonly Vector3 Velocity;
        public readonly bool UsesGravity;
        public readonly bool HomingEnabled;
        public readonly float HomingRadius;
        public readonly bool HasHomingTarget;
        public readonly Vector3 HomingTargetPosition;
        public readonly bool OrbitEnabled;
        public readonly Vector3 OrbitCenter;
        public readonly Vector3 OrbitForward;
        public readonly Vector3 OrbitRight;
        public readonly Vector3 OrbitUp;
        public readonly float OrbitRadius;
        public readonly bool HasRecentReflection;
        public readonly Vector3 LastCollisionPoint;
        public readonly Vector3 LastCollisionNormal;
        public readonly Vector3 LastReflectedDirection;
        public readonly float LastCollisionTime;

        public ProjectileDebugSnapshot(
            Vector3 position, Vector3 velocity, bool usesGravity,
            bool homingEnabled, float homingRadius,
            bool hasHomingTarget, Vector3 homingTargetPosition,
            bool orbitEnabled, Vector3 orbitCenter, Vector3 orbitForward,
            Vector3 orbitRight, Vector3 orbitUp, float orbitRadius,
            bool hasRecentReflection, Vector3 lastCollisionPoint,
            Vector3 lastCollisionNormal, Vector3 lastReflectedDirection,
            float lastCollisionTime)
        {
            Position = position;
            Velocity = velocity;
            UsesGravity = usesGravity;
            HomingEnabled = homingEnabled;
            HomingRadius = homingRadius;
            HasHomingTarget = hasHomingTarget;
            HomingTargetPosition = homingTargetPosition;
            OrbitEnabled = orbitEnabled;
            OrbitCenter = orbitCenter;
            OrbitForward = orbitForward;
            OrbitRight = orbitRight;
            OrbitUp = orbitUp;
            OrbitRadius = orbitRadius;
            HasRecentReflection = hasRecentReflection;
            LastCollisionPoint = lastCollisionPoint;
            LastCollisionNormal = lastCollisionNormal;
            LastReflectedDirection = lastReflectedDirection;
            LastCollisionTime = lastCollisionTime;
        }
    }
}
```

- [ ] **Step 4：暴露只读活跃注册信息**

在 `s_active` 附近增加：

```csharp
public static int ActiveCount => s_active.Count;
internal static IReadOnlyList<ProjectileBase> ActiveProjectiles => s_active;
```

不公开可修改 `List`，不让 Debug 工具破坏碰撞注册表。

- [ ] **Step 5：记录最近 Bounce/Shield Reflection**

增加字段和 helper：

```csharp
private bool _hasDebugReflection;
private Vector3 _lastDebugCollisionPoint;
private Vector3 _lastDebugCollisionNormal;
private Vector3 _lastDebugReflectedDirection;
private float _lastDebugCollisionTime;
private bool _debugMissingRigidbodyWarned;

private void RecordDebugReflection(
    Vector3 point, Vector3 normal, Vector3 reflectedDirection)
{
    _hasDebugReflection = true;
    _lastDebugCollisionPoint = point;
    _lastDebugCollisionNormal = normal;
    _lastDebugReflectedDirection = reflectedDirection;
    _lastDebugCollisionTime = Time.time;
}
```

在 `ReflectByShield` 的 `reflected.Normalize()` 后调用：

```csharp
RecordDebugReflection(transform.position, normal, reflected);
```

在 `TryBounce` 的 `reflected.Normalize()` 后调用：

```csharp
ContactPoint contact = collision.GetContact(0);
RecordDebugReflection(contact.point, normal, reflected);
```

不得改动反射公式、速度或碰撞分支顺序。

- [ ] **Step 6：实现按需 GetDebugSnapshot**

```csharp
public ProjectileDebugSnapshot GetDebugSnapshot()
{
    if (_rb == null && !_debugMissingRigidbodyWarned)
    {
        _debugMissingRigidbodyWarned = true;
        GameLog.Warn(
            $"Projectile {name} 缺少 Rigidbody，Debug Snapshot 使用零速度",
            "ProjectileDebug");
    }

    Vector3 velocity = _rb != null ? _rb.linearVelocity : Vector3.zero;
    bool hasTarget = _homingTarget != null;

    return new ProjectileDebugSnapshot(
        transform.position,
        velocity,
        _rb != null && _rb.useGravity,
        _homingEnabled,
        _homingRadius,
        hasTarget,
        hasTarget ? _homingTarget.position : Vector3.zero,
        _orbitEnabled,
        _orbitCenter,
        _orbitForward,
        _orbitRight,
        _orbitUp,
        _orbitRadius,
        _hasDebugReflection,
        _lastDebugCollisionPoint,
        _lastDebugCollisionNormal,
        _lastDebugReflectedDirection,
        _lastDebugCollisionTime);
}
```

为该警告在 `ProjectileBase.cs` 顶部增加 `using Game.Core;`。`_debugMissingRigidbodyWarned` 保证不会被 Scene Gizmos 每次重绘时重复刷屏。

Snapshot 只在 Drawer 请求时创建值类型快照，不在每个 `FixedUpdate` 自动保存第二份运动状态。

- [ ] **Step 7：运行 Combat Tests**

```powershell
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
```

Expected：0 errors。Unity Test Runner 中全部 `Game.Combat.Tests` PASS。

- [ ] **Step 8：提交 Task 4**

```powershell
git add -- `
  'Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugSnapshot.cs' `
  'Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugSnapshot.cs.meta' `
  'Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs' `
  'Assets/_Project/Tests/Combat/ProjectileDebugSnapshotTests.cs' `
  'Assets/_Project/Tests/Combat/ProjectileDebugSnapshotTests.cs.meta'
git commit -m "feat(combat): expose projectile diagnostic snapshots"
```

---

## Task 5：绘制 Scene Gizmos 并标记运动采样

**Files:**

- Create: `Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugGizmoDrawer.cs`
- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs`

**Interfaces:**

- Consumes: `ActiveProjectiles`、`GetDebugSnapshot()`
- Produces: Scene View Gizmos only
- Produces: `Projectile.Motion` Profiler sample

- [ ] **Step 1：给 FixedUpdate 增加静态 Marker**

```csharp
using Unity.Profiling;

private static readonly ProfilerMarker s_motionMarker =
    new ProfilerMarker("Projectile.Motion");
```

将 `FixedUpdate` 完整替换为：

```csharp
protected virtual void FixedUpdate()
{
    if (_consumed || _rb == null)
        return;

    using (s_motionMarker.Auto())
    {
        if (_orbitEnabled)
            TickOrbit(Time.fixedDeltaTime);
        else
            TickHoming(Time.fixedDeltaTime);

        if (!FaceVelocityInFlight)
            return;

        Vector3 velocity = _rb.linearVelocity;
        if (velocity.sqrMagnitude > 1e-6f)
        {
            transform.rotation = Quaternion.LookRotation(velocity)
                * Quaternion.Euler(_modelForwardOffsetEuler);
        }
    }
}
```

- [ ] **Step 2：创建 Drawer 的字段和 Editor 边界**

```csharp
using System.Collections.Generic;
using UnityEngine;

#if UNITY_EDITOR
using UnityEditor;
#endif

namespace Game.Combat
{
    public class ProjectileDebugGizmoDrawer : MonoBehaviour
    {
        [Header("显示分类")]
        [SerializeField] private bool _drawVelocity = true;
        [SerializeField] private bool _drawGravity = true;
        [SerializeField] private bool _drawHoming = true;
        [SerializeField] private bool _drawOrbit = true;
        [SerializeField] private bool _drawCollision = true;

        [Header("显示范围")]
        [SerializeField] private bool _onlyDrawSelected;
        [SerializeField, Min(0.01f)] private float _vectorScale = 0.15f;
        [SerializeField, Min(0.05f)] private float _collisionHistorySeconds = 0.5f;
        [SerializeField, Range(8, 64)] private int _orbitCircleSegments = 24;

#if UNITY_EDITOR
        private void OnDrawGizmos()
        {
            IReadOnlyList<ProjectileBase> active = ProjectileBase.ActiveProjectiles;
            for (int i = 0; i < active.Count; i++)
            {
                ProjectileBase projectile = active[i];
                if (projectile == null || !ShouldDraw(projectile))
                    continue;

                DrawSnapshot(projectile.GetDebugSnapshot());
            }
        }
#endif
    }
}
```

- [ ] **Step 3：实现 Only Selected**

```csharp
private bool ShouldDraw(ProjectileBase projectile)
{
    if (!_onlyDrawSelected)
        return true;

    Transform selected = Selection.activeTransform;
    return selected != null
        && (selected == projectile.transform
            || selected.IsChildOf(projectile.transform));
}
```

- [ ] **Step 4：实现分类绘制**

`DrawSnapshot` 固定规则：

```csharp
private void DrawSnapshot(ProjectileDebugSnapshot snapshot)
{
    if (_drawVelocity && snapshot.Velocity.sqrMagnitude > 1e-6f)
    {
        Gizmos.color = Color.white;
        DrawArrow(snapshot.Position, snapshot.Velocity * _vectorScale);
    }

    if (_drawGravity && snapshot.UsesGravity)
    {
        Gizmos.color = Color.yellow;
        DrawArrow(snapshot.Position, Vector3.down * 1.5f);
    }

    if (_drawHoming && snapshot.HomingEnabled)
    {
        Gizmos.color = Color.cyan;
        Gizmos.DrawWireSphere(snapshot.Position, snapshot.HomingRadius);
        if (snapshot.HasHomingTarget)
        {
            Gizmos.color = Color.green;
            Gizmos.DrawLine(snapshot.Position, snapshot.HomingTargetPosition);
        }
    }

    if (_drawOrbit && snapshot.OrbitEnabled)
    {
        Gizmos.color = Color.blue;
        Gizmos.DrawWireSphere(snapshot.OrbitCenter, 0.12f);
        DrawArrow(snapshot.OrbitCenter, snapshot.OrbitForward);

        Gizmos.color = new Color(1f, 0.5f, 0f);
        DrawCircle(snapshot.OrbitCenter, snapshot.OrbitRight,
            snapshot.OrbitUp, snapshot.OrbitRadius, _orbitCircleSegments);
    }

    if (_drawCollision
        && snapshot.HasRecentReflection
        && Time.time - snapshot.LastCollisionTime <= _collisionHistorySeconds)
    {
        Gizmos.color = Color.red;
        DrawArrow(snapshot.LastCollisionPoint, snapshot.LastCollisionNormal);
        Gizmos.color = Color.green;
        DrawArrow(snapshot.LastCollisionPoint, snapshot.LastReflectedDirection);
    }
}
```

- [ ] **Step 5：实现无数组分配的 Arrow/Circle**

```csharp
private static void DrawArrow(Vector3 origin, Vector3 vector)
{
    if (vector.sqrMagnitude <= 1e-6f) return;

    Vector3 end = origin + vector;
    Vector3 direction = vector.normalized;
    Vector3 side = Vector3.Cross(direction, Vector3.up);
    if (side.sqrMagnitude <= 1e-6f)
        side = Vector3.Cross(direction, Vector3.right);
    side.Normalize();

    float length = Mathf.Min(0.25f, vector.magnitude * 0.25f);
    Vector3 headBase = end - direction * length;
    Gizmos.DrawLine(origin, end);
    Gizmos.DrawLine(end, headBase + side * length * 0.5f);
    Gizmos.DrawLine(end, headBase - side * length * 0.5f);
}

private static void DrawCircle(
    Vector3 center, Vector3 right, Vector3 up,
    float radius, int segments)
{
    if (radius <= 0f || segments < 3) return;

    Vector3 previous = center + right * radius;
    for (int i = 1; i <= segments; i++)
    {
        float angle = i * Mathf.PI * 2f / segments;
        Vector3 current = center
            + (right * Mathf.Cos(angle) + up * Mathf.Sin(angle)) * radius;
        Gizmos.DrawLine(previous, current);
        previous = current;
    }
}
```

- [ ] **Step 6：编译**

```powershell
dotnet build Game.Combat.csproj --no-restore /m:1
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
```

Expected：0 errors。

- [ ] **Step 7：Editor Gate 2**

1. 新建 `ProjectileDebugGizmos` 空对象并挂 Drawer。
2. 打开 Scene View `Gizmos`。
3. 普通火球：白色速度箭头。
4. Gravity：白色速度 + 黄色重力。
5. Homing：青色搜索球，锁定后有目标连线。
6. Orbit：蓝色圆心/前进轴 + 橙色轨道圆。
7. Bounce/Shield Reflection：红色法线 + 绿色反射方向，约 0.5 秒后消失。
8. `Only Draw Selected` 开启后，只显示选中投射物。
9. 验证完关闭 Drawer 或 Scene Gizmos。

Gate：用户确认后才进入 Task 6。

- [ ] **Step 8：提交 Task 5**

```powershell
git add -- `
  'Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugGizmoDrawer.cs' `
  'Assets/_Project/Scripts/Combat/_Debug/ProjectileDebugGizmoDrawer.cs.meta' `
  'Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs'
git commit -m "feat(combat): add projectile scene diagnostics"
```

# 第三阶段：SpellPerformanceLab

## Task 6：实现标准阶梯与自定义压力驱动器

**Files:**

- Create: `Assets/_Project/Scripts/Character/_Debug/SpellPerformanceLabController.cs`

**Interfaces:**

- Consumes: `SpellCaster.CastWand(Vector3, Vector3, byte, int, Collider)`
- Consumes: `SpellCaster.TraceLevel`
- Consumes: `ProjectileBase.ActiveCount`
- Produces: ContextMenu `Start Lab Run` / `Stop Lab Run`
- Produces: `PerformanceLab.Fill` Profiler sample

- [ ] **Step 1：定义 Scenario、RunMode 和 RunState**

```csharp
using System;
using UnityEngine;
using Unity.Profiling;
using Game.Core;
using Game.Combat;

namespace Game.Character
{
    public enum SpellPerformanceRunMode : byte
    {
        StandardLadder = 0,
        Custom = 1
    }

    public enum SpellPerformanceRunState : byte
    {
        Idle = 0,
        Filling = 1,
        Holding = 2,
        Completed = 3
    }

    [Serializable]
    public sealed class SpellPerformanceScenario
    {
        public string Name = "Scenario";
        public bool Enabled = true;
        public SpellCaster Caster;
        public Transform SpawnPoint;
        public Transform AimTarget;
        public byte Team = 1;
        public int AttackerId = 1;
        public Collider CasterCollider;
    }
```

- [ ] **Step 2：实现 Controller 配置与只读状态**

```csharp
    public class SpellPerformanceLabController : MonoBehaviour
    {
        [Header("Scenario")]
        [SerializeField] private SpellPerformanceScenario[] _scenarios;
        [SerializeField, Min(0)] private int _selectedScenarioIndex;
        [SerializeField] private SpellPerformanceRunMode _runMode;
        [SerializeField] private bool _autoStart;

        [Header("标准阶梯")]
        [SerializeField] private int[] _standardTargets = { 1, 10, 50, 100, 200 };

        [Header("自定义")]
        [SerializeField, Min(1)] private int _customTarget = 50;
        [SerializeField] private bool _maintainCustomIndefinitely;

        [Header("公共参数")]
        [SerializeField, Min(1)] private int _maxCastsPerFrame = 1;
        [SerializeField, Min(0.1f)] private float _holdSeconds = 3f;
        [SerializeField, Min(0.5f)] private float _fillTimeoutSeconds = 10f;

        private static readonly ProfilerMarker s_fillMarker =
            new ProfilerMarker("PerformanceLab.Fill");

        private SpellPerformanceScenario _activeScenario;
        private SpellPerformanceRunState _state;
        private int _tierIndex;
        private int _currentTarget;
        private int _peakActive;
        private float _phaseTimer;

        public SpellPerformanceRunState State => _state;
        public int CurrentTarget => _currentTarget;
        public int PeakActive => _peakActive;

        private void Start()
        {
            if (_autoStart)
                StartLabRun();
        }
```

- [ ] **Step 3：实现开始、停止和 Scenario 校验**

```csharp
        [ContextMenu("Start Lab Run")]
        public void StartLabRun()
        {
            if (!Application.isPlaying)
            {
                GameLog.Warn("SpellPerformanceLab 只能在 Play Mode 运行", "PerformanceLab");
                return;
            }

            if (!TryResolveScenario(out _activeScenario))
                return;

            if (_activeScenario.Caster.TraceLevel != CastTraceLevel.Off)
            {
                GameLog.Warn(
                    "SpellCaster Trace 未关闭，Console 开销会污染性能数据",
                    "PerformanceLab");
            }

            _tierIndex = 0;
            _peakActive = ProjectileBase.ActiveCount;
            BeginTarget(_runMode == SpellPerformanceRunMode.Custom
                ? Mathf.Max(1, _customTarget)
                : ResolveStandardTarget(0));
        }

        [ContextMenu("Stop Lab Run")]
        public void StopLabRun()
        {
            _state = SpellPerformanceRunState.Idle;
            GameLog.Info("Performance Lab 已停止", "PerformanceLab");
        }

        private bool TryResolveScenario(out SpellPerformanceScenario scenario)
        {
            scenario = null;
            if (_scenarios == null
                || _selectedScenarioIndex < 0
                || _selectedScenarioIndex >= _scenarios.Length)
            {
                GameLog.Warn("Scenario Index 越界或列表为空", "PerformanceLab");
                return false;
            }

            scenario = _scenarios[_selectedScenarioIndex];
            if (scenario == null || !scenario.Enabled)
            {
                GameLog.Warn("选中的 Scenario 为空或未启用", "PerformanceLab");
                return false;
            }

            if (scenario.Caster == null
                || scenario.SpawnPoint == null
                || scenario.AimTarget == null)
            {
                GameLog.Warn(
                    "Scenario 必须配置 Caster、SpawnPoint、AimTarget",
                    "PerformanceLab");
                return false;
            }

            if (scenario.Caster.Wand == null)
            {
                GameLog.Warn("Scenario 的 SpellCaster 没有 WandLoadout", "PerformanceLab");
                return false;
            }

            return true;
        }

        private int ResolveStandardTarget(int index)
        {
            if (_standardTargets == null || index < 0 || index >= _standardTargets.Length)
                return -1;
            return Mathf.Max(1, _standardTargets[index]);
        }
```

- [ ] **Step 4：实现 Update 状态机**

```csharp
        private void Update()
        {
            if (_state == SpellPerformanceRunState.Idle
                || _state == SpellPerformanceRunState.Completed)
                return;

            int active = ProjectileBase.ActiveCount;
            if (active > _peakActive)
                _peakActive = active;

            _phaseTimer += Time.unscaledDeltaTime;

            if (_state == SpellPerformanceRunState.Filling)
                TickFilling(active);
            else if (_state == SpellPerformanceRunState.Holding)
                TickHolding(active);
        }

        private void TickFilling(int active)
        {
            if (active < _currentTarget)
                FillToTarget();

            active = ProjectileBase.ActiveCount;
            if (active >= _currentTarget)
            {
                _state = SpellPerformanceRunState.Holding;
                _phaseTimer = 0f;
                GameLog.Info($"达到目标 {_currentTarget}，开始 Hold", "PerformanceLab");
                return;
            }

            if (_phaseTimer >= _fillTimeoutSeconds)
            {
                GameLog.Warn(
                    $"填充超时：目标 {_currentTarget}，峰值 {_peakActive}",
                    "PerformanceLab");
                _state = SpellPerformanceRunState.Completed;
            }
        }

        private void TickHolding(int active)
        {
            if (active < _currentTarget)
                FillToTarget();

            if (_runMode == SpellPerformanceRunMode.Custom
                && _maintainCustomIndefinitely)
                return;

            if (_phaseTimer >= _holdSeconds)
                CompleteCurrentTarget();
        }
```

- [ ] **Step 5：实现受限 Fill、阶梯推进和完成**

```csharp
        private void FillToTarget()
        {
            using (s_fillMarker.Auto())
            {
                int maxCasts = Mathf.Max(1, _maxCastsPerFrame);
                for (int i = 0; i < maxCasts; i++)
                {
                    if (ProjectileBase.ActiveCount >= _currentTarget)
                        break;

                    int emitted = _activeScenario.Caster.CastWand(
                        _activeScenario.SpawnPoint.position,
                        _activeScenario.AimTarget.position,
                        _activeScenario.Team,
                        _activeScenario.AttackerId,
                        _activeScenario.CasterCollider);

                    if (emitted <= 0)
                        break;
                }
            }
        }

        private void CompleteCurrentTarget()
        {
            GameLog.Info(
                $"完成目标 {_currentTarget}，当前 {ProjectileBase.ActiveCount}，峰值 {_peakActive}",
                "PerformanceLab");

            if (_runMode == SpellPerformanceRunMode.Custom)
            {
                _state = SpellPerformanceRunState.Completed;
                return;
            }

            _tierIndex++;
            int nextTarget = ResolveStandardTarget(_tierIndex);
            if (nextTarget < 0)
            {
                _state = SpellPerformanceRunState.Completed;
                GameLog.Info($"标准阶梯完成，峰值 {_peakActive}", "PerformanceLab");
                return;
            }

            BeginTarget(nextTarget);
        }

        private void BeginTarget(int target)
        {
            if (target <= 0)
            {
                _state = SpellPerformanceRunState.Completed;
                GameLog.Warn("目标数量无效，测试终止", "PerformanceLab");
                return;
            }

            _currentTarget = target;
            _phaseTimer = 0f;
            _state = SpellPerformanceRunState.Filling;
            GameLog.Info(
                $"开始目标 {_currentTarget}，当前 Active={ProjectileBase.ActiveCount}",
                "PerformanceLab");
        }
    }
}
```

标准阶梯不清理上一档，而是从已有数量继续增加。自定义测试建议重新进入 Play Mode，避免残留投射物影响初始值。

- [ ] **Step 6：编译**

Unity 刷新新脚本后运行：

```powershell
dotnet build Game.Combat.csproj --no-restore /m:1
dotnet build Game.Skills.csproj --no-restore /m:1
dotnet build Game.Character.csproj --no-restore /m:1
```

Expected：全部 0 errors。

- [ ] **Step 7：提交 Task 6**

```powershell
git add -- `
  'Assets/_Project/Scripts/Character/_Debug/SpellPerformanceLabController.cs' `
  'Assets/_Project/Scripts/Character/_Debug/SpellPerformanceLabController.cs.meta'
git commit -m "feat(character): add spell performance lab driver"
```

---

## Task 7：在 Unity Editor 搭建 SpellPerformanceLab

**Files:**

- Create in Editor: `Assets/_Project/Scenes/SpellPerformanceLab.unity`
- Optional create in Editor: Lab 专用 `WandLoadout` / Spell assets
- Do not manually edit: `.unity` / `.meta`

**Produces:**

- 可重复运行标准阶梯的独立场景
- 可切换 Scenario 的自定义压力入口
- 第一版 CPU/Rendering/GC 基线

- [ ] **Step 1：创建并保存场景**

1. `File > New Scene`，选择 Basic/Empty。
2. `File > Save As`。
3. 保存为 `Assets/_Project/Scenes/SpellPerformanceLab.unity`。
4. 不必加入正式 Build Profiles；它是开发实验场景。

- [ ] **Step 2：建立最小 Hierarchy**

```text
SpellPerformanceLab
├─ Main Camera
├─ Directional Light
├─ Ground
├─ ProjectileCatcher
├─ SpawnPoint
├─ AimTarget
├─ ScenarioCasters
├─ PerformanceLabController
└─ ProjectileDebugGizmos
```

设置：

- `Ground` 使用大平面并带 Collider。
- `SpawnPoint` 离地约 1.5-2 米。
- `AimTarget` 与 SpawnPoint 同高，朝长距离空旷区域。
- 普通火球 `MaxLifetime=5`、速度约 20 时约飞 100 米，因此 Catcher 至少放在 120 米外，或第一版不放墙。
- 不放 Enemy AI、玩家 UI 和环境 VFX，控制变量。

- [ ] **Step 3：创建真实 Scenario Caster**

在 `ScenarioCasters` 下创建：

```text
Caster_SingleFireball
Caster_TripleFireball
Caster_TriggerPayload
Caster_GravityHoming
Caster_Orbit
```

每个对象添加：

- `SpellCaster`。
- `ManaComponent`。
- `SphereCollider`。

每个 `SpellCaster.Wand` 拖对应 `WandLoadout`。Lab 专用 Mana 建议：

```text
Max Mana = 1000000
Mana Regen Per Second = 1000000
Start Full = true
```

只改 Lab Scene 对象，不改正式玩家 Prefab。

- [ ] **Step 4：配置 PerformanceLabController**

默认：

```text
Run Mode = Standard Ladder
Selected Scenario Index = 0
Standard Targets = [1, 10, 50, 100, 200]
Max Casts Per Frame = 1
Hold Seconds = 3
Fill Timeout Seconds = 10
Auto Start = false
```

建立 5 个 Scenario，分别拖入 Caster，共用 SpawnPoint/AimTarget；`CasterCollider` 指向对应 SphereCollider；Team=1；AttackerId 使用正整数。

- [ ] **Step 5：配置 Gizmo Drawer，但性能基线时关闭**

`ProjectileDebugGizmos` 挂 Drawer。空间 Debug 时启用；记录性能前禁用 GameObject 或关闭 Scene Gizmos，并确保所有 `SpellCaster.TraceLevel = Off`。

- [ ] **Step 6：验证标准阶梯**

进入 Play Mode，在组件菜单执行 `Start Lab Run`。Console 应按顺序出现：

```text
开始目标 1 -> 达到目标 1 -> 完成目标 1
开始目标 10 -> 达到目标 10 -> 完成目标 10
标准阶梯完成
```

如果超时，依次检查：Mana、Wand 是否有 `ProjectileBase` Emit、是否提前碰撞、Lifetime 是否过短、Trace 是否刷屏。

- [ ] **Step 7：验证自定义模式**

退出 Play Mode，设置：

```text
Run Mode = Custom
Custom Target = 80
Maintain Custom Indefinitely = false
Max Casts Per Frame = 2
Hold Seconds = 5
```

重进 Play，选择 GravityHoming/Orbit Scenario。Expected：达到 80，保持 5 秒，进入 Completed。

再开启 `Maintain Custom Indefinitely`：持续补充，直到执行 `Stop Lab Run` 或退出 Play Mode。

- [ ] **Step 8：打开 Unity Profiler 建立基线**

1. `Window > Analysis > Profiler`。
2. 启用 `CPU Usage`、`Rendering`、`Memory`、`Physics`。
3. 关闭 `Deep Profile`。
4. 清空 Console；Trace Off；Gizmos Off。
5. 运行标准阶梯。
6. CPU Hierarchy 搜索：
   - `Spell.Evaluate`
   - `Spell.RuntimeSpawn`
   - `Projectile.Motion`
   - `PerformanceLab.Fill`
7. 观察 1/10/50/100/200 档：Main Thread、Physics.FixedUpdate、GC Alloc、Batches、SetPass Calls、峰值 ActiveCount。

Editor 数据只作为相对基线，不宣称等于 Release Build 性能。

- [ ] **Step 9：Editor Gate 3**

必须确认：

- 标准阶梯推进或明确超时，不死循环。
- 自定义模式和无限维持模式可用。
- Profiler 能找到 Marker。
- Trace/Gizmos 关闭后 Console 不刷屏。
- P0 新代码没有持续每帧 GC Alloc。

- [ ] **Step 10：用户确认后单独提交 Scene**

```powershell
git add -- `
  'Assets/_Project/Scenes/SpellPerformanceLab.unity' `
  'Assets/_Project/Scenes/SpellPerformanceLab.unity.meta'
git commit -m "test(performance): add spell performance lab scene"
```

若创建 Lab 专用 ScriptableObject，逐个核对再显式加入，禁止 `git add .`。

---

## Task 8：完整回归与交付

**Files:**

- Verify: all P0 source/tests
- Do not create: `Assets/_Project/Docs` 解析文档，除非用户另行要求

- [ ] **Step 1：运行全部相关编译**

```powershell
dotnet build Game.Core.csproj --no-restore /m:1
dotnet build Game.Combat.csproj --no-restore /m:1
dotnet build Game.Skills.csproj --no-restore /m:1
dotnet build Game.Character.csproj --no-restore /m:1
dotnet build Game.Combat.Tests.csproj --no-restore /m:1
dotnet build Game.Skills.Tests.csproj --no-restore /m:1
```

Expected：每条 0 errors。新增 warning 必须逐条判断。

- [ ] **Step 2：运行 Unity EditMode Tests**

Test Runner 中运行全部 `Game.Skills.Tests` 和 `Game.Combat.Tests`。Expected：0 failed。

- [ ] **Step 3：执行架构与热路径扫描**

```powershell
rg -n "Debug\.Log|Debug\.LogWarning|Debug\.LogError" `
  Assets/_Project/Scripts/Skills/Diagnostics `
  Assets/_Project/Scripts/Character/_Debug `
  Assets/_Project/Scripts/Combat/_Debug

rg -n "Game\.Skills|ProjectileMotionMode" Assets/_Project/Scripts/Combat/_Debug

rg -n "System\.Linq|Select\(|Where\(|FirstOrDefault" `
  Assets/_Project/Scripts/Combat/_Debug `
  Assets/_Project/Scripts/Character/_Debug
```

Expected：无直接 Debug.Log；Combat Debug 不引用 Skills；hot path 无 LINQ。

- [ ] **Step 4：检查 Git 范围**

```powershell
git status --short
git diff --check
git log --oneline -8
```

确认：未误提交 `Packages`、`ProjectSettings`、SampleScene 用户改动；新脚本 `.meta` 已由 Unity 生成并跟踪；没有自行生成 `Assets/_Project/Docs` 文档。

- [ ] **Step 5：正常游戏场景人工回归**

逐项验证：普通火球、三重、Gravity、Homing、Orbit、Bounce、Trigger、Timed Trigger、Meteor、Static Shield、Interactive Shield、mana 扣除与恢复。

Expected：Trace Off 时行为与 P0 前一致；P0 只增加观察能力。

- [ ] **Step 6：最终交付说明**

报告：

- 新增/修改文件。
- 命令行编译结果。
- Unity Test Runner 和三个 Editor Gate 的结果。
- 第一版性能基线观察事实，不包装为 Release 结论。
- 现有无关 dirty files 保持未动。

后续若用户要求复盘，再单独在 `Assets/_Project/Docs` 生成中文解析文档；本计划不自行创建。

---

## 执行顺序摘要

```text
Task 1 Trace 数据模型
-> Task 2 CastEvaluator Trace + Tests
-> Task 3 SpellCaster Console Trace
-> Editor Gate 1

-> Task 4 Projectile Snapshot + Tests
-> Task 5 Scene Gizmos
-> Editor Gate 2

-> Task 6 Performance Lab Driver
-> Task 7 用户创建 Lab Scene + Profiler Baseline
-> Editor Gate 3

-> Task 8 全量回归与交付
```

任何 Gate 未通过，都先修复当前阶段，不提前进入下一阶段。
