# 连续关卡稀疏元素世界实施计划

> **执行方式：** 在当前会话中按 Task 顺序执行，每个 Task 都经过 TDD、静态检查、Unity Test Runner 和用户 Editor Gate；遵守用户要求，不使用 SubAgent。

**Goal：** 把当前仅覆盖 `8m × 4m × 8m` 固定实验房的 P6-A ElementField，扩展为支持约 `40m～60m` 连续小型关卡的稀疏元素世界：法术可以在关卡任意合法位置创建 Water/Fire Chunk，玩家附近 Chunk 高频模拟与渲染，远处非空 Chunk 在本次 Play Session 内休眠并保留数据，玩家返回后继续模拟。

**Architecture：** 保留 `ElementFieldRuntime` 和现有固定数组 Simulator 作为 P6-A 回归基线，不让 `ElementFieldLabRoot` 跟随玩家。新增固定世界坐标的 `ElementWorldRuntime`：用全局 Cell/Chunk 坐标和稀疏 `Dictionary<ElementChunkKey, ElementWorldChunk>` 保存真正出现过元素的 Chunk；玩家只决定 Active/Sleep 集合，不改变世界 Origin。新 World Simulator 以稳定 Snapshot + Delta 处理跨 Chunk Reaction/Down Flow/Lateral Flow/Fire Decay；Projectile、Exposure、Water/Fire Rendering 通过新接口接入，Gameplay 不依赖 Rendering。

**Tech Stack：** Unity 6.3、C#、URP 17.3、NUnit EditMode Tests、`ScriptableObject`、稀疏 Chunk Dictionary、固定 `10 Hz` Tick、预分配工作缓冲、NonAlloc Physics、动态 Chunk Mesh、共享三层 `ParticleSystem`、Unity Profiler。

## Global Constraints

- 玩家职业只保留 `WizardPlayer`；Warrior/Archer 不进入当前 Demo 玩家流程。
- 当前元素范围只包含 Water/Fire；Sticky/Poison 属于 Demo 完整后扩展项。
- 世界坐标系固定，严禁把元素世界 Root Parent 到玩家或逐帧移动 World Origin。
- `CellSize = 0.25m`，`ChunkSize = 8`，一个 Chunk 为 `2m × 2m × 2m`；Chunk Key 允许负坐标。
- 玩家是主要 Interest Point；法术写入点也可以激活命中 Chunk。
- Active Chunk 按 `10 Hz` 模拟；Sleep Chunk 保留非空 Cell，但不模拟、不 Exposure、不渲染。
- 第一版只保证同一次 Play Session 内持久化，不实现磁盘存档、联网同步或跨 Scene 保存。
- 非空 Chunk 不因距离被丢弃；空且休眠超过 Grace Period 的 Chunk才可以回收。
- 到达 `MaximumResidentChunks` 后拒绝创建新 Chunk，并通过限频 `GameLog.Warn` 暴露错误，不能静默覆盖旧元素。
- 不为 Cell 创建 GameObject、Transform 或 Collider；一个可见 Water Chunk最多一个 Mesh GameObject，Fire 继续复用全世界三层 ParticleSystem。
- `Game.ElementField` 依赖 `Game.Core + Game.Combat`；`Game.Rendering` 只读 Element World；`Game.Combat` 不反向依赖 ElementField。
- 热路径禁止 LINQ、Closure、Boxing 和每 Tick `new`；Dictionary 扩容必须在初始化 Capacity 预算内完成。
- 保留现有固定 `ElementFieldRuntime` Tests 和 SampleScene Gate，所有新增改动不得破坏 P6-A。
- 每完成一个 Task，更新 `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`，写明基础原理、脚本职责、架构集成、公式、测试、踩坑和 Editor Gate。

## 本计划不包含的下一子项目

本计划只完成“连续关卡可以承载元素互动”的世界基础。其后单独制定灰盒关卡与流程计划，包括：S 形 `40m～60m` 地图、三个战斗区、Encounter Gate、最终精英、出口和通关 UI。这样可以先证明任意位置 Cell 正常，再搭关卡，避免地编完成后因 Runtime 重构返工。

## 方案比较与最终选择

| 方案 | 优点 | 主要问题 | 结论 |
|---|---|---|---|
| 让现有 `ElementFieldLabRoot` 跟随玩家 | 改动看似最少 | Origin 改变导致旧 Cell、Solid、Mesh 和世界位置错位 | 禁止 |
| 扩大单个连续数组覆盖整张关卡 | 可复用现有 Simulator | 大量 Empty Cell 常驻；每 Tick 全量扫描；尺寸迅速超过 131K | 不采用 |
| 多个手工固定 Field 拼接 | 容易理解 | Field 边界阻断水流和反应；关卡作者负担高 | 仅可作临时原型 |
| 稀疏世界 Chunk + 玩家 Active Set | 任意命中点按需创建；跨区持久；规模由真实互动控制 | 需要新增跨 Chunk Solver 和 Renderer 生命周期 | 最终采用 |

---

## Task 0：冻结 P6-A 基线并建立 P6-B 学习入口

**Files:**
- Modify: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`
- Modify: `docs/superpowers/plans/2026-07-15 P6实施计划.md`

**Purpose：** 在开始架构扩展前记录固定 Field 已经通过的能力和未完成的 Task 9 性能证据。新世界开发出现问题时，必须能判断是 P6-A 基础规则回归，还是 P6-B Streaming 新逻辑错误。

- [x] **Step 1：运行全项目 EditMode 基线**

在 Unity `Window → General → Test Runner → EditMode → Run All`。Expected：现有 `243+` Tests 全绿；记录实际总数，不能硬写旧数字。

- [x] **Step 2：执行固定实验场三项 Smoke Gate**

1. Water Projectile 在 `ElementFieldLabRoot` 内命中后形成水面并扩散。
2. Fireball 命中 Water 后消耗对应水区。
3. Wizard 站在 Water/Fire 中分别获得 Wet/Burning。

- [x] **Step 3：在学习文档增加 P6-B 范围说明**

明确 `ElementFieldRuntime = 固定实验场回归实现`，`ElementWorldRuntime = 连续关卡实现`，两者不能在同一 Gameplay Scene 同时成为 Active Write Sink。

- [x] **Step 4：提交基线文档**

```bash
git add -- "docs/superpowers/plans/2026-07-15 P6实施计划.md" "Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md"
git commit -m "docs(element-field): define sparse world extension baseline"
```

---

## Task 1：建立稳定的全局 Cell/Chunk 坐标与稀疏数据容器

**Files:**
- Create: `Assets/_Project/Scripts/ElementField/World/ElementChunkKey.cs`
- Create: `Assets/_Project/Scripts/ElementField/World/ElementWorldCoordinates.cs`
- Create: `Assets/_Project/Scripts/ElementField/World/ElementWorldChunk.cs`
- Create: `Assets/_Project/Scripts/ElementField/World/ElementWorldStore.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementWorldCoordinatesTests.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementWorldStoreTests.cs`
- Modify: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`

**Interfaces:**
- Consumes: existing `ElementCell`, `ElementMaterialKind`.
- Produces: `ElementChunkKey`, `ElementWorldCoordinates.WorldToGlobalCell(...)`, `GlobalCellToChunkAndLocal(...)`, `ElementWorldStore.TryGetChunk(...)`, `GetOrCreateChunk(...)`, `TryGetCell(...)`.

- [x] **Step 1：先写负坐标与边界 RED Tests**

必须覆盖：`-0.01m` 映射到 Cell `-1`；Global Cell `-1` 映射到 Chunk `-1`、Local `7`；Cell `8` 映射到 Chunk `1`、Local `0`；World→Cell→Chunk 组合在正负坐标一致。

```csharp
[TestCase(-1, -1, 7)]
[TestCase(0, 0, 0)]
[TestCase(7, 0, 7)]
[TestCase(8, 1, 0)]
public void GlobalCellX_MapsToStableChunkAndPositiveLocal(
    int globalX, int expectedChunkX, int expectedLocalX)
{
    ElementWorldCoordinates.SplitAxis(
        globalX, chunkSize: 8, out int chunkX, out int localX);
    Assert.That(chunkX, Is.EqualTo(expectedChunkX));
    Assert.That(localX, Is.EqualTo(expectedLocalX));
}
```

- [x] **Step 2：运行目标 Tests 确认 RED**

Unity Test Runner 运行 `ElementWorldCoordinatesTests`。Expected：因类型尚不存在编译失败。

- [x] **Step 3：实现 `ElementChunkKey` 与 Floor Division**

`ElementChunkKey` 使用三个 `int`、实现 `IEquatable<ElementChunkKey>` 和稳定 `GetHashCode`。负数必须使用数学 Floor Division，不能直接依赖 C# 向零截断的 `/` 与 `%`。

```text
chunk = floor(globalCell / chunkSize)
local = globalCell - chunk × chunkSize
保证 0 ≤ local < chunkSize
```

- [x] **Step 4：写 Store RED Tests**

覆盖：未知 Chunk 返回 Empty；第一次创建增加 Resident Count；同 Key 重复获取返回同实例；超过 Capacity 拒绝；非空 Sleep Chunk 保持 Cell；空 Chunk 可以删除。

- [x] **Step 5：实现固定容量预算的 `ElementWorldStore`**

初始化 Dictionary Capacity，创建 Chunk 时一次性分配 `8³` Current/Next、Solid、Delta 与 Dirty Buffer。Chunk 复用 `ElementGrid` 的 Dirty Version，并持有 `HasAnyElement`；生命周期需要的 `LastTouchedTick` 留在 Task 2 引入，避免数据容器提前承担调度职责。Chunk 不拥有 Unity GameObject。

- [x] **Step 6：运行 ElementField Tests**

结果：RED 阶段完整清理后得到 27 个预期的缺失类型编译错误；GREEN 阶段 `Game.ElementField` 与 Tests 工程 0 warning / 0 error。用户在 Unity Test Runner 中确认新增 Coordinates/Store Tests 与现有 ElementField Tests 全绿。

- [x] **Step 7：更新学习文档**

解释 Euclidean/Floor Division、负坐标、Hash Key、稀疏存储与连续数组的区别、一个 8³ Chunk 的内存组成，以及为什么 Chunk 不是 GameObject。

- [x] **Step 8：提交 Task 1**

```bash
git add -- "Assets/_Project/Scripts/ElementField/World" "Assets/_Project/Tests/ElementField/ElementWorldCoordinatesTests.cs" "Assets/_Project/Tests/ElementField/ElementWorldStoreTests.cs" "Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md"
git commit -m "feat(element-field): add sparse world chunk storage"
```

---

## Task 2：建立玩家 Interest Point 与 Active/Sleep Chunk 生命周期

**Files:**
- Create: `Assets/_Project/Scripts/ElementField/Data/ElementWorldProfile.cs`
- Create: `Assets/_Project/Scripts/ElementField/World/ElementChunkActivityPlanner.cs`
- Create: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementChunkActivityPlannerTests.cs`
- Modify: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`

**Interfaces:**
- Consumes: Task 1 Store/Coordinates, a serialized Wizard `Transform` used only as Interest Point.
- Produces: `IElementWorldReadOnly`, `ElementWorldRuntime.TryEnqueueWrite(...)`, Active Chunk enumeration without per-frame allocation.

- [x] **Step 1：写 Activity Planner RED Tests**

覆盖：玩家仍在同 Chunk 时不改变 Active Set；跨过 2m Chunk 边界时中心 Key 改变；只激活已 Resident 的近处 Chunk；远处非空 Chunk进入 Sleep 而不删除；空 Chunk超过 Grace Tick 后可回收。

- [x] **Step 2：实现纯 `ElementChunkActivityPlanner`**

Planner 输入 Player Chunk、Resident Keys、`ActiveRadiusXZChunks`、`ActiveRadiusYChunks`，输出每个 Chunk 的 Active/Sleep 状态。Editor Gate 前根据连续关卡体验反馈把默认值扩大为 XZ 半径 `10 Chunk = 20m`、Y 半径 `3 Chunk = 6m`；因为每个 Chunk 边长 2m，玩家周围水平活跃窗口约为 `40m × 40m`。法术命中或反应触及玩家窗口之外的 Chunk 时，通过 `LastRelevantTick` 获得一个 Grace Lease，避免远程法术命中后环境立即冻结。

- [x] **Step 3：创建 `ElementWorldProfile`**

Inspector 字段：CellSize 0.25、ChunkSize 8、TickRate 10、Active Radius XZ 10、Active Radius Y 3、Sleep Grace 2 秒、Maximum Resident Chunks 512、Max Pending Writes 256、Max Catch-Up Ticks 3，以及现有 Water Flow/Fire Decay/Reaction Profile。用户已在 `Assets/_Project/ScriptableObjects/ElementField/ElementWorldProfile_P6B.asset` 创建并配置实例。

- [x] **Step 4：实现 `ElementWorldRuntime` 生命周期骨架**

Root Transform 必须保持 Identity Rotation/Unit Scale，Origin 在 Awake Snapshot 后固定。`Update` 只计算玩家 Chunk 变化和 Fixed Tick；同一帧不能通过 `FindObjectOfType` 查玩家，必须由 Inspector 引用 Wizard Transform。

- [x] **Step 5：实现安全上限和限频 Warning**

超过 Resident Cap 时拒绝新 Chunk；缺失 Interest Point 时禁用 Runtime 并明确报错；Active Set 为空是合法状态。

- [x] **Step 6：运行 Tests 与静态检查**

最终证据：`Game.ElementField.Tests.csproj` 编译 0 warning / 0 error；非 Scene 源码与文档 `git diff --check` 通过。用户在范围与租约调整后重新运行最新 Unity Test Runner：`ElementChunkActivityPlannerTests 11/11`、`Game.ElementField.Tests 83/83`、全项目 EditMode `271/271` 通过，0 failed、0 ignored。Planner 覆盖玩家窗口、远程 Grace Lease、非空 Sleep、空 Chunk 回收边界与负坐标 Chunk。

- [x] **Step 7：Editor Skeleton Gate**

用户已在 SampleScene 创建顶层 `ElementWorldLabRoot`，挂 Runtime、赋 Wizard Transform 和 `ElementWorldProfile_P6B`；进入 Play Mode 后跨越多个 2m Chunk，Inspector 的 `Interest Global Cell` 与 `Interest Chunk Key` 正确更新。Resident/Active/Sleep 仍为 0，符合 Task 3 尚未迁移 World Write 的预期。

- [x] **Step 8：更新学习文档并提交**

解释 Interest Point、Active/Sleep/Resident 三种状态、Hysteresis/Grace Period、为什么玩家控制调度而不控制坐标系。

---

## Task 3：实现按需写入与跨 Chunk Water/Fire 模拟

**Files:**
- Create: `Assets/_Project/Scripts/ElementField/World/ElementWorldWriteProcessor.cs`
- Create: `Assets/_Project/Scripts/ElementField/World/ElementWorldSimulator.cs`
- Create: `Assets/_Project/Scripts/ElementField/World/ElementBoundaryNeighborPlanner.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementWorldWriteProcessorTests.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementWorldSimulatorTests.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Simulation/ElementReactionEvaluator.cs` only if a pure shared formula is missing; do not duplicate P2 formulas.
- Modify: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`

**Interfaces:**
- Consumes: existing `ElementWriteRequest`, shared Reaction Kernel, World Store.
- Produces: deterministic `ApplyWrites(...)` and `SimulateActiveChunks(...)` with cross-border conservation.

- [ ] **Step 1：写跨边界 RED Tests**

必须覆盖：球形 Deposit 横跨 Chunk X=0/1 时总 Amount 守恒；Water 从 Local X=7 流向相邻 Chunk Local X=0；Water 向下跨 Y Chunk；Water/Fire 在两个 Chunk 边界反应等量消耗；Sleep Chunk 不自行流动/衰减；相同输入得到相同输出。

- [ ] **Step 2：实现两遍归一化 World Deposit**

第一遍收集球形覆盖的 Global Cells 与权重，按需请求 Chunk；第二遍把 `TotalAmount` 按权重分配，余数使用稳定 Global Cell 顺序补齐，保证：

```text
Σ addedAmount = min(request.TotalAmount, availableCapacity)
```

- [ ] **Step 3：实现 Boundary Neighbor 预准备**

Stage 开始前扫描 Active Chunk 边界非空 Cell，把可能接收水/参与反应的相邻 Key 写入预分配 Key Buffer，再集中创建；禁止在 Dictionary Enumerator 内创建 Chunk。

- [ ] **Step 4：实现 World Snapshot + Delta Solver**

每个 Stage 只读稳定 Current Snapshot，把变化写入各 Chunk Delta/Next；完成一个 Stage 后统一 Commit。跨 Chunk 转移必须满足来源减少量等于目标增加量。

- [ ] **Step 5：复用现有规则顺序**

```text
Writes → Reaction → Down Flow → Horizontal Flow → Fire Decay
```

水平扩散继续使用 `difference / 8` 稳定公式；遍历方向按 Tick 奇偶翻转，但 Global Cell 排序必须稳定。

- [ ] **Step 6：运行全 ElementField Tests**

Expected：新增 World Tests 和旧 Fixed Field Tests 全绿。

- [ ] **Step 7：更新学习文档并提交**

重点解释跨 Chunk Halo、Snapshot/Delta、Dictionary 遍历不确定性、守恒、Stage Barrier 和 Sleep 的时间语义。

---

## Task 4：把 Projectile Deposit 与角色 Exposure 接入世界 Runtime

**Files:**
- Create: `Assets/_Project/Scripts/ElementField/Data/IElementWriteSink.cs`
- Create: `Assets/_Project/Scripts/ElementField/Runtime/ElementRuntimeRegistry.cs`
- Create: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldExposureSystem.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementFieldRuntime.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementDepositOnImpact.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementRuntimeRegistryTests.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementWorldExposureSystemTests.cs`
- Modify: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`

**Interfaces:**
- `IElementWriteSink.TryEnqueueWrite(in ElementWriteRequest request)`.
- Registry 只允许一个 Active Sink；固定 Runtime 和 World Runtime 实现同一写入契约。

- [ ] **Step 1：写 Registry 与 Exposure RED Tests**

覆盖：无 Sink 时 Deposit 失败；重复注册被拒绝；Disable 后释放；World Runtime优先级不依赖 Scene 查找；Collider Bounds 跨多个 World Chunk 时取最大 Water/Fire Amount；Sleep Chunk不施加状态。

- [ ] **Step 2：把 `ElementDepositOnImpact` 从具体 Runtime 解耦**

Projectile 仍只生成 `ElementWriteRequest`，再交给 Registry 的 Sink；它不认识 Chunk、Store 或玩家位置。保留缺失 Runtime 只警告一次的行为。

- [ ] **Step 3：实现 World Exposure**

使用 `Physics.OverlapBoxNonAlloc` 找 Player/Enemy Collider，把 `Collider.bounds` 转为 Global Cell Min/Max，查询 Active Chunk Cell，按现有比例公式给 `StatusController` 施加 Wet/Burning，并刷新 Decay Hold。

- [ ] **Step 4：运行 Combat + ElementField Tests**

Expected：P2 状态 Tests、P6-A Exposure Tests、新 World Exposure Tests 全绿。

- [ ] **Step 5：Play Mode Gate**

在固定实验 Field 和新 World Scene 分别验证 Water Projectile；两个 Scene 单独工作。同一 Scene 同时启用两种 Runtime 时必须明确报重复 Sink，而不是随机选一个。

- [ ] **Step 6：更新学习文档并提交**

解释 Adapter、Registry、Dependency Inversion、NonAlloc Broadphase 与“视觉存在不等于 Gameplay 存在”。

---

## Task 5：实现稀疏 Water/Fire Chunk Rendering 生命周期

**Files:**
- Create: `Assets/_Project/Scripts/Rendering/ElementField/ElementWorldWaterRenderer.cs`
- Create: `Assets/_Project/Scripts/Rendering/ElementField/ElementWorldFireRenderer.cs`
- Create: `Assets/_Project/Scripts/Rendering/ElementField/WorldWaterChunkViewPool.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/WaterChunkMeshBuilder.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/FireCellEmissionSampler.cs` only if Global Cell identity cannot be supplied without changing the deterministic Hash contract.
- Create: `Assets/_Project/Tests/Rendering/WorldWaterChunkViewPoolTests.cs`
- Create: `Assets/_Project/Tests/Rendering/WorldWaterChunkMeshBuilderTests.cs`
- Modify: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`

**Interfaces:**
- Consumes: World ReadOnly Active Chunk snapshot and stable Global Chunk Key.
- Produces: pooled Water Mesh Views and shared three-layer Fire emissions only for Active Chunks.

- [ ] **Step 1：写跨 Chunk Mesh RED Tests**

覆盖：相邻 Chunk 都有 Water 时不生成内部 Side；UV 使用 Global Cell 坐标跨 Chunk 连续；Sleep Chunk View 被回收；重新 Active 后复用 Mesh/GameObject；每帧 Rebuild Budget 有公平性。

- [ ] **Step 2：扩展 Builder 的只读邻域查询**

Builder 仍然是纯 Geometry，不访问 MonoBehaviour。顶面和侧壁判断允许查询相邻 Global Cell；UV：

```text
uv = globalCellXZ × cellSize
```

- [ ] **Step 3：实现 Water View Pool**

预创建/按预算扩展到 Profile Visual Cap；Active Chunk分配 View，Sleep 回收并 Disable Renderer；Mesh 长期复用并在 Destroy 显式释放。

- [ ] **Step 4：实现 World Fire Renderer**

只遍历 Active Resident Chunk；Hash Identity 由稳定 Global Cell 坐标产生，不能使用会随 Dictionary 顺序变化的临时 index。Body/Core/Sparks 继续使用 World Simulation Space 和既有 Materials。

- [ ] **Step 5：运行 Rendering Tests 与 Frame Debugger Gate**

确认 Water 按 Chunk Draw、Fire 只有三层共享系统；玩家跨 Chunk 时旧水面不瞬移，远处 View 关闭，返回后恢复。

- [ ] **Step 6：更新学习文档并提交**

解释 Chunk View Pool、Global UV、邻域剔面、Dirty Version、Hash Identity、Culling 与 Gameplay/Presentation 生命周期差异。

---

## Task 6：连续世界整体验收、性能预算与地编交接

**Files:**
- Create: `Assets/_Project/ScriptableObjects/ElementWorld/ElementWorldProfile_Demo.asset`（由用户在 Editor 创建，Codex 不手写 `.meta`）
- Modify: `Assets/_Project/Scenes/SampleScene.unity` only through user Editor operations unless explicitly authorized for text scene edits.
- Modify: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析（更新中）.md`
- Create: `docs/superpowers/plans/2026-07-17 首个魔法编程灰盒关卡实施计划.md` after this Task passes.

- [ ] **Step 1：全量 Test Runner**

运行 EditMode `Run All`；记录真实总数和 0 failed。若存在失败，先修复再进入 Play Mode。

- [ ] **Step 2：连续移动 Gate**

Wizard 沿至少 30m 路线移动并在三处相距超过 Active Radius 的位置生成 Water/Fire；确认近处模拟、远处休眠、返回恢复。

- [ ] **Step 3：跨 Chunk Gate**

把 Water 命中点放在 2m Chunk 边界两侧，观察水跨边界扩散且无裂缝；Fire 在另一侧与 Water 反应。

- [ ] **Step 4：持久与上限 Gate**

离开非空区域再返回，数据仍在；空区域超过 Grace 后 Resident Count下降；故意降低 MaximumResidentChunks，确认超限只拒绝新 Chunk且 Console 给出限频 Warning。

- [ ] **Step 5：Profiler/Frame Debugger**

记录：`ElementWorld.Simulate`、Apply Writes、Water Rebuild、Fire Emit、GC Alloc、Resident/Active Chunk Count、Water Draw、Fire Particle Count。目标不是伪造固定毫秒数，而是确认热路径 `GC Alloc = 0 B`，并找到当前机器真实 CPU/GPU边界。

- [ ] **Step 6：收口学习文档**

加入完整运行链、所有脚本表、公式、踩坑、Debug 清单、性能证据和模拟面试表达。P6-B 通过后才进入灰盒关卡计划。

- [ ] **Step 7：开始灰盒关卡子项目**

基于已选的 A「S 形分段推进」布局，创建新 Scene，不继续污染 SampleScene；随后逐物体给出 Name、Position、Scale、Collider、Layer、NavMesh 与 Play Mode Gate。

## 最终 Definition of Done

- Wizard 在连续 `40m～60m` 灰盒空间任意合法地面命中 Water/Fire 都能创建对应 Chunk。
- 玩家移动不会改变世界坐标映射，旧元素不会随 Root 漂移。
- Water/Fire 可以跨 Chunk 边界流动、反应、衰减并施加 Wet/Burning。
- 远处非空 Chunk休眠但在本次 Play Session 内持久；返回后恢复。
- Water Mesh 与 Fire Particle只表现 Active Chunk，不为 Cell创建 GameObject。
- 固定 P6-A 实验场全部回归通过。
- 全量 Test Runner 通过，Profiler 热路径无托管分配，性能边界有真实记录。
- 完成后另起灰盒关卡计划，包含三个战斗区域、最终精英与出口流程。
