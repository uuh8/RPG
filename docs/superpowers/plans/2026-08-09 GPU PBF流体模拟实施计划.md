# GPU PBF 流体模拟 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use `superpowers:executing-plans` to implement this plan task-by-task. Every implementation step uses checkbox syntax。用户已在 2026-08-21 明确授权连续执行 Task 14–17，且本轮没有授权 SubAgent；Task 18–25 仍未获实施授权。Task 14–17 的 `completed` 只表示开发交付，Phase F 最终视觉/Profiler Gate 仍按证据单独判定。

**Goal:** 先把现有 GPU PBF Water 收口成连续、可聚合、可断裂与重新融合的可靠 `Liquid Simulation Backend`，再在不推翻 Task 0–13 主链的前提下建立最小 `Material World` 语义层，使 Water 与第二种 Liquid（Poison）共享同一个 PBF Runtime/Pool，却拥有不同身份、Simulation Parameters、Gameplay Status、Rendering Profile 与 Reaction Properties；长期遵守“统一 Material Semantic，不统一 Simulation Algorithm”。

**Architecture:** Task 0–17 继续由 `Game.ElementField` 的 `GpuPbfFluidRuntime` 持有 Liquid GPU Particle Truth，由 `Game.Rendering` 只读重建表面，CPU Occupancy 只是低频 Projection。Task 18 起新增很小的 `Game.Materials` Semantic Assembly，只回答“Material 是谁、属于哪种 Behavior”，而 `ElementWorldRuntime` 仍是唯一写入口，并通过初始化期 Route Snapshot 把 Material 送往 Cell 或 Liquid Backend。Reaction Core 只看 Material Semantic Contact/Amount，Carrier Adapter 才认识 GPU Particle、CPU Cell 或 Status；任何时刻一份 Material Amount 只能由一个 Backend 权威持有。

**Tech Stack:** Unity 6.3（6000.3.16f1）、URP 17.3、C#、HLSL、`ScriptableObject`、asmdef、`ComputeShader`、`GraphicsBuffer`、`Graphics.RenderPrimitivesIndirect`、`AsyncGPUReadback`、NUnit EditMode/PlayMode、Unity Profiler、GPU Profiler、Frame Debugger、Windows Development Build。

## Global Constraints

- 默认使用简体中文叙述和注释，图形学、数学与 Unity 专业术语保留英文原词。
- Phase A–F 只实现 Water Liquid Quality；Phase G 建立最小 Material Semantic Foundation；Phase H 只增加 Poison 作为第二种 Liquid 验证。Gas、Smoke、Sand、Oil、Lava、Slime、Static-to-Rigidbody Transfer 与 Volume Rendering 不进入 Task 14–25。
- 从既有 `byte ElementMaterialKind` 的稳定数值迁移出唯一 Canonical `MaterialId`；迁移完成后禁止同时保留两套可独立取值的 Material Identity。`Empty=0 / Water=1 / Fire=2 / Poison=3 / Sticky=4` 的整数协议不得改变。`Sticky=4` 目前只是 Stable Identity Reservation，不创建 Definition、不指定 Behavior、不配置 Route。
- Task 0–17 不新增 `Game.Fluid` asmdef；液体模拟继续属于 `Game.ElementField`，Presentation 继续属于 `Game.Rendering`。Phase G 新增的 `Game.Materials` 只保存跨 Domain 的 Identity/Definition/Catalog，不包含 Solver、StatusController、Renderer、VFX、Audio 或 Scene Object。
- `MaterialId`、`MaterialBehaviorKind`、Simulation Representation 与 `MaterialSimulationBackendKind` 是四个不同概念：Identity 回答“是谁”，Behavior 回答“宏观属于哪类物质”，Representation 回答“这一份物质当前以什么数据/对象形态存在”，Backend 回答“哪个系统拥有并推进该形态”。同一种 Material 未来可以更换 Representation；不同 Material 也可以共享同一个 Backend。
- Task 18 的 `MaterialBehaviorKind` 只声明当前真实消费者需要的 `None / Liquid / ReactiveField`。未来 Stone 无论处于 Scene Mesh 还是 Rigidbody Actor 都应是同一个 `Solid` Behavior，但 `Solid / Granular / Gas` 只在对应玩法正式立项时增加；不得把 `StaticSolid / DetachedSolid` 写入 Material Semantic。
- `WaterSimulationMode` 只属于 Legacy Compatibility/Initialization Policy：初始化阶段先把请求模式解析成 Water 的有效 Backend，再构造不可变 `MaterialSimulationRouteSnapshot`。Router 热路径只能调用 `TryResolve(MaterialId, out MaterialSimulationBackendKind)`，不得继续接收或特判 `WaterSimulationMode`。
- `Material Amount` 的单位固定解释为无量纲 `Gameplay Material Unit`（下文简称 GMU），不是 kg、m³、PBF Density 或 ParticleCount。`ParticleMass / RestDensity / ParticleRadius / SmoothingRadius` 是 Simulation Quantity；GMU 与粒子表示只能通过每种 Liquid 明确配置的 `AmountUnitsPerParticle` 转换，任何 Reaction Core 都消费 GMU，不读取 `ParticleMass`。
- Task 22 的 Per-material Parameterization 必须从 Ingress/Spawn 一直贯穿到 Density/Solver/Cohesion/Surface：同一个 Material row 同时提供 `AmountUnitsPerParticle`、`ParticleMass` 与 `RestDensity`，CPU 由后两者计算该 Material 的 `RestSpacing` 并把它快照进 `FluidSpawnRequest`；GPU Packed Spawn 禁止继续读取 Water/Global RestSpacing。
- Task 18–25 不实现真实 `Multi-phase PBF`：不同 Liquid 首版只和相同 `MaterialId` 的粒子建立 Density/Cohesion Neighbor，不实现互溶、分层、浮力交换、扩散或异质界面张力。
- `Game.ElementField` 不得引用 `Game.Rendering`；`Game.Combat` 不得反向引用 `Game.ElementField`。
- `ElementWorldRuntime` 仍是场景唯一 `IElementWriteSink`。禁止同时注册 Legacy Water Runtime 与 PBF Runtime，避免同一水弹写入两份权威状态。
- PBF 粒子位置、速度、Metadata 与 Alive Slot 是 PBF Liquid 的唯一 Simulation Truth。CPU Occupancy 只是低频只读投影，不参加流动、Constraint 或表面重建；Density/Marching Cubes/Shader 也只是 Rendering Representation。
- Legacy Cell Water、`WorldWaterVolumeMeshBuilder`、`ElementWorldWaterRenderer` 与旧 Material 在迁移期间全部保留，通过 `WaterSimulationMode` 回退；不得先删除再验证。
- Runtime 每帧与每个 Simulation Substep 禁止 `new`、LINQ、Closure、Boxing、临时数组和同步 GPU Readback。
- Runtime Frame/Tick 热路径禁止调用 `GraphicsBuffer.GetData()`、`AsyncGPUReadbackRequest.WaitForCompletion()`；同步 Readback 只允许出现在明确标记为 Test-only 的 GPU 数值测试中，或在 `OnDestroy` 为安全释放仍被异步请求占用的 `NativeArray` 执行一次性 Teardown 等待，并必须用注释说明它不属于正常帧路径。
- 所有长期 GPU 资源在初始化边界一次性分配，在 `OnDisable`/`OnDestroy` 幂等释放；不得每帧重建 `GraphicsBuffer`、`RenderTexture`、`MaterialPropertyBlock` 或 `Material`。
- GPU Buffer 的 C# `Stride`、HLSL `StructuredBuffer` 字段顺序和 16-byte 对齐必须由 Contract Test 锁定。
- Unity 6.3 API 审计锁定：`GraphicsBuffer.Target.IndirectArguments` 是创建 Indirect Args Buffer 的 Target，`GraphicsBuffer.IndirectDrawArgs` 是 16-byte C# 数据布局；`Graphics.RenderPrimitivesIndirect` 使用 `RenderParams + MeshTopology + GraphicsBuffer`，不得混淆两者。
- URP Procedural Indirect Shader 必须先 `#define UNITY_INDIRECT_DRAW_ARGS IndirectDrawArgs`，再 include `UnityIndirect.cginc`；Vertex Shader 调用 `InitIndirectDrawArgs(0)` 后通过 `GetIndirectVertexID` 取得跨 Graphics API 修正后的 Vertex ID。
- Gameplay Occupancy Readback 必须同时得到 Position、Active 与 MaterialId。首选 GPU 侧 packed gameplay sample 单 Buffer；若分开读 Position/Metadata，则必须用同一版本号等两次请求都成功后原子发布，禁止只读 Position 推断不存在的状态。
- 直接使用 `NativeArray<T>` 的 Production/Test asmdef 必须显式引用 `Unity.Collections`，并把 `com.unity.collections` 作为 manifest 的直接依赖；不能依赖其他 package 的传递依赖长期存在。
- 新脚本和重要修改必须包含面向初学者的简体中文注释，解释坐标空间、Kernel、Density Constraint、Spatial Hash、Shader Stage、Buffer 生命周期、GPU/CPU 同步和设计取舍。
- 所有日志使用 `Game.Core.GameLog`，不得直接使用 `Debug.Log`。
- 不手写 `.meta`。新 `.cs`、`.compute`、`.hlsl`、`.shader`、`.asset`、`.mat` 和 Scene 的 `.meta` 均由 Unity 导入生成。
- 不修改当前工作区中与流体无关的 Wizard、Jump、Spell、UI、Scene 或 ThirdParty 改动。
- 当前 `Assets/_Project/Scenes/P7_DemoRun.unity` 已有用户未提交修改。Phase A-D 只使用新 Sandbox；Task 13 接入 P7 前必须先让用户确认现有 Scene 改动已经保存且允许在同一文件继续编辑，禁止覆盖或回退用户版本。
- 不把 `dotnet build`、EditMode、PlayMode、Scene Playthrough、Profiler、Frame Debugger 和 Windows Build 的证据互相冒充。
- 不自动创建 Git commit。用户之后明确要求提交时，再按每个 Phase 的建议 `Conventional Commit` 处理。
- 整个阶段只维护一份学习解析文档：`Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`。
- 解析文档创建后状态必须写为“更新中”；每完成一个 Task，立即追加该 Task 的几百字概述、基础原理、脚本职责、Runtime 链路、数学/Unity API、方案取舍、测试、Debug 路径和面试复盘，然后才允许勾选 Task 完成。Phase F 可以单独记录“Water/Liquid Quality 已收口”，但只有 Phase E 遗留 Gate、Task 14–17 与当前已承诺的 Task 18–25 必要 Gate 都验收，才能把整份文档状态改为“已完成”；Phase I 以后属于未来独立立项，不阻塞 Task 25 的阶段性收口。

---

## 1. 已确认范围与非目标

### 1.1 本轮玩家可见目标

1. Water Projectile 命中后不再出现按 `0.25m` Cell 跳动、横向铺格或阶梯式轮廓，而是生成具有惯性、碰撞、汇聚和飞溅的粒子水。
2. 水流撞上地面、墙体、斜面和简单容器后，由 PBF Collision Constraint 投影回可行区域，不穿过受支持的 Collider Proxy。
3. 最终画面不直接显示粒子球；粒子经 Density Kernel 形成连续 Scalar Field，再由 GPU Marching Cubes 输出封闭三角面。
4. 表面保留现有 URP Water 的 Depth、Fresnel、Foam、Triplanar Noise 与 PBR Lighting，并增加适合快速流动水团的 Anisotropic Kernel。
5. Water 仍能让角色获得 `Wet`，并能与现有 Cell Fire 发生灭火反应；允许这类 Gameplay Query 相对 GPU 位置存在一个低频 Snapshot 周期的延迟。
6. Legacy Cell Water 可通过 Profile 一键恢复，用于 Compute Shader 不支持、GPU 路径回归失败或视觉对照。

### 1.2 本轮明确不做

- Gas、Smoke、Poison Gas、Eulerian Grid、Navier-Stokes Pressure Projection 和 Volume Ray Marching。
- 水对角色、敌人或 Rigidbody 的双向力学反馈；本轮 Collider 只单向影响液体。
- Arbitrary Mesh Collider 的实时 GPU Voxelization；首版使用显式 `FluidColliderAuthoring` 的 Box/Sphere/Capsule Proxy。
- Phase F 不增加第二种 Liquid；Phase H 虽会让 Water/Poison 共用单 Runtime，但仍不实现二者直接接触时的互溶、分层、浮力交换或异质界面张力。Poison–Fire 只属于离散 Gameplay Reaction，不等于 Multi-phase Fluid Chemistry。
- 网络同步、Replay Determinism、Save/Load 粒子状态和跨 Scene 持久化。
- 移动平台高速连续碰撞、复杂破碎容器和 GPU Mesh Shader。
- 在本轮顺带启用 URP Render Graph。当前项目 `m_EnableRenderGraph=0`，首版使用 Unity 6 公共 `Graphics.RenderPrimitivesIndirect`，避免把渲染管线迁移混入流体 Debug。

---

## 2. 当前根因与迁移边界

当前项目并不是“只会画方块水”。已有链路是：

```text
ElementWorldSimulator 中的 Water Cell / byte Amount
-> WorldWaterVisualDensityFilter
-> Rounded Primitive / Signed Distance
-> CPU Surface Nets
-> Unity Mesh Upload
-> ElementWater.shader
```

`WorldWaterVisualDensityFilter + Signed Distance + Surface Nets` 已经把 Cell 表现为连续 Mesh，但输入状态仍由 `ElementWorldSimulator` 每 10Hz 在 Global Cell 间做 Down Flow 和 Horizontal Flow。因此玩家看到的运动路径、最小水量单元、碰撞边界与传播速度仍然被 Cell 网格决定；继续调 `Smooth Union` 或 Shader Normal 只能柔化外观，不能获得真实的惯性、飞溅或连续轨迹。

迁移后链路是：

```text
ElementWriteRequest(Water)
-> GpuPbfFluidRuntime Spawn Queue
-> Predict Position
-> Spatial Hash + Bitonic Sort
-> Density / Lambda / Position Correction × SolverIterations
-> Collision Projection
-> Velocity / XSPH / Commit
-> GPU Particle Buffer（Water 唯一 Simulation Truth）
   ├-> Density Field -> Marching Cubes -> Indirect Draw
   └-> 低频 AsyncGPUReadback -> Gameplay Occupancy -> Wet / Fire Reaction
```

必须保留的现有边界：

- `ElementDepositOnImpact` 与 `ElementWorldInitialDeposit` 继续只创建 `ElementWriteRequest`，不认识粒子、GPU Buffer 或 Renderer。
- `ElementRuntimeRegistry` 继续只保存一个 `IElementWriteSink`。
- Fire Cell、Fire Decay、Persistent Fire Source 与 Fire Renderer 继续使用现有 CPU ElementWorld。
- `ElementWorldWaterRenderer` 和 CPU Surface Nets 继续服务 `LegacyCell` 模式。
- `Game.Rendering` 只能绑定 `IFluidGpuSource` 暴露的只读 Buffer，不得调用 Spawn、Consume 或修改粒子。

---

## 3. 方案研究与选择

| 方案 | 优点 | 主要问题 | 结论 |
|---|---|---|---|
| CPU Jobs/Burst PBF + CPU Marching Cubes | 与参考视频实现接近，逐步调试直观，适合 2K 级演示 | CPU 邻居搜索、Constraint Iteration、Meshing 和 Mesh Upload 会共同占用主线程/Worker；扩展多液体后预算风险高 | 只作为数学与视觉参考，不作为本项目生产链 |
| GPU PBF + CPU Surface Nets/Marching Cubes | 可复用当前 CPU Mesher | 每帧必须把大量粒子或 Density Field 从 GPU 拉回 CPU，再上传 Mesh；同步与带宽抵消 GPU Solver 优势 | 拒绝 |
| GPU PBF + GPU Density + GPU Marching Cubes + Indirect Draw | 粒子、密度、三角面都留在 GPU；CPU 只提交参数；适合后续多液体 Profile | Buffer 生命周期、Shader Debug、GPU Overflow 和 Gameplay Query 更复杂 | **采用** |

主要参考：

- [Position Based Fluids 论文](https://matthias-research.github.io/pages/publications/pbf_sig_preprint.pdf)：Density Constraint、Lambda、Position Correction、Artificial Pressure 和 Vorticity/XSPH 的数学来源。
- [Reconstructing Surfaces of Particle-Based Fluids Using Anisotropic Kernels](https://faculty.cc.gatech.edu/~turk/my_papers/sph_surfaces.pdf)：粒子邻域协方差、方向拉伸与连续表面重建。
- [lamp-cap/Unity_Slime](https://github.com/lamp-cap/Unity_Slime)：用户提供视频的源码入口，验证 CPU PBF、Anisotropic Kernel、Density Grid 与 Marching Cubes 能得到目标外观。
- [Scrawk/PBD-Fluid-in-Unity](https://github.com/Scrawk/PBD-Fluid-in-Unity)：GPU PBD/PBF、Grid Hash 与 Bitonic Sort 的工程参考。只参考算法分层，不直接复制实现。
- [Unity Graphics.RenderPrimitivesIndirect](https://docs.unity3d.com/ja/current/ScriptReference/Graphics.RenderPrimitivesIndirect.html)：通过 GPU `IndirectDrawArgs` 决定顶点数，并用 `RenderParams.worldBounds` 做整批 Culling/Sorting。
- [Unity AsyncGPUReadback.RequestIntoNativeArray](https://docs.unity3d.com/cn/current/ScriptReference/Rendering.AsyncGPUReadback.RequestIntoNativeArray.html)：把 GPU Gameplay Snapshot 写入长期 `NativeArray`，避免阻塞主线程。

---

## 4. Assembly、状态所有权与数据流

### 4.1 Assembly 位置

```text
Game.Core
├── Game.Combat
├── Game.ElementField
│   ├── Legacy Cell Water / Fire
│   ├── GPU PBF Liquid Simulation
│   └── Gameplay Occupancy / Reaction Bridge
└── Game.Rendering
    ├── Legacy CPU Surface Nets Water Renderer
    └── GPU Density / Marching Cubes / Indirect Liquid Renderer
```

不新增 `Game.Fluid` 的原因：液体沉积、Wet 和灭火都已经属于 `Game.ElementField` 的环境 Gameplay。如果立即拆新 Assembly，`Game.ElementField` 仍必须依赖它，而 Renderer 也要同时依赖两者；在只有 Water 一个可玩材质时只会增加边界与序列化成本。代码先按 `Scripts/ElementField/Fluid/` 文件夹隔离，等多液体、Gas 或独立流体玩法真正形成第二个 Domain 后再评估 asmdef。

### 4.2 三种“状态”必须区分

1. **Resident Particle**：粒子仍占用固定 GPU Pool Slot，位置和材质数据存在。
2. **Interest Active**：粒子落在玩家附近的活动 Bounds 中，允许被模拟和表面重建。
3. **Requires Simulation**：粒子或其邻域当前真的需要执行 PBF；稳定粒子可以 Resident 且可见，但 Solver-Sleeping。

这延续现有 ElementWorld 的经验：Gameplay 数据是否存在、玩家是否在附近、Solver 是否必须工作是三个不同问题。不得因为粒子休眠就把它从 Renderer/Occupancy 中删除，也不得因为粒子 Resident 就强迫它永远参与所有 Constraint Pass。

### 4.3 CPU/GPU 真相边界

| 数据 | Owner | 写入者 | 读取者 |
|---|---|---|---|
| Particle Position/Velocity/Alive/MaterialId | `GpuPbfFluidRuntime` GPU Buffer | PBF Compute Kernel | PBF、Surface Renderer、Readback Bridge |
| Spatial Hash/Cell Range | `GpuPbfFluidRuntime` GPU Buffer | Spatial Hash Compute | PBF Neighbor Query、Density Field |
| Surface Density/Anisotropy/Triangles | `Game.Rendering` GPU Resource | Rendering Compute | Procedural Liquid Shader |
| Liquid Gameplay Occupancy | `FluidGameplayOccupancyBridge` CPU Snapshot | Async Readback 完成后的 Binning | Exposure、Reaction |
| Fire Cell/Amount | `ElementWorldRuntime` | 现有 Write/Decay + Reaction Delta | Fire Renderer、Exposure、Reaction |

---

## 5. 文件结构与职责锁定

### 5.1 新建：`Game.ElementField`

| 文件 | 单一职责 |
|---|---|
| `Assets/_Project/Scripts/ElementField/Fluid/Data/WaterSimulationMode.cs` | `LegacyCell` / `GpuPbf` 模式枚举，默认值保持 Legacy。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationProfile.cs` | PBF Gameplay/Simulation `ScriptableObject`：容量、粒径、Smoothing Radius、Rest Density、迭代、Viscosity、碰撞和 Amount→Particle 映射。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationSettings.cs` | Profile 在初始化时生成的不可变、已校验 Runtime Snapshot。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Data/FluidSpawnRequest.cs` | CPU→GPU 的值类型 Spawn Command。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Data/IFluidGpuSource.cs` | Rendering 可见的最小只读 GPU Snapshot 契约。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidCapacityPlanner.cs` | Power-of-two Capacity、Spawn Count、Hash Table Size、Dispatch Count 的 Pure Math。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSimulationClock.cs` | 固定 Tick、Substep 和 Max Catch-up 的 Pure Scheduler。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSpawnQueue.cs` | 固定容量 Ring Buffer，离散写入时零 GC。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSpatialHash.cs` | 与 HLSL 一致的 Hash、Inactive Sentinel 和 27 邻域 Pure Reference。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Simulation/PbfKernelMath.cs` | Poly6/Spiky、Density Constraint 与参数边界的 CPU Reference，只用于测试与教学。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Runtime/FluidGpuResourceSet.cs` | 创建、绑定、清零和幂等释放 Simulation Buffer。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs` | Unity 生命周期与 Compute Dispatch Orchestrator；实现液体写入和只读 GPU Source。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Collision/FluidColliderAuthoring.cs` | 显式标记允许进入 GPU 的 Box/Sphere/Capsule Collider。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Collision/FluidColliderProxy.cs` | C#/HLSL 对齐的 Collider Proxy 值结构。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Collision/FluidColliderProxyCollector.cs` | 初始化/Dirty 时把场景 Collider 转换为固定 Buffer，不在每帧 Find。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/ILiquidOccupancyReadOnly.cs` | Exposure/Reaction 使用的低频液体占用查询。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/LiquidGameplaySnapshot.cs` | 固定 Dense Active-Region byte Grid 与 World↔Cell 映射。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidGameplayOccupancyBridge.cs` | 双缓冲 `NativeArray`、AsyncGPUReadback 生命周期和 Snapshot Binning。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidReactionCommandQueue.cs` | Water 粒子消费与 Fire Cell 负增量的固定容量命令队列。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidFireReactionSystem.cs` | 复用 `ElementReactionEvaluator`，规划并提交跨 GPU/CPU 的灭火消耗。 |
| `Assets/_Project/Scripts/ElementField/Fluid/Activity/FluidActivityPlanner.cs` | Resident/Interest/RequiresSimulation 的 Pure 判定和 Sleep/Wake 规则。 |

### 5.2 新建：GPU Simulation/Surface Assets

| 文件 | 单一职责 |
|---|---|
| `Assets/_Project/Art/Elemental/Compute/FluidGpuCommon.hlsl` | C#/HLSL 共识的 Flags、Hash、Kernel 常量和 Buffer 字段说明。 |
| `Assets/_Project/Art/Elemental/Compute/PbfParticleLifecycle.compute` | Pool 初始化、Spawn、Consume、Predict、Commit、Activity Flag。 |
| `Assets/_Project/Art/Elemental/Compute/PbfSpatialHash.compute` | Build Entry、Bitonic Sort、Clear/Build Cell Range。 |
| `Assets/_Project/Art/Elemental/Compute/PbfSolver.compute` | Density/Lambda、Delta Position、XSPH/Vorticity、Velocity Update。 |
| `Assets/_Project/Art/Elemental/Compute/PbfCollision.compute` | OBB/Sphere/Capsule Collision Projection。 |
| `Assets/_Project/Art/Elemental/Compute/FluidDensity.compute` | Isotropic/Anisotropic Particle Kernel → 3D Scalar Field。 |
| `Assets/_Project/Art/Elemental/Compute/FluidAnisotropy.compute` | 邻域均值、Covariance、Eigenvalue Clamp 与 Kernel Transform。 |
| `Assets/_Project/Art/Elemental/Compute/FluidMarchingCubes.compute` | Cell Case、Edge Interpolation、Gradient Normal、Triangle Counter 与 Draw Args。 |
| `Assets/_Project/Art/Elemental/Compute/MarchingCubesTables.hlsl` | Corner/Edge/Triangle Lookup Table，表项与 Case Index 约定集中维护。 |

### 5.3 新建：`Game.Rendering`

| 文件 | 单一职责 |
|---|---|
| `Assets/_Project/Scripts/Rendering/ElementField/LiquidRenderProfile.cs` | Density Resolution、Iso Level、最大 Triangle、Anisotropy 和 Bounds Padding 的 Presentation SO。 |
| `Assets/_Project/Scripts/Rendering/ElementField/FluidSurfaceGridPlanner.cs` | Active Bounds → 3D Resolution/World Voxel Size/Capacity 的 Pure Math。 |
| `Assets/_Project/Scripts/Rendering/ElementField/FluidSurfaceGpuResources.cs` | Density Texture、Anisotropy、Triangle、Counter、Indirect Args 的生命周期。 |
| `Assets/_Project/Scripts/Rendering/ElementField/GpuFluidParticleDebugRenderer.cs` | Phase A/B 直接绘制 GPU 粒子 Billboard 的 Debug-only Renderer。 |
| `Assets/_Project/Scripts/Rendering/ElementField/GpuLiquidSurfaceRenderer.cs` | Density、Marching Cubes Dispatch 与 `Graphics.RenderPrimitivesIndirect` 提交。 |
| `Assets/_Project/Art/Elemental/Shaders/FluidParticleDebug.shader` | 通过 `SV_VertexID` 展开粒子 Billboard，验证 Simulation 而非最终画面。 |
| `Assets/_Project/Art/Elemental/Shaders/ElementLiquidProcedural.shader` | 通过 `SV_VertexID` 读取 GPU Triangle Vertex，再复用 Water Lighting。 |

### 5.4 修改

| 文件 | 修改目的 |
|---|---|
| `Assets/_Project/Scripts/ElementField/Data/ElementWorldProfile.cs` | 增加默认 `LegacyCell` 的 `WaterSimulationMode`。 |
| `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs` | 保持唯一 Write Sink；Water 在 PBF 模式路由到 Fluid Runtime；提供液体 Active Bounds。 |
| `Assets/_Project/Scripts/ElementField/Simulation/ElementWriteRequest.cs` | 增加向后兼容的 `InitialVelocity` 值字段；旧构造函数默认零速度。 |
| `Assets/_Project/Scripts/ElementField/Runtime/ElementDepositOnImpact.cs` | 把 Projectile Impact Direction 转成可调的 Water Spawn 初速度；Fire/Initial Deposit 保持零速度。 |
| `Assets/_Project/Scripts/ElementField/Simulation/ElementFieldSimulationSettings.cs` | 增加 `SimulateCellWater` Snapshot。 |
| `Assets/_Project/Scripts/ElementField/World/ElementWorldSimulator.cs` | PBF 模式跳过 Cell Water Reaction/Down/Horizontal，仅保留 Fire Decay 与非液体逻辑。 |
| `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldExposureSystem.cs` | Water 从 `ILiquidOccupancyReadOnly` 读取，Fire 继续从 Cell World 读取。 |
| `Assets/_Project/Scripts/Rendering/ElementField/ElementWorldWaterRenderer.cs` | 仅在 `LegacyCell` 模式运行，防止两套水面重叠。 |
| `Assets/_Project/Art/Elemental/Shaders/ElementWaterCore.hlsl` | 只增加可被 Procedural Shader 复用的通用表面函数，不改变 Legacy Property 语义。 |
| `Assets/_Project/Tests/ElementField/Game.ElementField.Tests.asmdef` | 若 GPU 测试需要，显式加入 `Unity.Collections` 引用；以 Unity 实际生成引用为准。 |
| `Assets/_Project/Tests/Rendering/ElementWaterShaderContractTests.cs` | 锁定 Procedural Buffer、Indirect Draw 和 Legacy Property 契约。 |
| `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md` | 每个 Task 增量维护的唯一新手学习文档。 |

### 5.5 明确保留，不在本轮删除

- `WorldWaterVisualDensityFilter.cs`
- `WorldWaterImplicitField.cs`
- `WorldWaterVolumeMeshBuilder.cs`
- `WaterVolumeMeshingWorkspace.cs`
- `WorldWaterChunkViewPool.cs`
- `ElementWaterVolumeRenderProfile.cs`
- `ElementWater.shader`
- 以上所有现有 Rendering Tests

---

## 6. 核心数据布局与数学约定

### 6.1 GPU Particle 使用 SoA

采用 `Structure of Arrays (SoA)`，而不是把所有字段塞进一个大 Struct：

```hlsl
RWStructuredBuffer<float4> _Positions;          // xyz=当前世界坐标，w=1 active / 0 free
RWStructuredBuffer<float4> _PredictedPositions; // xyz=Constraint 输入，w 保留 active
RWStructuredBuffer<float4> _Velocities;         // xyz=速度，w 不承载整数语义
RWStructuredBuffer<float2> _DensityLambda;      // x=density，y=lambda
RWStructuredBuffer<uint2>  _ParticleMetadata;   // x=MaterialId，y=Activity/Generation Flags
RWStructuredBuffer<uint2>  _SpatialEntries;     // x=hash bucket，y=particle index
RWStructuredBuffer<uint2>  _CellRanges;         // x=start inclusive，y=end exclusive
```

原因：不同 Kernel 只读写自己需要的 Buffer，降低带宽和误写；`MaterialId` 保持整数，不塞进 float 的小数位；C#/HLSL Stride 易于 Contract Test。所有 Buffer 长度固定为初始化 Capacity；空 Slot 使用 `active=0` 和 `hash=0xffffffff`。

### 6.2 Spatial Hash

1. 用 `cell=floor(position/smoothingRadius)` 得到整数邻域坐标。
2. 对 `int3 cell` 做稳定 32-bit Hash，再 `% hashTableCapacity` 得到 Bucket。
3. `SpatialEntries=(bucket, particleIndex)`，Inactive Entry 使用最大 `uint`。
4. 对固定 Power-of-two Capacity 做 Bitonic Sort，比较顺序为 Bucket→ParticleIndex。
5. 相邻排序 Entry 的 Bucket 发生变化时写入 `_CellRanges[bucket]`。
6. 查询 27 个相邻 Cell 时先定位 Bucket Range，再重新计算候选粒子的真实 `int3 cell`，过滤 Hash Collision。

Bitonic Sort 并非理论上最快，但实现、验证和固定容量行为更清晰；只有 Profiler 证明它成为瓶颈后，才单独规划 Radix Sort，不能在首版同时承担两套排序实现。

### 6.3 PBF Substep

```text
Apply Spawn/Consume Commands
-> External Force + Predict
-> Build/Sort Spatial Hash
-> for SolverIterations:
     Density + Lambda
     Delta Position + Artificial Pressure
     Apply Delta
     Collision Projection
-> Velocity = (Predicted - Previous) / dt
-> XSPH Viscosity + optional Vorticity Confinement
-> Commit Position
```

核心约束：

```text
C_i = density_i / restDensity - 1
lambda_i = -C_i / (sum_j |gradient C_ij|² + epsilon)
deltaP_i = (1 / restDensity) * sum_j (lambda_i + lambda_j + s_corr) * gradient W_spiky
```

- `epsilon` 防止邻居太少时除零。
- `s_corr` 是 Artificial Pressure，用于抑制粒子结团和表面空洞，不是额外重力。
- Position Correction 必须先写 `_DeltaPositions`，再统一 Apply；禁止同一个 Kernel 一边读邻居旧位置一边直接改当前位置，否则结果依赖 GPU Thread 调度顺序。
- PBF 每个 Substep 只 Build 一次 Spatial Hash；Position Correction 应小于 Smoothing Radius 的安全比例，越界由 Profile Clamp 和稳定性测试约束。

### 6.4 Surface Reconstruction

1. Active Interest Bounds 映射为固定 3D Lattice。
2. 每个 Voxel Thread 查询其 27 个 Spatial Hash 邻域，累加 Particle Kernel 得到 Scalar Density。
3. Isotropic 阶段使用球形 Kernel；Anisotropic 阶段用每粒子的 Covariance/Eigenvalue 生成方向缩放矩阵。
4. 每个 Marching Cubes Cell 读取 8 个 Corner Density，组成 8-bit Case Index。
5. 对 Lookup Table 指定的 Edge 做线性插值：`t=(iso-d0)/(d1-d0)`。
6. Normal 使用 Density Central Difference；高密度是内部，因此 Outward Normal 为 `-normalize(gradient)`。
7. 每个 GPU Thread 通过独立 Atomic Triangle Counter 一次保留一个 Triangle Slot，再连续写三个 Vertex，避免三个独立 Append 被其他 Thread 交错。
8. `BuildIndirectArgs` 写入 `vertexCountPerInstance=min(triangleCount,maxTriangles)*3`、`instanceCount=1`。

### 6.5 Gameplay Occupancy

- Readback 周期与当前 Exposure 默认周期对齐为 `0.25s`。
- 使用两个长期 `NativeArray<float4>`：一个正在由 GPU 写，一个可被 CPU Binning；请求完成前不得 Dispose 或复用目标数组。
- CPU 遍历 Active Particle，按现有 `ElementWorldCoordinates.WorldToGlobalCell` 写入固定 Dense Active-Region byte Grid。
- `amount = min(255, particleCountInCell * amountUnitsPerParticle)`。
- Occupancy 不参与 PBF，不写回 Water Cell，不触发 Legacy Flow。
- 同一时刻最多允许一个 Outstanding Readback；错误请求保留上一份有效 Snapshot，并通过 `GameLog.Warn` 限频报告。

---

## 7. 资产依赖与 Editor 前置条件

### 7.1 用户外部资产

本阶段 **不需要用户下载任何模型、贴图、Normal Map、Noise Texture、VFX、Audio、Font 或付费插件**。

- Density、Anisotropy 与 Marching Cubes 完全程序化。
- 水面 Noise 复用现有 `ElementWaterCore.hlsl`，不新增纹理 Channel/Import Setting。
- Sandbox 使用 Unity Primitive + `BoxCollider`/`SphereCollider`/`CapsuleCollider`。
- 如果以后需要泡沫 Texture、折射 Noise、Splash VFX 或水声，应另行列资产清单，不能偷偷混入本阶段。

### 7.2 项目内需要由 Unity 创建的资产

| Asset | 位置 | 创建方式 | 初始用途 |
|---|---|---|---|
| Water Simulation Profile | `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidSimulationProfile_Water.asset` | Create > Game > Element Field > Liquid Simulation Profile | Water PBF 参数 |
| Water Render Profile | `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidRenderProfile_Water.asset` | Create > Game > Rendering > Liquid Render Profile | Density/Marching Cubes 参数 |
| PBF Water Material | `Assets/_Project/Art/Elemental/Materials/M_ElementWater_PBF.mat` | Material 使用 `Game/Elemental/Liquid Procedural` | GPU Surface Draw |
| Sandbox Scene | `Assets/_Project/Scenes/PBF_FluidSandbox.unity` | Unity Editor 新建并保存 | 与 P7/P8 隔离验证 |

这些 `.asset/.mat/.unity` 由用户在 Editor 中创建和绑定，Codex 不手写 `.meta`。

### 7.3 平台前提与回退

- Windows PC 为正式目标；启动时检查 `SystemInfo.supportsComputeShaders`。
- Gameplay Occupancy 额外检查 `SystemInfo.supportsAsyncGPUReadback`。
- Compute 不支持：强制回退 `LegacyCell`，禁用 GPU Runtime/Renderer，保留明确 `GameLog.Warn`。
- Compute 支持但 Async Readback 不支持：可以显示 PBF 水，但 PBF 模式不得宣称 Wet/Fire Reaction 已完整；正式 P7 接入 Gate 必须使用 Legacy 或阻止进入 PBF 模式。

---

## 8. 验收证据分层与性能预算

| 证据 | 能证明什么 | 不能替代什么 |
|---|---|---|
| Pure EditMode Test | Capacity、Queue、Hash、Kernel Reference、Grid Mapping 与 Route 决策 | GPU Kernel 真正执行 |
| GPU Integration Test | Buffer Layout、Kernel 名称、Dispatch 后数值与 Overflow Flag | 最终水面观感与长时间稳定 |
| PlayMode Sandbox | Spawn、PBF、Collider、Surface 与生命周期组合 | P7 Prefab/Scene 接入 |
| P7 Scene Playthrough | 水弹、Wet、灭火、Pause、Interest Bounds 与回退链 | Windows Player 和性能预算 |
| Profiler/GPU Profiler | CPU/GPU 时间、GC、Dispatch、Readback | Build 打包和 Graphics API 差异 |
| Frame Debugger | Indirect Draw、Pass、Depth Texture、Material Property | PBF 数学正确性 |
| Windows Development Build | Compute/Shader Import、资源打包、真实 Player 路径 | 全量 Test Runner |

性能 Gate 使用可复现基线，不把旧 P6-B 历史数据当成当前测量：

- 目标分辨率：1920×1080，VSync 与 TargetFrameRate 设置在对照前保持一致，Deep Profile 关闭。
- 标准负载：8192 Resident Water Particles、至少 4096 RequiresSimulation、一个 128×64×128 级别 Surface Grid、4 Solver Iterations、2 Substeps。
- Runtime `GC Alloc=0 B/frame`；Profiler 中不得出现每帧 Buffer/Texture/Material 分配。
- 不允许 Runtime 同步 Readback；Frame Debugger/Profiler 中不出现因 `GetData/WaitForCompletion` 引起的主线程等待。
- 相对空 Sandbox Baseline，Fluid CPU Main Thread Marker 中位增量目标不高于 1.0ms；Fluid GPU Marker 中位增量目标不高于 5.0ms、95th Percentile 不高于 7.0ms。
- 若未达预算，固定优化顺序：先降低 Surface Grid Resolution/重建频率；再把 Anisotropy 改为隔帧更新；再降低可见粒子 Capacity；最后才调整 Solver Iterations/Substeps，并重新通过密度误差与穿透测试。不得先牺牲 Constraint 稳定性来掩盖 Meshing 成本。
- 连续运行 10 分钟不得产生 NaN/Infinity、Buffer Overflow 未报告、粒子无故丢失、Collider 穿透持续累积或显存持续增长。

---

# Phase A：契约、GPU 生命周期与可见粒子

## Task 0：建立当前基线与唯一增量解析文档

**Files:**

- Create: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Read only: `Assets/_Project/Docs/2026-07-15 P6有限元素场完整解析.md`
- Read only: `docs/superpowers/plans/2026-07-29 P6-B统一体积水表现实施计划.md`
- Read only: `Assets/_Project/Scripts/ElementField/World/ElementWorldSimulator.cs`
- Read only: `Assets/_Project/Scripts/Rendering/ElementField/ElementWorldWaterRenderer.cs`

**Interfaces:**

- Consumes: 当前 Legacy Cell Water 的源码、现有 Profiler Marker 与 P7 水弹路径。
- Produces: 一份状态为“更新中”的 Living Explainer、可复现实验步骤、当前 CPU/Rendering Baseline 表格；后续每个 Task 都追加到同一文件。

- [ ] **Step 1: 创建解析文档骨架**

  文档开头必须包含：创建时间、当前状态、目标读者、阶段目标、明确非目标、参考资料、当前 Legacy Runtime 链、证据分层和 Task 索引。`## 0. 先建立心智模型` 要用图形学新手能理解的语言解释“粒子是数值离散点，Density Field 才是连续表面的中间表示”。

- [ ] **Step 2: 写 Task 0 的几百字概述**

  Task 0 内部从 `1.` 重新编号，至少解释：玩家看到的方格感、Simulation Discretization 与 Visual Continuity 的区别、现有 Surface Nets 已经解决了什么、PBF 真正要替换什么、为什么不能只换 Shader。

- [ ] **Step 3: 在 Unity Editor 记录 Legacy Baseline**

  打开 `P7_DemoRun`，关闭 Deep Profile；记录空闲 300 帧，再在相同位置连续发射 20 次 Water Projectile 并记录 300 帧。保存 `ElementWorld.Simulate`、`ElementWorldWater.RebuildChunk`、`ElementWorldWater.UploadMesh`、Main Thread、Render Thread、GPU Frame、GC Alloc、Resident/Active/Sleeping Chunk 和 Active Element Cell 数据。

- [ ] **Step 4: 把证据写入解析文档**

  必须用“症状 → 证据 → 能排除什么 → 尚不能证明什么”的格式；如果本机没有 GPU Profiler 数据，要明确写“未取得”，不能引用旧的 262.95ms 历史值冒充当前结果。

- [ ] **Step 5: 核对工作区边界**

  运行 `git status --short`，确认本 Task 只新增解析文档；所有 Wizard/Jump/Spell/UI/Scene/ThirdParty 现有改动保持原样。

**Task 0 完成 Gate：** 用户确认 Baseline 记录和解析文档阅读深度符合图形学新手要求。

**建议提交名（仅在用户明确要求提交时）：** `docs(element-field): start gpu pbf fluid learning guide`

---

## Task 1：建立 PBF 数据契约、Profile 与固定容量 Pure Logic

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/WaterSimulationMode.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationProfile.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationSettings.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/FluidSpawnRequest.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidCapacityPlanner.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSimulationClock.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSpawnQueue.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidCapacityPlannerTests.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidSimulationClockTests.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidSpawnQueueTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

- Consumes: `ElementMaterialKind.Water`、`ElementWriteRequest.TotalAmount/Radius/UseLinearFalloff`。
- Produces:

```csharp
public enum WaterSimulationMode : byte
{
    LegacyCell = 0,
    GpuPbf = 1,
}

public readonly struct FluidSpawnRequest
{
    public readonly Vector3 WorldPosition;
    public readonly Vector3 InitialVelocity;
    public readonly float Radius;
    public readonly uint ParticleCount;
    public readonly uint MaterialId;
    public readonly uint Seed;
    public readonly uint Flags;
}

public sealed class FluidSpawnQueue
{
    public int Capacity { get; }
    public int Count { get; }
    public bool TryEnqueue(in FluidSpawnRequest request);
    public int CopyAndClear(FluidSpawnRequest[] destination);
}
```

- [ ] **Step 1: 先写 Pure RED Tests**

  覆盖：请求容量归一到 Power-of-two；0/负容量拒绝；`ceil(totalAmount/amountUnitsPerParticle)`；Particle Count 不超过 Profile 单次上限；Hash Table Capacity 至少为 Particle Capacity 的两倍；Clock 在超长帧只消费 `MaxCatchUpTicks`；Queue 满时拒绝且 FIFO 顺序不变。

- [ ] **Step 2: 运行 RED**

  在 Unity 刷新生成 `.csproj` 后运行：

```powershell
dotnet build Game.ElementField.Tests.csproj --no-restore
```

  预期：因上述类型尚不存在而编译失败；不能接受无关 Wizard/Jump 文件导致的失败作为本 Task RED。

- [ ] **Step 3: 实现不可变 Settings Snapshot**

  `LiquidSimulationProfile.CreateSettings()` 必须在初始化边界校验并快照：Particle Capacity、Max Spawn Requests、Amount Units Per Particle、Particle Radius、Particle Mass、Smoothing Radius、Rest Density、Fixed Tick Rate、Substeps、Solver Iterations、Lambda Epsilon、Artificial Pressure、Viscosity、Vorticity、Gravity、Max Speed、Collision Friction/Restitution、Sleep Threshold 和 Max Catch-up。

  Profile 默认值只是首轮起点：Capacity 8192、Substeps 2、Solver Iterations 4；运行时不得每帧重新读取 `ScriptableObject`。

- [ ] **Step 4: 实现 Queue 与 Clock**

  Queue 构造时一次性分配数组；`CopyAndClear` 复制到调用方长期数组并清零 Count。Clock 使用 Accumulator，返回本帧需要执行的 Fixed Tick 数；Pause 时由 Runtime 不调用 Clock，不偷偷积累 Catch-up Debt。

- [ ] **Step 5: 运行 GREEN 与全量 ElementField 回归**

  预期新测试全绿；现有 `ElementWorldSimulatorTests`、`ElementWorldWriteProcessorTests`、`ElementWorldExposureSystemTests` 行为不变。

- [ ] **Step 6: 更新解析文档**

  新增 Task 1 几百字概述，并从 `1.` 重新编号解释：为什么 Capacity 用 Power-of-two、Ring Buffer 如何零 GC、SO 与 Runtime Snapshot 的区别、Amount Unit 为什么不是粒子数、Fixed Tick/Substep/Catch-up 的区别、每个新脚本的 Assembly 位置和测试边界。

**建议提交名：** `feat(element-field): add gpu fluid data contracts`

---

## Task 2：GPU Buffer 生命周期、Spawn Kernel 与粒子 Debug Renderer

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/IFluidGpuSource.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/FluidGpuResourceSet.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Create: `Assets/_Project/Art/Elemental/Compute/FluidGpuCommon.hlsl`
- Create: `Assets/_Project/Art/Elemental/Compute/PbfParticleLifecycle.compute`
- Create: `Assets/_Project/Scripts/Rendering/ElementField/GpuFluidParticleDebugRenderer.cs`
- Create: `Assets/_Project/Art/Elemental/Shaders/FluidParticleDebug.shader`
- Create: `Assets/_Project/Tests/ElementField/FluidGpuLayoutContractTests.cs`
- Create: `Assets/_Project/Tests/ElementField/GpuFluidParticleLifecycleTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

```csharp
public readonly struct FluidGpuSnapshot
{
    public readonly GraphicsBuffer Positions;
    public readonly GraphicsBuffer PredictedPositions;
    public readonly GraphicsBuffer Velocities;
    public readonly GraphicsBuffer Metadata;
    public readonly GraphicsBuffer SpatialEntries;
    public readonly GraphicsBuffer CellRanges;
    public readonly int ParticleCapacity;
    public readonly int HashTableCapacity;
    public readonly float SmoothingRadius;
    public readonly Bounds ActiveBounds;
    public readonly uint LayoutVersion;
}

public interface IFluidGpuSource
{
    bool IsFluidInitialized { get; }
    bool TryGetGpuSnapshot(out FluidGpuSnapshot snapshot);
}
```

- [ ] **Step 1: 写 GPU Layout 和 Lifecycle RED Tests**

  Contract Test 锁定 `Vector4=16 bytes`、Metadata/Spatial Entry `uint2=8 bytes`、Collider Proxy 后续为 64-byte 对齐；Kernel Contract 检查 `InitializePool`、`SpawnParticles`、`PredictPositions`、`CommitPositions` 存在。GPU Test 使用 Test-only 同步 Readback 验证初始化后所有 Slot inactive、Spawn 后恰好 N 个 Slot active、Dispose 可重复调用。

- [ ] **Step 2: 实现 `FluidGpuResourceSet`**

  构造阶段创建 Positions、Predicted、Velocities、DensityLambda、Metadata、DeltaPositions、SpatialEntries、CellRanges、FreeIndices、Counters 和 SpawnRequests Buffer；资源字段只读暴露给 Runtime，释放后全部置 null。不得依赖 Finalizer 回收显存。

- [ ] **Step 3: 实现 Spawn Command 上传**

  `GpuPbfFluidRuntime.Update()` 从固定 Queue 复制到长期 `_spawnUploadBuffer`，只对有效范围调用 `GraphicsBuffer.SetData(array, 0, 0, count)`。Spawn Kernel 通过 Free Stack 取得 Slot，以请求 Seed + Local Index 生成确定性球内/圆盘分布；`UseLinearFalloff` 只影响 Spawn 密度分布，不修改已经生成粒子的质量。

- [ ] **Step 4: 实现最小生命周期 Tick**

  这一 Task 只做 Gravity→Predict→Commit，不做邻居约束。Runtime 检查 Compute 支持、Profile、ComputeShader 和 Bounds Provider；失败时禁用自身并使用 `GameLog` 一次性诊断。`GpuPbfFluidRuntime` 使用 `[DefaultExecutionOrder(-200)]` 在普通 Gameplay Update 前段提交 Simulation；`OnDisable`/`OnDestroy` 都调用同一个幂等 `ReleaseResources()`。

- [ ] **Step 5: 实现 Debug Billboard Renderer**

  `GpuFluidParticleDebugRenderer` 在初始化时创建一个 `MaterialPropertyBlock`，每帧只更新 Buffer 和 Bounds；Shader 用 `SV_VertexID` 把每个 Slot 展开成 6 个 Camera-facing Triangle Vertices，Inactive Slot 在 Clip Space 外丢弃。它只用于 Phase A/B，不进入最终 P7 Renderer。

- [ ] **Step 6: 运行 GPU Test 与静态编译**

  Test-only Readback 允许 `WaitForCompletion`，文件注释必须明确禁止把该写法复制到 Runtime。完成后构建 `Game.ElementField`、`Game.Rendering` 和两套 Tests。

- [ ] **Step 7: 更新解析文档**

  新增 Task 2，基础解释 CPU Memory、VRAM、`GraphicsBuffer.Count/Stride`、SoA、`StructuredBuffer/RWStructuredBuffer`、Compute Thread、Dispatch Group、`SV_VertexID`、Billboard、Unity 生命周期和为何 GPU 资源必须手动 Dispose；列出每个 Buffer 谁写谁读。

### Phase A Unity Editor Gate

1. 创建 `PBF_FluidSandbox` Scene、Water Simulation Profile 和一个固定 Bounds。
2. 场景只放 `GpuPbfFluidRuntime`、Debug Renderer、Camera、Light 和地面参考网格。
3. 通过 Context Menu/Debug Inspector 提交 256、1024、4096 粒子。
4. 确认粒子可见、受重力、Pause 时停止、反复退出 Play Mode 不报 Buffer disposed/leak。
5. Unity Profiler 确认稳定帧 `GC Alloc=0 B`。

只有用户确认 Phase A Gate 后才进入 Phase B。

**建议提交名：** `feat(element-field): add gpu fluid particle lifecycle`

---

# Phase B：Spatial Hash、PBF Constraint 与碰撞

## Task 3：GPU Spatial Hash、Bitonic Sort 与 27 邻域查询

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSpatialHash.cs`
- Create: `Assets/_Project/Art/Elemental/Compute/PbfSpatialHash.compute`
- Create: `Assets/_Project/Tests/ElementField/FluidSpatialHashTests.cs`
- Create: `Assets/_Project/Tests/ElementField/GpuFluidSpatialHashTests.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

- Consumes: fixed Particle Capacity、Positions、Smoothing Radius。
- Produces: 排序后的 `_SpatialEntries` 与 `_CellRanges`；PBF 和 Surface Density 都通过同一查询约定读取。

- [ ] **Step 1: 写 Hash Reference RED Tests**

  覆盖负坐标 Floor、相同 Cell 相同 Bucket、27 个偏移无重复、Inactive Sentinel 排在末尾、Hash Collision 必须再比较真实 `int3 cell`、Power-of-two Bitonic Stage/Pass 数量正确。

- [ ] **Step 2: 写 GPU Sort/Range RED Test**

  上传包含负坐标、同 Cell、不同 Cell、Inactive 的小型位置集合；Dispatch 后 Test-only Readback，断言 `(bucket,index)` 单调、每个 Range 是 `[start,end)`、Range 中所有匹配 Cell 可被找到且 Collision 候选被真实 Cell Filter 排除。

- [ ] **Step 3: 实现 Kernel 顺序**

```text
BuildSpatialEntries
-> BitonicSort(stage, pass) 多次 Dispatch
-> ClearCellRanges
-> BuildCellRanges
```

  Kernel ID 在 `Awake` 缓存；Property ID 用 `Shader.PropertyToID` 静态缓存；不得在 Tick 中 `FindKernel` 或按字符串找属性。

- [ ] **Step 4: 接入 Runtime Marker**

  增加 `GpuFluid.SpatialHash.Build`、`GpuFluid.SpatialHash.Sort`、`GpuFluid.SpatialHash.Ranges` CPU Submission Marker，并在 GPU Profiler 中使用对应 CommandBuffer Sample 名称。

- [ ] **Step 5: 运行 Tests 与 8K 粒子 Hash Smoke Test**

  确认排序结果、邻域计数和负世界坐标正常；Profiler 记录 2K/4K/8K/16K Sort 时间，但本 Task 不据此提前改 Radix Sort。

- [ ] **Step 6: 更新解析文档**

  解释为什么 Naive Neighbor Search 是 `O(N²)`、Uniform Grid 将候选限制到附近、Hash Collision 不等于粒子碰撞、Bitonic Sort 的 Stage/Pass、GPU Thread 无固定执行顺序，以及为何真实 Cell 二次过滤不可省略。

**建议提交名：** `feat(element-field): add gpu fluid spatial hash`

---

## Task 4：PBF Density Constraint、Position Correction 与速度更新

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/PbfKernelMath.cs`
- Create: `Assets/_Project/Art/Elemental/Compute/PbfSolver.compute`
- Create: `Assets/_Project/Tests/ElementField/PbfKernelMathTests.cs`
- Create: `Assets/_Project/Tests/ElementField/GpuPbfSolverTests.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

- Consumes: Spatial Entries/Ranges、Predicted Positions、LiquidSimulationSettings。
- Produces: Density/Lambda、DeltaPositions、Corrected Predicted Positions、Velocity、Committed Positions。

- [ ] **Step 1: 写 CPU Kernel Reference RED Tests**

  验证 Poly6 在 `r=0` 最大、`r>=h` 为 0；Spiky Gradient 在支持域外为 0；孤立粒子 Lambda 有限；对称邻域的 Delta Position 总和近似 0；非法 h/restDensity/epsilon 在 Settings 边界被拒绝。

- [ ] **Step 2: 写 GPU Numerical RED Tests**

  构造 2×2×2 过密粒子块，比较 0 次与 4 次 Iteration 的平均绝对 Density Error；断言误差下降、所有 Position/Velocity/Density/Lambda 有限、粒子总数不变。另测单粒子和两个完全重合粒子不产生 NaN。

- [ ] **Step 3: 实现两阶段 Constraint Kernel**

  每次 Iteration 严格 Dispatch：`ComputeDensityLambda` → `ComputeDeltaPosition` → `ApplyDeltaPosition`。`ApplyDeltaPosition` 对单步修正长度做 Profile 上限 Clamp，并设置数值异常 Counter；不得在 `ComputeDeltaPosition` 直接改邻居读取的 Predicted Buffer。

- [ ] **Step 4: 实现 Velocity/XSPH/Vorticity**

  先用 `(predicted-old)/dt` 更新基础速度；XSPH 读取旧 Velocity、写入独立 Delta Velocity，再 Apply。Vorticity 默认可关闭；开启时也使用两阶段 Buffer，避免 Thread Race。最后 Clamp Max Speed 并 Commit。

- [ ] **Step 5: 接入 Fixed Tick/Substep**

  一个 Fixed Tick 切成 Profile 指定 Substeps，每个 Substep 使用 `fixedDelta/Substeps`。如果本帧 Catch-up 超限，丢弃多余 Debt 并累加 Debug Counter，不能让后续一帧无限补算。

- [ ] **Step 6: 更新解析文档**

  用“预测位置→违反不可压缩约束→迭代拉回”的直觉讲解 PBD/PBF；逐项解释 Density、Rest Density、Lambda、Gradient、Epsilon、Artificial Pressure、Viscosity、Vorticity、Substep 和 Solver Iteration，附一套从水爆炸/结团/抖动症状反查参数的 Debug 表。

**建议提交名：** `feat(element-field): solve gpu position based fluids`

---

## Task 5：Box/Sphere/Capsule Collider Proxy 与稳定性 Gate

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Collision/FluidColliderAuthoring.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Collision/FluidColliderProxy.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Collision/FluidColliderProxyCollector.cs`
- Create: `Assets/_Project/Art/Elemental/Compute/PbfCollision.compute`
- Create: `Assets/_Project/Tests/ElementField/FluidColliderProxyTests.cs`
- Create: `Assets/_Project/Tests/ElementField/GpuPbfCollisionTests.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

```csharp
public enum FluidColliderShape : uint
{
    OrientedBox = 0,
    Sphere = 1,
    Capsule = 2,
}

// 固定 64 bytes；三行 WorldToLocal + Shape/参数，具体字段由 Contract Test 锁定。
public readonly struct FluidColliderProxy { }
```

- [ ] **Step 1: 写 Proxy Conversion RED Tests**

  覆盖非均匀 Scale 的 Box、旋转 OBB、Sphere 最大轴 Scale、Capsule Direction、Disabled/Trigger/错误 Collider 类型过滤、超过 Capacity 时确定性保留顺序和警告计数。

- [ ] **Step 2: 实现显式 Authoring 收集**

  Runtime 初始化时只扫描其配置 Root 下的 `FluidColliderAuthoring`；不得每帧 `FindObjectsByType`。Static Proxy 只上传一次；Dynamic Proxy 由 Authoring 的 Transform 变化或显式 `MarkDirty()` 触发重建。

- [ ] **Step 3: 实现 Collision Projection**

  把 Predicted Position 变换到 Collider Local Space；计算到 Box/Sphere/Capsule 内部的最短退出方向，把粒子中心投影到表面外加 Particle Radius；按 Normal 分解速度，应用 Restitution 与 Tangential Friction。Collision 放在每次 Position Iteration 的 Apply 后，保证密度修正不会再次把粒子推入墙体而无人处理。

- [ ] **Step 4: 写 GPU 穿透测试**

  对水平面 Box、45° OBB、Sphere 容器外壁和 Capsule 障碍分别运行至少 120 Fixed Ticks；断言粒子中心不持续留在禁止区域、速度有限、Active Count 不变。

- [ ] **Step 5: Phase B 长时间 Sandbox**

  运行落水、斜面、挡板、狭缝、容器五种场景；记录 2K/4K/8K 粒子的 Density Error、Max Correction、NaN Counter、穿透数和 CPU/GPU Marker。

- [ ] **Step 6: 更新解析文档**

  从向量投影、Normal/Tangent、World Space/Local Space、矩阵逆变换开始解释 Collider Constraint；说明为什么首版不用每粒子 Unity Physics、为何 MeshCollider 需要 SDF/Voxelization 才适合 GPU、单向耦合与双向耦合的区别。

### Phase B Unity Editor Gate

用户必须确认：水团能自由落体、撞地铺开、绕过挡板、沿斜面流下、落入简单容器；Debug 粒子没有持续穿透、爆炸、冻结或明显成团空洞。未通过时只调 PBF/Collision，不进入 Surface Reconstruction。

**建议提交名：** `feat(element-field): constrain gpu fluid against colliders`

---

# Phase C：GPU Density、Marching Cubes 与最终水材质

## Task 6：Isotropic Density Field 与 Surface Grid 资源

**Files:**

- Create: `Assets/_Project/Scripts/Rendering/ElementField/LiquidRenderProfile.cs`
- Create: `Assets/_Project/Scripts/Rendering/ElementField/FluidSurfaceGridPlanner.cs`
- Create: `Assets/_Project/Scripts/Rendering/ElementField/FluidSurfaceGpuResources.cs`
- Create: `Assets/_Project/Art/Elemental/Compute/FluidDensity.compute`
- Create: `Assets/_Project/Tests/Rendering/FluidSurfaceGridPlannerTests.cs`
- Create: `Assets/_Project/Tests/Rendering/GpuFluidDensityTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

```csharp
public readonly struct FluidSurfaceGridSettings
{
    public readonly Vector3Int Resolution;
    public readonly Vector3 WorldOrigin;
    public readonly Vector3 VoxelSize;
    public readonly int MaximumTriangleCount;
}
```

- [ ] **Step 1: 写 Grid Planner RED Tests**

  覆盖 Bounds Padding、Voxel Size→Resolution Ceiling、每轴最大分辨率 Clamp、乘法 Overflow、防止 0 Dimension、Interest Bounds 平移只改变 Origin 不重新分配、Profile 变化只在重新进入 Play Mode 后 Snapshot。

- [ ] **Step 2: 实现 Surface GPU Resource 生命周期**

  创建 `R32_SFloat` 3D Random-write Density Texture、Triangle Buffer、Triangle Counter、Overflow Counter、Indirect Args 和 Anisotropy Buffer。分辨率由 Active Bounds 尺寸与 Profile 决定，Interest Bounds 只平移时复用同一资源。

- [ ] **Step 3: 实现 Isotropic Density Gather**

  每个 Voxel Thread 计算 World Position，通过 Simulation 的 Spatial Entries/Ranges 查询邻粒子并累加 Kernel；直接覆盖 Density Texture，不先 Clear。只采样 `active=1` 且位于 Surface Bounds+Kernel Padding 的粒子。

- [ ] **Step 4: 写 GPU Density Test**

  单粒子中心 Density 大于支持域边缘，域外为 0；两个相邻粒子 Density 叠加；负世界坐标和 Bounds 平移保持相同局部结果；输出无 NaN。

- [ ] **Step 5: 增加 Debug Slice**

  只在 Editor/Development Build 提供选择 Z Slice 的灰度 Debug 显示或 Async Test Dump，帮助区分“PBF 没粒子”和“Density 阈值不对”；不得为每帧 Debug 创建 Texture2D。

- [ ] **Step 6: 更新解析文档**

  解释 Scalar Field、Voxel/Lattice、Kernel Support、Sampling Resolution、Nyquist 直觉、3D Texture、World-to-Grid Mapping、为什么 Density 连续但仍需离散采样，以及分辨率三次方增长的成本。

**建议提交名：** `feat(rendering): build gpu fluid density field`

---

## Task 7：GPU Marching Cubes、Indirect Draw 与 Procedural Water Shader

**Files:**

- Create: `Assets/_Project/Art/Elemental/Compute/MarchingCubesTables.hlsl`
- Create: `Assets/_Project/Art/Elemental/Compute/FluidMarchingCubes.compute`
- Create: `Assets/_Project/Scripts/Rendering/ElementField/GpuLiquidSurfaceRenderer.cs`
- Create: `Assets/_Project/Art/Elemental/Shaders/ElementLiquidProcedural.shader`
- Create: `Assets/_Project/Tests/Rendering/MarchingCubesContractTests.cs`
- Create: `Assets/_Project/Tests/Rendering/GpuMarchingCubesTests.cs`
- Modify: `Assets/_Project/Art/Elemental/Shaders/ElementWaterCore.hlsl`
- Modify: `Assets/_Project/Tests/Rendering/ElementWaterShaderContractTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

```hlsl
struct FluidSurfaceVertex
{
    float3 positionWS;
    float padding0;
    float3 normalWS;
    float padding1;
};

struct FluidSurfaceTriangle
{
    FluidSurfaceVertex v0;
    FluidSurfaceVertex v1;
    FluidSurfaceVertex v2;
};
```

- [ ] **Step 1: 写 Lookup/Case RED Tests**

  锁定 256 个 Case、16-entry Triangle Row、Edge Index 0..11、空/满 Case 无 Triangle、单 Corner Case 的 Edge 组合和最大 5 Triangles/Cell。若 Lookup 来自 MIT Reference，保留 License/Attribution；优先按公开算法重新录入并用 Test 校验。

- [ ] **Step 2: 实现 Triangle Counter 与 Overflow 防护**

  每个 Cell Thread 先算 Case；对每个 Triangle 用 `InterlockedAdd` 取得独立 Triangle Index。若 Index 超过 Capacity，不写越界，只设置 Overflow Counter。单个 Triangle Struct 连续写三个 Vertex，避免 Append Vertex 交错破坏拓扑。

- [ ] **Step 3: 实现 Edge Interpolation 与 Gradient Normal**

  分母接近 0 时固定 `t=0.5`；否则 Clamp 到 `[0,1]`。Central Difference 从 Density Texture 采样，Outward Normal 使用负 Gradient；长度过小时使用 Case 几何 Normal 兜底。GPU Test 验证球形 Field 生成非零 Triangle、Bounds 有限、Normal 朝外且近似单位长度。

- [ ] **Step 4: 实现 Indirect Args**

  `BuildIndirectArgs` 将 Triangle Count 转成 Vertex Count×3，写 `GraphicsBuffer.IndirectDrawArgs`；`instanceCount=1`，Start Vertex/Instance=0。CPU 不读取 Triangle Count，只读取低频 Debug Counter。

- [ ] **Step 5: 实现 Procedural Shader**

  Vertex Shader 通过 `UnityIndirect.cginc`、`InitIndirectDrawArgs(0)` 和 `GetIndirectVertexID` 找到 Triangle/Corner，读取 World Position/Normal 并变换到 Clip Space。Fragment 阶段复用现有 Depth、Fresnel、Foam、Triplanar Wave 与 URP PBR；Legacy `ElementWater.shader` 的 Property 名称和默认行为保持不变。

- [ ] **Step 6: 实现 Renderer 热路径**

  `GpuLiquidSurfaceRenderer` 初始化时创建 `MaterialPropertyBlock` 和 `RenderParams` 所需长期状态；在 `LateUpdate` 中按 Density→Marching Cubes→Build Args 顺序 Dispatch，再调用 `Graphics.RenderPrimitivesIndirect`，保证所有普通 `Update` Simulation Dispatch 已先提交到同一 GPU Queue。`worldBounds` 使用真实 Active Bounds+Padding，不能用 10000m 假 Bounds，否则透明排序/Culling 失真。

- [ ] **Step 7: Shader/Frame Debugger Gate**

  Contract Test 检查 `_FluidSurfaceTriangles`、Indirect Shader Setup 和所有 Legacy Water Properties。Frame Debugger 确认只出现一个 PBF Surface Draw、Triangle Count 来自 Indirect Args、Queue 为 Transparent、Depth Texture 可采样、Legacy Renderer 未被误改。

- [ ] **Step 8: 更新解析文档**

  从 Vertex/Fragment/Rasterization、`SV_VertexID`、Clip Space、Triangle Winding、Backface Culling、Gradient Normal、Indirect Draw Args、透明排序和 Overdraw 开始详细讲解；用一个 Marching Cubes Cell 的 8 Corner/12 Edge 例子走完整 Case Index。

**建议提交名：** `feat(rendering): reconstruct gpu fluid surface`

---

## Task 8：Anisotropic Kernel 还原流动水团外形

**Files:**

- Create: `Assets/_Project/Art/Elemental/Compute/FluidAnisotropy.compute`
- Create: `Assets/_Project/Tests/Rendering/GpuFluidAnisotropyTests.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/LiquidRenderProfile.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/FluidSurfaceGpuResources.cs`
- Modify: `Assets/_Project/Art/Elemental/Compute/FluidDensity.compute`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/GpuLiquidSurfaceRenderer.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

- Consumes: 粒子邻域、Spatial Hash、Isotropic Density 路径。
- Produces: 每粒子 Symmetric Kernel Transform；Profile 可在 Isotropic/Anisotropic 之间切换用于 A/B。

- [ ] **Step 1: 写 Anisotropy RED Tests**

  球形对称邻域输出三个近似相等尺度；线状邻域沿主轴拉长；孤立/邻居不足粒子回退 Isotropic；Eigenvalue 全部 Clamp 到正数范围；矩阵与 Density 输出无 NaN/Infinity。

- [ ] **Step 2: 实现邻域均值与 Covariance**

  第一 Pass 求加权 Mean，第二 Pass 求 `Σ w(p-mean)(p-mean)^T`。使用与 Density 一致的邻域查询；邻居数低于 Profile 阈值时直接写 Identity Transform。

- [ ] **Step 3: 实现稳定 Eigenvalue Clamp**

  取得主方向后，把长/短轴比例限制在 Profile 的 `MaximumAnisotropyRatio`，并保持 Kernel 体积近似不变；所有分母加 Epsilon。不得让稀疏表面粒子被无限拉成长针。

- [ ] **Step 4: 接入 Density A/B**

  `LiquidRenderProfile` 提供 `UseAnisotropy`、Update Interval、Neighbor Threshold、Min/Max Scale；关闭时完全跳过 Anisotropy Dispatch，方便定位 Surface 问题。若隔帧更新，Particle Transform Buffer 保留上次有效数据。

- [ ] **Step 5: 视频目标视觉 Gate**

  对比 Isotropic 与 Anisotropic：落下水团、横向高速水流、越过台阶和容器倾倒。目标是消除“串珠/棉花糖球”感，同时不产生尖刺、表面断裂或体积明显膨胀。

- [ ] **Step 6: 更新解析文档**

  从 Mean、Variance、Covariance Matrix、Eigenvector/Eigenvalue 的几何直觉讲起；解释为什么粒子沿流动方向排列时 Kernel 应拉长、为何必须 Clamp、它只改变 Surface Reconstruction 而不改变 PBF Gameplay Position。

### Phase C Unity Editor Gate

用户必须确认最终 Surface 已达到参考视频方向：不显示粒子、轮廓连续、流动方向自然、落地/越障/容器场景无明显网格阶梯；Frame Debugger、GPU Profiler 和 Shader Console 无错误。未通过时保留 Debug Particle、Density Slice、Isotropic A/B 三层定位手段，不进入 P7 Gameplay Integration。

**建议提交名：** `feat(rendering): add anisotropic fluid kernels`

---

# Phase D：现有 ElementWorld、Wet 与 Fire Reaction 接入

## Task 9：Water 写入路由、Legacy 回退与 P7 Source 兼容

**Files:**

- Modify: `Assets/_Project/Scripts/ElementField/Data/ElementWorldProfile.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Simulation/ElementWriteRequest.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementDepositOnImpact.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Simulation/ElementFieldSimulationSettings.cs`
- Modify: `Assets/_Project/Scripts/ElementField/World/ElementWorldSimulator.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/ElementWorldWaterRenderer.cs`
- Create: `Assets/_Project/Tests/ElementField/ElementWorldFluidRoutingTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementDepositOnImpactTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldSimulatorTests.cs`
- Create: `Assets/_Project/Tests/Rendering/ElementWorldWaterRendererModeTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

- Consumes: 现有 `ElementWriteRequest`、唯一 Registry、PBF Runtime。
- Produces: `LegacyCell` 行为完全不变；`GpuPbf` 中 Water 只进入 Spawn Queue，Fire 只进入 Cell Queue。

- [ ] **Step 1: 写 Routing RED Tests**

  用 Fake Liquid Sink 验证：Legacy Water→Cell；PBF Water→Liquid；两种模式 Fire→Cell；PBF 模式缺少/未初始化 Fluid Runtime 时拒绝请求而不是偷偷写回 Legacy；Queue 满时返回 false；一次请求只到一个 Sink。

- [ ] **Step 2: 增加 Profile Mode**

  `WaterSimulationMode` 默认枚举值为 `LegacyCell=0`，确保现有 `.asset` 未设置新字段时保持旧行为。`ElementWorldProfile.CreateSimulationSettings()` 把 `SimulateCellWater` 快照为 `mode==LegacyCell`。

- [ ] **Step 3: 修改 Runtime 路由**

  `ElementWorldRuntime.TryEnqueueWrite` 是唯一分流入口。Water/PBF 调 `GpuPbfFluidRuntime.TryEnqueueDeposit(in request)`；其余写入原 Queue。Fluid Runtime 不注册 `ElementRuntimeRegistry`，只作为 ElementWorldRuntime 的子系统引用。

- [ ] **Step 4: 跳过 Cell Water Solver**

  PBF 模式下 `ElementWorldSimulator` 不执行 Cell Water Reaction、Down Flow 和 Horizontal Flow，但继续执行 Fire Decay、Chunk Activity 和非液体逻辑。Legacy 模式的旧测试数值必须逐项不变。

- [ ] **Step 5: 防止双重 Renderer**

  `ElementWorldWaterRenderer` 在 PBF 模式明确禁用自身并保留诊断；`GpuLiquidSurfaceRenderer` 在 Legacy 模式不提交 Draw。切换 Profile 后要求退出并重新进入 Play Mode，避免中途销毁/重建大 Buffer。

- [ ] **Step 6: 扩展 Impact Initial Velocity 且保持 Source 解耦**

  `ElementWriteRequest` 新增只读 `InitialVelocity`；保留原构造函数并转调完整构造函数，默认 `Vector3.zero`，因此 Fire、Persistent Source 和 Initial Deposit 无需改调用。`ElementDepositOnImpact` 增加可调 `_fluidInitialSpeed`，在 `OnProjectileImpacted(hitPoint, hitDirection)` 中写入 `hitDirection.normalized * speed`。Water Projectile Prefab 仍不增加 GPU 引用；`TotalAmount` 由 Fluid Profile 转成 Particle Count，`Radius/UseLinearFalloff` 转成 Spawn Distribution，PBF Runtime 只消费请求值。

- [ ] **Step 7: 更新解析文档**

  解释 Adapter/Router、Single Writer、Feature Toggle、序列化枚举默认值、为何不让 Projectile 依赖 GPU、为什么 PBF 模式不能把 Water 同时写入 Cell 作为“保险”，并画出 Legacy/PBF 两条互斥链。

**建议提交名：** `feat(element-field): route water deposits to gpu pbf`

---

## Task 10：低频 AsyncGPUReadback、Liquid Occupancy 与 Wet

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/ILiquidOccupancyReadOnly.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/LiquidGameplaySnapshot.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidGameplayOccupancyBridge.cs`
- Create: `Assets/_Project/Tests/ElementField/LiquidGameplaySnapshotTests.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidGameplayOccupancyBridgeTests.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldExposureSystem.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldExposureSystemTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

```csharp
public interface ILiquidOccupancyReadOnly
{
    bool HasValidSnapshot { get; }
    Bounds SnapshotBounds { get; }
    bool TryGetAmount(
        Vector3Int globalCell,
        ElementMaterialKind materialKind,
        out byte amount);
}
```

- [ ] **Step 1: 写 Snapshot Pure RED Tests**

  覆盖 World→Global Cell、负坐标、Bounds 边界 Inset、多粒子累加 Saturate 255、Inactive 跳过、Interest Bounds 平移后旧数据清空、查询范围外返回 false、MaterialId 不是 Water 时不进入 Water Channel。

- [ ] **Step 2: 实现双 NativeArray Readback**

  初始化时按 Particle Capacity 分配两个 `NativeArray<Vector4>(Allocator.Persistent)`；缓存 Method-group Callback，不在请求处创建 Lambda。请求从 `[DefaultExecutionOrder(100)]` 的 Bridge `LateUpdate` 发出，保证 Simulation Dispatch 已提交；请求未完成时不发第二个，完成后交换 Read/Write Buffer 并 Binning。`OnDisable` 停止新请求；若 Teardown 时仍有请求，正常 Scene Disable 先轮询到完成再 Dispose，最终 `OnDestroy` 仅在无法继续轮询时允许执行一次 `WaitForCompletion` 后释放，绝不把等待带入正常帧路径。

- [ ] **Step 3: 处理不支持与错误**

  `supportsAsyncGPUReadback=false` 时 PBF Gameplay Bridge 标为不可用；Request `hasError` 时保留上一有效 Snapshot、增加错误计数并限频警告。禁止回退到每帧 `GetData()`。

- [ ] **Step 4: 改造 Exposure 读取边界**

  PBF 模式：Water Amount 读 `ILiquidOccupancyReadOnly`；Fire Amount 继续读 `ElementWorldRuntime.TryGetActiveCell`。Legacy 模式：两者都保持原 Cell 路径。现有 `OverlapBoxNonAlloc`、Target 去重、0.25s Interval 和 Status Apply 语义不变。

- [ ] **Step 5: PlayMode Wet Gate**

  角色进入稳定水池、穿过飞溅水团、离开水体三种场景；记录 Snapshot Age、最大 Water Amount、Wet Apply Count。预期延迟不超过一个完成的 Readback/Exposure 周期加一帧，不出现每帧 GC。

- [ ] **Step 6: 更新解析文档**

  解释 GPU 异步、CPU/GPU Pipeline、为什么同步 Readback 会 Stall、Double Buffer、Persistent NativeArray、Snapshot Latency 与 Gameplay Trade-off；明确“视觉位置是即时 GPU，Wet 是低频近似”。

**建议提交名：** `feat(element-field): project gpu liquid occupancy to gameplay`

---

## Task 11：GPU Water 与 CPU Fire 的灭火反应

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidReactionCommandQueue.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidFireReactionSystem.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidReactionCommandQueueTests.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidFireReactionSystemTests.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs`
- Modify: `Assets/_Project/Art/Elemental/Compute/PbfParticleLifecycle.compute`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldSimulatorTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

- Consumes: Water Occupancy Snapshot、Fire Cell、`ElementReactionEvaluator.CalculateExtinguishConsumption`。
- Produces: Fire Cell 负 Amount Delta 与 GPU Particle Consume Command；两边在各自 Tick Boundary 应用。

- [ ] **Step 1: 写 Reaction Planner RED Tests**

  覆盖 Water/Fire 同 Cell、六邻域接触、Formal Threshold、低强度反应、Particle Quantization、一次 Snapshot Version 只消费一次、Queue 满时两边都不部分提交、Legacy 模式仍走旧 Reaction。

- [ ] **Step 2: 实现事务式命令规划**

  先计算 `consumedAmount`，再确认 Fire Delta Queue 与 Fluid Consume Queue 都有容量；两者均可接收后才同时入队。禁止先减 Fire、后发现 GPU Queue 满，造成单边消耗。

- [ ] **Step 3: 在 Tick Boundary 应用 Fire Delta**

  `ElementWorldRuntime` 在 Apply Writes 阶段处理内部 `ElementCellDeltaRequest`，只允许减少匹配的 Fire Amount，Clamp 到 0，并更新 Chunk Version/Activity/Wake；不把负 Amount 暴露给 Projectile 公共写接口。

- [ ] **Step 4: 实现 Particle Consume Kernel**

  Command 描述 Global Cell AABB、MaterialId、最大粒子数和确定性 Seed。Kernel 只释放 Bounds 内 Water Slot，把 Index 压回 Free Stack，并更新 Counter；按 `AmountUnitsPerParticle` 量化，最大误差小于一个粒子的 Amount。

- [ ] **Step 5: 反应组合验证**

  Water Projectile 命中 Persistent Fire、稳定水池接触 Fire、低量飞溅擦过 Fire；确认 Fire Renderer 变弱/熄灭、水粒子有对应消耗、Wet 仍正确、旧 Legacy Reaction Tests 不变。

- [ ] **Step 6: 更新解析文档**

  解释两个不同 Timeline 之间为何需要 Command Queue 和 Transaction、Snapshot Version 如何防重复、离散粒子带来的 Quantization、反应守恒的近似边界，以及从“火没灭/水没少/两边都消失过多”分别检查哪些 Counter。

### Phase D Unity Editor Gate

用户在独立 Sandbox 和 P7 水弹链各验证一次：Water 只生成 PBF 粒子；Legacy Mesh 不重叠；角色能 Wet；Fire 能被消耗；切回 Legacy 后旧水、Wet、灭火恢复。Gate 通过前不得删除任何 Legacy 类型或资产。

**建议提交名：** `feat(element-field): bridge gpu water fire reactions`

---

# Phase E：Activity、P7 集成、性能与文档收口

## Task 12：Resident/Interest/RequiresSimulation、Sleep/Wake 与容量保护

**Files:**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Activity/FluidActivityPlanner.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidActivityPlannerTests.cs`
- Modify: `Assets/_Project/Art/Elemental/Compute/PbfParticleLifecycle.compute`
- Modify: `Assets/_Project/Art/Elemental/Compute/PbfSolver.compute`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/GpuLiquidSurfaceRenderer.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces:**

- Consumes: Active Interest Bounds、Velocity、Density Error、Spawn/Consume/Collider Dirty Events。
- Produces: Resident、Interest Active、RequiresSimulation Flags 与 Debug Counters。

- [ ] **Step 1: 写 Activity RED Tests**

  稳定速度和 Density Error 连续达到 N Tick 后休眠；仅出 Interest Bounds 不能释放 Resident Slot；新 Spawn、邻近 Awake Particle、Collider Dirty、Consume 和 Bounds 重新进入都能 Wake；Sleeping Particle 仍出现在 Renderer/Occupancy；Free Particle 不属于任何状态。

- [ ] **Step 2: 实现 Activity Flags**

  Metadata Bit 明确区分 Alive、InterestActive、RequiresSimulation、Sleeping。Predict/Solver 只推进 RequiresSimulation；Spatial Hash 与 Density Surface 仍包含 Active Bounds 内 Alive/Sleeping Particle，使稳定水不会从画面或邻域约束中消失。

- [ ] **Step 3: 实现 Whole-solver Fast Path**

  没有 Spawn/Consume、没有 Collider Dirty、GPU Awake Counter 为 0 时跳过 PBF Dispatch，只保留 Surface/Readback 所需状态。若无法在不 Readback 的情况下判断，使用 GPU Indirect Dispatch Args，而不是同步读取 Awake Count。

- [ ] **Step 4: 容量与 Overflow 保护**

  Free Slot 不足时 Spawn Kernel 原子记录 Rejected Particle Count；Triangle、Collider、Spawn Request 和 Reaction Queue 都有独立 Overflow Counter。Development Inspector 显示这些 Counter，但读取频率低于 2Hz，使用 Async Readback。

- [ ] **Step 5: 更新解析文档**

  联系旧 ElementWorld 的 Resident/Interest Active/Solver-Sleeping，说明数据寿命与计算寿命为何分离；记录 Sleep 误判导致水冻结、邻居唤醒风暴和 Counter Overflow 的 Debug 方法。

**建议提交名：** `perf(element-field): sleep stable gpu fluid particles`

---

## Task 13：P7 场景接入、性能回归、Windows Build 与解析文档完结

**Files:**

- Unity-created: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidSimulationProfile_Water.asset`
- Unity-created: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidRenderProfile_Water.asset`
- Unity-created: `Assets/_Project/Art/Elemental/Materials/M_ElementWater_PBF.mat`
- Unity-created: `Assets/_Project/Scenes/PBF_FluidSandbox.unity`
- Editor modify after user approval: `Assets/_Project/Scenes/P7_DemoRun.unity`
- Editor modify if required: `Assets/_Project/Art/Elemental/Prefabs/PF_ElementWaterProjectile.prefab`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Modify: `docs/superpowers/plans/2026-08-09 GPU PBF流体模拟实施计划.md`（只填写实际完成证据，不改写原要求）

**Interfaces:**

- Consumes: Phase A-D 全部 Runtime、Renderer、Profiles、Tests 和 Editor Gate。
- Produces: P7 可玩的 PBF Water、Legacy 回退、完整验证记录和状态为“已完成”的阶段解析文档。

- [x] **Step 1: 由用户在 Editor 创建和绑定资产**

  先由用户确认当前 Dirty 的 `P7_DemoRun.unity` 已保存并允许继续编辑，再创建两个 Profile、PBF Material 和 Sandbox Scene；P7 的 `ElementWorldRuntime` 绑定 `GpuPbfFluidRuntime`，GPU Runtime 绑定 ComputeShader/Profile/Bounds Provider，Surface Renderer 绑定 Fluid Source/Render Profile/Material。为水会接触的地面、墙、斜面和容器添加 `FluidColliderAuthoring`；不修改与水无关的 Collider，也不覆盖现有 Wizard/Jump Scene 改动。

  > 2026-08-16 实际证据：用户确认已推送远端后授权修改；Unity Editor 创建两份 Liquid Profile、P7 PBF World Profile、Material、4K Sandbox、Water Validation Wand，并为 P7 追加独立 PBF Root 与 69 个受支持 Collider Authoring。所有 Scene Asset 引用均经 GUID/local fileID 验证。

- [ ] **Step 2: 执行完整静态与 Test Runner 回归**

```powershell
dotnet build Game.ElementField.csproj --no-restore
dotnet build Game.ElementField.Tests.csproj --no-restore
dotnet build Game.Rendering.csproj --no-restore
dotnet build Game.Rendering.Tests.csproj --no-restore
```

  然后在 Unity Test Runner 执行全量 EditMode 与相关 PlayMode。记录实际 Passed/Failed/Skipped 数；命令行编译不能替代 Unity Shader/Compute Import。

  > 部分证据：五个相关 C# Assembly 串行 fresh Rebuild 均 0 warning / 0 error；Unity 全量 EditMode 为 624 Passed / 23 Failed，PBF focused EditMode/GPU 为 369 Passed / 0 Failed。既有 23 个失败与相关 PlayMode 尚未清零，故本 Step 不勾选。

- [ ] **Step 3: 执行 PBF Sandbox Matrix**

  粒子数 2K/4K/8K/16K；Surface Isotropic/Anisotropic；Box/Sphere/Capsule；落水/斜面/挡板/容器；运行 10 分钟。记录 CPU/GPU Marker、GC、Density Error、NaN、Rejected Spawn、Triangle Overflow 和 Readback Age。

  > 部分证据：DX12 4K/Anisotropic/Box+Ramp+Container 实际运行 60 秒，所有 Overflow=0，忽略前 10 秒初始化后的 Max GC Alloc=0 B。其余规模、Isotropic、10 分钟、GPU Marker、Density Error/Readback Age 尚未取得，故不勾选。

- [ ] **Step 4: 执行 P7 Scene Playthrough**

  从真实 Wizard Cast 发射 Water Projectile，验证命中地面、墙、敌人附近、连续快速施法、Pause/Wand Editor、Interest Bounds 移动、Wet、Persistent Fire、回到旧区域、切换 Legacy。不能只用 Inspector Context Menu 冒充 Gameplay 链。

  > 部分证据：Windows Player 从 Main Menu 进入 P7，独立 Water Wand 经真实 Wizard Cast 连续施放 6 次；日志无 Compute/Profile/NaN/Exception，并明确禁用 Legacy Water Renderer。Pause/Wand Editor、Wet、灭火、Bounds 往返与 Legacy 切换尚未执行，故不勾选。

- [ ] **Step 5: Frame Debugger 与 Rendering 验证**

  确认 PBF 模式没有 `WorldWaterChunkView_*` Legacy Draw；只有预期 Procedural Surface Draw；Depth/Fresnel/Foam、Backface Culling、透明 Queue、Bounds Culling 正确；无 Triangle Winding 反转和水面只在下方可见。

  > 未取得 Frame Debugger 证据；仅有 Player 日志中的 Legacy Renderer Disable 与视觉截图，不能替代 Draw Call/Depth/Cull/Queue 验证。

- [ ] **Step 6: Windows Development Build Smoke Test**

  使用 Windows Development Build，实际启动 Main Menu→P7；验证 Compute Shader、Material、Profile 和 Scene 资源被打包，真实 Player 中水弹、Wet、灭火和 Legacy Fallback 可用。记录 Graphics API、分辨率、构建结果和 Smoke Test 路径。

  > 部分证据：最终 Windows x64 Development Build `Succeeded`，0 error / 0 warning，1,764,303,147 bytes；DX12 1920×1080 Player Main Menu→P7 与真实 Water Cast 已通过。Wet、灭火和 Legacy Fallback 未完成，故不勾选。

- [ ] **Step 7: 完结解析文档**

  把状态从“更新中”改为“已完成”；补齐总体架构图、完整 Runtime Call Chain、所有创建/修改脚本表、坐标空间/数学总复习、替代方案、性能 Debug 全过程、遗留边界、面试 Q&A 和“模拟面试者完整讲解”。

  > Task 13 章节、文件表、Debug 链、证据边界与模拟面试回答已追加；因 Phase Gate 尚缺多层证据，文档按约束继续标记“更新中”，故不勾选。

  Debug 章节必须使用：

```text
症状 -> Profiler/Counter/Frame Debugger 证据
-> 排除 Simulation / Surface / Shader / Gameplay Bridge 哪一层
-> 根因
-> 被否决修复及原因
-> 最终修复
-> 回归证据
-> 仍未覆盖边界
```

- [x] **Step 8: 最终工作区审计**

  `git diff --check`；列出本阶段所有文件；确认没有手写 `.meta`、没有修改无关 Dirty Files、没有 Runtime `GetData/WaitForCompletion`、没有每帧 `new/LINQ/boxing`、没有直接 `Debug.Log`。

  > 实际证据：`git diff --check` Exit 0；新增文件 trailing whitespace=0；Task 13 新 C#/modified Runtime 无直接 `Debug.Log`、LINQ、Runtime `GetData/WaitForCompletion`；测试中的 `WaitForCompletion` 仅用于 GPU 数值 fixture。新增 `.meta` 均由 Unity Importer 生成，GUID 无重复；Collector 热路径只访问初始化时分配的固定数组。

### Phase E 最终 Gate

必须同时具备：静态编译、Unity Test Runner、Sandbox、P7 Scene Playthrough、Profiler、Frame Debugger、Windows Development Build 和完整解析文档证据。缺少任意一层时，只能描述已经取得的证据，不能宣称整个流体阶段完成。

**建议提交名：** `feat(element-field): integrate gpu pbf water`

---

# 2026-08-21 架构重新定位：从 PBF Water 到 3D Material World

> 本节是对后续开发方向的增量修订，不删除、不重写 Task 0–13 的历史。Task 0–13 仍然代表已经落地的第一个 `Simulation Backend` 与第一个 Material；Phase F 继续完成 Liquid Quality，Phase G/H 才增加 Material Semantic Foundation 和第二种 Liquid。源码永远高于本计划中的设计意图。

## A. 当前源码事实与证据边界

### A.1 已实现且已有实际证据

- `ElementWriteRequest -> ElementWorldRuntime -> ElementWorldWriteRouter` 已形成场景唯一写入口；PBF Water 失败不会偷偷写回 Cell，因此不会把一次 Deposit 复制成两份权威水量。
- `GpuPbfFluidRuntime` 已实现固定容量 Particle Pool、Free Stack、Spawn/Consume、Spatial Hash、PBF Solver、Box/Sphere/Capsule Collision、Activity/Sleep/Wake 与 current-position Hash 发布。
- GPU `Metadata.x` 已保存 Material ID，`Metadata.y` 已保存 `Alive / InterestActive / RequiresSimulation / Sleeping`；Gameplay packed sample 同时读取 Position、Alive 与 Material ID。
- GPU Particle Position/Velocity/Metadata 是 Water Simulation Truth；`LiquidGameplaySnapshot` 是低频 CPU Projection；Density/Anisotropy/Marching Cubes/Shader 是 Presentation Representation。
- `ElementReactionEvaluator` 已是 Pure Semantic Math；Legacy Cell Water–Fire 与 GPU Water–CPU Fire 都复用 `CalculateExtinguishConsumption`，没有各写一套灭火公式。
- Task 13 报告已有 DX12 focused EditMode/GPU `369/369`、4K Sandbox 60 秒、真实 P7 Water Cast 与 Windows Development Build 证据。

### A.2 源码存在，但证据仍不足

- Task 13 仍未取得 D3D11、Frame Debugger、正式 GPU Profiler 时间、完整 2K/4K/8K/16K×10 分钟 Matrix、Wet/Consume/Legacy 切换的完整 P7 Gameplay Gate。
- `progress.md` 的 Task 13 仍是陈旧 `pending`；本轮只把它更正为“开发交付完成、最终 Gate 未闭合”，不能把缺失证据改写成 Passed。
- Phase F 的 `FluidSpawnPackingPlanner`、`DensityPacked`、Cohesion Kernel 与 Connected Surface Fixture 当前只存在于计划，Production/Test 源码中尚不存在。

### A.3 当前 Water-specific 程度

| 层 | 已具备的复用能力 | 当前阻碍第二种 Liquid 的硬编码 |
|---|---|---|
| 写入 | `ElementWriteRequest` 已携带 Material、Amount、Position、Radius、Velocity | Router 只特判 Water；`FluidDepositQueueAdapter` 只接受 Water |
| Particle Storage | Spawn/Metadata/Consume 已携带 uint Material ID | Runtime 只有一份全局 `LiquidSimulationProfile` |
| Solver | Pool、Hash、PBF、Collision、Activity 与 Material 名称无关 | Neighbor 不比较 Material ID；Mass/RestDensity/Viscosity/Cohesion 全局共享 |
| Gameplay Projection | packed sample 已原子携带 Material ID | `LiquidGameplaySnapshot` 只 Binning Water，Key 只有 Global Cell |
| Reaction | Pure Evaluator 已不认识 Particle/Cell | Contact Discovery、规则选择与 Apply 仍分别硬编码 Cell Water–Fire、GPU Water–Cell Fire |
| Rendering | `GpuLiquidSurfaceRenderer` 已是只读 Backend Consumer | Water Mode Gate、单 Material/Profile、Density 不按 Material 过滤 |

## B. 三层评价

1. **Water Simulation：正确且应继续。** Single Truth、Legacy Fallback、零 GC、GPU Surface 与 Gameplay Snapshot 边界均正确；Phase F 只需要修复 Spawn/Cohesion/Surface Quality。
2. **Liquid Backend：算法主链已成立，Material 参数化未完成。** Particle Pool、Hash、PBF、Collision、Sleep、Readback 和 Meshing 都可复用；Ingress、Per-material Parameters、Neighbor Filter、Occupancy 与 Surface Filter 是第二种 Liquid 前的必要增量。
3. **Material World：已有 Semantic Seed，尚无正式 Domain Model。** 稳定 byte ID、Pure Reaction Math、Carrier/Projection 分离都存在；缺少跨 Assembly 的 Canonical Identity、最小 Definition/Catalog、Behavior-to-Backend Routing 和 Material-aware Query。

## C. 选择的架构与被否决方案

| 方案 | 收益 | 代价/问题 | 结论 |
|---|---|---|---|
| 继续在 `ElementField` 增加 Water/Poison/Oil 分支 | 短期文件最少 | 第三种 Material 后 Router、Exposure、Reaction、Renderer 形成条件分支网；Combat 无法引用 Canonical Identity | 拒绝作为长期方向 |
| 新增小型 `Game.Materials` Semantic Assembly | Identity/Definition/Catalog 可被 Combat、ElementField、Rendering 单向依赖；Solver 保持专用 | 需要一次稳定 ID 迁移和 asmdef 调整 | **采用** |
| 把所有系统改成 `UniversalMaterialRuntime` | 名义上“统一” | 形成 God Object、Universal Solver、循环依赖与不可测试热路径 | 明确拒绝 |

核心原则：

```text
统一：MaterialId / MaterialDefinition / Reaction Semantic / Query Vocabulary
不统一：PBF / Cellular Automata / Gas Grid / Static Scene / Rigidbody PhysX
```

目标数据流：

```text
Game.Materials
├── MaterialId
├── MaterialBehaviorKind
├── MaterialDefinition
└── MaterialCatalogSnapshot
             │                         WaterSimulationMode + Platform Capability
             │                                      │
             │                         WaterSimulationCompatibilityPolicy
             │                                      │
             └──────────────┬────────── Final MaterialSimulationRouteSnapshot
                            │
ElementWriteRequest
             │
ElementWorldRuntime（唯一 IElementWriteSink）
             │ TryResolve(MaterialId)
MaterialSimulationRouter（热路径不知道 WaterSimulationMode）
├── Element Cell Backend ------------ Fire / Legacy Water
├── GPU Liquid/PBF Backend ---------- Water / Poison
├── Granular Backend ---------------- Future
├── Gas Backend --------------------- Future
├── Static Solid -------------------- Future
└── Rigidbody Bridge ---------------- Future
             │
             ├── Authoritative Simulation State
             ├── Gameplay Query / Snapshot（Derived）
             ├── Status Projection（Derived）
             └── Rendering Representation（Derived）
```

### C.1 Identity、Behavior、Representation 与 Backend 四层边界

| 概念 | 回答的问题 | Phase G/H 的例子 | 是否随运行时生命周期变化 |
|---|---|---|---|
| `MaterialId` | “它是谁？” | Water、Fire、Poison | 不变；Stone 断裂后仍是 Stone |
| `MaterialBehaviorKind` | “它在语义上属于哪类物质？” | Water/Poison=`Liquid`，Fire=`ReactiveField` | 通常不随 Representation 改变 |
| Simulation Representation | “这份 Material 当前以什么数据或 Unity 对象存在？” | GPU Particle Slots、Element Cells、未来 Scene Mesh/Rigidbody Actor | 可以变化；它是运行时状态而非 Definition 字段 |
| `MaterialSimulationBackendKind` | “哪个系统拥有并推进该 Representation？” | `ElementCell`、`GpuPbfLiquid` | Route/Transfer 决定；不是 Material Identity |

Task 18–25 不创建 `MaterialRepresentationKind` 枚举或 Transfer Handle，因为当前没有同一份 Material 在运行中跨 Backend 转换的玩法消费者。首版 Route 是初始化期的 Material→Backend Policy；未来 Stone 断裂时再引入“具体 Representation Instance + Transactional Transfer”。这既修正 `StaticSolid/DetachedSolid` 混入 Behavior 的问题，也避免为尚不存在的 Solid 生命周期过度设计。

### C.2 Material Identity 与 Representation 的 Authority Rule

同一份具体 Material Amount 在任一时刻只能由一个 Backend 权威持有。例如：

```text
Water Material Identity
└── GPU Particle Slots                 <- Authoritative Simulation State
    ├── CPU Occupancy Snapshot         <- Derived Gameplay Projection
    ├── Wet Status                     <- Derived Character State
    └── Density / Mesh / Shader         <- Derived Presentation
```

`Snapshot`、`Status` 和 `Mesh` 都不能反向修改或替代 GPU Truth。未来 Static Stone 转成 Detached Rigidbody 时，必须单独设计“旧 Backend Reserve -> 新 Backend Create -> 原子 Commit/Cancel”的守恒事务；Task 18–25 不提前实现该 Transfer System。

### C.3 Assembly 依赖目标

```text
Game.Core
├── Game.Materials      （Identity / Definition / Catalog；不含 Solver/Renderer/Status）
│   ├── Game.Combat     （Reaction/Status；只读 Material Semantic）
│   ├── Game.ElementField（Simulation Router + Cell/Liquid Backend）
│   └── Game.Rendering  （Material-filtered Presentation；只读 ElementField Source）
└── 其他既有模块
```

`Game.Materials` 不依赖 `Game.Combat`、`Game.ElementField` 或 `Game.Rendering`。`Game.Combat` 不反向引用 `Game.ElementField`；`Game.ElementField` 不引用 `Game.Rendering`。跨模块的瞬时广播仍使用 `EventBus<T>`，但初始化期只读 Definition/Catalog 可以通过普通 asmdef 引用消费。

## D. 明确保留、不做统一重构的源码

- 保留 `ElementWorldRuntime` 唯一入口、`ElementWriteRequest` 值命令与 `ElementRuntimeRegistry`。
- 保留 `GpuPbfFluidRuntime` 名称、GPU SoA Buffer、Free Stack、Spatial Hash、PBF Dispatch、Collision、Activity、Async Readback 生命周期。
- 保留 `GpuLiquidSurfaceRenderer`、Density、Anisotropy、Marching Cubes、Indirect Draw 技术主链。
- 保留 `ElementReactionEvaluator` 现有公式、`ElementReactionRuntime` 角色跨帧 Process、`StatusController` 的 Status Owner 职责。
- 保留 `WaterSimulationMode` 与全部 Legacy Water Renderer/Mesher/Profile/Material，直到新的 Regression Gate 明确允许删除。
- 保留 `SpellElement` 作为内容分类标签；它不替代 Material ID。
- 不把现有全部 `Fluid* / Liquid* / Water* / Element*` 类型全局改名；只迁移真正承担 Canonical Identity 的 `ElementMaterialKind`。

## E. 最小 Material Definition，避免 God Object

Phase G 的 `MaterialDefinition` 首版只包含：

```text
MaterialId Id
MaterialBehaviorKind Behavior
```

`MaterialBehaviorKind` 首版只定义 `None / Liquid / ReactiveField`。概念上未来可以加入 `Granular / Gas / Solid`，但本轮没有 Sand/Gas/Stone Definition 与消费者，因此不预占这些枚举成员；更不能用 `StaticSolid / DetachedSolid` 表示运行时 Representation。

以下数据不得塞进基础 Definition：

- PBF Mass/RestDensity/Viscosity/Cohesion：属于 `Game.ElementField` 的 `LiquidMaterialProfile`。
- Shader/Color/Foam/Texture：属于 `Game.Rendering` 的 Liquid Render Binding。
- Wet/Poisoned/Burning：属于 `Game.ElementField -> Game.Combat` 的 Status Projection Binding。
- Reaction 数值与 Process：继续属于 `Game.Combat` 的 Reaction Profile/Evaluator。
- VFX/Audio/Prefab：属于 Presentation/Content Asset，不属于 Material Identity。

## F. Simulation Capability 与 Semantic Capability

Phase H 完成后，“支持 Water + Poison”只表示：

- 同一个 Runtime、Pool、Hash、Collision 与 Dispatch 主链可以保存两种 Material ID；
- 同种 Material 粒子读取自己的参数并互为 PBF Neighbor；
- Gameplay Query、Status、Reaction Binding 与 Surface 可区分 Material；
- 不复制 `GpuPbfWaterRuntime` / `GpuPbfPoisonRuntime` 源码或实例。

它不表示：

- Water/Poison 互溶；
- 密度差浮力分层；
- 异质界面张力；
- 扩散、浓度场或真实化学反应；
- 不同 Smoothing Radius 共存。

首版采用“同 Material Neighbor Filter”。两种 Liquid 可以同时存在，但不同 Material 不参与彼此的 PBF Density/Cohesion Constraint；相互接触的视觉和力学真实性不是 Phase H Gate。只有后续玩法证明需要，才单独研究 Multi-phase PBF。

## G. 统一命名词典

| 术语 | 本计划中的唯一含义 |
|---|---|
| `Material` | 世界物质的稳定身份与固有语义 |
| `Material Behavior` | Material 的宏观语义类别；不描述当前存储形态或负责推进它的系统 |
| `Simulation Representation` | 某份 Material 当前实际存在的数据/对象形态，例如 Particle Slots、Cells、Scene Mesh、Rigidbody Actor |
| `Simulation Backend` | 拥有并推进 Representation 的实现系统，例如 GPU PBF、Element Cell Solver、未来 PhysX Bridge |
| `Element` | 既有 ElementWorld 模块名、元素法术主题或 Reaction Channel；新 Domain 类型不再用它代替 Material |
| `Status` | 角色/目标身上的派生 Gameplay 状态，不是世界 Material 本体 |
| `Reaction` | 两种 Material/Status Semantic 接触后的规则结果；Core 与 Carrier Adapter 分开 |
| `Liquid` | Material 的一种 Behavior Family |
| `Fluid` | 现有 GPU/PBF 工程基础设施命名；不扩大为所有 Material 总称 |
| `Water` | 具体 Material，或明确标注的 Legacy Compatibility Policy |
| `Particle` | PBF Backend 的存储/计算单元，不是 Material Identity |
| `Cell` | Grid Backend 的空间 Carrier，不是 Material Identity |
| `Simulation State` | Backend 权威持有的动态位置、速度、数量与生命周期 |
| `Gameplay Snapshot` | 从权威状态异步派生、允许延迟的只读查询数据 |
| `Rendering Representation` | Density、Triangle、Mesh、Shader、VFX 等视觉派生结果 |

## H. Material Amount / Gameplay Quantity Contract

`Material Amount` 在本项目中统一使用无量纲 **Gameplay Material Unit（GMU）**。它服务 Deposit、Occupancy、Status 强度输入和 Reaction 守恒，不试图伪装成真实世界的 kg 或 m³：

| 数量 | 单位/职责 | 明确不是什么 |
|---|---|---|
| `ElementWriteRequest.TotalAmount` | 一次写入请求的总 GMU，存储为 `ushort` | 不是 ParticleCount 或 kg |
| `ElementCell.Amount` / Occupancy byte | 单个 Global Cell 中可查询的 0–255 GMU | 不是 PBF Density |
| `AmountUnitsPerParticle` | 某种 Liquid 每个 Alive Particle 代表多少 GMU；是 Gameplay↔Particle Representation Conversion | 不是 `ParticleMass` |
| `ParticleMass` | PBF Density Kernel 的模拟权重 | 不参与 Gameplay Reaction Amount 换算 |
| `RestDensity` | PBF Constraint 的目标密度 | 不决定一个水弹写入多少 GMU |

首版转换规则固定如下：

```text
SpawnParticleCount = ceil(RequestedGameplayAmountGMU / AmountUnitsPerParticle[MaterialId])
ProjectedGameplayAmountGMU = AliveParticleCount × AmountUnitsPerParticle[MaterialId]
RequestedConsumeParticleCount = ceil(ReactionRequestedGMU / AmountUnitsPerParticle[MaterialId])
```

Occupancy Binning 按粒子中心所属 Global Cell 累加该 Material 的 `AmountUnitsPerParticle`，并在 byte 255 饱和。`AmountUnitsPerParticle` 从 Task 22 起是 Per-material CPU Snapshot 字段；它不进入 32-byte PBF GPU Parameter Buffer，也不能由 `ParticleMass/RestDensity` 推导。Water 继续保留当前 `8 GMU/particle` 作为迁移基线，Poison 必须显式配置自己的数值。

因为现有粒子 Metadata 没有保存“末粒子的部分 GMU”，`ceil` 得到的**计划最大粒子数**相对请求量会有严格小于一个粒子单位的向上 Quantization Error。它不等于“实际 GPU 已删除量”：当前 Consume Command 没有回传本 Tick 实际删除数，异步期间粒子还可能移出目标 Cell，所以少删量不能仅凭 `ceil` 证明有同样上界。因此 Task 14–25 的承诺只包括：同一 Snapshot 不重复预留、CPU/GPU 两条 Queue 原子 Admission、Reaction 计划量使用 GMU、计划粒子数的向上量化误差 `< AmountUnitsPerParticle[Material]`；实际删除结果要等后续 Snapshot 观察，不能描述成“无损”“逐 GMU 精确守恒”或“严格小于一粒子的端到端误差”。

Water–Fire 的 Semantic Result 仍请求双方等量 GMU，Carrier Adapter 再执行上述粒子量化；这保证 Semantic Planning 一致，不等于已经确认 GPU 实际删掉等量 Water。Poison–Fire 中 Fire 是 Catalyst，计划只消费 Poison GMU。若未来玩法要求逐 GMU 精确跨 Backend 守恒，必须单独增加 Partial-particle Amount 或 GPU Consume Result/Acknowledgement，不能复用 `ParticleMass` 补差，也不能在本阶段用同步 Readback 实现。

## I. 2026-08-21 第二轮架构审阅结论

| 审阅问题 | 结论 | 计划中的落点 |
|---|---|---|
| Behavior 混入 Static/Detached Representation | **同意修正**；Static/Detached 描述当前形态，不是 Stone 的固有 Behavior | Task 18 枚举缩为当前所需三值；四层边界表；未来 Transfer 独立立项 |
| 通用 Router 接收 `WaterSimulationMode` | **同意修正**；这是 Legacy Compatibility 输入，不是 Material Semantic | Task 19 新增 Compatibility Decision；Final Route/Hot Router 不再接收 Water Mode |
| Per-material 参数只进入 Solver | **同意修正**；错误 Spawn RestSpacing 会在第一个 Solver Tick 制造爆炸修正 | Task 22 把 Material row 贯穿 Amount→Count→Packing→64-byte Request→GPU Spawn→Solver/Density |
| Gameplay Amount 与 Physics Quantity 混用 | **同意修正**；现有 8 units/particle 已证明两者不是同一单位 | GMU Contract；Task 20 Per-material Scale；Task 21 GMU Reaction；量化误差边界 |
| Sticky 提前声明 Liquid | **同意移除**；Stable ID 不等于已确定 Behavior | Task 18 不创建 Sticky Definition/Asset；Task 19 无 Route，后续真实玩法再决定 |

# Phase F：Liquid Backend Quality——密度一致 Spawn、短程 Cohesion 与连续水团收口

Phase F 不是推翻 Task 0–13，而是修复 P7 真实 Water Cast 暴露出的两个缺口：生产 Water 目前把 200 个粒子随机撒进半径 `0.75m` 的球体，使出生平均密度只有 `RestDensity` 的约 `11.3%`；随机采样又允许局部近重合，PBF 会把这些局部高密度团猛烈推出。命中点同时位于 Collider 表面，完整 Spawn 球约有一半出生在 solid 内，Collision Projection 会从不同退出面把它们推出。结果是 Simulation 中形成彼此超过 `SmoothingRadius` 的孤立微团，Surface Reconstruction 只能忠实重建成满地碎片。

本 Phase 保留固定容量 GPU Pool、Free Stack、Spatial Hash、PBF Density/Lambda、Collision Manifold、Sleep/Wake、Gameplay Readback、Wet/Reaction、Density/Anisotropy/Marching Cubes 和 Indirect Draw。只增加兼容的碰撞面法线 Contract、按 `ParticleMass/RestDensity` 规划的确定性 Packed Spawn，以及位于 Velocity 阶段的短程 Cohesion Pass。`CohesionStrength=0` 与未设置 `DensityPacked` Flag 时必须保留旧行为，形成可观测的 A/B 与回滚边界。

**用户目标行为：** 水主要保持一个连续主体；沿障碍拉伸、下垂；强冲击时最多分裂成少量液滴；液滴重新进入 `SmoothingRadius` 后融合；不允许远距离“磁铁吸回”，也不允许靠极低 `IsoLevel` 跨空隙制造假连接。

**外部资产依赖：** 无。继续使用现有 `M_ElementWater_PBF.mat`、Water Shader、ComputeShader、Profile 和 P7 Scene，不需要用户提供 Texture、Normal Map、Noise、Mesh、VFX、Audio 或 Font。

## Task 14：Surface Normal Contract 与 Density-aware Spawn Planner

**Files:**

- Create: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileImpactContext.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSpawnPackingPlanner.cs`
- Create: `Assets/_Project/Tests/Combat/ProjectileImpactContextTests.cs`
- Create: `Assets/_Project/Tests/ElementField/FluidSpawnPackingPlannerTests.cs`
- Modify: `Assets/_Project/Scripts/Combat/Projectiles/ProjectileBase.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementDepositOnImpact.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Simulation/ElementWriteRequest.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidDepositQueueAdapter.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/IFluidGpuSource.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementDepositOnImpactTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldFluidRoutingTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-14-report.md`

**Interfaces:**

- Produces `readonly struct ProjectileImpactContext` in `Game.Combat` with exact read-only fields `Vector3 Point`, `Vector3 IncomingDirection`, `Vector3 SurfaceNormal` and a validating constructor that preserves values without normalizing them.
- Adds `ProjectileBase.SurfaceImpacted : event Action<ProjectileImpactContext>` while keeping the existing `Impacted(Vector3, Vector3)` event unchanged for `SpellCaster` and `ProjectileImpactVfx`.
- Extends `ElementWriteRequest` with `Vector3 SurfaceNormal`; all old constructors delegate with `Vector3.zero`, so Fire, Initial Deposit, Debug Injector and direct Registry writes remain source-compatible.
- Produces `readonly struct FluidSpawnPacking` with `Vector3 Center`, `float RestSpacing`, `float RequiredRadius`, `int LatticeSide`.
- Produces `FluidSpawnPackingPlanner.TryCreate(uint particleCount, float particleMass, float restDensity, float particleRadius, float requestedRadius, Vector3 hitPoint, Vector3 surfaceNormal, out FluidSpawnPacking packing) : bool`.
- Defines `FluidSpawnPackingPlanner.MaximumPackedParticleCount = 65536u`; larger single requests are rejected even if a future Pool is larger, so the D3D11-compatible uint permutation in Task 15 cannot overflow. The current Water Pool/request maximum 8192 remains well inside this bound.
- Adds `FluidSpawnFlags.DensityPacked = 1u << 1`; `UseLinearFalloff` remains unchanged for Legacy/direct low-level tests. GPU Buffer layout stays 48 bytes, so `FluidGpuLayout.LayoutVersion` does not change in this Task.

- [x] **Step 1: 写 Impact 与 Packing RED Tests**

  `ProjectileImpactContextTests` 锁定 Point/Incoming/Normal 的值快照；`ElementDepositOnImpactTests` 锁定三参数 overload、旧 overload 的 `-IncomingDirection` 回退以及两者都无效时拒绝。`FluidSpawnPackingPlannerTests` 必须覆盖：

```text
N=200, mass=1, rho0=1000
-> RestSpacing = 0.1m
-> LatticeSide = 6
-> RequiredRadius = 0.5 * (6-1) * 0.1 * sqrt(3) ≈ 0.4330127m

requestedRadius=0
-> 自动接受 RequiredRadius

requestedRadius=0.75
-> 接受且不把 RequiredRadius 扩成 0.75

0 < requestedRadius < RequiredRadius
-> false，不静默压缩

Center = hitPoint + normal * (RequiredRadius + particleRadius + 0.001m skin)
```

  还要逐项拒绝 `NaN/Infinity`、零法线、零 count、count 大于 65536、非正 mass/density、负 radius，并证明输出失败时为 default。

- [x] **Step 2: 运行真实 RED**

```powershell
dotnet build Game.Combat.Tests.csproj --no-restore -t:Rebuild
dotnet build Game.ElementField.Tests.csproj --no-restore -t:Rebuild
```

  若 Unity 尚未刷新 `.csproj`，使用只作用于本 Task 新源的 `task-14-compile.targets` 强制 Include；报告必须区分“C# Compile RED”和“Unity NUnit RED”，不得把旧 `.csproj` 的成功当成新测试通过。

- [x] **Step 3: 增加兼容的 Surface Impact 通道**

  `ProjectileBase.OnCollisionEnter` 从同一个 `ContactPoint` 读取 `point` 与 `normal`，在 `OnImpact` 之后、Destroy 之前依次发布 `SurfaceImpacted(context)` 与旧 `Impacted(point, incoming)`。`ElementDepositOnImpact` 只把订阅从旧事件迁移到新事件；`SpellCaster`、`ProjectileImpactVfx` 继续订阅旧事件，避免 Payload/VFX 重复触发。旧 `TryBuildImpactRequest(position, direction, out request)` 继续可用，并把 `-direction.normalized` 作为兼容 Surface Normal。

- [x] **Step 4: 实现 Pure Packing Planner 与 Adapter**

  Planner 使用：

```text
RestSpacing = cbrt(ParticleMass / RestDensity)
LatticeSide = ceil(cbrt(ParticleCount))
RequiredRadius = 0.5 * (LatticeSide - 1) * RestSpacing * sqrt(3)
Center = HitPoint + NormalizedSurfaceNormal
    * (RequiredRadius + ParticleRadius + 0.001)
```

  `FluidDepositQueueAdapter` 构造函数新增 `particleMass/restDensity/particleRadius` snapshot；Water 请求先 Ceiling 得到 count，再调用 Planner。Planner 拒绝、Queue 满或 capacity 越界都返回 false，不能 fallback 到 Legacy Cell，失败也不能推进 Seed。成功请求的 `WorldPosition=packing.Center`、`Radius=packing.RequiredRadius`、Flag 追加 `DensityPacked`；`UseLinearFalloff` 可以继续快照，但 Packed GPU 路径不再用它改变粒子质量密度。

- [x] **Step 5: 运行 GREEN 与兼容回归**

  运行 Combat/ElementField Production+Tests fresh Rebuild；在 Unity Test Runner 运行 `ProjectileImpactContextTests`、`ElementDepositOnImpactTests`、`FluidSpawnPackingPlannerTests`、`ElementWorldFluidRoutingTests`。确认旧 Spell Payload 和 VFX 测试没有重复 callback，Fire/Legacy Request 的 Surface Normal 仍为 zero。

- [x] **Step 6: 更新解析文档**

  新增 Task 14 几百字概述，从“质量/密度为什么决定体积”开始解释 `cuberoot`、Lattice Side、包围球半径、碰撞面法线和兼容事件；列出每个创建/修改脚本、Assembly 位置、Runtime Call Chain、RED/GREEN 证据与未取得的 Unity GPU Gate。

**Task 14 建议提交名：** `feat(element-field): plan density-aware fluid spawns`

## Task 15：确定性 GPU Packed Lattice Spawn

**Files:**

- Modify: `Assets/_Project/Art/Elemental/Compute/PbfParticleLifecycle.compute`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Tests/ElementField/FluidGpuLayoutContractTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/GpuFluidParticleLifecycleTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-15-report.md`

**Interfaces:**

- Consumes unchanged 48-byte `FluidGpuSpawnRequest` and `FluidSpawnFlags.DensityPacked` from Task 14.
- Adds `_SpawnRestSpacing : float` uniform to `PbfParticleLifecycle.compute`; Runtime sets it once per Spawn upload from `cbrt(settings.ParticleMass/settings.RestDensity)`.
- Produces HLSL `PackedLatticeOffset(FluidGpuSpawnRequest request, uint localIndex) : float3`; old `SpawnOffset` remains the fallback when `DensityPacked` is absent.

- [x] **Step 1: 先扩展 Layout/GPU RED Tests**

  Contract Test 明确 `SpawnRequestStride==48`、LayoutVersion 不变、Packed Flag 与旧 Falloff Flag 不冲突。GPU Fixture 上传 N=200 的 Packed Request，并在 Test-only Readback 后断言：Active Count 精确 200、所有 Position finite、AABB 在 Center±RequiredRadius 内、任意两粒子距离至少 `RestSpacing-1e-5`、相同 Seed 两轮 byte-for-byte 相同、NumericalError=0。再上传未带 Packed Flag 的旧 Request，证明旧随机路径仍可回滚。

- [x] **Step 2: 记录 GPU RED 边界**

  优先用 Unity Test Runner 执行 `GpuFluidParticleLifecycleTests`；若 Editor 被另一实例占用，只能记录 fixture test-first 与 C# Compile，不得称 GPU RED。Compute Import、D3D11 和 D3D12 分别记录，不能相互替代。

- [x] **Step 3: 实现无重叠的确定性 Offset**

  对 `side=ceil(cbrt(count))`，定义 `totalSlots=side^3`、`step=side^2+side+1`。因为 `gcd(step, side)=1`，`step` 与 `side^3` 互质，故：

```text
slot = (localIndex * step + seed % totalSlots) % totalSlots
x = slot % side
y = (slot / side) % side
z = slot / (side*side)
offset = (float3(x,y,z) - 0.5*(side-1)) * SpawnRestSpacing
```

  在 `[0,count)` 内不会重复 slot；最小轴向距离为 RestSpacing。Task 14 把单 request 限制为 65536，此时 `side<=41`、`step<=1723`、`localIndex*step+seed%totalSlots<113,000,000`，明确低于 `uint.MaxValue`，所以 D3D11 不需要 64-bit integer。`count<=ParticleCapacity` 仍由 CPU/GPU 双边验证。Packed 分支不调用 Random、`pow(radiusSample,1/3)` 或 Linear Falloff；旧分支原样保留。

- [x] **Step 4: Runtime 绑定与 Spawn 后版本语义**

  Runtime 在非空 Spawn Dispatch 前设置 `_SpawnRestSpacing`，继续在 Dispatch 提交后才 `PublishAfterSpawnDispatch`。不得在 Queue Enqueue 时提前发布 TopologyVersion，不得新增 Buffer、同步 Readback或逐帧分配。

- [x] **Step 5: GREEN、Import 与密度 Oracle**

  fresh Rebuild ElementField/Tests；Unity GPU Fixture 通过后，用现有 Poly6 CPU Oracle 计算出生帧平均 density，要求 Packed 结果显著高于旧 `0.75m` Random Spawn，且所有值 finite。该断言只比较相同 N/mass/h 的 A/B，不把边界粒子强行要求为 100% RestDensity。

- [x] **Step 6: 更新解析文档**

  解释为什么“随机分布均匀”不等于“适合 PBF 初始条件”、仿射模排列为何保证不重复、为何不改 48-byte GPU Layout，以及旧 Random Branch 如何提供回滚证据。

**Task 15 建议提交名：** `feat(element-field): add packed gpu fluid spawning`

## Task 16：短程 Cohesion Velocity Pass 与有界断裂/融合

**Files:**

- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationProfile.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationSettings.cs`
- Modify: `Assets/_Project/Art/Elemental/Compute/PbfSolver.compute`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Tests/ElementField/LiquidSimulationProfileTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/PbfKernelMathTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/GpuPbfSolverTests.cs`
- Modify through Unity serialization: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidSimulationProfile_Water.asset`
- Modify: `Assets/_Project/Editor/PbfFluidTask13Setup.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-16-report.md`

**Interfaces:**

- Adds Settings/Profile fields `CohesionStrength` (`m/s²`, default `12f`), `CohesionRestDistanceRatio` (default `1f`) and `MaximumCohesionDeltaSpeed` (`m/s` per Substep, default `0.2f`).
- Produces pure `PbfKernelMath.CohesionWeight(float distance, float restDistance, float smoothingRadius) : float`.
- Adds HLSL kernels `ComputeCohesionDeltaVelocities` and reuses existing `ApplyDeltaVelocities`; no new GraphicsBuffer is allocated.
- Runtime order becomes `UpdateVelocities -> optional Cohesion Compute/Apply -> XSPH Compute/Apply -> Vorticity Compute/Apply -> Collision Velocity Gate -> Clamp -> Commit`.

- [x] **Step 1: 写 Settings/Curve/GPU RED Tests**

  Pure Tests 锁定 Cohesion 曲线：`r<=restDistance` 为 0；区间内 `q=(r-rest)/(h-rest)`、`4q(1-q)`；中点为 1；`r>=h` 为 0；非法输入安全返回 0。Settings 拒绝负数/NaN/Infinity、`restSpacing*ratio>=smoothingRadius` 和非正 MaxDeltaSpeed；`strength=0` 合法并关闭 Dispatch。

  GPU Tests 至少覆盖：静止规则 Lattice 的内部粒子方向和近似抵消；自由表面粒子朝邻域质心；两个间距位于 `(rest,h)` 的小团距离下降；间距 `>=h` 完全不相吸；巨大 strength 仍被 MaximumDeltaSpeed 限制；NumericalError=0。

- [x] **Step 2: 运行 RED 并区分证据**

  先运行 ElementField/Tests forced Rebuild，预期缺少新 Settings/Kernel Contract；再运行 Unity GPU fixture。Compute 尚未 Import 时记录 Import RED，不以 C# GREEN 替代 GPU 行为。

- [x] **Step 3: 实现 Smooth Compact-support Cohesion**

  每个 RequiresSimulation 粒子遍历现有 27-cell Hash Neighbor。只对 `restDistance<r<h` 累加：

```text
q = (r-restDistance)/(h-restDistance)
w = 4*q*(1-q)
weightedDirection += normalize(xj-xi) * w
weightSum += w
acceleration = CohesionStrength * weightedDirection / max(weightSum, epsilon)
deltaV = clampMagnitude(acceleration * substepDeltaTime,
                        MaximumCohesionDeltaSpeed)
```

  规则邻域内部方向相消，Free Surface 向水体内部收缩；超过 h 没有远程吸引；强冲击把邻域拉断后自然分裂。任何非有限 neighbor/density/delta 都清零当前私有 Delta 并增加 NumericalError，不能写 NaN。`CohesionStrength<=0` 时 Runtime 不提交两个 Cohesion Dispatch，从调度层精确恢复旧路径。

- [x] **Step 4: 保持 Collision 与 Sleeping 顺序**

  Cohesion/XSPH/Vorticity 全部完成后才执行 `ApplyCollisionVelocities`，保证最后一次 Contact Half-space Gate 不会被吸引力重新破坏。Cohesion 只 Dispatch RequiresSimulation 粒子；Sleeping 仍作为 Spatial Neighbor 参与读取，但不会成为写入 target。CPU 不新增 Awake Readback，继续使用 Solver Indirect Args。

- [x] **Step 5: 写入 Water Profile 的首轮可玩值**

```text
CohesionStrength = 12
CohesionRestDistanceRatio = 1
MaximumCohesionDeltaSpeed = 0.2
Viscosity = 0.08
Vorticity = 0.002
MaxSpeed = 10
SolverIterations = 4（保持）
Substeps = 2（保持）
```

  这些值通过 `LiquidSimulationProfile` 数据驱动，并由 `PbfFluidTask13Setup` 幂等写入验证 Profile；不把数值硬编码进 Compute。若 Settings 的 Collision Sweep 上界拒绝 `MaxSpeed=10`，以既有公式计算的更小合法值为准并把精确结果写入报告，不能放宽 CCD 安全约束。

- [ ] **Step 6: GREEN 与 120 Tick Solver Gate**

  当前状态：局部 Cohesion GPU Fixture 与跨 Backend 回归已通过；独立 200 粒子 × 120 Fixed Tick 长程 Fixture 尚未取得，本 Step 保持未勾选。

  在 Unity GPU Test 中让 200 Packed 粒子运行至少 120 Fixed Ticks，断言 Active Count 不变、NumericalError=0、AABB 不超过初始 RequiredRadius 加重力/碰撞允许的解析上界、平均 Density Error 相对首 Tick 下降、末 30 Tick 平均速度低于前 30 Tick。再运行 `CohesionStrength=0` A/B，确认旧 Solver 数值路径没有被无条件修改。

- [x] **Step 7: 更新解析文档**

  从“PBF 不可压缩约束为何不等于表面张力”讲起，解释 Compact Support、Rest Distance、Smooth Weight、Free-surface Direction Bias、断裂阈值、与 XSPH/Vorticity/Collision 的顺序及单位；记录失败参数、被否决的全局中心吸引和低 Iso 假连接。

**Task 16 建议提交名：** `feat(element-field): add bounded fluid cohesion`

## Task 17：Connected Surface Gate、P7 视觉校准与 Phase F 收口

**Files:**

- Create: `Assets/_Project/Tests/Rendering/GpuCohesiveFluidSurfaceTests.cs`
- Modify through Unity serialization: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidRenderProfile_Water.asset`
- Modify: `Assets/_Project/Editor/PbfFluidTask13Setup.cs`
- Modify: `Assets/_Project/Editor/PbfFluidTask13GateRunner.cs`
- Modify only if new serialized fields require rebinding: `Assets/_Project/Scenes/PBF_FluidSandbox.unity`
- Modify only if validation profile reference changes: `Assets/_Project/Scenes/P7_DemoRun.unity`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-17-report.md`

**Interfaces:**

- Consumes Task 15 Packed Spawn、Task 16 Cohesion、现有 Density/Anisotropy/Marching Cubes。
- Produces Test-only 6-connected voxel component analysis；它只处理 AsyncGPUReadback 已完成后的测试数组，不进入 Runtime Player。
- Produces P7/Sandbox 可重复 Gate 和 Phase F 证据，不改变 Gameplay Occupancy 的 Water Truth。

- [ ] **Step 1: 先写 Connected Surface RED Fixture**

  当前状态：已完成 Test-only 6-connected 算法、实际 200 Packed 粒子 GPU Density Component，以及独立 Cohesion/Anisotropy/MC Fixtures；尚未在单一 Fixture 中完整串联 Solver→Anisotropy→Marching Cubes，本 Step 保持未勾选。

  以现有 Water Profile 生成 200 Packed 粒子并运行 Solver，然后 Dispatch Density/Anisotropy/Marching Cubes。Test-only Readback 对 `density>=IsoLevel` 的 voxel 做 6-neighbor flood fill，忽略少于 8 voxel 的噪声 component，并断言：普通单团只有 1 个主要 Component；人为构造两个距离 `>=h` 的水团保持 2 个；将两团放到 `<h` 后 Cohesion 运行可合并为 1 个；Triangle/Anisotropy/Numerical Overflow 为 0，所有 Vertex/Normal finite。

- [x] **Step 2: Rendering Profile 使用物理连续后的起始值**

```text
TargetVoxelSize = 0.10m
MaximumResolutionPerAxis = 96（保持）
IsoLevel = 500（保持为 0.5 * RestDensity）
UseAnisotropy = true
AnisotropyUpdateIntervalFrames = 2（TopologyVersion 变化仍强制刷新）
NeighborThreshold = 5（保持）
MinimumScale = 0.65 / MaximumScale = 1.8 / Ratio = 2.5（保持）
```

  不降低 IsoLevel 来桥接真实空隙。若 Grid 因 `0.10m` 超过 96³，Planner 仍按既有规则放大实际 voxel；报告记录 Interest Bounds、最终 Resolution 与实际 Voxel Size。

- [x] **Step 3: Unity Test Runner 与 Backend Matrix**

  D3D12 运行 Phase F focused fixtures，再切 D3D11 重新 Import Lifecycle/Solver/Density/Anisotropy/MC/Procedural Shader 并运行同组 tests。分别记录 Passed/Failed/Skipped；D3D12 成功不能代替 D3D11。完整项目既有失败继续单列，不得用 focused 结果宣称全量回归全绿。

- [ ] **Step 4: P7 真实 Water Cast Gate**

  当前状态：Setup 已验证 P7 69 个 Collider 引用；真实 Wizard Water Cast 人工矩阵未执行。

  从 Main Menu 进入 P7，以真实 Wizard Water Wand 分别命中地面、墙、斜面、台阶和敌人附近。验收：`0.25s` 内出现一个主要连续水团；普通落地不产生满地单粒子碎片；翻越障碍时主体拉伸/下垂；有界强冲击最多形成少量液滴；液滴重新进入 h 后融合；墙背面无出生水；快速连发不触发 Queue/Particle/Triangle/Numerical Overflow。

- [ ] **Step 5: Frame Debugger、Profiler 与 GC Gate**

  当前状态：60 秒 DX12 4K Sandbox Overflow 全为 0，但全局 Editor MaxGC=39,910B、Sleeping=0；Frame Debugger 与正式 GPU Profiler 尚未取证，本 Step 保持未勾选。

  Frame Debugger 确认只有 PBF Procedural Surface Draw、无 Legacy Water Draw、Triangle Winding/Cull/Depth/Transparent Queue 正确。Profiler 分开记录 Spawn、Spatial Hash、PBF、Cohesion、Density、Anisotropy、MC 的 CPU Submission/GPU 时间；稳定帧 `GC Alloc=0 B`。全体 Sleeping 时 Solver Indirect Args 为 0，Cohesion 不产生工作组。

- [ ] **Step 6: Windows Development Build Smoke**

  当前状态：Windows x64 Development Build 已成功；当前 Build 的 Main Menu→P7→Water Cast Player smoke 尚未执行，本 Step 保持未勾选。

  Windows x64 Development Build 分别记录 DX12 与允许时的 DX11；实际 Main Menu→P7→Water Cast。检查 Profile/Compute/Material 打包、Surface Normal Contract、Packed Spawn、Cohesion、Wet/Fire Reaction 与 Legacy Fallback。截图只能作为视觉时刻证据，Draw 归属仍以 Frame Debugger 为准。

- [x] **Step 7: 更新解析文档与状态**

  新增 Task 17 几百字概述、完整调用链、Connected Component 基础、所有脚本表、Profile 参数单位、Debug 全过程、性能证据和面试回答。只有 Phase E+F 所有必要 Gate 都取得时才把总文档改为“已完成”；否则保持“更新中”并逐项列出未取得证据。

- [ ] **Step 8: 最终工作区审计**

  当前状态：Task 14–17 scoped `git diff --check`、Runtime API 与热路径静态审计通过；全工作区 `git diff --check` 仍报告用户/历史 Dirty `P7_DemoRun.unity` 中 Unity 序列化的空 `m_Name` 尾随空格。为避免清理或重写用户场景，本 Step 保持未勾选。

  `git diff --check`；确认未清理用户/Task13 Dirty；没有手写 `.meta`；Production 无 `Debug.Log`、LINQ、Runtime `GetData/WaitForCompletion`；Update/FixedUpdate/LateUpdate 无新增 `new`、closure、boxing；GPU UAV 数仍满足 D3D11 每 Kernel 上限；48-byte Spawn Layout 和旧 Flag 回滚测试通过。

**Task 17 建议提交名：** `fix(rendering): keep pbf water surfaces cohesive`

### 2026-08-22 增量修复：有界 Tensile Constraint 恢复静水凸起

**问题与目标：** 2026-08-21 的单向 `Incompressibility Constraint` 已消除自由表面因负压力产生的 6–10m/s 往返振荡，但 Play Mode 回归表明水滩也因此过度平铺。`Cohesion` 是速度级的短程轮廓收拢：当粒子已经落在近似同一高度时，它可以让边缘连续，却不能单独提供将中心粒子顶向上层所需的 `Capillary Pressure`。本增量不回退单向不可压缩、不修改 Rendering/IsoLevel，也不推翻 Task 14–17；只在现有每次 PBF Iteration 中增加一个独立、可关闭、位置修正量有硬上限的欠密度 `Tensile Constraint`。

**外部资产依赖：** 无。不需要 Texture、Normal Map、VFX、Model、Audio 或第三方 Package；只修改 Profile、Runtime 参数绑定、Compute Kernel、测试与 Living Explainer。

**Files：**

- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationProfile.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationSettings.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/PbfKernelMath.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Art/Elemental/Compute/PbfSolver.compute`
- Modify through deterministic Editor authoring: `Assets/_Project/Editor/PbfFluidTask13Setup.cs`
- Modify serialized Water value: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidSimulationProfile_Water.asset`
- Modify: `Assets/_Project/Tests/ElementField/LiquidSimulationProfileTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/PbfKernelMathTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/GpuPbfSolverTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

- [x] **Step 1: RED 锁定参数、欠密度吸引与硬上限**

  Profile Test 先要求 `TensileStrength` 与 `MaximumTensilePositionCorrection` 被 Snapshot；GPU Test 以两个欠密度粒子证明距离必须缩短、单粒子每 Iteration 位移不得超过 `0.0001m`，并以 `density>=restDensity` 证明 Tensile 必须严格为零。旧 Compute 没有该 Kernel/字段，因此测试必须先 RED。

- [x] **Step 2: 实现独立 Tensile 数学，不改 Compression Lambda**

  保持 `C=max(density/restDensity-1,0)` 和原 `DeltaPosition` 完全不变。对每对邻居计算 `deficit=saturate(1-density/restDensity)`，以两端 deficit 平均值乘 `TensileStrength` 和 `SpikyGradient/restDensity` 得到对称吸引；汇总后的 Tensile 向量独立 `clampMagnitude` 到 `MaximumTensilePositionCorrection`。`strength=0` 时不提交 Apply Dispatch，精确回到当前稳定路径。

- [x] **Step 3: 接入 GPU Iteration 与 Water Profile**

  `ComputeDeltaPosition` 同时把普通 Density Correction 与 Tensile Correction 写入两个既有临时 Buffer（Tensile 复用尚未进入速度阶段的 `DeltaVelocities` 存储，避免新增常驻 GPU Buffer）；先 Apply 不可压缩修正，再由单独 Kernel Apply 已有界的 Tensile 修正。首轮 Water 值为 `TensileStrength=0.00005`、`MaximumTensilePositionCorrection=0.0001m/Iteration`；理论最坏位置注入速度为 `0.0001 * 4 Iterations * 2 Substeps * 60Hz = 0.048m/s`，远低于旧版 `0.08m/Iteration` 导致的约 `9.6m/s`。

- [ ] **Step 4: GREEN、静水与强冲击回归**

  运行 `Game.ElementField.Tests + Game.Rendering.Tests`；锁定欠密度吸引 finite、单次位移硬上限、压缩 Lambda/修正不变、NumericalError=0、Sleeping 粒子不被 Solver 写入。然后在 P7/Sandbox 检查静水中心形成至少一个粒子层级的凸起、边缘不恢复持续抖动；强冲击仍可断开 Compact Support，并在重新进入邻域后融合。若无法自动取得玩家视角动态证据，必须把人工 Play Mode 项明确列为待用户复核，不能用 EditMode 代替。

  当前状态：Unity DX12 focused gate 已通过 `422 passed / 0 failed`，包括真实 GPU Tensile Fixture；玩家视角的动态凸起、静水边缘与强冲击观感仍需在重新进入 P7 Play Mode 后由用户复核，因此本项保持未勾选。

- [x] **Step 5: 更新 Living Explainer 与审计**

  追加 `Cohesion`、`Incompressibility`、`Tensile/Capillary Pressure` 三者职责区别，记录 RED/GREEN、单位、上限推导、Runtime 调用链、修改文件、Rejected Alternatives 与剩余人工验证边界；运行 scoped `git diff --check`，不清理用户其他 Dirty。

### Phase F 最终 Gate

必须同时有 Pure RED/GREEN、Packed Spawn GPU Fixture、Cohesion Solver Fixture、Connected Surface Fixture、D3D11/D3D12 Import/Test、P7 真实 Water Cast、Frame Debugger、Profiler/GC 和 Windows Player 证据，才允许描述为“达到参考图方向的连续聚合流体”。若只完成代码编译或参数写入，只能描述对应证据层，不得宣称视觉目标已完成。

**执行方式：** 2026-08-21 用户已明确授权本轮连续执行 Task 14→17，未授权 SubAgent；Task 18–25 仍未授权。Task 14–17 已按 Task 分别更新 Living Explainer 与 report，无法自动取得的 Unity Editor/Player Gate 保持未勾选并明确交付。

---

# Phase G：Material World Foundation——最小语义层，不实现新 Solver

Phase G 只建立 Water→多 Liquid→未来 Material World 必需的最小 Contract。它不新增 Poison 可玩内容，不修改 PBF 数学，不实现 Sand/Gas/Static-Rigidbody Transfer，也不提供通用化学 Rule Graph。完成后 Water 的 Player-visible 行为必须与 Phase F 相同；如果只改架构却改变 Water 数值、Surface 或 Wet/Reaction，Phase G 不能通过。

**外部资产依赖：** 无。Material Definition/Catalog/Routing Profile 均由 Unity 创建普通 `.asset`；显示名暂用中英文纯文本，不需要 Icon、Texture、VFX、Audio、Model 或 Font。若以后需要 Material 图标，由独立 Content/UI Phase 提供，不能提前把 Sprite 字段塞进 Foundation Definition。

### Task 18–25 依赖顺序审计

顺序保持不变，但前置 Contract 现在显式写清，实施时禁止跨 Task 临时发明接口：

| Task | 必须消费的前置产物 | 本 Task 交给后续的稳定产物 |
|---|---|---|
| 18 Canonical Semantic | 既有稳定 byte ID | `MaterialId`、最小 Behavior、Definition/Catalog Snapshot；Sticky 仅保留 ID |
| 19 Backend Routing | Task 18 Catalog | Water Compatibility Decision、无 Water Mode 参数的 Final Route Snapshot |
| 20 Material Query | Task 19 Route | Composite Occupancy Key、Per-material GMU Scale Snapshot、Material-aware Query |
| 21 Reaction Boundary | Task 18 Semantic + Task 20 GMU 定义 | GMU `MaterialContactSnapshot/MaterialReactionResult`；不依赖 Poison/PBF 实现 |
| 22 Liquid Parameters + Spawn | Task 14/15 Packed Spawn + Task 18/20 Contracts | Water/Poison Settings Table、64-byte per-request RestSpacing、Same-material Solver |
| 23 Poison Gameplay | Task 19 Route + Task 20 Query + Task 22 Settings | Poison Ingress、Occupancy 与 Poisoned Projection |
| 24 Poison Rendering | Task 22 GPU Parameter Row + Task 23 可生成 Poison | Material-filtered Density/Surface；仍只读同一 GPU Source |
| 25 Poison–Fire | Task 21 Semantic + Task 23 Query + Task 22 Amount Scale | Toxic Process、Carrier Adapter 与双 Liquid Final Gate |

Task 20 先定义 `LiquidMaterialAmountScaleSnapshot`，Production 当时只填 Water；这是第二种 Liquid Query 必需的 Contract，不是提前启用 Poison。Task 22 再由 `LiquidMaterialSettingsTable` 生成 Water/Poison Scale 并修改 Spawn/Solver。因而不需要把 Task 22 提前，也不会让 Task 20/21 依赖尚不存在的 Poison Gameplay。

## Task 18：Canonical MaterialId、Definition 与 Catalog Snapshot

**Goal：** 建立跨 `Combat / ElementField / Rendering` 共用的唯一 Material Identity，同时保留当前整数协议和所有已序列化 Water/Fire 数据。

**Non-goal：** 不修改 Simulation Router，不让 Poison 进入 PBF，不引入 Density/Viscosity/Shader/Status 字段。

**Files：**

- Create: `Assets/_Project/Scripts/Materials/Game.Materials.asmdef`
- Move with existing `.meta`: `Assets/_Project/Scripts/ElementField/Data/ElementMaterialKind.cs` -> `Assets/_Project/Scripts/Materials/MaterialId.cs`
- Create: `Assets/_Project/Scripts/Materials/MaterialBehaviorKind.cs`
- Create: `Assets/_Project/Scripts/Materials/MaterialDefinition.cs`
- Create: `Assets/_Project/Scripts/Materials/MaterialDefinitionSnapshot.cs`
- Create: `Assets/_Project/Scripts/Materials/MaterialCatalog.cs`
- Create: `Assets/_Project/Scripts/Materials/MaterialCatalogSnapshot.cs`
- Modify: `Assets/_Project/Scripts/Combat/Game.Combat.asmdef`
- Modify: `Assets/_Project/Scripts/ElementField/Game.ElementField.asmdef`
- Modify: `Assets/_Project/Scripts/Rendering/Game.Rendering.asmdef`
- Modify: `Assets/_Project/Tests/Combat/Game.Combat.Tests.asmdef`
- Modify: `Assets/_Project/Tests/ElementField/Game.ElementField.Tests.asmdef`
- Modify: `Assets/_Project/Tests/Rendering/Game.Rendering.Tests.asmdef`
- Create: `Assets/_Project/Tests/Materials/Game.Materials.Tests.asmdef`
- Create: `Assets/_Project/Tests/Materials/MaterialIdentityContractTests.cs`
- Create: `Assets/_Project/Tests/Materials/MaterialCatalogTests.cs`
- Modify mechanically: every `.cs` reference returned by an exact `ElementMaterialKind` source scan; after GREEN the scan count must be zero outside migration notes/history docs
- Create through Unity: `Assets/_Project/ScriptableObjects/Materials/Material_Water.asset`
- Create through Unity: `Assets/_Project/ScriptableObjects/Materials/Material_Fire.asset`
- Create through Unity: `Assets/_Project/ScriptableObjects/Materials/Material_Poison.asset`
- Create through Unity: `Assets/_Project/ScriptableObjects/Materials/MaterialCatalog_Default.asset`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-18-report.md`

**Interfaces：**

```csharp
namespace Game.Materials
{
    public enum MaterialId : byte
    {
        Empty = 0,
        Water = 1,
        Fire = 2,
        Poison = 3,
        Sticky = 4,
    }

    public enum MaterialBehaviorKind : byte
    {
        None = 0,
        Liquid = 1,
        ReactiveField = 2,
    }

    public readonly struct MaterialDefinitionSnapshot
    {
        public readonly MaterialId Id;
        public readonly MaterialBehaviorKind Behavior;
    }

    public sealed class MaterialCatalogSnapshot
    {
        public bool TryGet(MaterialId id, out MaterialDefinitionSnapshot definition);
    }
}
```

`MaterialDefinition` 首版只有 `Id` 与 `Behavior`。Water/Poison=`Liquid`，Fire=`ReactiveField`。`MaterialId.Sticky=4` 只保留数值协议：不创建 `Material_Sticky.asset`，Catalog 对它返回 false，直到真实 Goo/Sticky 玩法决定它应是高黏度 PBF、Cell Goo 还是 Hybrid Representation。`MaterialCatalog.CreateSnapshot()` 在初始化边界建立 256-slot 直接索引表，拒绝 `Empty` Definition、重复 ID、null、未定义 Behavior；数组只在初始化创建，热路径查询零分配。

- [x] **Step 1: 写稳定 ID、Catalog 唯一性与 Assembly RED**

  Tests 精确锁定五个整数值；`MaterialDefinition` 不存在 Solver/Renderer/Status/Representation 字段；Catalog 对 Water/Fire/Poison 返回正确 Behavior，对 Empty/Sticky/未知 ID 返回 false；重复 ID、Empty Definition、None Behavior 抛明确异常。另锁 `MaterialBehaviorKind` 只有当前需要的三个值，并锁 `Game.Materials.asmdef` 不引用 Combat/ElementField/Rendering。

- [x] **Step 2: 运行 RED**

  先用独立 compile target 强制包含 Tests，预期因 `Game.Materials`/`MaterialId` 缺失得到指定 `CS0246/CS2001`。不能把旧 Unity `.csproj` 未刷新或普通 build 成功当作 RED/GREEN。

- [x] **Step 3: 移动 Canonical ID 并一次性迁移类型**

  使用 `git mv` 同时移动 `.cs` 与 `.meta`，修改 namespace/type；保留所有 byte 数值。`ElementCell.MaterialKind`、`ElementWriteRequest.MaterialKind`、序列化 backing field 名称暂时保持原名，只替换字段类型，避免无收益 YAML Field Rename。禁止创建并长期保留 `ElementMaterialKind` compatibility enum；编译器负责暴露遗漏引用。

- [x] **Step 4: 实现最小 Definition/Catalog Snapshot**

  `MaterialDefinition` 只复制两个值到 Snapshot；Catalog 在 `CreateSnapshot()` 校验并填固定索引表。不得使用运行期 `Resources.Load`、`FindObjectsByType`、Dictionary/LINQ 热查询或按帧读取 ScriptableObject。

- [x] **Step 5: GREEN、序列化与全局扫描**

  串行 Rebuild `Game.Materials`、`Game.Materials.Tests`、Combat/ElementField/Rendering 及其 Tests；运行 Material focused EditMode。用文本扫描确认旧 Scene/Profile 中 `_materialKind` 数值没有被清空、`ElementMaterialKind` 生产引用为零、enum 整数未变化。Unity Import/Asset round-trip 与 C# build 分开记录。

- [x] **Step 6: 更新 Living Explainer**

  从“Identity 不是 Behavior，Behavior 也不是 Representation/Backend”讲起，解释 enum 数值协议、asmdef 单向依赖、ScriptableObject Authoring 与 Runtime Snapshot、为何不把参数或 Representation 塞入 Definition、为何 Sticky 只有 Stable ID，以及一次性迁移如何避免双 ID。

**Rollback：** 恢复本 Task 的单一 commit/diff 即回到 `ElementMaterialKind`；不能只恢复一半 asmdef 或保留两个 enum。旧 Asset 字段名和 byte 值不变，因此回滚不需要批量重写 Scene/Prefab YAML。

**学习价值：** Domain Model、Stable ID、Assembly Boundary、序列化兼容、Authoring Data 与 Runtime Snapshot。

**Task 18 建议提交名：** `refactor(materials): establish canonical material identity`

## Task 19：Material-aware Simulation Routing 与 Single Writer 回归

**Goal：** 让 `ElementWorldRuntime` 根据初始化期 Route Snapshot 选择 Backend，不再把“是否为 Water”当成通用路由规则。

**Non-goal：** Poison 仍保持 Unsupported；不创建第二个 Runtime，不修改 Solver/Renderer。

**Files：**

- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialSimulationBackendKind.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialSimulationRoute.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/IMaterialSimulationRouteReadOnly.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialSimulationRoutingProfile.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialSimulationRouteSnapshot.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/WaterSimulationCompatibilityPolicy.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Simulation/ElementWorldWriteRouter.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Data/ElementWorldProfile.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidDepositQueueAdapter.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldFluidRoutingTests.cs`
- Create: `Assets/_Project/Tests/ElementField/MaterialSimulationRoutingProfileTests.cs`
- Create: `Assets/_Project/Tests/ElementField/WaterSimulationCompatibilityPolicyTests.cs`
- Modify: `Assets/_Project/Editor/PbfFluidTask13Setup.cs`
- Create through Unity: `Assets/_Project/ScriptableObjects/Materials/MaterialSimulationRouting_Default.asset`
- Modify only through Unity serialization when binding profile: `Assets/_Project/ScriptableObjects/ElementField/ElementWorldProfile_P7_PBF.asset`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-19-report.md`

**Interfaces：**

```csharp
public enum MaterialSimulationBackendKind : byte
{
    Unsupported = 0,
    ElementCell = 1,
    GpuPbfLiquid = 2,
}

public readonly struct MaterialSimulationRoute
{
    public readonly MaterialId Material;
    public readonly MaterialSimulationBackendKind Backend;
}

public interface IMaterialSimulationRouteReadOnly
{
    bool TryResolve(MaterialId material, out MaterialSimulationBackendKind backend);
}

public sealed class MaterialSimulationRouteSnapshot : IMaterialSimulationRouteReadOnly
{
    public static MaterialSimulationRouteSnapshot Create(
        MaterialCatalogSnapshot catalog,
        MaterialSimulationRoutingProfile profile,
        in WaterSimulationCompatibilityDecision waterDecision);

    public bool TryResolve(MaterialId material, out MaterialSimulationBackendKind backend);
}

public readonly struct WaterSimulationCompatibilityDecision
{
    public readonly WaterSimulationMode RequestedMode;
    public readonly WaterSimulationMode EffectiveMode;
    public readonly MaterialSimulationBackendKind WaterBackend;
    public readonly bool UsedUnsupportedHardwareFallback;
}

public static class WaterSimulationCompatibilityPolicy
{
    public static WaterSimulationCompatibilityDecision Resolve(
        WaterSimulationMode requestedMode,
        bool supportsComputeShaders);
}
```

`WaterSimulationMode` 只在 Runtime 初始化阶段传入 `WaterSimulationCompatibilityPolicy`。Default Routing Profile 不再保存 Water 的第二份选择，只保存 Fire=`ElementCell`、Poison=`Unsupported`；Sticky 因 Catalog 无 Definition，不产生 Route。初始化器把 Compatibility Decision 中的 Water Backend 与 Profile 条目合成最终不可变 Snapshot，并由 `ElementWorldRuntime.MaterialRoutes : IMaterialSimulationRouteReadOnly` 只读公开。`TryResolve` 对 Catalog 中存在且显式配置为 `Unsupported` 的 Material 返回 true+`Unsupported`，对未知/无 Definition 返回 false；Router、Query 和后续 Liquid Renderer Policy 从此不再接收 Water Mode。

初始化顺序固定为：①创建 Catalog/Profile Authoring Snapshot；②根据 Requested Water Mode 与 `supportsComputeShaders` 生成 Compatibility Decision；③若 Decision 要求 PBF，则验证 Component/Profile/Compute 并初始化唯一 `GpuPbfFluidRuntime`；④只有成功后才发布最终 Route Snapshot；⑤Router/Query/Renderer Compatibility View 开始消费。Requested PBF 遇到不支持 Compute/Null Device 才允许 `UsedUnsupportedHardwareFallback=true` 并生成 Water→`ElementCell`；硬件支持但 PBF Component 缺失、配置错误或初始化异常时整个初始化失败，绝不偷偷改成 Legacy。

- [x] **Step 1: 扩展 Routing RED Matrix**

  锁定 Legacy Water Decision→`ElementCell`、受支持的 PBF Water Decision→`GpuPbfLiquid`、不支持 Compute 的 Requested PBF→显式 Legacy Fallback；Fire→`ElementCell`、Poison→`Unsupported`、Sticky/未知 ID→无 Route。Router tests 只传 Route Snapshot，证明热路径签名中没有 `WaterSimulationMode`；null sink、queue full 都返回 false，一次请求最多命中一个 Sink。再测试 Liquid ID 可绑定 PBF、ReactiveField 不可绑定 PBF、重复 Route 被拒绝。

- [x] **Step 2: 运行 RED 并确认失败归因**

  强制 ElementField.Tests Rebuild，预期缺 Route 类型/API；Unity Test Runner 运行 pure routing tests。不得通过直接调用 `GpuPbfFluidRuntime.TryEnqueueSpawn` 绕过 Router 来证明 Routing。

- [x] **Step 3: 实现 Route Snapshot 与 Runtime 初始化**

  `ElementWorldProfile` 引用 Catalog/Routing Profile；`ElementWorldRuntime.TryInitialize` 按上面的五步顺序生成 Water Compatibility Decision 与最终 Route Snapshot。Profile Asset 不被改写。`EffectiveWaterSimulationMode` 暂时保留为从最终 Water Route 反推的 Legacy Renderer/诊断 Compatibility View，但它不再是 Router 或 Material Query 的输入。

- [x] **Step 4: 泛化 Fluid Deposit Adapter**

  Adapter 不再声明 `WaterMaterialId`；它接收已经通过 Route/Catalog 的 `ElementWriteRequest`，把 Canonical `MaterialId` 写入 `FluidSpawnRequest.MaterialId`。防御性检查仍拒绝非 Liquid、未知 ID、零 Amount、超 Capacity 与 non-finite 输入。

- [x] **Step 5: GREEN 与 Legacy/PBF 回归**

  运行 Compatibility/Route/Profile/Impact/InitialDeposit focused tests，再运行现有 PBF Layout/Lifecycle compile。Unity Editor 绑定 Default Route 后分别切 Legacy/PBF：Water 单写、Fire Cell、Poison 明确 Unsupported、Sticky 无 Definition、旧 Renderer 互斥必须保持。额外模拟“Compute 不支持”与“PBF 配置错误”，前者回退且只 Warn 一次，后者初始化失败且不写 Cell。

- [x] **Step 6: 更新 Living Explainer**

  解释 Identity、Behavior、Representation、Backend 四者为什么不能合并；画出 Requested Water Mode→Compatibility Decision→Final Route Snapshot→Hot Router 调用链；记录 Unsupported 与 Legacy Fallback 的区别、Single Writer 失败症状和回滚路径。

**Rollback：** `ElementWorldRuntime` 切回旧 Water Router，保留 Task 18 的 MaterialId；Default Route Asset 不删除，避免之后 GUID 重建。

**学习价值：** Dependency Inversion、Policy Snapshot、Single Writer、Fallback 与 Silent Failover 的差别。

**Task 19 建议提交名：** `refactor(element-field): route materials by simulation backend`

## Task 20：Material-aware Gameplay Query 与多 Material Occupancy

**Goal：** 让低频 Liquid Snapshot 能在同一 Global Cell 独立保存 Water/Poison Amount，同时继续作为只读 Derived Projection。

**Non-goal：** 不让 Snapshot 驱动 Solver；不实现多 Material 混合比例、温度或浓度。

**Files：**

- Create: `Assets/_Project/Scripts/ElementField/Materials/IMaterialAmountReadOnly.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialStateQueryRouter.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/LiquidMaterialAmountScaleSnapshot.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/ILiquidOccupancyReadOnly.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/LiquidGameplaySnapshot.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidGameplayReadbackContract.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidGameplayOccupancyBridge.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidFireReactionSystem.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/IFluidGpuSource.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldExposureSystem.cs`
- Modify: `Assets/_Project/Tests/ElementField/LiquidGameplaySnapshotTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/FluidGameplayOccupancyBridgeTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldExposureSystemTests.cs`
- Create: `Assets/_Project/Tests/ElementField/MaterialStateQueryRouterTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-20-report.md`

**Interfaces：**

```csharp
public interface IMaterialAmountReadOnly
{
    bool TryGetAmount(Vector3Int globalCell, MaterialId material, out byte amount);
}

public interface ILiquidOccupancyReadOnly : IMaterialAmountReadOnly
{
    bool HasValidSnapshot { get; }
    Bounds SnapshotBounds { get; }
    uint SnapshotVersion { get; }
    bool TryGetAmountUnitsPerParticle(MaterialId material, out uint amountUnits);
}

public sealed class MaterialStateQueryRouter : IMaterialAmountReadOnly
{
    public MaterialStateQueryRouter(
        IMaterialSimulationRouteReadOnly routes,
        IMaterialAmountReadOnly elementCells,
        ILiquidOccupancyReadOnly liquidOccupancy);

    public bool TryGetAmount(
        Vector3Int globalCell,
        MaterialId material,
        out byte amount);
}

public readonly struct LiquidMaterialAmountScale
{
    public readonly MaterialId Material;
    public readonly uint AmountUnitsPerParticle;
}

public sealed class LiquidMaterialAmountScaleSnapshot
{
    public static LiquidMaterialAmountScaleSnapshot Create(
        LiquidMaterialAmountScale[] entries);

    public bool TryGet(MaterialId material, out uint amountUnitsPerParticle);
}
```

`LiquidMaterialAmountScaleSnapshot.Create` 校验后把输入数组复制进内部固定 256-slot Table，外部随后修改原数组不能改变 Snapshot。它只保存 `MaterialId -> GMU per particle`，不保存任何 PBF Physics 参数。Task 20 Production 先用现有 Water Profile 构造 Water-only Table；Tests 可构造 Water/Poison 不同 Scale 来锁定多 Material Contract，Task 22 再由 `LiquidMaterialSettingsTable` 生成正式 Water/Poison Table。`FluidGameplayReadbackMetadata/Lease` 必须冻结该 Table 与版本，不能只冻结一个全局整数。

`LiquidGameplaySnapshot` 的 Open Addressing Key 变为 `(Vector3Int GlobalCell, MaterialId Material)`；同一 Cell 的 Water/Poison 使用不同 Slot。Binning 固定按 GPU slot 顺序，未知 Catalog ID、缺 Amount Scale、inactive、non-finite、out-of-bounds 跳过，每种材料按自己的 GMU Scale 累加并各自 byte saturation。`MaterialStateQueryRouter` 根据 Route Snapshot 只查询权威 Backend 的 Projection，禁止 Cell residual Water 与 GPU Water 相加。

- [x] **Step 1: 写 Multi-material Query RED**

  同 Cell 放入 1 个 Water Particle（8 GMU）与 2 个 Poison Particle（每个 4 GMU），分别查询得到 Water=8、Poison=8；证明 Amount Scale 来自 Material ID 而不是 `ParticleMass`。非 Water 不再被硬编码过滤；未知 ID/缺 Scale 跳过；同 Material 累加 saturation；bounds translation 清旧数据；失败 Readback 保留完整旧 Snapshot。Router 测试锁定 PBF Water 只读 Occupancy、ElementCell Fire 只读 Cell、Unsupported 返回 false。

- [ ] **Step 2: 运行 RED**

  强制 ElementField.Tests Rebuild 与 focused EditMode，预期旧 Snapshot 的 Water filter/单 Cell Key 断言失败；不得把 packed sample 中已有 Material Tag 当作 CPU Binning 已支持多 Material 的证据。

- [x] **Step 3: 扩展 Key 与原子发布**

  在初始化期按 ParticleCapacity 预分配 Key/Amount/Occupied；不使用 Dictionary、Tuple boxing 或 LINQ。`GpuPbfFluidRuntime` 先从当前 Water Settings 建立 Water-only Amount Scale Snapshot，`FluidGameplayReadbackLease` 冻结该引用；Readback 的 Bounds/Origin/CellSize/Layout/Topology/Snapshot Version/Amount Scale 继续和数据一起原子交换。删除 `ILiquidOccupancyReadOnly` 的单值 `AmountUnitsPerParticle`，避免它在第二种 Liquid 加入后表达不可能成立的“全局单位”。

- [x] **Step 4: 接入 Query Router，不改 Status 语义**

  Exposure 本 Task 仍只请求 Water/Fire，并得到与 Task 10 相同结果；Poison Status 投影留给 Task 23。`FluidFireReactionSystem` 显式查询 Water 的 GMU Scale，不读取 `ParticleMass`。Fire 不得因为 Liquid Snapshot 无效而被提前 return 吞掉。

- [x] **Step 5: GREEN、Async 生命周期与 GC Gate**

  运行 Snapshot/Bridge/Exposure 现有和新增 tests；Unity 中验证 single-flight、hasError 保旧、Disable outstanding 不 Dispose、Destroy 最多 Wait 一次。Profiler 验证 Callback/Binning 无托管分配；低频 Callback 成本单列，不与每帧 Solver 时间混为一谈。

- [x] **Step 6: 更新 Living Explainer**

  解释 Composite Key、Open Addressing、Atomic Publish、Readback 延迟、GMU Scale，以及“Query 统一不等于 Truth 合并、Gameplay Amount 不等于 Physics Quantity”。

**Rollback：** Catalog 只允许 Water，Query Router 保持旧 Water/Fire选择；Snapshot 的复合 Key 可以保留，不需要降回单 Material Layout。

**学习价值：** Hash Key、Derived Projection、Atomic Publish、CPU/GPU Timeline、Source Routing。

**Task 20 建议提交名：** `refactor(element-field): query liquid occupancy by material`

## Task 21：Reaction Semantic Contact/Result Boundary

**Goal：** 让 Reaction Core 接收 Material Identity/GMU，而不是 PBF Particle 或 ElementCell，同时保持现有 Water–Fire 数值、Status Process、Queue Admission、Reservation 与粒子量化语义不变。

**Non-goal：** 不做任意公式 DSL、不实现 Poison–Fire World Process、不生成 Steam Material。

**Files：**

- Create: `Assets/_Project/Scripts/Combat/Status/Reactions/MaterialContactSnapshot.cs`
- Create: `Assets/_Project/Scripts/Combat/Status/Reactions/MaterialReactionResult.cs`
- Create: `Assets/_Project/Scripts/Combat/Status/Reactions/MaterialReactionBinding.cs`
- Create: `Assets/_Project/Scripts/Combat/Status/Reactions/MaterialReactionBindingProfile.cs`
- Create: `Assets/_Project/Scripts/Combat/Status/Reactions/MaterialReactionCatalogSnapshot.cs`
- Modify: `Assets/_Project/Scripts/Combat/Status/Reactions/ElementReactionEvaluator.cs`
- Modify: `Assets/_Project/Scripts/ElementField/World/ElementWorldSimulator.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidFireReactionSystem.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Data/ElementWorldProfile.cs`
- Create: `Assets/_Project/Tests/Combat/MaterialReactionSemanticTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldSimulatorTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/FluidFireReactionSystemTests.cs`
- Create through Unity: `Assets/_Project/ScriptableObjects/Combat/Reactions/MaterialReactionBindings_Default.asset`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-21-report.md`

**Interfaces：**

```csharp
public readonly struct MaterialContactSnapshot
{
    public readonly MaterialId FirstMaterial;
    public readonly float FirstAmountUnits;
    public readonly MaterialId SecondMaterial;
    public readonly float SecondAmountUnits;
}

public readonly struct MaterialReactionResult
{
    public readonly ElementReactionId Reaction;
    public readonly float FirstConsumedAmountUnits;
    public readonly float SecondConsumedAmountUnits;
}

public static bool TryEvaluateMaterialContact(
    in MaterialContactSnapshot contact,
    float deltaTime,
    ElementReactionId reaction,
    in ElementReactionTuningSnapshot tuning,
    out MaterialReactionResult result);
```

Binding Catalog 只负责无序 Material Pair→`ElementReactionId`；算法仍由 typed `ElementReactionEvaluator` 执行。`FirstAmountUnits/SecondAmountUnits` 与 Result 全部使用 GMU，绝不接收 kg、m³、PBF Density、ParticleCount 或 `ParticleMass`。Phase G 只配置 Water+Fire→Extinguish。Evaluator 为保持旧公式，可以在函数内部把 0–255 GMU 映射到既有 0–100 Intensity，再把结果映射回 GMU；Result 中 First/Second consumption 必须按输入顺序返回，Carrier Adapter 最后才翻译为 Cell Delta 或量化后的 GPU Consume。

- [x] **Step 1: 写顺序无关与旧公式等价 RED**

  Water/Fire、Fire/Water 两种输入都解析 Extinguish；结果消费位置与输入顺序对应；未知 Pair false；重复无序 Pair 被拒绝。对 Low/Formal Rate、dt=0、GMU 上界运行旧 API 与新 API A/B，逐值一致。额外证明改变 Water `ParticleMass/RestDensity` 不改变相同 GMU Contact 的 Reaction Result。

- [x] **Step 2: 运行 RED**

  先编译缺新类型/API的 expected RED，再运行 Combat/ElementField focused tests。若旧反应测试先失败，必须修测试接线，不能通过改变既有数值“适配”新抽象。

- [x] **Step 3: 实现最小 Semantic Evaluator**

  新入口只做 Pair 方向映射、GMU↔既有 Intensity 的显式换算、调用 `CalculateExtinguishConsumption`、构建 Result；不访问 Unity Time、Physics、Cell、GraphicsBuffer、Queue、`ParticleMass` 或 MonoBehaviour。`ElementReactionRuntime` 和 `StatusController` 原调用链不迁移成通用集合。

- [x] **Step 4: 迁移两个 Carrier Adapter**

  Legacy `ReactPair` 与 `FluidFireReactionSystem` 都先构建 GMU `MaterialContactSnapshot`，再将 Result 应用到各自 owner。GPU/CPU Queue 的双边 Admission、SnapshotVersion 去重、Water reservation 和 GMU→Particle ceil 保持原样；报告必须明确只有“计划最大粒子数的向上舍入误差 `< Water AmountUnitsPerParticle`”，实际 GPU 删除数没有 Command Acknowledgement，不得与 Planned GMU 混写成精确守恒。

- [x] **Step 5: GREEN 与守恒 Gate**

  运行 Combat Reaction、StatusController、Legacy Cell Reaction、GPU Water/CPU Fire planner tests；验证同一 Snapshot 只提交一次、共享 Water 不重复消费、Queue full 不部分提交、Semantic Result 请求 Water/Fire 等量 GMU，并单独断言 `ceil` 得到的计划粒子数向上误差小于一个 Water 粒子单位。GPU Fixture 只证明实际 Slot/Free Counter 与 Material Filter，不把它冒充逐 GMU Acknowledgement。

- [x] **Step 6: 更新 Living Explainer**

  解释 Reaction Core 与 Carrier Adapter、无序 Pair、Result Direction、为什么保留 typed formulas 而不做万能 Rule Graph，并列出 Status Runtime 与 World Reaction Coordinator 的不同职责。

**Rollback：** 两个 Adapter 可恢复直接调用旧公式；Material Identity/Catalog/Query 不受影响。Binding Asset 只是规则选择，不持有运行时进度。

**学习价值：** Functional Core/Imperative Shell、Semantic Adapter、守恒事务、规则选择与公式执行分离。

**Task 21 建议提交名：** `refactor(combat): add material reaction semantic boundary`

### Phase G Unity Editor Gate

1. Material Catalog/Routing/Reaction Binding Assets 均无重复 ID、Missing Reference 或初始化异常。
2. Legacy Water、PBF Water、Fire Cell、Wet、Extinguish 与 Renderer 互斥回归数值保持一致。
3. Poison Deposit 仍被明确拒绝，证明 Foundation 没有偷偷启用未准备好的 Backend 内容。
4. `Game.Materials` Assembly Dependency Graph 无反向依赖；Rendering 仍只读 ElementField。
5. C# Compile、EditMode、PlayMode、Scene Playthrough、Profiler 证据分别记录；Phase G 不以“架构编译通过”宣称 Water 视觉已验证。

---

# Phase H：Poison 第二种 Liquid——验证单 Runtime、多 Material

Phase H 的目的不是堆 Poison 内容，而是证明同一个 `GpuPbfFluidRuntime`、Particle Pool、Spatial Hash、Collision、Activity 与 Readback 主链可以同时承载 Water/Poison，而不复制 Runtime。Poison 使用不同 Simulation Parameters、Poisoned Status、绿色 Surface Binding 和 Toxic Combustion Reaction Property。

**外部资产依赖：** 无必需外部资产。Poison 首版复用 `ElementLiquidProcedural.shader`、现有 Triplanar Noise 与 Foam 数学，只新建绿色/黄绿色 `.mat` 和 Profile。验证 Projectile 可由现有 Water Projectile Prefab 通过 Unity Duplicate 后修改 Material ID 得到；若没有专用 Icon/VFX/SFX，使用纯色 Material、现有 Impact VFX 或无额外 VFX 作为可接受 fallback。目标不是美术定稿。

## Task 22：Per-material Liquid Amount/Simulation Parameters、Packed Spawn 与 Same-material Neighbor Filter

> **实施状态（2026-08-22）：** 代码、资源、自动化测试与解析文档已完成；Step 7 的 Unity Profiler/长时运行证据合并到 Phase H 最终人工 Gate，尚未勾选。

**Goal：** 单个 Runtime/Pool 中，每个 Deposit 从对应 `MaterialId` 的 GMU Scale、Mass、RestDensity 计算 ParticleCount/RestSpacing，并让已生成粒子继续读取同一 Material row 的 PBF 参数；不同 Liquid 首版不互为 Density/Cohesion Neighbor。

**Non-goal：** 不支持不同 Smoothing Radius/Particle Radius，不实现异质界面力、混合或浮力分层。

**Files：**

- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidMaterialProfile.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidMaterialSettings.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidMaterialSettingsTable.cs`
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Data/FluidGpuLiquidMaterialParameters.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationProfile.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/LiquidSimulationSettings.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/FluidSpawnRequest.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Data/IFluidGpuSource.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidDepositQueueAdapter.cs`
- Reuse unchanged: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidSpawnPackingPlanner.cs`（Task 14 已通过显式 `particleMass/restDensity` 参数解耦，不再持有全局设置）
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/LiquidMaterialAmountScaleSnapshot.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidGameplayReadbackContract.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidGameplayOccupancyBridge.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/FluidGpuResourceSet.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Runtime/GpuPbfFluidRuntime.cs`
- Modify: `Assets/_Project/Art/Elemental/Compute/FluidGpuCommon.hlsl`
- Modify: `Assets/_Project/Art/Elemental/Compute/PbfParticleLifecycle.compute`
- Modify: `Assets/_Project/Art/Elemental/Compute/PbfSolver.compute`
- Modify: `Assets/_Project/Art/Elemental/Compute/FluidDensity.compute`
- Modify: `Assets/_Project/Tests/ElementField/LiquidSimulationProfileTests.cs`
- Create: `Assets/_Project/Tests/ElementField/LiquidMaterialSpawnPlanningTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/FluidSpawnQueueTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/LiquidGameplaySnapshotTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/FluidFireReactionSystemTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/FluidGpuLayoutContractTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/GpuPbfSolverTests.cs`
- Modify: `Assets/_Project/Tests/Rendering/GpuFluidDensityTests.cs`
- Create through Unity: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidMaterialProfile_Water.asset`
- Create through Unity: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidMaterialProfile_Poison.asset`
- Modify through Unity: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidSimulationProfile_Water.asset`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-22-report.md`

**Interfaces 与 GPU Layout：**

```csharp
public readonly struct LiquidMaterialSettings
{
    public readonly MaterialId Material;
    public readonly uint AmountUnitsPerParticle;
    public readonly float ParticleMass;
    public readonly float RestDensity;
    public readonly float Viscosity;
    public readonly float ArtificialPressure;
    public readonly float CohesionStrength;
    public readonly float CohesionRestDistanceRatio;
    public readonly float MaximumCohesionDeltaSpeed;
    public readonly float SleepDensityErrorThreshold;
}

public sealed class LiquidMaterialSettingsTable
{
    public bool TryGet(MaterialId material, out LiquidMaterialSettings settings);
    public LiquidMaterialAmountScaleSnapshot CreateAmountScaleSnapshot();
}

public readonly struct FluidSpawnRequest
{
    public readonly Vector3 WorldPosition;
    public readonly Vector3 InitialVelocity;
    public readonly float Radius;
    public readonly uint ParticleCount;
    public readonly uint MaterialId;
    public readonly uint Seed;
    public readonly uint Flags;
    public readonly float RestSpacing;
}

[StructLayout(LayoutKind.Sequential)]
public readonly struct FluidGpuLiquidMaterialParameters
{
    public readonly Vector4 MassDensityViscosityPressure;
    public readonly Vector4 CohesionAndSleep;
}
```

```hlsl
struct FluidGpuSpawnRequest
{
    float4 positionRadius;
    float4 velocity;
    uint4 materialSeedFlagsParticleCount;
    float4 packingParameters; // x = RestSpacing；yzw 保留为 0。
};
```

`FluidGpuLiquidMaterialParameters` 固定 Stride=32 bytes、Buffer Count=256，直接以 byte Material ID 索引；`AmountUnitsPerParticle` 只留在 CPU `LiquidMaterialSettingsTable/LiquidMaterialAmountScaleSnapshot`，不浪费 GPU Solver row。零行表示 invalid；Spawn 边界拒绝没有有效 Liquid Profile 的 ID。Backend-wide 参数继续共享：Capacity、TickRate、Substeps、SolverIterations、ParticleRadius、SmoothingRadius、Gravity、MaxSpeed、Bounds、Collision。Task 16 的 Water Cohesion 数值迁移到 Water Material Profile，A/B 必须一致。

Task 22 完成后的参数所有权必须是单一来源：

| Per-material（从全局 Settings 移出） | Backend-wide（首版明确共享） |
|---|---|
| `AmountUnitsPerParticle`、`ParticleMass`、`RestDensity`、`Viscosity`、`ArtificialPressure`、`CohesionStrength`、`CohesionRestDistanceRatio`、`MaximumCohesionDeltaSpeed`、`SleepDensityErrorThreshold` | Capacity/Queue/Hash、TickRate/Substeps/Iterations、`ParticleRadius`、`SmoothingRadius`、Lambda/MaxCorrection、Vorticity、Gravity/MaxSpeed、Collision、SleepVelocity/StableTicks、Bounds |

右列保留共享不是因为它们永远不能成为 Material Property，而是 Phase H 没有验证不同值的真实玩法需求；后续只有在测试场景证明需要时再迁移。左列在 Task 22 后不得继续保留一份可独立取值的 global Runtime field/uniform，避免 Water 值和 Material row 同时成为来源。

Task 15 的 48-byte GPU Spawn Request 在本 Task 升级为 64 bytes：前三个 `float4/uint4` 保持原顺序，新增第四个 `float4 packingParameters`，其中 `.x=RestSpacing`、`.yzw=0`。`FluidSpawnRequest.RestSpacing` 是 Adapter 在 Enqueue 时从该 Material 的 `ParticleMass/RestDensity` 计算并快照的值；Runtime 不再给整批 Request 设置单一 `_SpawnRestSpacing` uniform。未带 `DensityPacked` Flag 的 Legacy Random Spawn 允许 `RestSpacing=0` 并保持旧行为。

- [x] **Step 1: 写 Layout/Profile/Neighbor RED**

  锁定 32-byte PBF Parameter Row、64-byte Spawn Request、Water/Poison 两行与各自 GMU Scale；重复/Empty/非 Liquid/invalid finite/zero Amount Scale 拒绝。Spawn chain tests 使用相同 TotalAmount、不同 `AmountUnitsPerParticle/ParticleMass/RestDensity`，断言 ParticleCount 与 `RestSpacing=cbrt(mass/restDensity)` 都来自正确 Material row。GPU fixture 把 Water/Poison 放进同一 Hash Cell，断言 Water Density 只累计 Water，Poison 只累计 Poison，改变 Poison RestDensity/Viscosity 不改变 Water 结果。

- [x] **Step 2: 运行 RED**

  强制 ElementField/Rendering Tests Rebuild，预期缺类型/Buffer/API；再运行 GPU fixture，旧 Solver 因不比较 Material ID 应出现确定的 Density Oracle 不一致。

- [x] **Step 3: 建立 CPU Settings/Amount Scale 与 GPU Parameter Buffer**

  初始化时构造固定 256-slot `LiquidMaterialSettingsTable`，由它生成 Task 20 的 Amount Scale Snapshot，并把纯 PBF 参数 row 上传一次。把上表左列从 `LiquidSimulationSettings` 的全局字段迁出；Water Asset 数值原样写入 `LiquidMaterialProfile_Water`，没有第二份 fallback 值。Runtime Profile 不在 Tick 中读取。ResourceSet 创建/释放 Buffer，`FluidGpuSnapshot` 只读暴露它；LayoutVersion 递增并同步 Contract Tests。不得每粒子复制 32-byte 参数，也不得把 GMU Scale 与 kg-like `ParticleMass` 合并。

- [x] **Step 4: 让 Per-material 参数贯穿 Deposit→Packed Spawn**

  `FluidDepositQueueAdapter` 用 `request.MaterialKind`（字段类型已经是 Canonical `MaterialId`）查询 Settings；用该 row 的 GMU Scale 计算 `ceil(TotalAmount/units)`，再把同一 row 的 Mass/RestDensity 与 Backend-wide ParticleRadius 传给不变的 `FluidSpawnPackingPlanner`。Planner 输出的 RestSpacing 写进 `FluidSpawnRequest`；C#→GPU upload 保持该快照；`PbfParticleLifecycle.compute` 的 Packed 分支读取 `request.packingParameters.x`。移除 Task 15 的全局 `_SpawnRestSpacing`，避免同一 Batch 中 Water/Poison 互相覆盖 Spawn 间距。

  精确链路必须在 Tests 与 Living Explainer 中出现：

  ```text
  ElementWriteRequest.MaterialKind : MaterialId + TotalAmount : GMU
  -> LiquidMaterialSettingsTable.TryGet
  -> AmountUnitsPerParticle + ParticleMass + RestDensity
  -> ParticleCount + RestSpacing
  -> FluidSpawnPackingPlanner
  -> FluidSpawnRequest(RestSpacing)
  -> 64-byte FluidGpuSpawnRequest
  -> GPU Packed Spawn
  ```

- [x] **Step 5: 修改 Solver/Activity/Density Predicate**

  Solver target 先加载 target Material row；Neighbor 必须同时满足 Activity predicate 与 `neighborMetadata.x == targetMetadata.x`。Activity 使用每粒子 RestDensity；XSPH/Cohesion 使用 target 参数；Density Renderer 通过 Target Material ID 过滤并读取该 Material Mass。Spatial Hash 仍可混排所有 Material，避免复制 Hash/Sort。

- [x] **Step 6: GREEN、Amount Quantization 与 Water 数值 A/B**

  运行 Spawn Planning、Layout、PBF、Activity、Density、Anisotropy、Lifecycle、Occupancy、Reaction Conversion 全套 focused tests；只装 Water Profile 时继续使用 8 GMU/particle，并与 Task 17 Water baseline 在容差内一致。Poison/Water 同 Runtime 测试必须断言只有一个 ResourceSet/Pool，没有第二个 Runtime 实例；改变 Poison ParticleMass 不改变 Poison GMU Scale，改变 GMU Scale也不改变 Solver Density Math。

- [ ] **Step 7: Profiler 与文档**

  记录额外 Buffer 8KB、Spawn Request 从 48→64 bytes、每 Neighbor 一次 ID compare/参数读取的 GPU 成本；解释为何共享 h/radius、为何 Same-material Filter 不是 Multi-phase Fluid、为何参数表按 ID 直接索引而不用 GPU Dictionary，以及 GMU/ParticleMass/RestDensity/RestSpacing 分别解决什么问题。

**Rollback：** LiquidSimulationProfile 只绑定 Water Material Profile，关闭 Poison Route；参数 Buffer 和同 ID Filter 可以保留，不需要恢复全局 uniform。若 Water 数值回归，恢复 Task 22 commit 并继续使用 Task 16 全局参数。

**学习价值：** GPU Lookup Table、SoA/AoS Trade-off、Phase Separation、Parameter Ownership、Layout Versioning。

**Task 22 建议提交名：** `feat(element-field): parameterize pbf liquids by material`

## Task 23：Poison Ingress、Occupancy 与 Poisoned Status Projection

> **实施状态（2026-08-22）：** 代码、资源、自动化测试与解析文档已完成；Step 5 的真实 Projectile/角色接触玩法验收留在 Phase H 最终人工 Gate。

**Goal：** Poison Deposit 进入同一 PBF Runtime，Gameplay Query 能读 Poison，角色接触后获得 `Poisoned`；Water/Wet 与 Fire/Burning 通过同一 Binding 机制回归。

**Non-goal：** 不在本 Task 实现 Poison–Fire World Reaction 或 Poison Surface。

**Files：**

- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialStatusProjectionBinding.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialStatusProjectionProfile.cs`
- Create: `Assets/_Project/Scripts/ElementField/Materials/MaterialStatusProjectionSnapshot.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldExposureSystem.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementDepositOnImpact.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Simulation/ElementWorldWriteRouter.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Simulation/FluidDepositQueueAdapter.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldFluidRoutingTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementDepositOnImpactTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/ElementWorldExposureSystemTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/LiquidGameplaySnapshotTests.cs`
- Modify through Unity: `Assets/_Project/ScriptableObjects/Materials/MaterialSimulationRouting_Default.asset`
- Create through Unity: `Assets/_Project/ScriptableObjects/Materials/MaterialStatusProjection_Default.asset`
- Create through Unity Duplicate: `Assets/_Project/Prefabs/ElementField/PF_ElementPoisonProjectile.prefab`
- Create through Unity: `Assets/_Project/ScriptableObjects/Spell/Spell_Emit_PoisonProjectile.asset`
- Create through Unity: `Assets/_Project/ScriptableObjects/Spell/Wand_PoisonValidation.asset`
- Modify only through Unity when adding validation source: `Assets/_Project/Scenes/PBF_FluidSandbox.unity`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-23-report.md`

**Interfaces：**

```csharp
[Serializable]
public struct MaterialStatusProjectionBinding
{
    public MaterialId Material;
    public StatusKind Status;
    public float MaximumApplyPerExposureTick;
}
```

Default Binding：Water→Wet、Fire→Burning、Poison→Poisoned。Profile 初始化时生成固定数组 Snapshot；Exposure 使用预分配 `targets × bindingCount` byte workspace，对每个 Material 找 Collider Bounds 内最大 Amount，再调用 `ApplySustainedStatus`。基础 `MaterialDefinition` 不引用 `StatusKind`。

- [x] **Step 1: 写 Routing/Projection RED**

  Poison→同一 fake Liquid Sink、Water/Poison 各命中一次；未知/无 Liquid Profile 的 Material 被拒绝。Exposure 锁定 Water=Wet、Fire=Burning、Poison=Poisoned，多 Collider 同 Target 只应用一次、最大 Amount 生效、无 Liquid Snapshot 时 Fire 仍生效。

- [x] **Step 2: 运行 RED**

  先强制 compile expected missing Profile/API，再运行 focused EditMode。Poison packed sample 已存在不等于 Poison Status 已接入，必须由 Exposure 行为测试证明。

- [x] **Step 3: 启用 Poison Route 与 Deposit**

  只有 Catalog、Route 与 Liquid Material Profile 三者都有效时 Poison 才能进入 Adapter；Spawn 仍写 Canonical Material ID，TopologyVersion 只在 GPU Dispatch 后发布。Water 旧 Impact/InitialDeposit 不变。

- [x] **Step 4: 数据驱动 Status Projection**

  将 Water/Fire 现有硬编码搬入 Binding Snapshot，再增加 Poison；热路径数组在 Awake 分配，不使用 Dictionary/LINQ/closure。`StatusController` 继续拥有强度、来源、衰减与 Reaction Runtime。

- [ ] **Step 5: Unity Gameplay Gate**

  Sandbox/P7 验证真实 SpellCaster→Poison Projectile→Impact→Router→同一 PBF Runtime；角色只接触 Water 得 Wet、只接触 Poison 得 Poisoned、接触 Fire 得 Burning。Inspector/Debug Counter 显示共用 Pool Capacity，而不是两个 Runtime Capacity。

- [x] **Step 6: 更新 Living Explainer**

  解释 Material 本体、Occupancy 与 Status 的一对多 Projection；列出 Projectile→Router→GPU→Readback→Exposure→StatusController 完整链路和延迟边界。

**Rollback：** 把 Poison Route 改回 Unsupported、移除 Poison Validation Wand/Scene 引用；Water/Fire Binding 保留数据驱动实现。

**学习价值：** Data-driven Mapping、Gameplay Projection、Source Attribution、异步状态延迟与去重。

**Task 23 建议提交名：** `feat(element-field): route poison through the liquid backend`

## Task 24：Material-filtered Density、Surface 与 Poison Render Binding

> **实施状态（2026-08-22）：** 代码、资源、自动化测试与解析文档已完成；Step 5 的 Frame Debugger/Profiler/透明叠放视觉为最终人工 Gate。

**Goal：** Water/Poison 从同一 GPU Source 分别重建表面和 Draw，不合并成一张 Mesh，不让 Rendering 成为 Gameplay Truth。

**Non-goal：** 不实现跨材质透明排序、混合界面或一次 Marching Cubes 同时输出多个材质。

**Files：**

- Create: `Assets/_Project/Scripts/Rendering/ElementField/LiquidSurfaceMaterialBinding.cs`
- Create: `Assets/_Project/Scripts/Rendering/ElementField/LiquidBackendAvailabilityPolicy.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/GpuLiquidSurfaceRenderer.cs`
- Modify: `Assets/_Project/Art/Elemental/Compute/FluidDensity.compute`
- Modify: `Assets/_Project/Art/Elemental/Compute/FluidAnisotropy.compute` only if target filtering is required by measured transform cost
- Modify: `Assets/_Project/Art/Elemental/Shaders/ElementLiquidProcedural.shader` only for material-neutral property naming aliases; existing Water properties remain compatible
- Modify: `Assets/_Project/Tests/Rendering/GpuFluidDensityTests.cs`
- Modify: `Assets/_Project/Tests/Rendering/GpuLiquidSurfaceRendererInitializationTests.cs`
- Create: `Assets/_Project/Tests/Rendering/GpuMultiMaterialLiquidSurfaceTests.cs`
- Create through Unity: `Assets/_Project/ScriptableObjects/ElementField/Fluid/LiquidRenderProfile_Poison.asset`
- Create through Unity: `Assets/_Project/Art/Elemental/Materials/M_ElementPoison_PBF.mat`
- Modify through Unity: `Assets/_Project/Scenes/PBF_FluidSandbox.unity`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-24-report.md`

**Interfaces：**

```csharp
[Serializable]
public struct LiquidSurfaceMaterialBinding
{
    public MaterialId Material;
    public LiquidRenderProfile Profile;
    public Material SurfaceMaterial;
}

public static class LiquidBackendAvailabilityPolicy
{
    public static bool ShouldRender(
        MaterialId material,
        IMaterialSimulationRouteReadOnly routes);
}
```

`LiquidBackendAvailabilityPolicy.ShouldRender` 只有在 Final Route 把目标 Material 解析成 `GpuPbfLiquid` 时返回 true；它不接收 `WaterSimulationMode`，因此 Poison 不会借用 Water Compatibility Policy。首版允许一个 `GpuLiquidSurfaceRenderer` Component 配置一个 Target Material；Water/Poison 使用两个 Presentation Component，读取同一个 `IFluidGpuSource`，各自持有 Density/MC/Triangle Resource。它们不是两个 Simulation Runtime。现有 Water Component 新字段默认 Water；Empty 配置初始化失败，不静默渲染所有 Material。

- [x] **Step 1: 写 Material Filter RED**

  GPU fixture 在同一 Pool 放 Water 球与 Poison 球；Water Density Texture 只在 Water 区域非零，Poison 同理；两个 MC Buffer 的 Vertex Bounds 不交叉；Counter/Args/Normals finite。初始化测试锁定 Empty/Unknown/duplicate Component Binding 失败和 Water missing-field 迁移默认值；Availability tests 直接传 Route Snapshot，证明 ElementCell/Unsupported=false、GpuPbfLiquid=true，API 中不存在 Water Mode。

- [x] **Step 2: 运行 RED**

  旧 Density 无 Material Filter，应使隔离 oracle 失败；记录真实 GPU RED/Skip。C# build 只能证明新字段/API 编译，不能替代 Compute Dispatch。

- [x] **Step 3: 实现 Target Material Density**

  Renderer 设置 `_TargetMaterialId`；Density Candidate 同时检查 Interest 与 Metadata.x。Particle Mass 从 Task 22 Material Parameter Buffer 读取。Marching Cubes/Indirect Draw 不认识 Material ID，只消费已经过滤的 Density Texture。

- [x] **Step 4: 创建 Poison Presentation**

  Poison Material 使用同一 Shader，首轮建议 `ShallowColor=(0.45,0.95,0.20)`、`DeepColor=(0.05,0.28,0.03)`、较低 Fresnel 蓝分量；实际值由 Game View 调整并记录。无外部 Texture 时沿用程序 Noise/Foam。不得修改 Water Material 来同时表现 Poison。

- [ ] **Step 5: Frame Debugger/Profiler Gate**

  Frame Debugger 必须看到 Water/Poison 两个独立 Procedural Draw、正确 Buffer/Material/Bounds；Legacy Water 不重叠。Profiler 分别记录两次 Density、MC、Draw 的 GPU/CPU Submission；明确成本近似随可见 Liquid Layer 数线性增长，首版 Sandbox/P7 同时可见层数上限设为 2。

- [x] **Step 6: 更新 Living Explainer**

  解释 Target Filter、为什么一个 Runtime 可以有多个 Renderer、为什么多个 Draw 不等于多份 Truth、Transparent Sorting 的边界与 Surface Resource 成本。

**Rollback：** 禁用 Poison Renderer/Material，Water Renderer 的 Target 保持 Water；不回滚 Simulation Buffer。若双 Surface 超预算，先按距离/可见性禁用 Poison Presentation，不删除 Poison Gameplay State。

**学习价值：** GPU Filter、Multiple Consumer、Indirect Draw、Transparent Rendering、Presentation/Gameplay 解耦。

**Task 24 建议提交名：** `feat(rendering): reconstruct liquid surfaces per material`

## Task 25：Poison–Fire Semantic Reaction、完整双 Liquid Gate 与阶段收口

> **实施状态（2026-08-22）：** 实现、自动化测试、范围审计、报告与解析文档已完成；Step 5/6 仍等待开发者执行 Unity 视觉、Profiler、D3D11/D3D12 与 Windows Player Gate，因此 Phase H 不宣称正式完成。

**Goal：** 用 Poison+Fire 验证 Reaction Core 不依赖 Carrier，并完成 Water+Poison 同 Runtime 的架构验收。

**Non-goal：** 不生成 Steam/Smoke，不实现任意 Reaction Product Graph，不做真实 Poison/Water 混合。

**Files：**

- Create: `Assets/_Project/Scripts/Combat/Status/Reactions/ToxicCombustionProcess.cs`
- Modify: `Assets/_Project/Scripts/Combat/Status/Reactions/ElementReactionRuntime.cs`
- Modify: `Assets/_Project/Scripts/Combat/Status/Reactions/ElementReactionEvaluator.cs`
- Modify: `Assets/_Project/Scripts/Combat/Status/Reactions/MaterialReactionResult.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidFireReactionSystem.cs` and rename with `.meta` to `LiquidCellReactionSystem.cs` only after the second implemented rule proves the broader responsibility
- Create: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/MaterialReactionProcessTable.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidReactionCommandQueue.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs`
- Modify: `Assets/_Project/Scripts/Combat/Status/Reactions/ElementReactionProfile.cs`
- Modify: `Assets/_Project/Tests/Combat/ElementReactionRuntimeTests.cs`
- Modify: `Assets/_Project/Tests/Combat/MaterialReactionSemanticTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/FluidFireReactionSystemTests.cs` and rename with `.meta` when Production rename occurs
- Create: `Assets/_Project/Tests/ElementField/MaterialReactionProcessTableTests.cs`
- Create: `Assets/_Project/Tests/ElementField/GpuMultiLiquidGameplayTests.cs`
- Modify through Unity: `Assets/_Project/ScriptableObjects/Combat/Reactions/MaterialReactionBindings_Default.asset`
- Modify through Unity: `Assets/_Project/Scenes/PBF_FluidSandbox.unity`
- Modify only after Sandbox Gate: `Assets/_Project/Scenes/P7_DemoRun.unity`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`
- Create: `.superpowers/sdd/2026-08-09 GPU PBF流体模拟实施计划/task-25-report.md`

**Reaction 语义：**

- Water+Fire→Extinguish：Semantic Result 继续请求 Water/Fire 等量 GMU，沿用 Snapshot reservation 和双队列 Admission；`ceil` 的计划最大粒子数向上误差 `< Water AmountUnitsPerParticle`，但实际 GPU 删除数没有逐 Command Acknowledgement，不能表述为 kg、逐 GMU 精确守恒或严格端到端误差上界。
- Poison+Fire→ToxicCombustion：Fire 是触发条件而不是等量燃料；固定 Wind-up 后请求消费实际可用 Poison GMU，Carrier 用 Poison 自己的 `AmountUnitsPerParticle` 量化，生成 `AreaDamageCommand`，Fire 不因该规则自动扣减。
- `ToxicCombustionProcess` 是 Pure value type，由 `ElementReactionRuntime` 与 World Process Table 共用；不得复制两套 Wind-up/Consume/Damage 数学。
- World `MaterialReactionProcessTable` 使用初始化期固定容量数组，以 `(ReactionId, FireCell, LiquidCell)` 为 Key；同一 Snapshot/Contact 不重复 Start。Process 完成前再次查询最新 Material Amount，实际 Consume 不超过当前 Poison。

- [x] **Step 1: 先提取 Toxic Process Characterization Tests**

  在修改 `ElementReactionRuntime` 前锁定 Started/Wind-up/Consumed/Damage/Cooldown/Cancel 的现有精确行为。新增 `ToxicCombustionProcess` tests 后先取得缺类型 RED，再让 Status Runtime 使用它并证明全部旧 tests 不变。

- [x] **Step 2: 写 World Semantic/Process/Transaction RED**

  锁定 Poison+Fire 与 Fire+Poison 顺序无关；Water+Fire 仍 Extinguish；Poison 不灭火；Wind-up 未结束不消费；结束时只请求消费 Poison GMU；Snapshot 更新后按最新 Amount clamp；Queue full 不消费/不发布 resolved；相同 contact/version 不重复启动；Area Damage 位置使用 contact world center。另用 Poison `AmountUnitsPerParticle != Water` 证明两条 Reaction 不会借用 Water Scale 或 ParticleMass。

- [x] **Step 3: 实现固定容量 Process 与 Generic Adapter**

  只有第二条 Liquid/Cell 规则落地后，才把 `FluidFireReactionSystem` 改名为 `LiquidCellReactionSystem`；保留原 `.meta`。Adapter 负责扫描 Cell/Occupancy、构建 Semantic Contact、维护 Process、把 Result 翻译成 Cell Delta/GPU Consume/AreaDamage；Evaluator 不认识 Carrier。

- [x] **Step 4: 完整 GREEN 与守恒**

  运行 Combat Reaction/Status 全套、Legacy Water/Fire、GPU Water/Fire、Poison/Fire Process、Consume Lifecycle、TopologyVersion、Occupancy 与 Exposure tests。GPU Counter 断言 Active+Free=Capacity，Water Consume 不命中 Poison，Poison Consume 不命中 Water；GMU tests 分开断言 Semantic Planned Amount、Queue Admission 与 `< per-material particle unit` 的计划粒子数量化边界。Slot Counter 不是 Amount Acknowledgement；报告必须把下一 Snapshot 观察到的实际减少量单列为异步结果。

- [ ] **Step 5: 双 Liquid Sandbox/P7 Gate**

  单场景、单 `GpuPbfFluidRuntime`、单 Pool 同时 Spawn Water/Poison：两者使用不同参数与 Surface；Water 施加 Wet，Poison 施加 Poisoned；Water+Fire Extinguish，Poison+Fire 触发 Toxic Combustion；Water/Poison 分开区域各自连续。明确观察两种 Liquid 接触时首版无真实混合/分层，不能把这一限制隐藏成 Bug 已解决。

- [ ] **Step 6: Performance/Backend/Build Gate**

  D3D12 与 D3D11 focused Compute/Shader tests；2K Water+2K Poison 60 秒和目标容量 10 分钟；Frame Debugger 两个 Surface Draw；Profiler 记录参数表、同 ID Neighbor Filter、双 Density/MC、Reaction Binning 与 `GC Alloc=0 B`；Windows Development Build 从 Main Menu 进入 P7/Sandbox 完成双 Liquid Smoke。

- [x] **Step 7: 更新 Living Explainer 与阶段状态**

  追加完整 Material World Foundation、Water/Poison Runtime Chain、Authority/Projection 图、Simulation Capability Matrix、Reaction Adapter、所有文件表、Debug/性能证据、风险与面试回答。只有 Phase E 遗留必要 Gate、Phase F、Phase G/H Gate 都闭合，才能把当前计划范围标为完成；否则逐项保留边界。

- [x] **Step 8: 最终范围审计**

  确认没有 `GpuPbfWaterRuntime/GpuPbfPoisonRuntime` 复制、没有 Universal Solver、没有基础 Definition 引用 Renderer/Status/Solver、没有 per-frame allocation/sync readback、没有手写 `.meta`、没有清理用户 dirty；运行 `git diff --check` 和 Assembly Dependency 检查。

**Rollback：** 禁用 Poison Route/Status/Renderer/Reaction Binding，即回到 Water-only；Water 的 Per-material Parameter Row 与 Material Semantic Foundation 保留。若 Toxic World Process 不稳定，可只禁用 Poison+Fire Binding，Poison Simulation/Status/Surface 继续工作。

**学习价值：** Shared Pure Process、Fixed-capacity State Table、Reaction Transaction、Cross-carrier Semantics、Multi-consumer GPU Architecture、Capability Boundary。

**Task 25 建议提交名：** `feat(element-field): validate poison as a second pbf liquid`

## Task 27：Poison 绿色表现、可见毒球与全 Carrier 毒爆 VFX

> **实施状态（2026-08-22）：** C# Runtime、测试脚本与 Living Explainer 已完成；按用户要求不再由 Codex 操作 Unity Editor。Poison Material、毒球 Material/Prefab、Scene Controller、Visual Profile 与 VFX 引用均保留为人工 Editor Gate，自动化 GREEN 也由开发者打开 Unity 后执行。Task 26 的 PBF Solver、Status 数值与 Reaction 数学未被推翻。

**Goal：** Poison 液面在所有深浅、Fresnel 与接触泡沫区域都保持明确绿色；毒球复用水球 Sphere 表现但使用独立绿色 Mesh Material；角色状态与世界 Poison+Fire 两条 Carrier 路径在 ToxicCombustion `Resolved` 时只发布一次统一位置事件，由场景级对象池播放同一 Burst VFX。

**Architecture：** `Game.Combat` 的 `ElementReactionEvent` 继续作为 Gameplay Fact，新增 `WorldPosition` 值快照；角色 `StatusController` 在发布反应生命周期时填入自身位置，`Game.ElementField` 在实际消费 World Reaction Damage Command 时填入命令中心。`Game.Rendering` 新增场景级 `ReactionBurstVisualController`，只订阅事实、预创建固定容量实例并播放，不决定反应、伤害或材料消耗。中毒持续 VFX 继续由 `StatusDefinition.VfxPrefab` 数据入口负责，不在角色 Prefab 复制字段。

**Tech Stack：** Unity 6.3、C#、URP HLSL、`EventBus<T>`、`ScriptableObject`、`ParticleSystem`、固定容量对象池、NUnit EditMode/PlayMode tests。

**外部资产依赖：** 当前实现不要求用户立即提供资源。`Status_Poisoned.VfxPrefab` 与 `StatusReactionVisualProfile.ToxicResolvedPrefab` 允许为空；为空只跳过 Presentation。后续中毒资源应为可循环 `ParticleSystem` GameObject Prefab，Pivot 置于角色脚底或身体中心；毒爆资源应为非循环 `ParticleSystem` GameObject Prefab，Pivot 置于爆炸中心，建议 1–3 秒、关闭 Play On Awake 也可由 Controller 显式 Play。目标目录为 `Assets/_Project/Art/VFX/Elemental/Poison/`；Texture 建议透明 RGBA、无损压缩或高质量压缩，避免 Read/Write Enabled。

**Files：**

- Modify: `Assets/_Project/Art/Elemental/Materials/M_ElementPoison_PBF.mat`
- Create through Unity: `Assets/_Project/Art/Elemental/Materials/M_ElementPoison_Projectile.mat`
- Modify through Unity: `Assets/_Project/Art/Elemental/Prefabs/PF_ElementPoisonProjectile.prefab`
- Modify: `Assets/_Project/Scripts/Combat/Status/Reactions/ElementReactionEvent.cs`
- Modify: `Assets/_Project/Scripts/Combat/Status/StatusController.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Fluid/Gameplay/FluidReactionCommandQueue.cs`
- Modify: `Assets/_Project/Scripts/ElementField/Runtime/ElementWorldRuntime.cs`
- Modify: `Assets/_Project/Scripts/Rendering/Elemental/StatusReactionVisualProfile.cs`
- Create: `Assets/_Project/Scripts/Rendering/Elemental/ReactionBurstVisualController.cs`
- Modify through Unity: `Assets/_Project/Scenes/P7_DemoRun.unity`
- Modify: `Assets/_Project/Tests/Rendering/StatusReactionVisualControllerTests.cs`
- Create: `Assets/_Project/Tests/Rendering/ReactionBurstVisualControllerTests.cs`
- Modify: `Assets/_Project/Tests/Combat/StatusControllerTests.cs`
- Modify: `Assets/_Project/Tests/ElementField/GpuMultiLiquidGameplayTests.cs`
- Modify: `Assets/_Project/Tests/Skills/PoisonSpellContentTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

**Interfaces：**

```csharp
public struct ElementReactionEvent : IGameEvent
{
    public int TargetId;
    public ElementReactionId Reaction;
    public ElementReactionPhase Phase;
    public Vector3 WorldPosition;
    public float NormalizedStrength;
    public float ExpectedDuration;
}

public readonly struct WorldReactionDamageCommand
{
    public readonly ElementReactionId Reaction;
    public readonly Vector3 WorldPosition;
    public readonly float Amount;
    public readonly float Radius;
}
```

`ReactionBurstVisualController` 消费所有 `TargetId`，但只在 `Reaction == ToxicCombustion && Phase == Resolved` 时从固定池取得一个实例，设置 `transform.position = event.WorldPosition` 并播放缓存的 `ParticleSystem[]`。池满时复用最早实例；这允许表现降级但不能阻塞 Gameplay。`StatusReactionVisualProfile` 暴露 `ToxicResolvedPrefab`、`ToxicResolvedLifetime` 和 `ToxicBurstPoolCapacity`，配置非法时在初始化边界 Clamp 并通过 `GameLog` 诊断。

- [x] **Step 1：写 Poison Material/Projectile RED**

  扩展 `PoisonSpellContentTests`：Poison Projectile 必须拥有 Sphere Mesh、启用的 `MeshRenderer`、与 Water Projectile 相同的普通 Mesh Shader、不同 Material Asset，且 Shader 不得为 `Game/Elemental/Liquid Procedural`。新增 Material assertions：Poison PBF/Projectile 的 Shallow、Deep、Fresnel、Foam 四组颜色都满足 `g > b` 且与 Water Material 有可见距离；Poison Projectile Prefab 仍保留 `MaterialId.Poison` Deposit。

- [x] **Step 2：运行 RED 并确认失败原因**

  使用 Unity Test Runner 只运行 Poison 内容测试。预期旧 Prefab 因使用 `Liquid Procedural` Shader 失败；旧 PBF Material 因 Fresnel/Foam 仍为青蓝色失败。只有这两个根因失败才进入 GREEN，编译错误或找不到资产不算有效 RED。

- [ ] **Step 3：创建普通毒球材质并统一 Poison 调色**

  通过 Unity Editor API 复制 `M_ElementWater_Handwritten` 为 `M_ElementPoison_Projectile`，让 Unity 自动生成 `.meta`；保留 `Game/Elemental/Water` Mesh Vertex/Fragment 路径，只修改 Shallow/Deep/Fresnel/Foam、Alpha 与 Smoothness。PBF Material 使用同一绿色 Palette，但继续使用 Procedural Shader；两者不能互换。Prefab 只替换 Material 引用，不修改 Rigidbody、Collider、飞行、碰撞或 Deposit。

- [x] **Step 4：写统一 Toxic Event RED**

  Combat test 订阅真实 `EventBus<ElementReactionEvent>`，令角色 Poisoned+Burning 完成 Toxic Process，断言恰好一个 `Resolved` 且 `WorldPosition == controller.transform.position`。ElementField test 让 World Toxic Damage Command 出队，断言 `Reaction == ToxicCombustion`，并由 Runtime 发布一个相同位置的 `Resolved`；Water+Fire 不发布 Toxic Burst，Wind-up/Cancelled 不发布 Resolved Burst。

- [x] **Step 5：实现两条 Carrier 的位置事实汇合**

  `StatusController.PublishReactionSignal` 写入角色位置快照。`WorldReactionDamageCommand` 保存明确的 Reaction ID；`MaterialReactionProcessTable` 只为完成的 ToxicCombustion 构造该命令。`ElementWorldRuntime` 在 Damage Command 被接受并执行的同一边界发布 `ElementReactionEvent(TargetId=0, Phase=Resolved, WorldPosition=command.WorldPosition)`。不得由 Rendering 猜测 Poison/Fire 接触，也不得在 Damage 命中每个 Collider 时重复发布。

- [x] **Step 6：写 Burst Pool 契约测试**

  `ReactionBurstVisualControllerTests` 使用 inactive Root 在 `Awake` 前注入 Profile，验证：其他 Reaction/Started/Cancelled 不播放；角色与 World `TargetId` 都能播放；实例位置等于事件快照；重复事件 childCount 不增长；池满时复用最早实例；Disable 后停止、清空并退订；Prefab 为空时无异常且无实例。

- [x] **Step 7：实现场景级 Burst Presentation 脚本**

  `Awake` 仅创建 `capacity` 个 Slot 并缓存每个子层级 `ParticleSystem[]`；事件回调只做索引推进、Transform 写入、SetActive、Stop/Play，不调用 `Instantiate`、`GetComponentsInChildren` 或 LINQ。Lifetime 用预分配的 `float[]` 在 `Update` 递减，不产生分配；到期后 StopEmittingAndClear 并停用。P7 场景只放一个全局 Controller，引用统一 Visual Profile。

- [x] **Step 8：确认中毒持续 VFX 数据入口**

  测试锁定 `Status_Poisoned.asset` 存在且 Kind=Poisoned，并验证 `StatusController` 对其 `VfxPrefab` 只在首次激活时实例化、重复 Apply 复用、强度归零销毁。资产当前保持空引用，交付 Editor 清单说明用户之后只需把循环 Prefab 拖入 `VfxPrefab`。

- [ ] **Step 9：GREEN、回归与静态审计**

  先运行 Poison/Reaction focused tests，再运行 `Game.Combat.Tests`、`Game.ElementField.Tests`、`Game.Rendering.Tests`、`Game.Skills.Tests`。运行相应 `.csproj` build、`git diff --check`，搜索 `ReactionBurstVisualController.Update` 确认无 `new`/LINQ/boxing，搜索毒球 Prefab 确认没有 Procedural Material GUID。既有无关基线红项单独报告，不改测试制造全绿。

- [x] **Step 10：更新 Living Explainer 与人工 Gate**

  解析文档新增 Task 27，按“症状→证据→根因→被否决修法→最终分层→测试→人工边界”讲解普通 Mesh Shader 与 Procedural Shader、完整绿色 Palette、Gameplay Fact/Presentation Subscriber、位置快照和固定对象池。Play Mode 人工验证：毒液明显绿色、毒球可见；角色/敌人/地面三种 ToxicCombustion 各播放一次同一 Burst；中毒 VFX 为空不报错，配置后跟随载体且结束清理；Profiler 检查毒爆帧 `GC Alloc=0 B`。

**Rollback：** 毒球可回到独立普通 Water Shader Material 而不影响 Deposit；Burst Controller 可从 Scene 禁用，Gameplay Reaction/Damage 仍运行；新增 Event Position 是向后兼容值字段。若对象池资源异常，只清空 Profile 的 Toxic Prefab，不关闭 ToxicCombustion。

**Task 27 建议提交名：** `fix(rendering): distinguish poison and visualize toxic combustion`

## Task 28：World-anchored Liquid Surface 与稳定的状态获得顺序

> **实施状态（2026-08-24）：** C# Runtime、纯逻辑测试、程序集编译与 Living Explainer 已完成；Unity Test Runner、真实相机移动、Frame Debugger 与 Profiler Gate 按用户要求由开发者手动执行。Fire Cell 化仅完成后续方案讨论，本 Task 不修改 Fire Runtime、Shader 或资产。

**Goal：** 当角色与相机移动时，静止 Water/Poison 的 `Marching Cubes` 边缘不再因 Density Grid 连续平移而闪烁或时隐时现；状态 Icon 按首次获得顺序从左到右排列，同一状态的强度刷新不改变位置，结束后再次获得才成为最新的最右项。

**Root Cause：** `GpuPbfFluidRuntime.ActiveBounds` 跟随角色，而 `FluidSurfaceGridPlanner` 原本直接使用连续变化的 `Bounds.min - Padding` 作为 Density Lattice Origin。粒子即使静止，也会在每帧被不同相位的 Lattice 采样，`Iso Surface` 穿越 Cell 边界时产生轮廓跳动。UI 则把 Burning/Wet/Poisoned/Sticky 固定在 Hierarchy 槽位；前置槽位反复 Active/Inactive 会让 `HorizontalLayoutGroup` 反复压缩，造成后续 Icon 左右跳动。

**Architecture：** `Game.Rendering` 在纯规划边界把 Grid Origin 量化到 `ActualVoxelSize` 的整数倍，并把候选 Origin 限制在仍完整覆盖 `Interest Bounds` 的合法区间；资源 Resolution、Voxel Size 与 Triangle Capacity 不变，因此不触发 GPU Buffer/Texture 重建。`Game.UI` 新增固定容量 `StatusIconAcquisitionOrder`，只保存四种状态的 Active Flag 与顺序；`PlayerStatusBar` 仅在 `Activate` 首次返回 true 时调用 `SetAsLastSibling`，强度事件仍只更新显示。

**Files：**

- Modify: `Assets/_Project/Scripts/Rendering/ElementField/FluidSurfaceGridPlanner.cs`
- Modify: `Assets/_Project/Scripts/UI/PlayerStatusBar.cs`
- Create: `Assets/_Project/Scripts/UI/StatusIconAcquisitionOrder.cs`
- Modify: `Assets/_Project/Tests/Rendering/FluidSurfaceGridPlannerTests.cs`
- Create: `Assets/_Project/Tests/UI/Game.UI.Tests.asmdef`
- Create: `Assets/_Project/Tests/UI/StatusIconAcquisitionOrderTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

- [x] **Step 1：写 World-anchored Grid 回归测试**

  锁定小于一个 Voxel 的 Bounds 移动不改变 Origin；较大移动只能产生完整 Voxel 步长；量化后的 `WorldBounds` 始终覆盖 `Interest Bounds`，不能以稳定性换取边缘裁切。

- [x] **Step 2：实现覆盖安全的 Lattice Quantization**

  每轴先按 `round(desiredOrigin / voxelSize) * voxelSize` 得到世界锚定候选，再限制到 `[interestMax-gridSpan, interestMin]` 覆盖区间。若 Padding 太小、区间内不存在合法 Lattice 点，则退回连续 Origin，优先保证液体不会被裁掉。

- [x] **Step 3：写状态获得顺序测试**

  锁定 Wet→Burning 后仍为 Wet/Burning；Burning 的重复刷新不重排；状态结束后重新获得才移动到最右；移除旧状态只压缩数组，不改变其他状态相对顺序。

- [x] **Step 4：实现 Zero-GC 顺序 Tracker 与 UGUI 接入**

  Tracker 使用固定 `bool[4]` 与 `StatusKind[4]`，不使用 `List`、LINQ 或事件期临时分配。`PlayerStatusBar` 只在 inactive→active 边界调用 `Transform.SetAsLastSibling`；Active 状态的百分比更新不会修改 Hierarchy。

- [x] **Step 5：静态编译与文档**

  `Game.Rendering.csproj`、`Game.UI.csproj`、`Game.Rendering.Tests.csproj`、`Game.UI.Tests.csproj` 均完成 `dotnet build --no-restore`，0 warning / 0 error。Unity Test Assembly 不能由普通 `dotnet test` 实际执行，因此测试通过与 Play Mode 画面不在本步骤冒充完成。

- [ ] **Step 6：人工 Editor Gate**

  Unity Test Runner 运行 `Game.Rendering.Tests` 与 `Game.UI.Tests`；Play Mode 让角色围绕静止 Water/Poison 行走并旋转相机，观察边缘；按 Wet→Burning、Burning 消耗/重获、Poisoned 加入/退出顺序测试 HUD；Profiler 检查脚本 `GC Alloc=0 B`，Frame Debugger 确认仍为原有 Procedural Surface Draw。

**Fire 后续方案（本 Task 不实现）：** 保留当前 Gameplay `Material System` 与 Fire Reaction 语义，未来把表现替换为 `ReactiveField Cell + GPU Cell Blob`：CPU/Compact Grid 持有每个 Fire Cell 的 Material、强度、寿命与邻接反应，GPU 以低分辨率 Cell 实例/Blob 表现离散火团、上升和熄灭。只有玩法真正需要连续扩散、遮挡或体积浓度时才升级为 Gas Advection Backend，避免现在把 Fire 强行塞入 PBF Liquid Solver。

**Task 28 建议提交名：** `fix(rendering): anchor liquid lattice and stabilize status order`

## Task 29：局部高分辨率 Surface Bounds 消除液面星形毛刺

> **实施状态纠正（2026-08-25）：** 本 Task 及其后续 Task 30、41–45 形成的液体局部 Bounds、Clipmap、Far Splat、Underlay 与 Near/Mid/Far 合成属于实验性 LOD 路径，因实机持续出现薄片、毛刺和可见交界而从生产代码撤回。Task 28 的单 Surface 主链与 World-anchored Lattice 已恢复；Task 40 Remote Spawn Simulation Lease 等非 LOD 修复继续保留。历史步骤以下保留为 Debug 复盘证据，不再代表当前 Runtime。

**症状与证据：** P7 Simulation Bounds 为 `24×12×24m`，Liquid Profile 为 `TargetVoxelSize=0.1m、MaximumResolutionPerAxis=96、Padding=0.3m`。X/Z 达到 Resolution Clamp 后，实际 Voxel 为 `(24+0.6)/(96-1)=0.2589m`，已经大于 `SmoothingRadius=0.25m`。一个 Kernel 横向得不到两个 Sample，`Marching Cubes` 只能用粗 Cell 拼接轮廓，产生稳定但明显的星形尖角；这不是 Shader Normal 或 PBF Velocity 问题。

**Architecture：** 保留 `FluidGpuSnapshot.ActiveBounds` 作为 PBF、Sleep/Wake、Collision 与 Gameplay 的 24m Simulation Truth；`Game.Rendering` 新增纯 `FluidSurfaceInterestBoundsPlanner`，以同一中心把 Density/Marching Cubes 输入限制到 Profile 的 `MaximumSurfaceBoundsSize`。默认 `10×6×10m` 在 96 上得到 `Resolution=96×67×96` 与横向 Voxel 约 `0.1116m`，不提高最大 3D Texture 尺寸，也不修改粒子状态。

**Files：**

- Modify: `Assets/_Project/Scripts/Rendering/ElementField/LiquidRenderProfile.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/FluidSurfaceGridPlanner.cs`
- Modify: `Assets/_Project/Scripts/Rendering/ElementField/GpuLiquidSurfaceRenderer.cs`
- Modify: `Assets/_Project/Tests/Rendering/FluidSurfaceGridPlannerTests.cs`
- Modify: `Assets/_Project/Docs/2026-08-09 GPU PBF流体模拟完整解析.md`

- [x] **Step 1：写欠采样 RED**

  测试要求 24m Simulation Bounds 经 10m Surface LOD 后仍保持原 Simulation Size；较小 Simulation Bounds 不被反向扩大；局部 Grid 为 `96×67×96`，X/Z Voxel 不超过 `0.125m`。旧代码因不存在 Surface Planner 与 Profile 参数产生预期编译 RED。

- [x] **Step 2：实现 Presentation-only Bounds Planner**

  `Plan(simulationBounds, maximumSurfaceBoundsSize)` 逐轴取 `min` 并保留中心；所有 Center/Size 做 finite/positive 验证。Planner 与既有 World-anchored Origin 顺序组合，既降低空间采样间距，也保留移动时的 Lattice Phase 稳定。

- [x] **Step 3：数据化并接入 Renderer**

  `LiquidRenderProfile` 暴露 `Maximum Surface Bounds Size`，`CreateSettings` 生成不可变 Snapshot。Renderer 在初始化与 LateUpdate 都执行 `Simulation Bounds -> Surface Bounds -> Grid Plan`，资源兼容判断仍只看最终 Resolution/Voxel/Capacity；没有 Runtime 热重分配。

- [x] **Step 4：C# 编译 GREEN**

  `Game.Rendering.Tests.csproj` 使用新 Planner 与 Settings 完成 0 warning / 0 error 编译。普通 `dotnet test` 不执行 Unity Test Assembly，因此不能冒充 EditMode Passed。

- [ ] **Step 5：人工画质与性能 Gate**

  Water/Poison Profile 确认 `Maximum Surface Bounds Size=(10,6,10)`；运行 `Game.Rendering.Tests`；P7 静止、移动、旋转相机观察星形尖角；走到窗口边缘确认液体不在屏内突然出现/消失；Profiler 对比 Task 28 的 Density/MC GPU 时间与 `GC Alloc=0 B`。若仍有轻微多边形轮廓，先取得截图与 GPU 时间，再独立评估 Density Filter，不在本 Task 叠加第二个变量。

**Task 29 建议提交名：** `fix(rendering): refine local liquid surface sampling`

### Phase H 最终 Gate

必须同时证明：单 Runtime/Pool、Water/Poison 参数与 Spawn 间距隔离、Same-material Neighbor Filter、Per-material GMU Scale、Material-aware Occupancy、Wet/Poisoned、两个独立 Surface、Water–Fire 与 Poison–Fire 语义、Slot Counter 守恒、计划粒子数量化边界与实际删除 Acknowledgement 缺失这一证据边界、D3D11/D3D12、Profiler/GC、Windows Player。缺少任一层时只能描述对应层完成，不能宣称“Material World 已完成”或“支持真实 Multi-phase Fluid”。

---

# Phase I 以后：独立立项的 Backend Roadmap，不属于 Task 14–25

以下方向只记录进入条件，不在本计划中预先创建接口、文件或资产。每个方向第一次出现真实玩法需求时，必须重新执行 Source Audit→Brainstorm→独立 Implementation Plan→用户确认。

| Future Phase | 首个验证 Material | 推荐 Backend | 进入条件 | 当前明确不做 |
|---|---|---|---|---|
| Granular | Sand | Cell/Grid Cellular Automata | 需要可堆积、下落、斜坡滑动并堵塞路径的真实玩法 | 不让 Sand 进入 PBF，不先做通用颗粒 Solver |
| Gas | Steam 或 Smoke | 低分辨率 Cell/Grid Advection | Water/Lava 或 Fire 玩法需要可扩散、上升、遮挡/中毒 | 不把 Gas 当 PBF Particle，不提前做 Volume Renderer |
| Static Solid | Stone/Ice | Scene Mesh + Cell Solid Metadata | Reaction 需要生成/消耗静态阻挡 | 不复制完整 Mesh/Voxel Truth |
| Detached Solid | Stone Chunk | Rigidbody + Collider/PhysX | 首个可断裂、可推动 Chunk 玩法得到确认 | 不建立 Universal Physics Backend |
| Representation Transfer | Stone/Ice/Water | Transactional Bridge | 同一 Material 确实需要跨 Backend 生命周期转换 | 不在没有玩法消费者时设计 Entity Handle/Save 系统 |

未来计划必须继续遵守：Material Identity 不变，变化的是 Representation；旧 Backend Reserve、新 Backend Create、Commit/Cancel 必须守恒；Derived Snapshot/Renderer 不得参与 Transfer 决策。

---

## 9. 常见失败症状与固定排查顺序

### 9.1 粒子已经存在，但完全没有水面

1. Debug Particle Renderer 是否可见；不可见先查 Spawn/Alive/Bounds。
2. Density Slice 中心是否大于 Iso Level；全黑查 Spatial Hash/World-to-Grid。
3. Triangle Counter 是否为 0；为 0 查 MC Case/Table/Iso。
4. Triangle Count 非 0但不显示，查 Indirect Args、Shader Buffer、`worldBounds`、Cull/Winding。
5. 从下方可见而上方不可见，优先查 Gradient Normal 与 Triangle Winding，不要先改 Alpha。

### 9.2 水像爆炸或粒子飞走

1. 检查 `dt` 是否使用 Substep Delta，而不是整 Tick Delta。
2. 检查 Lambda Epsilon、Rest Density、Particle Mass 与 Smoothing Radius 单位。
3. 检查 Delta Position 是否两阶段写入以及 Max Correction Counter。
4. 检查 Collider 内部粒子退出方向是否反向。
5. 降低 dt/增加 Substep 做 A/B；不能先用 MaxSpeed 暴力掩盖根因。

### 9.3 画面流畅，但 Wet/灭火延迟或错误

1. 查看 Readback Outstanding、Age、Error Counter。
2. 检查 Snapshot Active Bounds 与 ElementWorld Global Cell Origin。
3. 检查 Exposure Interval 是否与 Snapshot 周期叠加成双重延迟。
4. 检查同一 Snapshot Version 是否重复运行 Reaction。
5. 视觉正确不能证明 CPU Occupancy 正确；必须单独看 Snapshot Debug。

### 9.4 连续施法后逐渐卡顿

1. 分开查看 Particle Capacity、RequiresSimulation Count、Surface Grid 和 Triangle Count。
2. CPU 看 `GpuFluid.*` Submission、Readback Callback/Binning 与 GC。
3. GPU 看 Spatial Sort、PBF Iterations、Density、Anisotropy、Marching Cubes。
4. 先确认是 Simulation 还是 Surface，不根据“看不到水”猜 Renderer。
5. 检查 Stable Particle 是否进入 Solver-Sleeping，以及 Spawn/Collider 是否造成 Wake Storm。

---

## 10. Rollback 策略

1. 所有现有 Scene/Profile 默认 `LegacyCell`，直到 Phase D Gate 通过。
2. PBF 代码与资产均为新增；Phase A-C 失败时可禁用组件，不改 Projectile Contract。
3. Phase D 路由失败时把 `ElementWorldProfile.WaterSimulationMode` 切回 `LegacyCell`，恢复旧 Solver/Renderer/Wet/Reaction。
4. 旧 Surface Nets 文件、Tests、Profile、Material 和 Prefab 引用本阶段不删除。
5. 若 Procedural Shader 重构影响 Legacy，先恢复 Legacy Shader include 边界，PBF 使用独立 Fragment 代码；不得为保新功能牺牲已经通过的回退路径。
6. 若 Async Readback 在目标 Player 不支持，正式玩法回退 Legacy；禁止用同步 Readback 临时顶替。
7. Phase F 的 Packed Spawn 由 `FluidSpawnFlags.DensityPacked` 显式启用；清除该 Flag 即回到 Task 2 的旧 Random Spawn，不能删除旧分支后伪装成可回滚。
8. `CohesionStrength=0` 时 Runtime 不提交 Cohesion Compute/Apply Dispatch，精确回到 Task 4 的 Velocity 顺序；不能只把 Shader 内结果乘 0 后仍持续支付 Dispatch 成本。
9. 若碰撞面法线 Contract 出现兼容回归，只让 `ElementDepositOnImpact` 回到旧 `Impacted` 与 `-IncomingDirection` 近似；不得删除 `SpellCaster`/VFX 继续依赖的旧事件。
10. 若 `0.10m` Surface Voxel 超过目标 GPU 预算，先把 `LiquidRenderProfile.TargetVoxelSize` 恢复 `0.15m`；不得降低 IsoLevel 跨空隙制造视觉假连接。
11. Phase G 任一迁移失败时，保留已经稳定工作的单 Water PBF Runtime，并由 Water Compatibility Policy 生成 Water→当前有效 Backend、Fire→`ElementCell`、Poison→`Unsupported` 的最终 Snapshot；不得让 Router 重新接收 Water Mode，也不得通过复制 `GpuPbfFluidRuntime` 绕过架构问题。
12. Phase H 任一 Poison Gate 失败时，依次关闭 Poison Reaction Binding、Poison Surface Binding、Poison Status Projection 与 Poison Route。Water 的 Canonical Identity、Material Catalog、共享 Liquid Parameter Table 继续保留，不需要回退 Task 18–21 的语义基础。
13. Task 25 结束仍不等于真实 `Multi-phase Fluid`。若 Water/Poison 直接接触的边界表现不满足玩法，只能明确保持“同 Material 邻居求解、共享空间但不混合”的首版限制，不能在本阶段临时引入跨 Material 密度约束、分层或界面张力。
14. Task 22 的 64-byte Spawn Layout 若产生 Backend 回归，应整体关闭 Poison Route 并回退 Task 22，让 Water 使用 Phase F 的 48-byte/global Water RestSpacing；不得只回退 GPU Layout、却让 CPU 继续上传 64 bytes。Amount Scale、CPU Request、C# GPU Struct、HLSL Struct 与 LayoutVersion 必须作为一个原子 Contract 回滚。

---

## 11. Plan Self-Review Checklist

- [x] Scope 覆盖 PBF Simulation、GPU Surface、现有写入、Wet、Fire Reaction、Fallback、Profiler、Editor、Build 和增量解析文档。
- [x] Phase A–F 排除 Gas 与多相液体交互；Phase G/H 只验证 Canonical Material Semantics 和 Water/Poison 共用单 Liquid Backend，没有伪装成真实 Multi-phase Fluid。
- [x] 每个新类型在首次消费前都有 Produces/Interface 定义。
- [x] `IFluidGpuSource` 只读依赖方向保持 `Rendering -> ElementField`；新增 `Game.Materials` 只承载身份/行为/目录快照，不反向依赖 Combat、ElementField、Rendering 或 Unity 场景对象。
- [x] 每种启用的 Liquid 在任一时刻只有一个权威 Backend；GPU Particle State 是 PBF Liquid Truth，Occupancy、Status、Surface 都只是带 Version 的派生 Projection，不得回写 Simulation。
- [x] Runtime 没有同步 Readback；同步读取只存在于 Test-only 数值验证。
- [x] Legacy 路径保留且默认启用，支持逐 Phase 回滚。
- [x] 每个 Task 都包含解析文档更新，最终 Task 才允许改为“已完成”。
- [x] 用户外部资产依赖已经在实现前明确为“无”。
- [x] 2026-08-21 用户已授权连续执行 Task 14–17，且未授权 SubAgent；Task 18–25 的实施与 SubAgent 使用仍需重新获得用户确认。
- [x] Phase F 明确保留 Task 0–13 的 GPU Pool、Hash、PBF、Collision、Sleep、Gameplay Bridge 与 Surface 主链，只在 Impact/Spawn/Velocity 参数边界增量扩展。
- [x] Phase F Packed Spawn 先以 48-byte Layout + global Water RestSpacing 收口；Task 22 明确迁移为 64-byte per-request RestSpacing，旧 Random Flag 分支、Surface Normal fallback 和 too-small Radius reject 仍有精确 Contract 与回滚语义。
- [x] Cohesion 只在 `(RestSpacing, SmoothingRadius)` 内生效，强度、单位、Delta Speed 上限、Kernel 顺序和 `strength=0` 关闭语义无歧义。
- [x] “连续水团”由 Solver Neighbor 与 Density Connected Component 两层测试锁定，不靠低 IsoLevel 或截图单独判定。
- [x] Phase F 每个 Task 都更新同一 Living Explainer；Phase E/F Gate 不完整时文档继续标记“更新中”。
- [x] `MaterialId`、`MaterialBehaviorKind`、Simulation Representation、`MaterialSimulationBackendKind` 分别表达身份、宏观行为类别、当前数据/对象形态、负责推进的系统；没有把 Representation 塞进 Behavior，也没有创建万能枚举。
- [x] `MaterialBehaviorKind` 首版只有 `None/Liquid/ReactiveField`；没有 `StaticSolid/DetachedSolid`，也没有为尚未立项的 Sand/Gas/Stone 预建 Definition 或 Behavior Assignment。
- [x] `WaterSimulationMode` 只在初始化 Compatibility Policy 中消费；最终 Route Snapshot 与 Router/Query 热路径 API 都不接收 Water-specific mode。Unsupported Hardware Fallback 与配置/初始化失败已有不同结果。
- [x] Task 20 先建立 Per-material GMU Scale Contract，Task 22 再让 Amount Scale、ParticleMass、RestDensity 从 Deposit/Packed Spawn 贯穿到 Solver/Density；Task 22 后不存在 `_SpawnRestSpacing` 或单值 `ILiquidOccupancyReadOnly.AmountUnitsPerParticle` 残留。
- [x] `Material Amount` 明确定义为 GMU；Reaction Core 不读取 ParticleMass/RestDensity。Water=8 GMU/particle 迁移值、Per-material Quantization Formula、计划粒子数的舍入上界、实际 GPU Consume 无 Acknowledgement 的边界与未来 exact-conservation 升级条件均已写明。
- [x] `MaterialId.Sticky=4` 只保留数值协议；没有 Sticky Asset、Behavior、Route、Liquid Profile、Status 或 Renderer。
- [x] Phase G 只建立最小 Material Foundation：Canonical ID、Catalog、Route、Material-aware Query 与 Reaction Semantic Boundary；没有提前实现 Poison、Sand、Gas、Static/Rigidbody Solid 或通用 Reaction DSL。
- [x] Phase H 以 Poison 验证“单 Runtime/Pool + Per-material Amount/Parameter Row + Per-request Spawn Spacing + Same-material Neighbor Filter + 独立 Projection”，没有复制 Water Runtime，也没有让 Definition 承担 Solver/Renderer/Status 参数。
- [x] Phase I 以后按首个真实玩法需求独立立项；Representation Transfer 必须事务化并保持 Material Identity，不由 Derived Snapshot 或 Renderer 决策。
