# 静态投射物与陨石术 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `StaticProjectile` 法术类别，并实现第一种静态投射物“陨石术”：以释放点为落点，显示落点提示，从空中生成 `NovaFireball` 并砸向落点。

**Architecture:** `Game.Skills` 继续作为纯解释器层：新增 `SpellKind.StaticProjectile` 和 `SpellSpawnMode.SkyfallAtPoint`，让 `CastEvaluator` 把静态投射物 bake 成 `EmitCommand`。`Game.Character.SpellCaster` 根据 `EmitCommand.SpawnMode` 分流，普通投射物走原有 forward 发射，陨石术走 skyfall coroutine。`Game.Combat.NovaFireball` 已经负责落地爆炸与范围伤害，本任务不新增 `MeteorProjectile`。

**Tech Stack:** Unity 6.3, C#, `ScriptableObject`, `readonly struct`, NUnit EditMode tests, `Coroutine`, `WaitForSeconds`, `Rigidbody.linearVelocity`, `ProjectileBase`, `NovaFireball`, `AreaDamage`

## Global Constraints

- 不使用 git worktree，直接在本地项目修改。
- 不创建 `.meta` 文件；Unity Editor 会自动生成。
- 本计划只覆盖代码、spec/plan 和必要的 text asset 配置；不写 `Assets/_Project/Docs` 解析文档。
- `Game.Skills` 保持纯逻辑，不允许 `Instantiate`、场景查询、`MonoBehaviour` 生命周期。
- `Game.Combat` 不允许依赖 `Game.Skills` 或 `Game.Character`。
- `StaticProjectile` 必须追加为 `SpellKind = 3`，不能插入到已有 enum 中间，避免破坏 Unity 资产序列化。
- `Modify` 不消耗投射物预算；`Emit` 和 `StaticProjectile` 消耗投射物预算；`Multicast` 增加投射物预算。
- 本阶段不处理玩家自伤。
- 本阶段 `SkyfallAtPoint` 不响应 homing/orbit 主运动控制修正；陨石使用自己的 skyfall 生成与飞行语义。
- 新增 per-frame 逻辑不得产生 `GC Alloc`；本任务的 coroutine 和 Instantiate 是离散施法事件，允许一次性分配。
- Unity 编译、Prefab 赋值、Play Mode 测试由开发者在 Editor 中完成。

---

## File Structure

- Modify: `Assets/_Project/Scripts/Skills/SpellKind.cs`
  - 追加 `StaticProjectile = 3`。
  - 同文件新增 `SpellSpawnMode`，避免新增 `.cs` 后 Unity csproj 未刷新导致本地构建漏编译。
- Modify: `Assets/_Project/Scripts/Skills/SpellDefinition.cs`
  - 新增静态投射物生成字段：`SpawnMode`、`LandingSitePrefab`、`SkyfallHeight`、`SkyfallBackOffset`、`LandingSiteDuration`。
- Modify: `Assets/_Project/Scripts/Skills/EmitCommand.cs`
  - 快照 `SpawnMode` 和 skyfall 参数。
- Modify: `Assets/_Project/Scripts/Skills/CastEvaluator.cs`
  - 把 `Emit` 和 `StaticProjectile` 都视为产出类法术。
- Modify: `Assets/_Project/Scripts/Character/Spells/SpellCaster.cs`
  - 根据 `EmitCommand.SpawnMode` 分流：普通 forward projectile 与 skyfall projectile。
  - 添加 skyfall coroutine，显示 `LandingSitePrefab`，延迟后从空中生成 `NovaFireball`。
- Modify: `Assets/_Project/Tests/Skills/CastEvaluatorTests.cs`
  - 覆盖 `StaticProjectile` 预算、multicast、modify、payload capture。
- Create in Unity Editor: `Assets/_Project/ScriptableObjects/Spell_Static_Meteor.asset`
  - 由 Editor 创建，不手写 `.asset` 和 `.meta`。

---

### Task 1: 纯解释器语义 - 新增 StaticProjectile 和 SpawnMode

**Files:**
- Modify: `Assets/_Project/Scripts/Skills/SpellKind.cs`
- Modify: `Assets/_Project/Scripts/Skills/SpellDefinition.cs`
- Modify: `Assets/_Project/Scripts/Skills/EmitCommand.cs`
- Modify: `Assets/_Project/Scripts/Skills/CastEvaluator.cs`
- Modify: `Assets/_Project/Tests/Skills/CastEvaluatorTests.cs`

**Interfaces:**
- Produces:
  - `SpellKind.StaticProjectile = 3`
  - `SpellSpawnMode.ForwardProjectile = 0`
  - `SpellSpawnMode.SkyfallAtPoint = 1`
  - `SpellDefinition.SpawnMode`
  - `SpellDefinition.LandingSitePrefab`
  - `SpellDefinition.SkyfallHeight`
  - `SpellDefinition.SkyfallBackOffset`
  - `SpellDefinition.LandingSiteDuration`
  - `EmitCommand.SpawnMode`
  - `EmitCommand.LandingSitePrefab`
  - `EmitCommand.SkyfallHeight`
  - `EmitCommand.SkyfallBackOffset`
  - `EmitCommand.LandingSiteDuration`

- [ ] **Step 1: 在 `CastEvaluatorTests.cs` 添加 enum 值测试**

添加测试：

```csharp
[Test]
public void SpellKind_StaticProjectile_AppendsAfterExistingKinds()
{
    Assert.AreEqual(0, (int)SpellKind.Emit);
    Assert.AreEqual(1, (int)SpellKind.Modify);
    Assert.AreEqual(2, (int)SpellKind.Multicast);
    Assert.AreEqual(3, (int)SpellKind.StaticProjectile);
}
```

- [ ] **Step 2: 在 `CastEvaluatorTests.cs` 添加 Static helper**

放在 `Emit(...)` helper 后：

```csharp
private static SpellDefinition Static(float dmg = 10f, float speed = 20f, float mana = 0f)
{
    var s = ScriptableObject.CreateInstance<SpellDefinition>();
    s.Kind = SpellKind.StaticProjectile;
    s.SpawnMode = SpellSpawnMode.SkyfallAtPoint;
    s.BaseDamage = dmg;
    s.BaseSpeed = speed;
    s.DamageType = DamageType.Magical;
    s.ManaCost = mana;
    s.SkyfallHeight = 12f;
    s.SkyfallBackOffset = 0f;
    s.LandingSiteDuration = 0.8f;
    return s;
}
```

- [ ] **Step 3: 在 `CastEvaluatorTests.cs` 添加 StaticProjectile 基础产出测试**

```csharp
[Test]
public void StaticProjectile_EmitsOneCommand_WithSkyfallSpawnMode()
{
    Run(1, 999f, Static(dmg: 25f, speed: 18f));

    Assert.AreEqual(1, _out.Count);
    Assert.AreEqual(25f, _out[0].Damage, 1e-4f);
    Assert.AreEqual(18f, _out[0].Speed, 1e-4f);
    Assert.AreEqual(SpellSpawnMode.SkyfallAtPoint, _out[0].SpawnMode);
    Assert.AreEqual(12f, _out[0].SkyfallHeight, 1e-4f);
    Assert.AreEqual(0.8f, _out[0].LandingSiteDuration, 1e-4f);
}
```

- [ ] **Step 4: 在 `CastEvaluatorTests.cs` 添加预算测试**

```csharp
[Test]
public void StaticProjectile_ConsumesDrawBudget()
{
    Run(1, 999f, Static(), Emit());

    Assert.AreEqual(1, _out.Count);
    Assert.AreEqual(SpellSpawnMode.SkyfallAtPoint, _out[0].SpawnMode);
}
```

- [ ] **Step 5: 在 `CastEvaluatorTests.cs` 添加 Multicast 测试**

```csharp
[Test]
public void Multicast_CountsStaticProjectileAsProjectileOutput()
{
    Run(1, 999f, Multi(2), Static(), Emit(), Emit());

    Assert.AreEqual(3, _out.Count);
    Assert.AreEqual(SpellSpawnMode.SkyfallAtPoint, _out[0].SpawnMode);
    Assert.AreEqual(SpellSpawnMode.ForwardProjectile, _out[1].SpawnMode);
    Assert.AreEqual(SpellSpawnMode.ForwardProjectile, _out[2].SpawnMode);
}
```

- [ ] **Step 6: 在 `CastEvaluatorTests.cs` 添加 Modify 影响 StaticProjectile 测试**

```csharp
[Test]
public void Modify_AffectsFollowingStaticProjectile()
{
    Run(1, 999f, DamageMod(2f), Static(dmg: 25f));

    Assert.AreEqual(1, _out.Count);
    Assert.AreEqual(50f, _out[0].Damage, 1e-4f);
}
```

- [ ] **Step 7: 在 `CastEvaluatorTests.cs` 添加 payload 捕获测试**

```csharp
[Test]
public void TriggerPayload_CanCaptureStaticProjectile()
{
    Run(1, 999f, Emit(trigger: PayloadTriggerMode.OnImpact), Static());

    Assert.AreEqual(1, _out.Count);
    Assert.IsTrue(_out[0].HasPayload);
    Assert.AreEqual(1, _out[0].Payload.Count);
    Assert.AreEqual(SpellKind.StaticProjectile, _out[0].Payload[0].Kind);
}
```

- [ ] **Step 8: 运行构建确认测试先失败**

Run:

```powershell
dotnet build "Game.Skills.Tests.csproj" --no-restore /m:1
```

Expected:

```text
失败，原因是 SpellKind.StaticProjectile / SpellSpawnMode / SpellDefinition skyfall 字段 / EmitCommand skyfall 字段尚不存在。
```

- [ ] **Step 9: 修改 `SpellKind.cs`**

改成：

```csharp
namespace Game.Skills
{
    /// <summary>法术在求值器眼里的语义类别。</summary>
    public enum SpellKind : byte
    {
        Emit = 0,
        Modify = 1,
        Multicast = 2,
        StaticProjectile = 3,
    }

    /// <summary>产出类法术的运行时生成方式。</summary>
    public enum SpellSpawnMode : byte
    {
        ForwardProjectile = 0,
        SkyfallAtPoint = 1,
    }
}
```

- [ ] **Step 10: 修改 `SpellDefinition.cs`**

在 Emit 字段组中加入：

```csharp
[Tooltip("产出类法术的生成方式：ForwardProjectile=沿方向发射；SkyfallAtPoint=以释放点为落点从天而降。")]
public SpellSpawnMode SpawnMode = SpellSpawnMode.ForwardProjectile;

[Header("StaticProjectile / Skyfall（静态投射物）—— Kind=StaticProjectile 时使用")]
[Tooltip("落点提示预制体，如 NovaFireball_LandingSite。")]
public GameObject LandingSitePrefab;
[Min(0f)]
[Tooltip("陨石从落点上方多高处生成。")]
public float SkyfallHeight = 12f;
[Tooltip("沿释放方向反向偏移多少，0 表示垂直坠落。")]
public float SkyfallBackOffset = 0f;
[Min(0f)]
[Tooltip("落点提示提前显示多久后生成陨石。")]
public float LandingSiteDuration = 0.8f;
```

说明：

```text
Impact / Explosion 不放进 SpellDefinition。本项目已有 NovaFireball.OnImpact 负责 NovaExplosion_Hit 和 FireField。
```

- [ ] **Step 11: 修改 `EmitCommand.cs`**

新增字段：

```csharp
public readonly SpellSpawnMode SpawnMode;
public readonly GameObject LandingSitePrefab;
public readonly float SkyfallHeight;
public readonly float SkyfallBackOffset;
public readonly float LandingSiteDuration;
```

构造函数在 `projectilePrefab` 后或 payload 前加入对应参数，并赋值。

- [ ] **Step 12: 修改 `CastEvaluator.cs`**

把 `Emit` 和 `StaticProjectile` 合并为产出类分支：

```csharp
case SpellKind.Emit:
case SpellKind.StaticProjectile:
    if (drawBudget <= 0) break;
    if (spell.ManaCost > manaLeft)
    {
        fizzled = true;
        break;
    }
    manaLeft -= spell.ManaCost;
    manaSpent += spell.ManaCost;
    if (spell.PayloadTrigger != PayloadTriggerMode.None)
    {
        output.Add(BakeEmit(spell, mods, CaptureSuffix(spells, i + 1)));
        drawBudget--;
        ended = true;
        break;
    }
    output.Add(BakeEmit(spell, mods, null));
    drawBudget--;
    break;
```

`BakeEmit(...)` 传入：

```csharp
spell.SpawnMode,
spell.LandingSitePrefab,
spell.SkyfallHeight,
spell.SkyfallBackOffset,
spell.LandingSiteDuration,
```

- [ ] **Step 13: 运行 Skills 构建**

Run:

```powershell
dotnet build "Game.Skills.Tests.csproj" --no-restore /m:1
```

Expected:

```text
0 warnings, 0 errors
```

---

### Task 2: SpellCaster 支持 SkyfallAtPoint

**Files:**
- Modify: `Assets/_Project/Scripts/Character/Spells/SpellCaster.cs`

**Interfaces:**
- Consumes:
  - `EmitCommand.SpawnMode`
  - `EmitCommand.LandingSitePrefab`
  - `EmitCommand.SkyfallHeight`
  - `EmitCommand.SkyfallBackOffset`
  - `EmitCommand.LandingSiteDuration`
- Produces:
  - `SpawnForwardProjectile(...)`
  - `SpawnSkyfallProjectile(...)`
  - `IEnumerator SpawnSkyfallProjectileAfterDelay(...)`

- [ ] **Step 1: 添加 namespace**

确认 `SpellCaster.cs` 顶部有：

```csharp
using System.Collections;
```

保留现有：

```csharp
using System.Collections.Generic;
```

- [ ] **Step 2: 在 `RunCast(...)` 的循环里按 `cmd.SpawnMode` 分流**

把当前循环中直接实例化 projectile 的逻辑抽到方法中。

在音效和空 prefab 检查后，改为：

```csharp
switch (cmd.SpawnMode)
{
    case SpellSpawnMode.SkyfallAtPoint:
        SpawnSkyfallProjectile(cmd, spawnPos, baseDir, team, attackerId, casterCollider);
        break;

    default:
        SpawnForwardProjectile(cmd, spawnPos, baseDir, i, count, team, attackerId, casterCollider);
        break;
}
```

- [ ] **Step 3: 新增 `SpawnForwardProjectile(...)`**

从当前 `RunCast` 中搬出普通发射逻辑：

```csharp
private void SpawnForwardProjectile(EmitCommand cmd, Vector3 spawnPos, Vector3 baseDir, int index, int count,
                                    byte team, int attackerId, Collider casterCollider)
{
    float yaw = SpellAiming.SpreadOffsetDegrees(index, count, cmd.SpreadDegrees);
    Vector3 dir = Quaternion.AngleAxis(yaw, Vector3.up) * baseDir;

    GameObject go = Object.Instantiate(cmd.ProjectilePrefab, spawnPos, Quaternion.LookRotation(dir));
    ProjectileBase proj = go.GetComponent<ProjectileBase>();
    if (proj == null)
    {
        GameLog.Warn($"法术预制体 {cmd.ProjectilePrefab.name} 上没有 ProjectileBase 组件", "Skills");
        Object.Destroy(go);
        return;
    }

    WirePayload(proj, cmd, team, attackerId, casterCollider);
    ConfigureProjectileMotion(proj, cmd, index, count);
    proj.Init(team, attackerId, cmd.Damage, cmd.DamageType, dir * cmd.Speed, casterCollider, useGravity: cmd.UseGravity);
}
```

- [ ] **Step 4: 新增 `WirePayload(...)`**

把当前 payload 订阅逻辑提取成方法，供普通投射物和 skyfall projectile 复用：

```csharp
private void WirePayload(ProjectileBase proj, EmitCommand cmd, byte team, int attackerId, Collider casterCollider)
{
    if (!cmd.HasPayload) return;

    IReadOnlyList<SpellDefinition> payload = cmd.Payload;
    CastModifierState payloadMods = cmd.PayloadMods;

    switch (cmd.PayloadTrigger)
    {
        case PayloadTriggerMode.OnImpact:
            proj.Impacted += (hitPoint, hitDir) =>
                RunCast(payload, 1, payloadMods, hitPoint, hitDir, team, attackerId, casterCollider);
            break;

        case PayloadTriggerMode.AfterDelay:
            proj.TimedTriggerElapsed += (position, direction) =>
                RunCast(payload, 1, payloadMods, position, direction, team, attackerId, casterCollider);
            proj.ArmTimedTrigger(cmd.PayloadDelaySeconds);
            break;
    }
}
```

- [ ] **Step 5: 新增 `ConfigureProjectileMotion(...)`**

把当前 bounce/homing/orbit 配置提取成方法：

```csharp
private void ConfigureProjectileMotion(ProjectileBase proj, EmitCommand cmd, int index, int count)
{
    proj.ConfigureBounce(cmd.BounceCount);
    switch (cmd.MotionMode)
    {
        case ProjectileMotionMode.Homing:
            proj.ConfigureHoming(cmd.HomingRadius, cmd.HomingDuration, cmd.HomingTurnRateDegrees);
            proj.ConfigureOrbit(0f, 0f, 0f, 0f);
            break;

        case ProjectileMotionMode.Orbit:
            proj.ConfigureHoming(0f, 0f, 0f);
            float orbitPhase = cmd.OrbitPhaseOffsetDegrees + SpellAiming.PhaseOffsetDegrees(index, count);
            float orbitPlaneTilt = SpellAiming.PlaneTiltDegrees(index, count, cmd.OrbitPlaneTiltDegrees);
            proj.ConfigureOrbit(cmd.OrbitRadius, cmd.OrbitAngularSpeedDegrees, orbitPhase, orbitPlaneTilt);
            break;

        default:
            proj.ConfigureHoming(0f, 0f, 0f);
            proj.ConfigureOrbit(0f, 0f, 0f, 0f);
            break;
    }
}
```

- [ ] **Step 6: 新增 `SpawnSkyfallProjectile(...)`**

```csharp
private void SpawnSkyfallProjectile(EmitCommand cmd, Vector3 landingPoint, Vector3 baseDir,
                                    byte team, int attackerId, Collider casterCollider)
{
    if (cmd.LandingSitePrefab != null)
    {
        GameObject marker = Object.Instantiate(cmd.LandingSitePrefab, landingPoint, Quaternion.identity);
        Object.Destroy(marker, Mathf.Max(0.05f, cmd.LandingSiteDuration + 0.5f));
    }

    if (cmd.LandingSiteDuration > 0f)
        StartCoroutine(SpawnSkyfallProjectileAfterDelay(cmd, landingPoint, baseDir, team, attackerId, casterCollider));
    else
        SpawnSkyfallProjectileNow(cmd, landingPoint, baseDir, team, attackerId, casterCollider);
}
```

- [ ] **Step 7: 新增 coroutine**

```csharp
private IEnumerator SpawnSkyfallProjectileAfterDelay(EmitCommand cmd, Vector3 landingPoint, Vector3 baseDir,
                                                     byte team, int attackerId, Collider casterCollider)
{
    yield return new WaitForSeconds(cmd.LandingSiteDuration);
    SpawnSkyfallProjectileNow(cmd, landingPoint, baseDir, team, attackerId, casterCollider);
}
```

说明：

```text
这是离散施法事件，不是 per-frame hot path；WaitForSeconds 分配可接受。
```

- [ ] **Step 8: 新增 `SpawnSkyfallProjectileNow(...)`**

```csharp
private void SpawnSkyfallProjectileNow(EmitCommand cmd, Vector3 landingPoint, Vector3 baseDir,
                                       byte team, int attackerId, Collider casterCollider)
{
    Vector3 horizontalDir = baseDir;
    horizontalDir.y = 0f;
    if (horizontalDir.sqrMagnitude < 1e-6f)
        horizontalDir = transform.forward;
    horizontalDir.Normalize();

    float height = Mathf.Max(0f, cmd.SkyfallHeight);
    Vector3 spawnPos = landingPoint + Vector3.up * height - horizontalDir * cmd.SkyfallBackOffset;
    Vector3 fallDir = landingPoint - spawnPos;
    if (fallDir.sqrMagnitude < 1e-6f)
        fallDir = Vector3.down;
    fallDir.Normalize();

    GameObject go = Object.Instantiate(cmd.ProjectilePrefab, spawnPos, Quaternion.LookRotation(fallDir));
    ProjectileBase proj = go.GetComponent<ProjectileBase>();
    if (proj == null)
    {
        GameLog.Warn($"静态投射物预制体 {cmd.ProjectilePrefab.name} 上没有 ProjectileBase 组件", "Skills");
        Object.Destroy(go);
        return;
    }

    WirePayload(proj, cmd, team, attackerId, casterCollider);
    proj.ConfigureBounce(0);
    proj.ConfigureHoming(0f, 0f, 0f);
    proj.ConfigureOrbit(0f, 0f, 0f, 0f);
    proj.Init(team, attackerId, cmd.Damage, cmd.DamageType, fallDir * cmd.Speed, casterCollider, useGravity: false);
}
```

设计说明：

```text
SkyfallAtPoint 使用自己的主运动语义，不应用 homing/orbit/bounce。
增伤、速度、伤害类型仍然有效。
```

- [ ] **Step 9: 构建 Character**

Run:

```powershell
dotnet build "Game.Character.csproj" --no-restore /m:1
```

Expected:

```text
0 warnings, 0 errors
```

---

### Task 3: 静态检查与 Unity Editor 配置

**Files:**
- No code file for Editor asset creation
- Unity Editor asset to create: `Assets/_Project/ScriptableObjects/Spell_Static_Meteor.asset`
- Unity Editor prefab check: `Assets/_Project/Art/Prefabs/Wizard/FireMagic/NovaFireball.prefab`

- [ ] **Step 1: 静态检查构造调用点**

Run:

```powershell
rg -n "new EmitCommand\\(" "Assets\\_Project\\Scripts" "Assets\\_Project\\Tests"
rg -n "SpellKind.StaticProjectile|SpellSpawnMode|Skyfall" "Assets\\_Project\\Scripts" "Assets\\_Project\\Tests"
```

Expected:

```text
EmitCommand 构造调用只在 CastEvaluator.BakeEmit。
StaticProjectile / Skyfall 字段在 SpellKind、SpellDefinition、EmitCommand、CastEvaluator、SpellCaster、tests 中出现。
```

- [ ] **Step 2: 构建相关 asmdef**

Run:

```powershell
dotnet build "Game.Skills.Tests.csproj" --no-restore /m:1
dotnet build "Game.Character.csproj" --no-restore /m:1
dotnet build "Game.Combat.csproj" --no-restore /m:1
```

Expected:

```text
全部 0 warnings, 0 errors
```

- [ ] **Step 3: 在 Unity Editor 创建 `Spell_Static_Meteor`**

路径：

```text
Assets/_Project/ScriptableObjects
```

操作：

```text
右键 -> Create -> Game -> Skills -> Spell Definition
命名：Spell_Static_Meteor
```

Inspector：

```text
Kind = StaticProjectile
Display Name = 陨石术
Icon = 陨石术图标
Mana Cost = 0

Projectile Prefab = Assets/_Project/Art/Prefabs/Wizard/FireMagic/NovaFireball.prefab
Base Damage = 25
Base Speed = 18
Damage Type = Magical
Cast Sfx = None
Payload Trigger = None
Payload Delay Seconds = 1

Spawn Mode = SkyfallAtPoint
Landing Site Prefab = Assets/_Project/Art/Prefabs/Wizard/FireMagic/NovaFireball_LandingSite.prefab
Skyfall Height = 12
Skyfall Back Offset = 0
Landing Site Duration = 0.8

Modify 区字段保持默认：
Mod Damage Add Flat = 0
Mod Damage Mul = 1
Mod Speed Mul = 1
Mod Spread Add Degrees = 0
Mod Bounce Add = 0
Mod Use Gravity = false
Mod Homing Radius = 0
Mod Homing Duration = 0
Mod Homing Turn Rate Degrees = 0
Mod Orbit Radius = 0
Mod Orbit Angular Speed Degrees = 0
Mod Orbit Phase Offset Degrees = 0
Mod Orbit Plane Tilt Degrees = 0

Extra Draws = 0
```

- [ ] **Step 4: 检查 `NovaFireball.prefab`**

打开：

```text
Assets/_Project/Art/Prefabs/Wizard/FireMagic/NovaFireball.prefab
```

确认：

```text
有 Rigidbody
有 Collider
有 NovaFireball 脚本
NovaFireball._explosionPrefab = NovaExplosion_Hit
NovaFireball._fireFieldPrefab = FireField 或按你当前资源配置
```

如果 `_explosionPrefab` 为空：

```text
把 Assets/_Project/Art/Prefabs/Wizard/FireMagic/NovaExplosion_Hit.prefab 拖进去。
```

- [ ] **Step 5: 检查 `NovaExplosion_Hit.prefab`**

确认：

```text
根物体或子物体上有 AreaDamage
AreaDamage.Radius 合理，例如 3
AreaDamage.Damage Per Hit 合理，例如 25 或 30
AreaDamage.Tick Interval <= 0，表示一次性爆炸
```

实际伤害以 prefab 上 `AreaDamage` 为准；`SpellDefinition.BaseDamage` 仍会作为陨石直击伤害传给 `ProjectileBase.Init`。

- [ ] **Step 6: 加入 SpellLibrary**

打开：

```text
Assets/_Project/ScriptableObjects/SpellLibrary
```

把：

```text
Spell_Static_Meteor
```

加入列表，让 Wand Editor 可以拖拽。

- [ ] **Step 7: Play Mode 验证**

组合 1：

```text
[陨石术]
```

期望：

```text
玩家释放点附近出现 NovaFireball_LandingSite。
约 0.8 秒后 NovaFireball 从上方落下。
落地生成 NovaExplosion_Hit。
敌人在爆炸范围内受到伤害。
```

组合 2：

```text
[触发火球] [陨石术]
```

期望：

```text
触发火球命中点出现落点提示。
陨石从该点上方落下。
爆炸发生在命中点。
```

组合 3：

```text
[三重射击] [陨石术] [火球] [火球]
```

期望：

```text
产出 1 个陨石 + 2 个火球。
```

组合 4：

```text
[增伤] [陨石术]
```

期望：

```text
陨石直击伤害提高。
爆炸 AOE 伤害是否提高取决于 NovaExplosion_Hit 上的 AreaDamage 配置；本阶段 AreaDamage 伤害不从 SpellDefinition 动态改写。
```

---

## Implementation Notes

本阶段的关键取舍：

```text
StaticProjectile 是新的 SpellKind，但不是新系统。
SkyfallAtPoint 是新的生成模式，但落地爆炸继续复用 NovaFireball。
```

这避免了为陨石术写 hardcode，也避免把 `SpellCaster` 变成所有爆炸/范围伤害的集中处理器。

长期看，如果静态投射物越来越多，可以继续扩展：

```text
SpellSpawnMode.StaticArea
SpellSpawnMode.PlaceMine
SpellSpawnMode.SummonAtPoint
```

但本阶段只做：

```text
ForwardProjectile
SkyfallAtPoint
```

## Self-Review Checklist

- [ ] Spec coverage: 覆盖 StaticProjectile、陨石术、触发联动、多重联动、资源映射、Editor 配置。
- [ ] Placeholder scan: plan/spec 中不应出现常见未完成标记。
- [ ] Type consistency: `SpellKind.StaticProjectile`、`SpellSpawnMode.SkyfallAtPoint`、`SpawnMode`、`LandingSitePrefab`、`SkyfallHeight`、`SkyfallBackOffset`、`LandingSiteDuration` 命名一致。
- [ ] Architecture consistency: `Game.Skills` 保持纯逻辑；`SpellCaster` 负责 Instantiate；`NovaFireball` 负责落地爆炸。
- [ ] Editor handoff: 已列出 `Spell_Static_Meteor`、`NovaFireball`、`NovaExplosion_Hit`、`SpellLibrary` 的检查项。
